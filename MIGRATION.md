# Stisla v3 — BS5 → vanilla migration

> Transient. Tracks the refactor of the existing BS5-based v3 codebase to the V3.md spec.
> Delete this file once the migration ships (3.0.0).

**Spec:** V3.md (locked).
**Branch:** v3.
**Approach:** Option A — snapshot the BS5 version once, rewrite in place. See §3 step 0.

---

Prompt for each session:

"Let's continue the v3 migration. Read V3.md + MIGRATION.md, check git, and tell me what step we're on and what to do next."

---

## Status

_Latest entry first. One line per session: ISO date — step completed / current — blockers._

- 2026-06-10 — **Step 4.1 — modal (CSS rewrite + `Stisla.Modal` class + demo rewrite)** — first JS-coordinated component lands; sets the contract every later component (offcanvas, dropdown, tooltip, popover, toast, carousel) copies. **CSS — `_modal.scss` full rewrite** (~250 lines, the old 127-line BS5-overlay file wiped; it referenced `$prefix`, BS5 Sass vars, `.modal-dialog-centered` BS5 vocab and was dead code since Step 2 unbundled BS5 SCSS). **BEM rename map**: `.modal-backdrop` (BS5 sibling) → `.modal__backdrop` (child of `.modal`, moves inside so the click-target is structurally bound + scoped DOM ops are simpler); `.modal-dialog` → `.modal__dialog`; `.modal-content` → `.modal__content`; `.modal-header` → `.modal__header`; `.modal-title` → `.modal__title` (heading tag stays consumer's choice, font-size pinned via `--modal-title-font-size: 1.125rem`, same heading-tag-agnostic pattern accordion 3.6c uses with `* > .accordion__header`); `.modal-body` → `.modal__body`; `.modal-footer` → `.modal__footer`; `.modal-close` → `.modal__close` (kept the floating frosted dismiss chip, rerouted from rgba literals to `color-mix(in oklch, var(--st-foreground|background) X%, transparent)` so light + dark modes both adapt without a `@include color-mode(dark)` override block — drops 8 lines from old file); `.modal-sm`/`-lg`/`-xl`/`-fullscreen` → `.modal--sm`/`--lg`/`--xl`/`--fullscreen` on the **root** not the dialog (cleaner — JS reads sizes off root; modifiers retune `--modal-width` in their own scope so no per-size vars to maintain); `.modal-dialog-centered` **dropped wholesale** (center is the v3 default, matches shadcn/Radix); `.modal-dialog-top`/`-bottom` → `.modal__dialog--top`/`--bottom`; `.modal-dialog-scrollable` → `.modal__dialog--scrollable`; `.fade .show` **dropped wholesale** (replaced by `data-state="open|closed"` on the root, the canonical Radix-aligned paint hook). **18 component vars in 6 groups** (one-tier `--modal-*` naming, matches recent landings 3.5d/3.5e/3.6c — NOT two-tier `--st-modal-*` indirection; the modal can read global tokens like `var(--st-radius-lg)` directly since the radius-opt-in pattern from V3.md §3.4 has been simplified across landings): Geometry (5: width, margin, padding, radius, z-index), Surface (4: bg, color, border-color, shadow), Backdrop (2: backdrop-bg, backdrop-blur), Title (2: title-font-size, title-font-weight), Close chip (4: close-size, close-bg, close-bg-hover, close-color), Motion (1: transition-duration). **Radius**: `--modal-radius: var(--st-radius-lg)` — large-surface tier, matches card + accordion + list-group from 3.6c (Step 3.6c's three-tier radius rhythm: sm 8px for chips, default 12px for medium elements, lg 16px for large frames; modal is now the fourth large-frame component, the last one called out in 3.6c's note that future modal + offcanvas land at `--st-radius-lg` when Step 4 ships). **Open/close paint via data-state + CSS transitions**: `.modal { display: none }` rest; `.modal[data-state="open"] { display: flex }` shown; `.modal__backdrop { opacity: 0 }` + `.modal__dialog { opacity: 0; transform: translateY(-1rem) scale(0.98) }` rest; `.modal[data-state="open"] .modal__backdrop { opacity: 1 }` + `.modal[data-state="open"] .modal__dialog { opacity: 1; transform: none }` shown. Both backdrop + dialog transition the same 0.2s duration so the lift reads coordinated (single `transitionend` listener in JS keys off the dialog). The display-none → display-flex flip is the only way to keep modal fully out of focus order + AT tree when closed; the JS adds a `requestAnimationFrame` between setting `display: flex` and flipping `data-state="open"` so the transitions actually run (without the rAF the browser collapses both changes into one paint and skips the transition). **Static-backdrop shake animation** via `.modal__dialog.is-shaking { animation: modal-shake 0.2s ease-in-out }` — JS adds the class on backdrop click when `opts.backdrop === 'static'`, removes after 250ms. **Scroll lock**: `<html>.is-modal-open { overflow: hidden; scrollbar-gutter: stable }` — Stisla-original state hook (V3.md §3.6 `.is-*` for non-Radix concepts); JS adds the class on first modal open + removes when count returns to zero (counter handles edge cases where modal A's transitionend fires after modal B opens, though stacked-modal is explicitly out of scope for 4.1). `scrollbar-gutter` reserves the scrollbar's width so the page doesn't reflow horizontally when locked. **Reduced motion**: single `@media (prefers-reduced-motion: reduce)` block zeros both the backdrop/dialog transitions AND the shake keyframe — JS also short-circuits the `_waitForTransition` promise via `matchMedia` so opened/closed events fire on next frame instead of after a 0-duration transition that may never emit `transitionend`. **Reused floating dismiss chip** — old `.modal-close` had `rgba(0, 0, 0, 0.55)` + `@include color-mode(dark) { .modal-close { --bg: rgba(255, 255, 255, 0.1) ... } }` block; new `.modal__close` derives bg from `color-mix(in oklch, var(--st-foreground) 55%, transparent)` which automatically inverts in dark mode (var swap on `--st-foreground` flips light → dark and the mix tracks). Color routes through `color-mix(in oklch, var(--st-background) 92%, transparent)` — same inversion logic for the icon. 4 vars expose all four states (rest bg, hover bg, icon color) — one-block scope, no dark-mode override needed. **JS — `src/js/components/modal.js`** (~220 lines, the first occupant of the `src/js/components/` directory Step 4.0 prelude scaffolded). `class Modal extends Component` with `static eventNamespace = 'modal'` + `static defaults = { backdrop: true, keyboard: true, focus: true, returnFocus: true }`. Methods: **`open()`** emits cancelable `stisla:modal:opening` → returns if vetoed → saves `document.activeElement` for return-focus → walks modal → body inerting siblings at each ancestor level (custom `_inertSiblings(true)` so the modal's ancestor chain stays interactive, everything outside drops from focus + AT tree — chose this over BS5's "relocate modal to body" portal pattern because relocation breaks `<form>` containment + Vite HMR semantics) → adds `<html>.is-modal-open` if first modal → forces `display: flex` inline + reflow → `requestAnimationFrame` flips `data-state="open"` + `aria-hidden="false"` → creates `focus-trap` instance on `.modal__content` with `initialFocus` defaulting to `[autofocus]` element or first tabbable (fallbacks to content itself if no tabbables — `fallbackFocus: this._content`) and `escapeDeactivates: false` + `returnFocusOnDeactivate: false` because we handle both ourselves → adds keydown listener for ESC → awaits `_waitForTransition(this._dialog)` then fires non-cancelable `:opened`. **`close()`** mirrors: emits cancelable `:closing` → returns if vetoed → deactivates focus-trap (try/catch guards against deactivate-while-not-active) → removes keydown listener → flips `data-state="closed"` + `aria-hidden="true"` → awaits transition → clears inline display, un-inerts siblings, decrements scroll-lock counter (removes `.is-modal-open` from `<html>` only when no modals remain open), restores focus to the saved `_returnFocusEl` if `opts.returnFocus` → fires non-cancelable `:closed`. **`toggle()`** dispatches based on current `data-state`. **`shake()`** removes + re-adds `.is-shaking` on the dialog with an offsetWidth reflow between (forces the CSS animation to re-run even when triggered in rapid succession) + setTimeout cleanup at 250ms. **`destroy()`** force-cleans an open modal without transitions before calling super: deactivates focus-trap, removes keydown listener, un-inerts siblings, decrements scroll-lock counter — so `getOrCreate()` returning a new instance on a re-init doesn't strand `<html>.is-modal-open` or `inert` attributes from the destroyed instance. **`_waitForTransition(el)`** is a defensive promise wrapper: detects `prefers-reduced-motion` (resolves next frame), computes max transitionDuration from `getComputedStyle` (resolves next frame if zero), listens for `transitionend` filtered to `e.target === el` (so a child transition doesn't false-fire), AND sets a `setTimeout` fallback at `total * 1.5 + 50ms` (covers the rare case where `transitionend` never fires — paint thrash, tab visibility change mid-transition). **`_inertSiblings(on)`** walks from `this.el` up to `<body>`, inerting siblings at each level; saves the inerted elements in `_inertCleanup` array and only removes inert from those (preserves any inert attrs the consumer set independently). **Module-level**: single delegated click handler bound once at module load, guarded by `window.__stislaModalBound` sentinel (HMR-safe, mirrors the Step 4.0 prelude pattern for the interim handlers + the existing `__stislaInterimBound`); handler routes `[data-stisla-modal-trigger="<id>"]` → `Modal.getOrCreate(getElementById(id)).open()` and `[data-stisla-modal-dismiss]` → `getInstance(closest('.modal')).close()`; static-backdrop branch: if `inst.opts.backdrop === 'static'` AND `dismiss.classList.contains('modal__backdrop')` → `inst.shake()` instead of `.close()`. Explicit dismiss controls (`.modal__close` button, footer buttons with `data-stisla-modal-dismiss`) always close even in static mode — only the backdrop element shakes. **`src/js/index.js`** registers via `register('modal', Modal)` and exposes `Stisla.Modal = Modal` so consumers can `new Stisla.Modal(el, opts)` directly OR rely on the declarative scanner (idempotent — `data-stisla-modal` markup auto-instantiates on `DOMContentLoaded`). **Demo page (`src/site/pages/modal.njk`) full rewrite** — 10 sections (Basic, Sizes, Positioning, Scrollable, Static backdrop, Media hero, Lightbox, Confirmation, Success, Customization) replacing the BS5-vocabulary 9-section original; the new Customization section adds 6 grouped tables (Geometry/Surface/Backdrop/Title/Close chip/Motion) at 18 rows total per `slider.njk` reference. **BS5 vocab swept across the demo**: `data-bs-toggle="modal"` + `data-bs-target="#X"` → `data-stisla-modal-trigger="X"` (collapsed into a single attr); `data-bs-dismiss="modal"` → `data-stisla-modal-dismiss`; `data-bs-backdrop="static"` → `data-stisla-modal-backdrop="static"` (read by `readOpts` via per-attr kebab→camel); `data-bs-keyboard="false"` → `data-stisla-modal-keyboard="false"`; `.modal-dialog` etc. → BEM `__` versions; `<h5 class="modal-title">` → `<h3 class="modal__title">` (semantic level bump, v3 has no opinion on the level so chose h3 to fit page hierarchy under h1); `.btn-primary` → `.btn .btn--primary`; `.btn-ghost-secondary` → `.btn .btn--ghost.btn--neutral`; `.btn-outline-secondary` → `.btn .btn--outline.btn--neutral`; `.btn-outline-danger` → `.btn .btn--outline.btn--danger`; `.btn-danger` → `.btn .btn--danger`; `.btn-success` → `.btn .btn--success`; `.icon-box-danger` → `.icon-box.icon-box--danger.icon-box--round`; `.icon-box-success` → matching; `--bs-icon-box-size` → `--icon-box-size` (the modern one-tier var name); `.text-body-secondary` → `.text-muted-foreground` (per `[[feedback_muted_text_utility]]`); `.text-center`, `.pt-4`, `.mb-0/1/2/3`, `.w-100`, `.justify-content-center`, `.justify-content-end`, `.d-flex flex-wrap gap-2`, `.d-inline-block`, `.bg-transparent`, `.border-0`, `.shadow-none`, `.img-fluid`, `.rounded-top`, `.rounded` — all converted to inline `style="..."` per `[[feedback_muted_text_utility]]` + the established list-group/breadcrumb/navbar/pagination/progress pattern (v3 doesn't ship flex utilities yet, so inline style is the v3-idiomatic equivalent); `.link-primary` → `.link`. **Lead paragraph rewritten** per `[[feedback_docs_voice]]`: short, no em-dashes, no pixel callouts ("An overlay dialog that holds focus until the user resolves a task." — 13 words vs. old 23). **Confirmation modal** uses `role="alertdialog"` + `aria-describedby` (BS5 demo used `role="dialog"`) — a11y correctness for destructive-action dialogs that should preserve attention until user response. **Lightbox** technique: inline `style="--modal-bg: transparent; --modal-border-color: transparent; --modal-shadow: none"` on the `.modal` root strips the surface chrome without a `.modal--lightbox` modifier — shows the per-instance var override pattern in action. **Bundle**: `_modal.scss` uncommented at line 68 of `bundles/stisla-full.scss`. JS registered in `src/js/index.js`. **Verification**: build green (42 pages render), bundle CSS 102.0 → 105.99 KB raw (+3.99) / 16.45 → 17.21 KB gz (+0.76); JS 3.69 → 28.93 KB raw (+25.24) / 1.39 → 9.40 KB gz (+8.01) — the JS jump is focus-trap (~5 KB gz) + tabbable (~2 KB gz) + the Modal class itself (~1.4 KB gz). Per V3.md §3.1 the total JS budget is ~25-30 KB gz; we're at 9.40 KB gz with base + modal + focus-trap — leaves ~15-20 KB gz for the remaining 6 components (4.2 offcanvas reuses focus-trap free, 4.3-4.5 add `@floating-ui/dom` ~6 KB gz, 4.6 toast is JS-only no deps, 4.7 carousel adds Embla ~6 KB gz). Bundle should fit. 0 `--bs-*` leaks in compiled CSS. 0 BS5 vocab in rendered `/modal` HTML (`data-bs-*`, `modal-dialog`, `modal-content`, `modal-header`, `modal-body`, `modal-footer`, `modal-title` class form, `modal-close`, `modal-backdrop` class form, `modal-sm`/`-lg`/`-xl`/`-fullscreen`, `modal-dialog-centered`/`-scrollable`/`-top`/`-bottom`, `btn-primary`/`-secondary`/`-danger`/`-success`/`-ghost-secondary`/`-outline-secondary`/`-outline-danger`, `text-body-secondary`, `icon-box-danger`/`-success`, `link-primary`, `d-flex`/`d-inline-block`, `justify-content-*`, `mb-*`/`pt-*`/`w-100`, `img-fluid`, `rounded-top`/`rounded`, `bg-transparent`/`border-0`/`shadow-none`, `text-center` all return zero hits via the strict class-attribute grep — substring matches inside `--modal-backdrop-*` and `--modal-title-*` var names in the Customization section are false positives, filtered out). All 18 `--modal-*` vars emit in compiled CSS (`grep -oE -- "--modal-[a-z-]+"`). All 16 BEM selectors emit (`.modal`, `.modal--sm`, `.modal--lg`, `.modal--xl`, `.modal--fullscreen`, `.modal__backdrop`, `.modal__dialog`, `.modal__dialog--top`, `.modal__dialog--bottom`, `.modal__dialog--scrollable`, `.modal__content`, `.modal__header`, `.modal__title`, `.modal__body`, `.modal__footer`, `.modal__close`). All 5 Stisla data-attrs render in HTML (`data-stisla-modal`, `data-stisla-modal-backdrop`, `data-stisla-modal-dismiss`, `data-stisla-modal-keyboard`, `data-stisla-modal-trigger`). All 11 `modal__*` BEM markers render. CSS hooks `modal-shake` keyframes + `is-shaking` selector + `is-modal-open` selector all emit. focus-trap deps wired in JS bundle (6 focus-trap option strings all present: `escapeDeactivates`, `returnFocusOnDeactivate`, `allowOutsideClick`, `clickOutsideDeactivates`, `fallbackFocus`, `initialFocus`). The four Step 4.0 prelude interim handlers preserved (`__stislaInterimBound`, `data-app-shell-toggle`, `data-stisla-accordion-trigger`, etc. all present alongside the new `__stislaModalBound` sentinel). **Visual + interaction verification pending** — maintainer to eyeball `/modal` in light + dark, exercising: (a) Basic — trigger opens, focus moves to first tabbable (email input), ESC closes, focus returns to trigger; (b) Sizes — sm/default/lg/xl/fullscreen all paint correctly, fullscreen edge-bleeds, dialog width retunes via modifier scope; (c) Positioning — top/center/bottom land at correct cross-axis position via auto margins; (d) Scrollable — long body scrolls inside, header + footer pin; (e) Static backdrop — backdrop click shakes dialog without closing, ESC inert (because `keyboard: false`), `.modal__close` button still closes; (f) Media hero — image flush above body, frosted close chip readable over photo; (g) Lightbox — surface chrome stripped via inline `--modal-*` overrides, image fills, close chip floats; (h) Confirmation — `role="alertdialog"`, `.icon-box--danger.icon-box--round` paints, footer buttons centered; (i) Success — `.icon-box--success.icon-box--round`, full-width "View order" button; (j) Customization — 6 grouped tables read cleanly with Variable/Default/Use columns; (k) Focus — tab cycles inside modal, doesn't escape to background; (l) Scroll lock — page behind doesn't reflow when modal opens; (m) Dark mode — surface, border, backdrop dim, close chip all adapt; (n) Reduced motion — transitions zeroed, modals snap open/close. **Step 4.1 closes — first JS-coordinated component shipped, contract proven.** Next: **4.2 offcanvas** — same primitives (focus-trap, inert siblings, scroll lock, data-state, escape + dismiss attrs) repurposed for a side-drawer instead of a centered card. Then 4.3-4.5 Floating UI trio (dropdown, tooltip, popover — `@floating-ui/dom` lands in 4.3), 4.6 toast, 4.7 carousel. Each per the unified-PR plan locked in 3.6c.

- 2026-06-10 — **Step 4.0 prelude — `Stisla.Component` base class + declarative scanner + JS infrastructure** — first JS-side delta of the migration; no components ship in this PR, just the contract every subsequent JS-coordinated component (modal, offcanvas, dropdown, tooltip, popover, toast, carousel) gets built on top of. Two new files under `src/js/core/`: **`component.js`** (~80 lines) — exports `Component` base class + `getInstance(el)`; module-private `WeakMap` stashes one instance per element; static `eventNamespace`/`defaults`/`getOrCreate(el, opts)`; instance methods `on(target, type, handler)` (records bindings for leak-free destroy), `emit(name, detail, { cancelable, bubbles })` (single dispatch path, defaults cancelable+bubbling so "before" events can `preventDefault()`, "after" events pass `{ cancelable: false }`), `destroy()` (detaches recorded listeners, `INSTANCES.delete(el)`, nulls `this.el` so deferred RAF/transitionend callbacks bail safely); constructor auto-destroys any prior instance on re-init with a dev-only `console.warn` (gated on `import.meta.env.DEV`, dead-code-eliminated in prod) so the htmx/Turbo re-swap case works cleanly without masking accidental double-instantiation. **`init.js`** (~65 lines) — exports `register(name, Class)` + `init(root)` + `readOpts(el, name, Class)`; module-private `Map` registry; `init()` walks `[data-stisla-<name>]` for each registered name, skips elements with existing instances (idempotent so it survives manual re-scans after DOM swaps), builds opts via the shared reader; opts precedence locked at **`Class.defaults` → `data-stisla-opts` JSON (escape hatch, `try/catch` with `console.warn` on parse error) → per-attribute `data-stisla-<name>-<key>` (canonical, kebab→camel, `JSON.parse` with raw-string fallback so `"static"` stays string + `"true"` becomes `true` + `"42"` becomes `42`) → imperative `opts` arg from `new Class(el, opts)`** — locking both conventions in the prelude prevents 4.1 modal from setting an ad-hoc precedent the next seven components copy. SSR-safe auto-init at the bottom (`document.readyState === 'loading'` → `DOMContentLoaded` listener; otherwise immediate) so plain-HTML users keep the "just include the script" UX. **`src/js/components/`** directory created (empty, `.gitkeep` to track) — 4.1 modal lands the first occupant. **`src/js/index.js` refactor** (117 → ~140 lines): imports + re-exports as `Stisla = { version, Component, register, init, get }`, sets `window.Stisla`, then wraps the **four interim delegated handlers** (app-shell collapse/visibility + backdrop dismiss, navbar toggle, sidebar submenu toggle, accordion trigger) in a `window.__stislaInterimBound` sentinel + adds `import.meta.hot?.accept()` so Vite HMR doesn't double-bind on hot reload (real bug surfaced by the Plan agent — would've manifested as "every click fires twice after I save a file" in dev). The four handler bodies stay byte-identical — each promotes to a proper `Stisla.<AppShell|Navbar|Sidebar|Accordion>` class in its own future session, NOT in this prelude (per the Status log breadcrumbs of each component). **Design decisions locked** (5 deltas from the Plan agent design review on top of the initial draft): (1) `static eventNamespace` field on each class instead of register-time mutation — class stays self-describing, direct `new Stisla.Modal(el)` works without the registry; (2) single `emit()` path with optional `{ cancelable, bubbles }` overrides instead of subclasses constructing their own CustomEvents — keeps "did this event fire in the right namespace?" easy to audit + leaves room for future devtools/logging hooks; (3) re-init = destroy old + dev-warn + replace, NOT throw (htmx case) and NOT silently return existing (which BS5 chose and which caused real confusion when opts differed between calls); (4) `this.el = null` in `destroy()` + early-return on `!this.el` guard in public methods — prevents use-after-destroy from deferred callbacks landing mid-teardown (Plan agent flagged this will bite modal/offcanvas if not gated upfront); (5) scanner only supports `[data-stisla-<name>]` — the legacy non-namespaced attrs (`data-app-shell-toggle`, `data-navbar-toggle`, `data-sidebar-submenu-toggle`) stay in the interim handlers and die when their component is promoted; don't teach the scanner two conventions. **Phase 1 exploration** surfaced one already-committed fact worth recording: `Stisla.bs.Toast` + `Stisla.bs.ScrollSpy` are BS5-leftover references in `toasts.njk` + `scrollspy.njk` + `_demo.njk` + `site.js` — explicitly out of scope for the prelude (they get swept when toast lands in Step 4, scrollspy is probably deleted per §5 Step 5). No package.json deps added (Floating UI + focus-trap + Embla land with the components that need them — 4.1 pulls focus-trap, 4.3 pulls Floating UI, 4.7 pulls Embla). **Verification**: build green (42 pages render — unchanged from the 3.6c polish baseline), `stisla.js` 2.7 → 3.69 KB raw (+1.0) / ~1.0 → 1.39 KB gz (+0.4) — net new JS is the base class + scanner + opts reader; CSS bundle untouched at 102.0 KB raw / 16.45 KB gz. Compiled `stisla.js` smoke-checked end-to-end: `const S = { version: "3.0.0-alpha.0", Component: d, register: m, init: l, get: u }` + `window.Stisla = S` + auto-init via `DOMContentLoaded` (or immediate if `readyState !== "loading"`) + `__stislaInterimBound` sentinel + all four interim handlers present byte-for-byte under the guard + `stisla:${eventNamespace}:${name}` template in `emit()` + WeakMap registry intact. The dev-only re-init warn correctly dead-code-eliminates in prod (no `import.meta.env.DEV` literal anywhere in the bundle). Vite log shows 6 modules transformed (was 4 — `component.js` + `init.js` added). No `--bs-*` leaks (CSS-only carryover check; prelude didn't touch CSS). Public API smoke test paste-able in dev tools console once the dev server picks up the change — `window.Stisla.Component`, `Stisla.register('foo', class extends Stisla.Component { static eventNamespace = 'foo' })`, `Stisla.get(el)` all wire correctly. **4.0 closes the prelude.** Next: **4.1 modal** — the canonical reference component for the contract. Modal pulls `focus-trap` + `inert` on siblings (background scroll lock + AT-hide), keys off `data-state="open|closed"`, fires `stisla:modal:open` / `:opening` (cancelable before-events) + `:opened` / `:closed` (non-cancelable after-events once the open/close transition end fires). Modal CSS exists today (Step 3.5 carry-over — well, actually the file is empty / un-touched; need to verify the disposition table before 4.1 starts). Then 4.2 offcanvas (reuses modal primitives), 4.3-4.5 Floating UI trio (dropdown, tooltip, popover — `@floating-ui/dom` lands in 4.3), 4.6 toast, 4.7 carousel (Embla wrapper). Each lands CSS + JS + demo together per the unified-PR plan locked in 3.6c.

- 2026-06-10 — **Step 3.6c polish — selector fix, radius-lg hierarchy, card gap refactor, accordion margin drop** — same-session follow-up after maintainer eyeballed the initial 3.6c land. Four corrections, none of them visible without running the page. (1) **Selector bug — `>` to `> * >`.** All three open-state CSS hooks in `_accordion.scss` were using the direct-child combinator (`.accordion__item[data-state="open"] > .accordion__header`), but the demo (and the canonical ARIA accordion markup) wraps the `<button class="accordion__header">` inside an `<h2>`/`<h3>` heading for screen-reader navigation. Direct-child never matched — closed-hover paint, open-state divider line, and the chevron rotation **all silently failed** from the initial 3.6c land. Fixed by switching the affected three rules to `.accordion__item[data-state="..."] > * > .accordion__header` — the intermediate level matches any wrapper element (`<h2>` ... `<h6>`, also bare wrappers if a consumer skips the heading semantic) and crucially **doubles as a scope fence** for nested accordions inside an open body: an inner header sits at `.outer-item > .outer-body > .inner-accordion > .inner-item > * > .inner-header`, which the outer's `> * >` pattern can't reach. The icon rotation rule stays direct-child on the last hop (`.accordion__header > .accordion__icon`) because the SVG is a direct child of the button itself. (2) **Radius hierarchy: `--st-radius-lg` for large-surface frames.** Audited every component's radius source. Bumped three frame-owning components from `var(--st-radius)` (12 px) to `var(--st-radius-lg)` (16 px under default density): `.card` (was the canonical 12 px card, now reads softer), `.accordion` (frame), `.list-group` (frame around content rows). The three-tier rhythm reads cleanly: **`--st-radius-sm` (8 px) for chips** (kbd, breadcrumb hover paint, icon-box, sidebar/navbar internal buttons, `.btn--sm`, `.form-control--sm`, placeholders); **`--st-radius` (12 px) for medium elements** (btn, alert, form-control, input-group, toggle, toggle-group, button-group, pagination, popover, dropdown-menu, toast); **`--st-radius-lg` (16 px) for large surfaces** (card, accordion, list-group; future modal + offcanvas land here too when Step 4 ships). Accordion bonus: with 16 px outer + 4 px wrapper inset, the inner item lands at `calc(16px - 4px) = 12 px` — exactly the standalone `--st-radius` default — so the inner chip reads like a normal rounded element framed by a softer outer rim. Mirrors the shadcn convention (alert ships at `rounded-lg` too there) but maintainer left alert on `--st-radius` for this pass — the inline-with-forms argument held; alert often sits in tight form layouts where a 16 px banner above 12 px inputs would read inconsistent. (3) **Card refactored to gap-based composition** (matching the alert pattern already in `_alert.scss`). `.card__title` had `margin: 0 0 0.5rem`, `.card__subtitle` had the `margin: -0.25rem 0 0.75rem` negative-top hack to claw back the title's bottom margin, and `.card__text` had `margin: 0 0 1rem` + a `:last-child { margin-bottom: 0 }` workaround — three tells of the legacy typography-margin model, all of which now disappear. Replaced with: `.card__body { display: flex; flex-direction: column; gap: 0.5rem }` (parent owns sibling rhythm), all per-element margins stripped, `.card__text` block removed entirely (its color-inheritance behavior inside `.card__overlay` still applies via the existing descendant rule). The trigger was the maintainer screenshot showing `.card__title` reading off-center inside `.card__header` — `.card__header` is `display: flex; align-items: center; gap: 0.5rem`, so the title's `margin-bottom: 0.5rem` got baked into its flex margin box and the visible text got pushed UP off-center. Removing the title's margin lets `align-items: center` actually centre the text; the body's flex+gap takes over the body-context rhythm that title-margin used to provide. (4) **Accordion `--accordion-margin-bottom` deleted entirely** (maintainer call). The base `.accordion { margin-bottom: 1rem }` was the same legacy pattern — accordion shouldn't claim inter-block spacing; the parent (`.card__body`, page section, whatever) owns it. Matches alert and matches the gap-based card now. The `.accordion--flush` modifier also drops the `--accordion-margin-bottom: 0` override I'd just added, because the var no longer exists to override. Customization table loses the Geometry row for `--accordion-margin-bottom`. **Verification**: build green (42 pages render), bundle 102.2 → 102.0 KB raw (−0.2) / 16.5 → 16.4 KB gz (−0.1) — small shrink from dropped vars + dropped card margins. 0 `--bs-*` leaks. Accordion var count 25 → **24** (Geometry shrinks from 4 to 3: radius, padding, gap). Selector fix verified in compiled CSS — all three open-state rules now show `>*>` in the minified output (`grep -E "data-state=(open|closed)" site-dist/assets/stisla-full.css`). Card simplification verified — `.card__title{font-size:1rem;font-weight:600;line-height:1.3}` (no margin), `.card__subtitle{font-size:.875rem;color:var(--st-muted-foreground);font-weight:500}` (no margin, no negative top), `.card__body{display:flex;flex-direction:column;gap:.5rem;padding:var(--card-padding)}` (gap-based). Radius routing verified — `.card{--card-radius: var(--st-card-radius, var(--st-radius-lg))...}`, `.list-group{--list-group-radius: var(--st-list-group-radius, var(--st-radius-lg))...}`, `.accordion{--accordion-radius: var(--st-radius-lg)...}`. Visual verification pending: eyeball `/accordion` (closed-hover paints accent now; opening an item shows surface-2 chip + rim + bottom divider; chevron rotates 180°; flush-in-card sits clean with no margin gap below); `/card` (title in header reads centred, no off-axis pull); `/list-group` (frame reads softer at 16 px). **3.6c polish closes the accordion family clean.** No further follow-ups expected before Step 4 prelude.

- 2026-06-10 — **Step 3.6c — `components/_accordion.scss` rewrite** — last CSS-leading Phase-2 component before Step 4 takes over. ~190-line file from the 132-line BS5 overlay; full from-scratch rewrite, not a port — the BS5 file leaned on `bootstrap/scss/_accordion.scss` for the base shape and only patched the v3 chip layout on top, so with BS5 unbundled in Step 2 the whole stack was dead. **Architectural call (per maintainer)**: dropped the native `<details>` route. Reasons: (1) `::details-content` for animation isn't in Firefox yet, and the `interpolate-size: allow-keywords` + `transition-behavior: allow-discrete` combo needed for height-auto transitions is Chrome 129+ / Safari 18.4+ — both above the V3.md §2 baseline (Chrome 111 / Safari 16.4 / Firefox 121). (2) Native `<details>` can't cleanly coordinate single-open mode without intercepting the toggle event, which negates the "stay close to native" benefit. (3) The ARIA accordion pattern (`<button aria-expanded>` + `<div role="region" aria-labelledby>`) is what Radix Accordion + shadcn + Reka + bits-ui all ship, so the same CSS file serves vanilla and future React/Vue impls per V3.md §3.11 — no glue layer. **BEM rename map**: `.accordion-item` → `.accordion__item`; `.accordion-header` (BS5 wrapper `<h2>`) **dropped as a class** — the heading wrap stays in markup (`<h3>` or whatever level fits the page) but carries no class, so the consumer picks the semantic level without an extra surface to maintain. `.accordion-button` → `.accordion__header` — the trigger IS the named part, mirrors how Radix `<AccordionTrigger>` and shadcn's structure both expose only the button. `.accordion-collapse` + `.accordion-body` collapsed → `.accordion__body` (the BS5 `.accordion-collapse` outer wrapper was a CSS-transition handle from the BS5 collapse plugin; v3 won't need the wrapper because Step 4's JS-measured height transition runs on the body directly). New `.accordion__icon` for the chevron — rotates 0→180° on open via CSS `transform`. `.accordion-flush` → `.accordion--flush` (proper BEM modifier). **State hooks (V3.md §3.6 — Radix-aligned)**: `.accordion__item[data-state="open|closed"]` is the canonical paint hook; `.accordion__header[aria-expanded="true|false"]` is the source the JS handler flips (and what screen readers read); `aria-controls` ↔ `id` + `aria-labelledby` ↔ `id` wire the header to the body for assistive tech; body carries `role="region"`. `.collapsed` BS5 class **dropped wholesale** — `data-state="closed"` + `aria-expanded="false"` carry the meaning; no shadow class. `:disabled` on the header forwards to muted text + `not-allowed` cursor + the JS handler bails before flipping state. **Concentric inner radius (V3.md §3.4 canonical case)**: `.accordion { --accordion-radius: var(--st-radius); --accordion-padding: 0.25rem }` + `.accordion__item { border-radius: calc(var(--accordion-radius) - var(--accordion-padding)) }`. This is literally the V3.md §3.4 example — accordion is the canonical case. `overflow: hidden` on the item makes the header background fill the chip without bleeding past the rounded edge when the body is hidden via `display: none`. Flush mode wipes the inset (`--accordion-padding: 0`, `--accordion-gap: 0`, `--accordion-bg: transparent`, item radius 0, item `overflow: visible`) and inserts a single `1px solid var(--accordion-border-color)` divider between rows via the `+` adjacency. **Color routing** (revised mid-session per maintainer ref screenshot showing the BS5 v3 visual model): header text stays in `--st-foreground` in both states — the open chip itself is the open-signal, not a colour flip on the header. Closed item: transparent bg + transparent 1px border (so opening doesn't reflow rows); closed-hover header = `--st-accent` bg (transient hover, per V3.md §3.3 — same family as sidebar items, list-group items, table rows, breadcrumb links, pagination chips). Open item: the whole `.accordion__item` raises as a soft chip — `--st-surface-2` bg + `--st-border` rim (mirrors BS5 v3's `:has(.accordion-button:not(.collapsed)) { background-color: --surface-alt }` model, matches the V3.md §3.2 hint that surface-2 is the accordion-header tier); open header gets a `border-bottom: 1px solid --st-border` divider line between header and body; open-state hover stays transparent inside the chip so the persistent open-signal owns the row (single explicit override in the open-state cascade — no `--highlight` fill, no foreground flip, no min-height). Body text routes through `--st-foreground` (same as header). **Padding model**: header + body both at `calc(0.75rem * var(--st-density))` Y + `calc(1rem * var(--st-density))` X (12 / 16 px under default density). The 4 px frame padding + 16 px header X total 20 px from frame edge to content — aligns with `.card`'s `--card-padding: 1.25rem` so a nested accordion sits in the same horizontal rhythm as the card it's inside. **Animation (today vs Step 4)**: chevron rotation ships today via CSS `transform` transition keyed to `--accordion-icon-transition-duration` (0.15s default). Body collapse uses `display: none` for the closed state — instant, same model as sidebar submenu collapsed state. Step 4 swaps the body rule for a JS-measured `height` transition keyed off `--accordion-body-transition-duration` (0.2s default — the var is exposed today so the customization surface is stable across the JS upgrade). Reduced-motion zeros both the header `transition` and the icon rotation. **Interim JS handler in `src/js/index.js`** (~25 lines, mirrors `data-sidebar-submenu-toggle` shape): delegated click listener on `[data-stisla-accordion-trigger]` flips `data-state` on the closest `.accordion__item` and `aria-expanded` on the trigger; bails early if `trigger.disabled`. If the closest `.accordion` carries `data-stisla-accordion-single`, the handler closes sibling open items before opening the new one — radio-group semantics. Step 4 promotes this into Stisla.Accordion with full `class + destroy() + custom-events` contract (V3.md §3.7), keyboard nav (Up/Down/Home/End between headers), and the measured-height transition. **25 component vars in 7 groups** (post-revision): Geometry (4: radius, padding, gap, margin-bottom), Frame surface (3: bg, border-color, shadow — frame ships a `var(--st-shadow)` ambient elevation at rest), Open-item chip (2: item-open-bg, item-open-border-color), Header (8: padding-y, padding-x, font-size, font-weight, color, bg, hover-bg, divider-color), Icon (3: size, color, transition-duration), Body (4: padding-y, padding-x, color, transition-duration), Focus (1: ring). Grouped Customization tables per slider.njk reference. **Flush mode**: just drops the frame border + shadow (per maintainer) — root padding stays so items still chip out concentrically when opened. Useful when the accordion sits inside a card or other framed container that supplies the outer chrome. **Bundle**: `_accordion.scss` uncommented at line 59 of `bundles/stisla-full.scss`. **Demo page (`src/site/pages/accordion.njk`) sweep** — 6 sections (Default / Single open / Flush in a card / With leading icon / Disabled item / Customization) + grouped Customization table. BS5 vocab swept: `data-bs-toggle="collapse"` + `data-bs-target` + `data-bs-parent` all removed (`data-stisla-accordion-trigger` carries the toggle intent; `data-stisla-accordion-single` on the root carries the parent-coordination intent); `.accordion-button` → `.accordion__header` everywhere; `.accordion-item` → `.accordion__item`; `.accordion-collapse collapse show` wrapper → `.accordion__body` directly with `data-state="open"` on the parent item; `.accordion-flush` → `.accordion--flush`; `.collapsed` markup class dropped. The flush demo wraps the accordion inside a `.card` to show the in-card edge-to-edge use case. The single-open demo demonstrates the radio-group mode. The icon demo nests a leading `<svg>` inside a `<span>` wrapper next to the label so the chevron stays pinned to the row's end via the header's `justify-content: space-between`. **Verification**: build green (42 pages render — `/accordion` was already there as the stale BS5 page, now refreshed), bundle 97.8 → 102.2 KB raw (+4.4) / 15.8 → 16.5 KB gz (+0.7). 0 `--bs-*` leaks in compiled CSS. 0 BS5 leftover class names in rendered HTML (`accordion-button`, `accordion-flush`, `accordion-collapse`, `data-bs-*` all return zero hits in `accordion.html`). All 25 documented `--accordion-*` vars emit (verified by `grep -oE -- "--accordion-[a-z-]+"` across the compiled stylesheet). All 6 BEM selectors emit (`.accordion`, `.accordion--flush`, `.accordion__item`, `.accordion__header`, `.accordion__icon`, `.accordion__body`). 28 `accordion__item` + 28 `accordion__header` + 28 `accordion__icon` + 28 `accordion__body` references in rendered HTML (14 demo items × 2 for the demo viewer's escaped source pane); 5 `data-state="open"` + 10 `data-state="closed"`; 28 `data-stisla-accordion-trigger`; 3 `data-stisla-accordion-single`; 3 `accordion--flush`; 33 ARIA wiring markers (`aria-expanded`, `aria-controls`, `aria-labelledby`, `role="region"`). Sass deprecation warnings unchanged (forked BS5 mixins, pre-existing noise). Site sidebar already pointed at `/accordion` (no rename needed). **Visual verification pending** — maintainer to eyeball `/accordion` in light + dark: (a) Default — three-item accordion in `.card`-like frame, first item open with highlight-tinted header + chevron pointing up, others closed with chevrons pointing down; hover any closed header → soft accent fill; (b) Single open — clicking any header closes the others; (c) Flush in card — accordion sits edge-to-edge inside a card with a single divider between rows, no chip insets; (d) With icon — leading icon next to label, chevron stays pinned to end; (e) Disabled — locked header reads muted with no cursor; (f) Customization — 6 grouped tables read cleanly; (g) Focus — tab onto any header and the 2px outline paints at `--st-ring` with inset offset, no clipping past the rounded chip edge; (h) Dark mode — frame bg picks up dark surface, accent + highlight track the dark block, chevron stays on currentColor. **3.6c closes the CSS-leading cohort.** Next step is the Step 4 prelude — `src/js/components/` directory + `Stisla.Component` base class shape + `Stisla.init()` declarative scanner — before any JS-coordinated component lands. Then 4.1 modal (canonical `inert` + `focus-trap` reference), 4.2 offcanvas (reuses modal primitives), 4.3-4.5 Floating UI trio (dropdown, tooltip, popover), 4.6 toast, 4.7 carousel (Embla wrapper). Each lands CSS + JS + demo together per the unified-PR plan locked in this session.

- 2026-06-10 — **Step 3.6b — toggle + toggle-group split; button-group simplified** — vision shift after 3.6a landed. Maintainer flagged real duplication: the "Toggle group" + "Tabs/segmented" + "Checkbox & radio" sections in button-group's demo all visually rendered the same pattern (a row of selected/unselected chips); the existing `.toggle` was a 105-line native-input-driven file that did essentially the same job as the freshly-added `.btn-check`. **Resolution** — split into three components with clean semantic boundaries: (1) **`.toggle`** = standalone two-state press button. Rewritten from input-driven to standalone-button model. Visible element carries `.toggle` (button or label); state from three sources, all paint identically — `[aria-pressed="true"]` (JS press button), `[data-state="active"]` (JS single-select group member with `role="radio"` + `aria-checked`), `.toggle-input:checked + .toggle` (form-data path with hidden sibling input). Outline-neutral rest (`--st-foreground` text, `--st-border` rim, transparent bg); `--st-accent` hover fill; `--st-highlight` active bg + foreground-mix border (`color-mix(in oklch, var(--st-highlight) 75%, var(--st-foreground))` — reads as "slightly stronger than active bg" in both themes, per maintainer spec — "border should be slightly stronger than the active bg"). 12 component vars. Sizes `.toggle--sm` / `.toggle--lg` retune at 28/36/44 px to match `.btn` cadence. Shape `.toggle--icon-only` + `.toggle--icon-round` mirror `.btn--icon-only` + `.btn--icon-round`. `.toggle-input` is the hidden-input class for form-data paths (`<input type="checkbox|radio">`), visually-hidden geometry copied from `.visually-hidden`, focus + disabled forward to paired label. (2) **`.toggle-group`** = new pill container. Outer 36px default, padding 4px, inner toggle height re-derives as `outer - 2*padding = 28px` via `--toggle-height` override in member scope, inner radius re-derives as `outer - padding` (concentric corners per V3.md §3.4). Members go ghost-rest (transparent bg + transparent border) so the parent rim owns the frame, individual children sit quiet at rest; active member paints highlight bg + stronger border (inherits standalone `.toggle` active rules — no override needed). 6 component vars on the group itself (`--toggle-group-radius` / `-height` / `-padding` / `-gap` / `-bg` / `-border-color`). Sizes `.toggle-group--sm` (28/2/24) / `.toggle-group--lg` (44/4/36). `.toggle-group--vertical` stacks members with `height: auto` + `width: 100%` on children — no vertical compression, inner toggles keep their standalone default height. Single `> .toggle` member selector covers both JS-driven `<button class="toggle">` and form-data `<label class="toggle">` (the label-following-an-input is still a direct child of the group). (3) **`.btn-group`** simplified back to purely visual grouping of action buttons. Stripped: `.btn-check` rules entirely; the `> .btn-check + .btn` adjacency forms in seam dedupe; the `.btn-check`-related z-index lifts. Demo page dropped 3 sections (Toggle group, Tabs/segmented control, Checkbox & radio) — they now live at /toggle-group. Lead paragraph rewritten to point readers at /toggle + /toggle-group for state-bearing patterns. **`_btn.scss` reverted** — removed the `.btn-check:checked + &` synonym from all 8 active-state cascade rules (base, neutral, tertiary, outline, ghost, soft, outline.neutral, ghost.neutral). `.btn-check` is gone; `.toggle-input` forwards via its own component CSS, not via `.btn`. Comment block added near `.btn` base noting the redirect to /toggle and /toggle-group. **Site sidebar** picks up `/toggle-group` between `/toggle` and `/button-group`. **V3.md updates**: Phase 2 component list grew `toggle, toggle-group` entries; the "btn-group covers tabs/segmented" cut note rewrote to "toggle-group single-select mode covers tabs/segmented-control"; new paragraph spells out the toggle-vs-button-group semantic split. **Demo pages**: `/toggle` rewritten from input-driven model to standalone press-button model with JS + form-data examples across 8 sections (How it works / JS-driven press button / Form-data checkbox / Sizes / Icon-only / Icon-round / Disabled / Customization with 12-row table). `/toggle-group` new page with 8 sections (intro pointer / Single-select segmented control / Multi-select / With text labels / Icon + label / Form data radio set / Form data checkbox set / Sizes / Vertical / Customization with 6-row table). `/button-group` shrunk from 11 sections to 8 (lost Toggle group, Tabs/segmented, Checkbox & radio; kept Basic, Outline, Mixed, Sizes, With icons, Split button, Vertical, Toolbar, Customization). **Verification**: build green (41 pages render — gained /toggle-group), bundle 95.4 → 97.8 KB raw (+2.4) / ~15.5 → 15.8 KB gz (+0.3). 0 `--bs-*` leaks; 0 `btn-check` residue in compiled CSS or in any demo HTML (button-group, toggle, toggle-group all clean). All 21 `--toggle-*` / `--toggle-group-*` vars emit; all 11 BEM selectors emit (`.toggle`, `.toggle--sm`, `.toggle--lg`, `.toggle--icon-only`, `.toggle--icon-round`, `.toggle-input`, `.toggle__icon`, `.toggle-group`, `.toggle-group--sm`, `.toggle-group--lg`, `.toggle-group--vertical`). 31 `toggle-group` references + 10 active state markers in the new demo page. **Visual verification pending** — maintainer to eyeball `/toggle`, `/toggle-group`, and `/button-group` in light + dark per the new architecture. **3.6b stabilizes the JS-coordinated family's foundation.** Next: accordion (CSS today via native `<details>`), then with Step 4 alongside JS — modal, offcanvas, dropdown, tooltip, popover, toast, carousel.

- 2026-06-10 — **Step 3.6a — `components/_button-group.scss` rewrite** — first JS-coordinated-family file; CSS lands now, roving-tabindex + click handlers defer to Step 4. ~180-line file from the dead BS5 overlay. **BEM rename map**: `.btn-group-vertical` → `.btn-group--vertical`; `.btn-group-sm` / `.btn-group-lg` → `.btn-group--sm` / `.btn-group--lg`; `.btn-toolbar` stays its own block; `.btn-check` stays; `.dropdown-toggle-split` **dropped** — the split-button caret reuses `.btn--icon-only` with a chevron. **Sizes retune the child `--btn-*` vars in modifier scope** (`.btn-group--sm > .btn { --btn-height: 1.75rem; --btn-padding-x: ...; --btn-font-size: ...; --btn-radius: var(--st-radius-sm) }` + the `--lg` mirror) — no per-size vars on `.btn-group` itself, sizes match standalone `.btn--sm` / `.btn--lg` cadence (28 / 36 / 44 px). **Seam dedupe** via negative `margin-inline-start: -1px` (or `margin-top: -1px` vertical) on every non-first member; inner corners square via `border-radius: 0` on all members; outer corners re-round with logical-property selectors so RTL flips automatically. Two adjacency forms (`> .btn + .btn` and `> .btn + .btn-check + .btn`) cover both plain-button and input-driven markup. **Stacking-order lift on interactive members** — hover → `z-index: 1`, active (`[aria-pressed="true"]`, `[data-state="active"]`, `.btn-check:checked + &`) → 2, focus-visible → 3; without it the seam dedupe buries the active member's border under its sibling. **`_btn.scss` touch** — single-pass edit added `.btn-check:checked + &` as a synonym alongside `[aria-pressed="true"]` / `[data-state="active"]` across all 8 active-state cascade rules (base, neutral, tertiary, outline, ghost, soft, outline.neutral, ghost.neutral). A checked `<input>` now paints its `<label class="btn">` through every tone × shape combo automatically — no duplicated color-mix formulas in button-group. **`.btn-check` geometry** copied from `.visually-hidden` (position: absolute, 1×1px, clip rect 0, margin -1px) — `display: none` / `visibility: hidden` would drop the input from the a11y tree and tab order. Focus + disabled forward to the visible label via sibling-combinator selectors. **`.btn-toolbar` — separate block** with `--btn-toolbar-gap: 0.5rem` default; flex-wrap + gap. **Tabs / segmented control** demo added — `role="tablist"` wrapper + `role="tab"` members + `data-state="active"` on the current one. `.btn`'s existing `[data-state="active"]` cascade paints it; no new class. **The base `.btn` mechanic** (color-mix toward black for filled tones, toward background for tertiary, tint-on-transparent for outline/ghost/soft) handles every tabs/toggle paint case — button-group stays purely about layout. **Split-button** demoed visually only: primary `.btn` + `.btn--primary .btn--icon-only` chevron trigger with `aria-haspopup="menu"` + `aria-expanded="false"`. Dropdown attach lands with Stisla.Dropdown in Step 4. **2 component vars** (`--btn-group-radius` + `--btn-toolbar-gap`) — single ungrouped Customization table plus a pointer paragraph linking to `/buttons` for the child `.btn` surface. **Bundle**: `_button-group.scss` uncommented at line 51 of `bundles/stisla-full.scss`. **Demo page sweep** — 11 sections (Basic / Outline / Mixed / Sizes / Toggle group / Tabs+segmented (new) / Checkbox & radio / With icons / Split button / Vertical / Toolbar) + Customization. BS5 vocab swept: `.btn-primary` → `.btn .btn--primary`, `.btn-outline-secondary` → `.btn .btn--outline .btn--neutral`, `.btn-icon` → `.btn--icon-only`, `data-bs-toggle="button|dropdown"` removed, `.active` → `aria-pressed="true"`. BS5 utilities → inline `style="..."` per the v3 pattern. **Verification**: build green (40 pages render), bundle 90.2 → 95.3 KB raw (+5.1) / ~15.0 → 15.5 KB gz (+0.5). 0 `--bs-*` leaks; 0 BS5 leftover names in rendered HTML; all 4 BEM root selectors + `.btn-check` + `.btn-toolbar` emit; both `--btn-group-*` / `--btn-toolbar-*` vars emit. 34 `btn-group` references + 3 each of `--sm` / `--lg` / `--vertical`, 13 `btn-check`, 4 active-state markers in the rendered demo. Site sidebar already pointed at `/button-group`. **Visual verification pending** — eyeball in light + dark across the 11 sections, focus halos lift above seams, dark-mode active states stay perceptibly darker than rest. **3.6a opens the JS-coordinated family.** Remaining queue: accordion next (CSS today via native `<details>`), then with Step 4 alongside JS — modal, offcanvas, dropdown, tooltip, popover, toast, carousel.

- 2026-06-10 — **Step 3.5e — `components/_progress.scss` write** — fifth and final Display-family file (~140 lines, **pure write** from the 0-byte placeholder — no BS5 reference to port; V3.md §5 risk #1 retired). **Anatomy** — two-element BEM, single-bar only: `.progress` (track, block-level, `overflow: hidden`, pill, recessed into neutral tier) + `.progress__bar` (filled portion, block child, sized by inline `style="width: X%"`, default `width: 0` so authored-without-inline-width bars sit invisible rather than filling the whole track via block elements' `width: auto`). BS5's `.progress-bar` collapses to `.progress__bar` (canonical BEM `block__element`); the BS5 stripe variants (`.progress-bar-striped` + `.progress-bar-animated`) drop entirely. **BEM rename map**: `.progress-bar` → `.progress__bar`; `.bg-{success,warning,danger,info}` utility recolor (BS5) → `.progress__bar--{success,warning,danger,info}` element-modifiers (and `.progress__bar--primary` exists as a no-op for explicit-intent symmetry — same as the `.btn--primary` and `.alert--primary` shape); `.progress-bar-striped` + `.progress-bar-animated` **dropped wholesale**; `.progress-stacked` (BS5 wrapper that nested `.progress` inside `.progress`) **dropped wholesale** — and so is the stacked/segmented use case entirely (maintainer call: "I don't see any use cases for this"). The track is laid out `display: block` not `display: flex` because there's only ever one bar inside; the bar defaults to `width: 0` (block elements without explicit width would otherwise fill 100% of the parent). If a future use case shows up, a separate component (`.progress-stack` or similar) carries the segmented model cleanly without weighing the single-bar path down with flex semantics it doesn't need. **Size modifiers**: `.progress--sm` (4px) + `.progress--lg` (16px), default 8px — retune `--progress-height` in modifier scope (no per-size vars to maintain, same retune-existing-vars pattern as `.btn--sm` / `.pagination--sm`). Sizes ship at 4 / 8 / 16 px under default density — modern thin-progress cadence (shadcn-aligned), not BS5's chunky default 16px. **Animated treatment — intentional departure from BS5**: BS5's `.progress-bar-animated` ran a constant diagonal-stripe scroll (treadmill aesthetic). v3 ships **a soft sheen that sweeps across the fill every 3 seconds** then sits off-screen for the remainder of the cycle — a quiet "still working" pulse, not a constant scroll. Maintainer call ("rare, not constant"); 3s cycle picked over 5s default because BS5's habit of constant motion sets the user expectation that "moving = working" and a slower-than-3s cadence would read as broken. Implementation: `.progress__bar--animated::after` is a `position: absolute; inset: 0` narrow `linear-gradient(90deg, transparent 0%, var(--progress-shimmer-color) 50%, transparent 100%)` band that animates `transform: translateX(-100%) → translateX(100%)` during the first 33% of the cycle (1s of 3s), then sits at `translateX(100%)` for the remaining 67% (2s). The snap from `100%` back to `-100%` at loop boundary is invisible because both endpoints are off-screen. Shimmer color is `color-mix(in oklch, white 30%, transparent)` which reads on every intent in both themes (a white-tinted sheen on primary blue, success green, danger red — all stay legible). The bar needs `overflow: hidden` so the shimmer band clips to bar bounds rather than extending into the unfilled track; the track already has `overflow: hidden` for the pill clip, so this is the second clip layer (track clips to its rounded shape; bar clips its own contents). Striped variant **dropped** — the v3 take is "animated = active right now" with no static-stripe sibling; orthogonal-modifier visual vocabulary thinned to one direction. **Indeterminate state hook (V3.md §3.6 — Stisla-original, no Radix equivalent)**: `.progress.is-indeterminate` forces the bar to `width: 50%` and animates `transform: translateX(-100%) → translateX(200%)` over 1.5s `ease-in-out infinite`. Single shadcn-style slider (one bar sweeping edge to edge), not Material's two-segment chase (which is heavier visually and harder to read at small sizes). The geometry argument: bar is 50% of track width, so `translateX(-100%)` shifts by 100% of bar's own width = 50% of track, landing the bar's right edge at track's left edge (start position); `translateX(200%)` shifts by 200% of bar's own width = 100% of track, landing the bar's left edge at track's right edge (end position) — bar fully crosses edge-to-edge. The shimmer overlay is suppressed in indeterminate (`.progress.is-indeterminate .progress__bar::after { display: none }`) — the moving bar IS the indicator, a sheen on top would be noise. ARIA contract: indeterminate markup drops `aria-valuenow` (keeps `role="progressbar"`, `aria-valuemin`, `aria-valuemax`) — the absence of `aria-valuenow` is the standard signal for "indeterminate" in WAI-ARIA. **Radius opt-out**: `.progress` defaults `--progress-radius: 9999px` (pill, not `var(--st-radius)`) — added to V3.md §3.4 opt-out list with rationale "pill is intentional for a progress strip". Sits alongside `.badge`, `.avatar`, `.spinner`, `.form-check-input`, `.slider`, `.btn--icon-round` — six other components where shape is semantic not stylistic. Brutalist preset users who want sharp progress override `--progress-radius` per-component. Browser caps radius at half-dimension on small heights so the pill renders cleanly across all three sizes (4 / 8 / 16 px) without overflow. **Track bg picks `--st-neutral`**: same family as `.btn--neutral`, `.kbd`, default `.badge`, input-group addons — the "rest fill weight for static neutral surfaces" semantic from V3.md §3.3, not `--st-accent` (transient hover) or `--st-surface-3` (deepest tier — would conflict with cards-on-cards stacks). Bar bg default `--st-primary` (most progress bars are primary intent; intent modifiers actively override). **Width transition**: `--progress-bar-transition-duration: 0.6s` on the bar so JS-driven width changes tween smoothly (upload going from 30% → 50% animates rather than jumps). Reduced-motion zeros all three motions: the width transition on `.progress__bar`, the shimmer animation on `.progress__bar--animated::after`, AND the indeterminate animation on `.progress.is-indeterminate .progress__bar` (single `@media (prefers-reduced-motion: reduce)` block, two rules — one for `transition: none` on the bar, one for `animation: none` on both animated/indeterminate selectors). **9 component vars** in 4 conceptual groups: Geometry (3: `height`, `radius`, `margin-bottom`), Track (1: `bg`), Bar (2: `bar-bg`, `bar-transition-duration`), Animation (3: `shimmer-color`, `shimmer-duration`, `indeterminate-duration`). Single ungrouped Customization table at 9 rows (well under the 20-var grouped-table threshold from slider.njk), prose-styled per slider reference. Note: the `--progress-bar-*` var name pattern is the same convention used in `.pagination` (`--pagination-button-*`) — flat one-level prefix-element pattern, not nested. **Bundle**: `_progress.scss` uncommented at line 64 of `bundles/stisla-full.scss`. Compiles clean. **Demo page (`src/site/pages/progress.njk`) sweep** — full BS5 → v3 rewrite, 6 sections (Example / Basic / Intents / Sizes / Animated / Indeterminate) + Customization. Sections cut: **Labels** (no internal label support in v3 — labels live externally as markup the consumer owns; a 4 / 8 px bar can't fit text legibly anyway, and a 16px lg bar with internal text crowds against the bar's curved end); **Height** (collapsed into Sizes — three named sizes replace freeform `--bs-progress-height` overrides since the only common sizes are sm/default/lg); **Striped** (dropped entirely per "drop it" maintainer call); **Animated stripes** (replaced by the new Animated section showing the sheen treatment). Sections added: **Indeterminate** (new — BS5 had no indeterminate progress, consumers had to byo). **BS5 vocab swept**: `.progress-bar` → `.progress__bar` (every demo); `.bg-{success,warning,danger,info}` → `.progress__bar--{success,warning,danger,info}` element-modifiers; `--bs-progress-height` inline overrides → `.progress--sm` / `.progress--lg` modifier classes; `.progress-stacked` → dropped (stacked use case cut wholesale, see anatomy note above); `.progress-bar-striped` + `.progress-bar-animated` → `.progress__bar--animated` (single modifier replaces the chained-pair); `.fw-medium` kept (already v3-aligned via utility set); `.text-body-secondary` (BS5) → `.text-muted-foreground` (per `[[feedback_muted_text_utility]]`); `.d-flex justify-content-between mb-1` (BS5) → inline `style="display: flex; justify-content: space-between; margin-bottom: 0.25rem"` (matches the pagination / list-group / breadcrumb / navbar inline-style pattern since v3 doesn't ship flex utilities yet); `.d-flex flex-column gap-3 w-100` → inline `style="display: flex; flex-direction: column; gap: 1rem; width: 100%"`. **Verification**: build green (40 pages render), bundle 88.1 → 90.2 KB raw (+2.1) / ~15043 → 15070 B gz (+27 B, rounds to flat 14.7 KB) — small delta because the file is small and the two `@keyframes` blocks are the main weight (`progress-shimmer` + `progress-indeterminate` plus the 9 var declarations + 5 intent modifiers + 2 size modifiers + indeterminate rules + reduced-motion block). 0 `--bs-*` leaks in compiled CSS. 0 BS5 leftover names in rendered HTML or compiled CSS — the initial substring grep flagged `progress-bar-` in `progress.html` but both hits resolved to the v3 `--progress-bar-bg` and `--progress-bar-transition-duration` var names in the Customization table prose (tighter grep excluding `--progress-bar-*` vars returns empty). All 9 documented `--progress-*` vars emit (verified by `grep -oE -- "--progress-[a-z-]+"` across the compiled stylesheet). All 12 BEM selectors emit (`.progress`, `.progress--sm`, `.progress--lg`, `.progress.is-indeterminate`, `.progress__bar`, `.progress__bar--primary`, `.progress__bar--success`, `.progress__bar--warning`, `.progress__bar--danger`, `.progress__bar--info`, `.progress__bar--animated`, and the indeterminate-suppression rule). 57 `progress__bar` references in rendered HTML across the 6 demo sections + escaped-source duplicates via the demo viewer; 5 `is-indeterminate`; 5 `progress__bar--animated`; 16 intent-modifier bars. Sass deprecation warnings unchanged (forked BS5 mixins, pre-existing noise). Site sidebar already pointed at `/progress` (no rename needed). **V3.md §3.4 update**: added `.progress` to the radius opt-out list ("pill is intentional for a progress strip") alongside the six existing opt-outs. **Visual verification pending** — maintainer to eyeball `/progress` in light + dark: (a) Example — Uploading 72% with bar painted primary, "72%" muted on the trailing edge; (b) Basic — 5 stacked rows at 0/25/50/75/100% with default 8px height; (c) Intents — 5 rows at 60% each, primary / success / warning / danger / info; (d) Sizes — three rows at 4 / 8 / 16 px under default density; (e) Animated — two rows (primary + success), each showing the sheen sweep ~1s every 3s with 2s pause between sweeps; (f) Indeterminate — two rows (default + lg success), each with a half-width bar sliding edge-to-edge on 1.5s loop; (g) Customization — 9-row table reads cleanly; (h) Dark mode — track stays on `--st-neutral` (light neutral retints under dark), intent bars stay vivid via OKLCH tokens. **Step 3.5e closes the Display family.** Five files done (3.5a table, 3.5b list-group, 3.5c breadcrumb, 3.5d pagination, 3.5e progress). Remaining Phase-2 components are JS-coordinated and interleave with Step 4: button-group, modal, offcanvas, dropdown, tooltip, popover, accordion, toast, carousel.

- 2026-06-10 — **Step 3.5d — `components/_pagination.scss` rewrite** — fourth Display-family file (~180 lines, full rewrite from the BS5-coupled 49-line overlay which relied on BS5 Sass vars + the BS5 `_pagination.scss` source for the conjoined-bordered base shape — the v3 BS5 overlay had already flattened that to a standalone-chip rail visually, but every Sass var it patched was a BS5 emit; with BS5 unbundled in Step 2 the file was dead). **BEM rename map**: `.page-item` (the `<li>` wrapper) + `.page-link` (the chip) → **collapsed to one name** `.pagination__button` on the interactive element directly. The `<li>` carries no BEM hook because it has no visible role — the chip IS the styled thing. Matches the sidebar / navbar `__button` cross-component vocab (both use `__button` for "focusable chip in a nav rail"); pagination is the third in that family. Breadcrumb is the outlier and rightly so — it kept `__item` on the `<li>` because the chevron `::before` had to live on the container holding both link + divider; pagination has gap-only spacing with no divider, so no item-container needed. **New element**: `.pagination__ellipsis` for the truncation marker between page ranges (`1 2 3 4 5 … 12`). Same chip footprint (height + min-width === `--pagination-button-height`) so the row rhythm holds, but non-interactive — no hover, no focus, `pointer-events: none`, `user-select: none`, color tracks `--pagination-button-disabled-color`. Distinct semantic role from `__button` (a `<span aria-hidden="true">…</span>` is not "a chip with hidden content" — it's a visual gap indicator), worth the second name. **Size modifiers**: `.pagination-sm` → `.pagination--sm`, `.pagination-lg` → `.pagination--lg`. Modifier scope retunes `--pagination-button-height` + `--pagination-button-padding-x` + `--pagination-font-size` — no per-size vars to maintain, same retune-existing-vars pattern as `.btn--sm` / `.btn--lg`. Sizes ship at 28 / 36 / 44 px under default density to match the button cadence so a pagination row baselines with a "Load more" button. **Alignment modifiers**: `.justify-content-center` / `.justify-content-end` (BS5 utilities — don't exist in v3) → `.pagination--center` / `.pagination--end`. Two-line `justify-content` flips, cleaner than asking consumers to inline-style on the `<nav>` wrapper. **State hooks (V3.md §3.6)**: `.pagination__button[data-state="active"]` + `[aria-current="page"]` → `--st-highlight` bg + `--st-highlight-foreground` color (matches sidebar / list-group / table — current page is persistent selected state). `.pagination__button[aria-disabled="true"]` + `:disabled` → `--st-muted-foreground`, `pointer-events: none`. `.active` and `.disabled` BS5 escape hatches dropped. **Critical correction**: the v3 BS5 overlay routed both hover AND `.active` to `--bs-accent-bg` with the comment "selected page reads the same as a hover dwell because chips are toggleable, not destinations" — the v3 rewrite splits the trio properly per the established `--st-accent` (transient hover) vs `--st-highlight` (persistent selected) routing across sidebar / list-group / table / breadcrumb. The semantic argument: a current-page chip is persistent state, the user *is* there; that's exactly the highlight semantic, distinct from the lighter feedback of hover. Matches Radix / shadcn convention too. **Interactive opt-in via element selector**: `a.pagination__button, button.pagination__button` get `cursor: pointer` + `:hover` paint + `:focus-visible` ring. `<span class="pagination__button" aria-disabled="true">` (used for edge markers when the user is at start/end of range) sits quiet — no hover light-up. Same pattern as list-group 3.5b's interactive-row derivation; cleaner than BS5's `.page-item.disabled` `<li>` wrapper class. **Height architecture (V3.md §3.4)**: hard `height: var(--pagination-button-height)` + `padding-block: 0` + `min-width: var(--pagination-button-height)`. Single-glyph chips ("1") land as squares at the cadence height; longer labels ("Previous") grow from `padding-inline`. Same single-line height-aware-component pattern as `.btn` + `.form-control`. Density genuinely shrinks the chip (a 0.875 density yields 31.5 px chips, not a 36 px shell with shrunk padding). **Focus halo**: 2px outline at `--pagination-button-ring` with **positive `outline-offset: 2px`** because `.pagination` has no `overflow: hidden` (chips are gap-laid-out flex on a transparent row, nothing clips). Same as breadcrumb 3.5c — different from list-group's inset trick which exists only because list-group's `overflow: hidden` clips outer outlines. **Icons inside chips**: `> :is(svg, i) { width: 1em; height: 1em; flex-shrink: 0 }`. The `1em` (not `1rem`) ties icon size to the chip's font-size so `.pagination--sm` shrinks icons proportionally with the label and `.pagination--lg` grows them — no per-size icon override needed. **16 component vars + 1 internal**: Layout (3: `gap`, `margin-bottom`, `font-size`), Chip geometry (4: `button-height`, `button-padding-x`, `button-inner-gap`, `button-radius`), Chip surface (3: `button-bg`, `button-color`, `button-border-color`), Chip states (5: `button-hover-bg`, `button-hover-color`, `button-active-bg`, `button-active-color`, `button-disabled-color`), Focus (1: `button-ring`). Internal `--pagination-transition` is emitted but hidden from the Customization table (same pattern as list-group's internal `--list-group-transition`). All density-relevant geometry wraps `calc(X * var(--st-density))`. Single ungrouped 16-row Customization table per slider.njk reference (under the 20-var threshold). **Reduced-motion**: zeros the `transition: background-color .12s ease, color .12s ease` on `.pagination__button`. **Bundle**: `_pagination.scss` uncommented at line 60 of `bundles/stisla-full.scss`. Compiles clean. **Demo page (`src/site/pages/pagination.njk`) sweep** — 6 sections (Basic / With icons / Disabled / Ellipsis (new) / Sizes / Alignment) + Customization. All `.page-item` `<li>` classes dropped; all `.page-link` → `.pagination__button` swept; `.active` → `data-state="active"` + `aria-current="page"`; `.disabled` `<li>` wrapper → `<span class="pagination__button" aria-disabled="true">` (drops `href` so screen readers don't announce as a link — better a11y than the BS5 demo which kept the `<a>`); `mb-0` dropped (component owns the margin via `--pagination-margin-bottom: 0` default — eats the BS5 stutter); `pagination-sm` / `pagination-lg` → `pagination--sm` / `pagination--lg`; `.d-flex flex-column gap-3 w-100` wrapping divs → inline `style="display: flex; flex-direction: column; gap: 1rem; width: 100%"` (matches list-group / breadcrumb / navbar inline-style pattern since v3 doesn't ship flex utilities yet); `.justify-content-center` / `.justify-content-end` → `.pagination--center` / `.pagination--end`. **New ellipsis section** shows the `1 2 3 4 5 … 12` truncation pattern with prev/next chevron-only chips bracketing the row. Lead paragraph kept verbatim ("A row of page chips for moving through a long list or paginated view.") — already clean v3 voice. Customization section ships a single ungrouped 16-row prose-styled table per slider.njk reference. **Verification**: build green (40 pages render), bundle 87.2 → 88.1 KB raw (+0.9) / ~14.4 → 14.7 KB gz (+0.3) — smallest delta in the display family alongside breadcrumb, expected for a single-element chip component with no layered-bg mechanic. 0 `--bs-*` leaks in compiled CSS. 0 BS5 leftover names (`page-item`, `page-link`, `pagination-sm`, `pagination-lg`, `.disabled` on `<li>`, `.active` on `<li>`) in rendered HTML or compiled CSS; the single `justify-content` substring hit in `pagination.html` is in the Customization section prose explaining the `--center` / `--end` modifiers, not a class leak. All 16 documented `--pagination-*` vars emit (verified by `grep -oE -- "--pagination-[a-z-]+"` across the compiled stylesheet) + the internal `--pagination-transition`. All 7 BEM selectors emit (`.pagination`, `.pagination--sm`, `.pagination--lg`, `.pagination--center`, `.pagination--end`, `.pagination__button`, `.pagination__ellipsis`). 97 `pagination__button` references in rendered HTML (across the 6 demo sections + escaped-source duplicates via the demo viewer), 11 `data-state="active"`, 11 `aria-current="page"`, 2 `aria-disabled="true"`, 3 `pagination__ellipsis`, 3 each of `--sm` / `--lg` / `--center` / `--end`. Sass deprecation warnings unchanged (forked BS5 mixins, pre-existing noise). Site sidebar already pointed at `/pagination` (no rename needed). Visual verification pending — maintainer to eyeball `/pagination` in light + dark: (a) Basic — 5-chip row with chip 2 highlighted in primary tint; hover any neighbor → soft accent fill; (b) With icons — chevron icons pin to label baseline, scale with the chip's font; (c) Disabled — Previous chip greyed out with no pointer; (d) Ellipsis — `1 2 3 4 5 … 12` with the dots reading muted between chip 5 and chip 12 at the same height as the chips, prev/next chevron-only chips at the edges; (e) Sizes — three rows at 28 / 36 / 44 px under default density, icons scale with font automatically; (f) Alignment — center and end rows shift across the demo viewer width; (g) Focus — tab onto any chip and the 2px outline paints at `--st-ring` with positive offset, no clipping; (h) Customization — 16-row table reads cleanly with the Variable / Default / Use columns; (i) Dark mode — accent + highlight track the dark block; chip text inverts naturally via `--st-foreground` / `--st-highlight-foreground`. **3.5d closes the fourth Display-family file.** Remaining display queue: progress (the 0B file — pure write, no port reference); toast + accordion are JS-coordinated and slot into Step 4 interleave.

- 2026-06-10 — **Step 3.5c — `components/_breadcrumb.scss` rewrite** — third Display-family file (~125 lines, full rewrite from the BS5-coupled 54-line overlay which depended on the BS5 `_breadcrumb.scss` source for the base shape — display/padding/gap/list-style/divider-gap math all came from BS5; with BS5 unbundled in Step 2 the overlay was dead code anyway). Billed as a "port" in §4's disposition table but in practice a from-scratch rewrite on the v3 model — same pattern as 3.5b list-group (which was also marked port-ish but turned into a full rewrite). **BEM rename map**: `.breadcrumb` → `.breadcrumb` (root unchanged); `.breadcrumb-item` → `.breadcrumb__item` (proper BEM element); `.breadcrumb-item.active` → `.breadcrumb__item[aria-current="page"]` — the `.active` BS5 escape hatch is gone (matches list-group 3.5b and sidebar 3.4d's drops); `--bs-breadcrumb-divider` → `--breadcrumb-divider` (component-scoped, override on `.breadcrumb` itself, on the `<nav>` wrapper via inline `style=""`, or on any ancestor — cascade scopes the change). **State hooks (V3.md §3.6)**: `[aria-current="page"]` only — **no `[data-state="active"]`**. Breadcrumb's "current page" is full-emphasis text, not a highlight chip. The Radix-aligned `data-state` is appropriate when the active state paints chrome (sidebar / table row / list-group row / pagination — all use `--st-highlight`); breadcrumb just bumps the trail color from `--st-muted-foreground` up to `--st-foreground`. The a11y attr is the entire signal — no chrome flip means no chrome hook. **Interactional trio routing inverted vs. row-style components**: `--breadcrumb-color` rests at `var(--st-muted-foreground)` (NOT `--st-foreground` like rows / chips), `--breadcrumb-hover-color` lifts to `var(--st-foreground)` (NOT `--st-accent` bg), `--breadcrumb-active-color` lands at `var(--st-foreground)` (NOT `--st-highlight` bg). Because breadcrumb is *text-only* visual hierarchy, all three states route through the color knob — never the bg knob. This is the same shape `.link` ships with in `_utilities.scss` per the Preflight+prose foundation pass: text controls signal hover/active via color, not via background-fill chips. **Chevron uses `fill='currentColor'`** — the BS5 overlay's SVG baked `#6c757d` in (gray-600), which read flat under dark mode and ignored retint scopes. `currentColor` makes the divider inherit `--breadcrumb-color` automatically, so the chevron tracks the trail color through every theme + retint + hover. Same `currentColor` lesson the leading icon learned in list-group 3.5b — once the SVG paints the right way, zero per-state CSS overrides are needed. The string-divider demo (`'/'`) and the embedded-SVG-arrow demo also got swept to `currentColor` for consistency, with prose noting consumers can hardcode a hex if they want a tone that ignores the cascade. **Focus halo on links** — `:focus-visible` paints `outline: 2px solid var(--st-ring); outline-offset: 2px; border-radius: var(--st-radius-sm)` on `.breadcrumb__item > a`. The BS5 overlay shipped zero focus styling — a gap that's now closed across the family (table, list-group, breadcrumb all paint focus rings on interactive elements). Outline-offset is *positive* here (not the inset trick `.list-group` uses) because `.breadcrumb` doesn't have `overflow: hidden` — the trail is a horizontal flex band with no outer frame to clip the halo. **Single-gap layout** — `display: flex; flex-wrap: wrap; gap: var(--breadcrumb-gap)` on the root + `display: inline-flex; gap: var(--breadcrumb-gap)` on each `.breadcrumb__item`. One `--breadcrumb-gap` var (default `calc(0.5rem * var(--st-density))`) serves both item↔item spacing AND within-item (divider↔text, icon↔text) spacing. Cleaner than BS5's dual-padding trick where `.breadcrumb-item + .breadcrumb-item` gets `padding-inline-start` for divider-leading-space and the `::before` content gets `padding-inline-end` for divider-trailing-space — two knobs that always have to stay equal. The flex `gap` model handles both sides with one knob. The chevron `::before` lives at `.breadcrumb__item + .breadcrumb__item::before` (every crumb past the first); parent flex spacing wraps around it on both sides. **Icon support inside crumbs** — `> :is(svg, i), > a > :is(svg, i)` pins to `--breadcrumb-icon-size: 1rem` with `flex-shrink: 0`. Covers icons sitting bare in the crumb (current page where there's no `<a>` to nest under) AND icons inside the link. Color tracks `currentColor` so the icon brightens on hover with the link text — same one-rule pattern as list-group 3.5b. **12 component vars, single ungrouped Customization table** (under the 20-var threshold from the slider.njk customization-table memory rule): Layout (5: `padding-y`, `padding-x`, `gap`, `margin-bottom` — default 0, eats the `mb-0` BS5 stutter the old demos all carried, `font-size`); Surface (3: `bg` transparent, `radius` 0 — pill-wrap is opt-in, `color`); States (3: `hover-color`, `active-color`, `divider`); Icon (1: `icon-size`). All density-relevant geometry wraps `calc(X * var(--st-density))` — only `--breadcrumb-gap` actually wraps because every other geometry default is 0 or `inherit`. The Customization section documents pill-wrap (`bg` + `radius` + `padding-*`) in the table prose without a dedicated demo — the table-as-documentation pattern. **Reduced-motion** zeros the `transition: color 150ms ease` on link hover. **Bundle**: `_breadcrumb.scss` uncommented at line 59 of `bundles/stisla-full.scss`. Compiles clean. **Demo page (`src/site/pages/breadcrumb.njk`) sweep** — kept all 5 sections (Basic / Multiple levels / With icon / String divider / Embedded SVG divider) + added Customization. All `.breadcrumb-item` → `.breadcrumb__item` swaps, `.active` dropped, `mb-0` dropped (component owns the margin via `--breadcrumb-margin-bottom: 0` default), `--bs-breadcrumb-divider` → `--breadcrumb-divider` (2× in the string + embedded SVG sections), embedded-SVG demo's `stroke='%236c757d'` swapped to `stroke='currentColor'` for cascade-tracking. Lead paragraph trimmed to "A trail of links showing where the user is inside the page hierarchy" per docs voice rule. Customization section ships a single ungrouped 12-row prose-styled table per slider.njk reference (4-group split would apply above ~20 vars; below threshold the flat table reads cleaner — same call as list-group 3.5b at 16 vars). **Verification**: build green (40 pages render), bundle 85.0 → 87.2 KB raw (+2.2) / ~14.1 → 14.4 KB gz (+0.3) — smallest delta in the display family so far, expected for a 125-line file with no layered-bg mechanic. 0 `--bs-*` leaks in compiled CSS. 0 BS5 leftover names (`breadcrumb-item`, `--bs-breadcrumb-divider`, `class="active"` on a crumb) in rendered HTML or compiled CSS. All 12 `--breadcrumb-*` vars emit (verified by `grep -oE -- "--breadcrumb-[a-z-]+"` across the compiled stylesheet). Both BEM selectors emit (`.breadcrumb`, `.breadcrumb__item`). 28 `breadcrumb__item` references in rendered HTML (5 demo sections × ~3 crumbs each × 2 for preview + escaped-source duplicate via the demo viewer). 7 `aria-current="page"` (one per current-page crumb across the 5 sections + the Customization table cell that references the attr name). Sass deprecation warnings unchanged (forked BS5 mixins, pre-existing noise). Visual verification pending — maintainer to eyeball `/breadcrumb` in light + dark: (a) Basic — trail crumb muted, current crumb full body color, chevron between them; (b) Multiple levels — three crumbs separated by chevrons, current at the end full-emphasis; (c) With icon — leading `<i data-lucide="house">` pins to 1rem, tracks the crumb's color through hover (mouse over Home → icon + text both lift to `--st-foreground`); (d) String divider — `/` glyph between crumbs instead of chevron, tracks `currentColor` via the parent color; (e) Embedded SVG divider — arrow glyph instead of chevron, also tracks `currentColor` because the demo's `stroke='currentColor'`; (f) Customization — 12-row table reads cleanly with the Variable / Default / Use columns; (g) Hover any trail link — color lifts from muted to body color smoothly (`transition: color 150ms ease`); (h) Tab onto any trail link — focus halo at `--st-ring` paints with 2px offset and small radius, doesn't clip; (i) Dark mode flip — muted trail uses `--st-muted-foreground` from the dark block (around oklch 0.71), body current crumb at `--st-foreground` (oklch 0.97); chevron tracks both via currentColor. **3.5c closes the third Display-family file.** Remaining display queue: pagination (rewrite — Radix `[data-state="active"]` for current page), progress (the 0B file — pure write, no port reference); toast + accordion are JS-coordinated and slot into Step 4 interleave.

- 2026-06-10 — **Step 3.5b — `components/_list-group.scss` rewrite** — second Display-family component (~210 lines, full rewrite from the BS5-coupled overlay which was a 36-line file that only patched what BS5 vars couldn't reach + relied on the BS5 `_list-group.scss` SCSS source for the base shape; v3 rewrites the entire shape from scratch on the new model). **BEM rename map**: `.list-group-item` → `.list-group__item`; `.list-group-flush` → `.list-group--flush`; `.list-group-horizontal` + `.list-group-horizontal-{sm,md,lg,xl,xxl}` → `.list-group--horizontal` + `.list-group--horizontal-{sm,md,lg,xl,xxl}` (root modifiers — V3.md §3.6); `.list-group-numbered` → `.list-group--numbered`; `.list-group-item-{primary,success,info,warning,danger}` → `.list-group__item--{intent}` (proper element-modifier, applied to the row); `.list-group-item-secondary` → `.list-group__item--neutral` (routes through `--st-neutral` for retint, parity with `.table__row--neutral`); `.list-group-item-{light,dark}` cut entirely (V3.md killed light/dark color slots). `.list-group-item-action` opt-in **dropped** — the v3 model derives interactivity from the element instead: `a.list-group__item, button.list-group__item` get hover + focus-visible + cursor: pointer automatically, static rows (`<li>`, `<div>`) sit quiet. Cleaner ergonomics than BS5's opt-in class — semantic markup gets the right behavior with zero ceremony. **State hooks (V3.md §3.6)**: `.list-group__item[data-state="active"]` (persistent selected row, Radix-aligned — same hook as sidebar / table / dropdown); `.list-group__item[aria-current="page"]` + `[aria-current="true"]` (nav-link a11y, also paints highlight); `.list-group__item[aria-disabled="true"]` + `:disabled` (unreachable rows). `.active` (BS5 escape hatch) is gone. **Interactional trio routing** matches sidebar / table: `--list-group-item-hover-bg → var(--st-accent)`, `--list-group-item-active-bg → var(--st-highlight)`. Active beats hover via source-order specificity (`.list-group__item[data-state="active"]` at (0,2,0) beats `a.list-group__item:hover` at (0,1,2)) so hovering an already-selected row keeps the highlight surface instead of flashing back to accent. **No layered-slot mechanic** like `.table` — list-group items only stack at most 2 layers (variant tint + state), and the state always wants to fully override the tint (a status-tinted row that becomes the selected row should read as selected, not as "selected over tinted"). Direct `background-color` assignment per state is simpler than the inset-box-shadow chain and emits cleaner CSS. **Contextual variants** use the same `color-mix(in oklab, var(--st-{intent}) 10%, var(--list-group-bg))` recipe as `.table__row--{intent}` — important detail: `oklab` not `oklch` because `--list-group-bg` here is `--st-surface` (a chroma-0 neutral); `oklch` would pull the mix toward hue 0 (red) shortest-arc on every intent, while `oklab` has no hue wheel and preserves each intent's hue direction. Same fix-once-applies-everywhere lesson from 3.5a. **Focus halo on interactive rows**: `outline: 2px solid var(--list-group-ring); outline-offset: -2px; z-index: 1`. Inset (negative offset) because `.list-group` has `overflow: hidden` (same trick as `.card` — clips square inner-row corners to the rounded outer frame), and a 2px-out outline would get clipped at the top/bottom edges. The `z-index: 1` on the focused row (with `position: relative` baseline on `.list-group__item`) lifts the bottom-edge outline above the next row's top divider so the halo paints fully visible. **Leading icon convention preserved**: first-child `<svg>` / `<i>` pins to `--list-group-item-icon-size: 1rem` with `flex-shrink: 0` and `color: currentColor`. The BS5 file had to override icon color manually on hover/active because BS5 set `color: var(--bs-secondary-color)` on the icon explicitly; v3's `currentColor` inherits the row's color through every state automatically. One rule, zero hover overrides. **Card auto-integration** — `.card > .list-group` (direct child only) drops outer border-inline + radius, plus `:first-child`/`:last-child` drop the top/bottom edge so the list-group merges into the card frame seamlessly. Consumer drops `<ul class="list-group">` directly into `<div class="card">` and the chrome auto-resolves; no need for explicit `--flush`. BS5 demanded `--flush` for the in-card case; v3 inherits the better ergonomic from the card's `overflow: hidden` and a small selector. `--flush` still ships for non-card parents (popover, sidebar panel). **16 component vars across 4 groups** — Geometry (5: `radius`, `item-padding-y/x`, `item-gap`, `item-icon-size`), Surface (5: `bg`, `color`, `border-color`, `border-width`, `divider-color`), Item states (5: `hover-bg`, `hover-color`, `active-bg`, `active-color`, `disabled-color`), Focus (1: `ring`). All density-relevant geometry wraps `calc(X * var(--st-density))`. Below the 20-var threshold so a single ungrouped Customization table fits per the slider.njk reference shape. **Bundle**: `_list-group.scss` uncommented at line 65 of `bundles/stisla-full.scss`. Compiles clean against `quiet-deps` sass run. **Demo page rewritten** — 13 sections: Basic / Active and disabled / Links and buttons / Flush / Numbered / Horizontal / With badge / Contextual variants / Custom content / Contacts / Settings / Payment methods / Customization. BS5 utilities (`.d-flex`, `.justify-content-between`, `.align-items-center`, `.gap-3`, `.flex-grow-1`, `.flex-shrink-0`, `.flex-column`, `.text-body-secondary`, `.rounded-circle`, `.text-truncate`, `.py-3`, `.w-100`, `.m-0`, `.mb-1`, `.mb-0`, `.text-bg-primary`, `.badge-light-secondary`, `.btn-outline-danger`) all swept — replaced with inline `style="…"` blocks (the same pattern navbar + sidebar + table demos used), the `.text-muted-foreground` utility per the muted-text memory rule, `.fw-medium` for emphasized labels, the v3 BEM components inline (`.badge .badge--primary` / `.badge .badge--soft`, `.switch .switch__input .switch--lg`, `.form-control .form-control--sm`, `.btn .btn--outline .btn--danger .btn--sm`, `.card .card__header`). **Settings demo updated** to leverage the new card auto-integration — `<ul class="list-group">` (no `--flush`) as a direct child of `<div class="card">` produces the seamless embedded look. Payment methods demo stays as a standalone bordered list-group with outer chrome. The two settings/payment-methods demos retain their per-row helper text + control trailing-slot pattern via the `<div style="flex: 1;">` + `style="flex-shrink: 0;"` pair (the v3 utility set still doesn't ship `.ms-auto` / `.flex-grow-1` / `.flex-shrink-0` — inline styles per the navbar pattern). Contacts demo replaces BS5's `.rounded-circle` with inline `border-radius: 50%` on the avatar `<img>` and uses inline `overflow: hidden; text-overflow: ellipsis; white-space: nowrap` for the message-preview truncation. **Customization section** is a single ungrouped 16-row prose-styled table per slider.njk reference (4-group split applies above ~20 vars; below threshold the flat table reads cleaner). Lead paragraph trimmed to "A stack of rows on a shared rounded surface" per docs voice rule. **Verification**: build green (40 pages render), bundle 75.4 → 85.0 KB raw (+9.6) / ~12.6 → 14.1 KB gz (+1.5), 0 `--bs-*` leaks in compiled CSS, 0 BS5 leftover names (`list-group-item`, `list-group-flush`, `list-group-horizontal`, `list-group-numbered`, `list-group-item-action`, `list-group-item-{intent}`) in rendered HTML or compiled CSS (the only `list-group-item-*` substrings in the rendered HTML are the 9 component var names — `--list-group-item-{padding-y/x,gap,icon-size,hover-bg,hover-color,active-bg,active-color,disabled-color}` — referenced in the Customization table, same pattern as sidebar's `--sidebar-item-action-size`). All 16 `--list-group-*` vars emit. All BEM selectors emit: `.list-group`, `.list-group--flush`, `.list-group--numbered`, `.list-group--horizontal`, `.list-group--horizontal-{sm,md,lg,xl,xxl}`, `.list-group__item`, `.list-group__item--{primary,success,info,warning,danger,neutral}` — that's 17 unique selectors. Site sidebar entry already pointed at `/list-group` (no rename needed). 98 `list-group__item` references in rendered HTML (across the 13 demo sections and their escaped-source duplicates), 3 `data-state="active"`, 4 `aria-current="page"`, 3 each of `--flush` / `--numbered` / `--horizontal`. Sass deprecation warnings unchanged (forked BS5 mixins). Visual verification pending — maintainer to eyeball `/list-group` in light + dark: (a) basic list with rounded outer frame + inner row dividers in both themes; (b) active row (3 stacked dial-tones: data-state on the static `<li>`, aria-current on the link, both painting `--st-highlight`); (c) interactive `<a>` rows — hover paints `--st-accent`, focus paints inset 2px ring with no top/bottom clip (the z-index lift); (d) flush — outer border + radius gone, inner dividers stay; (e) numbered — counter prefix on each row, would cascade if nested; (f) horizontal — 3-col row with vertical dividers between cells; (g) badge — trailing badges right-aligned via `flex: 1` on the label span; (h) contextual variants — 6 row colors (neutral + 5 intents), each border + bg tracking the active `--st-{intent}` via color-mix; (i) custom content — multi-line rows with title + paragraph + small print stacking vertically; (j) contacts — avatar + 2-line label + trailing time/badge with ellipsis truncation on the preview line; (k) settings — card auto-integration (outer border + radius merged with card, header divider visible above first list-group row, last row's bottom border clipped by card's overflow:hidden); (l) payment methods — standalone bordered list-group with switch trailing slot. Cross-check: confirm the `.card > .list-group` direct-child selector doesn't fire for the contacts/contextual-variants demos that use `<div class="list-group">` standalone (no card wrapper — the integration rules don't apply). Also confirm dark mode: surface tier flips, accent stays subtle, highlight stays primary-tinted, intent variants stay vivid via the oklab interpolation. **3.5b closes the second Display-family file.** Remaining display queue: breadcrumb, pagination, progress (the 0B file — pure write, no port reference); toast + accordion are JS-coordinated and slot into Step 4 interleave.

- 2026-06-09 — **Step 3.5a — `components/_table.scss` rewrite** — first Display-family component (~210 lines, full rewrite from the BS5-coupled snapshot which still depended on `$table-cell-padding-*`, `$table-active-bg`, `$table-hover-bg`, the BS5 `_tables.scss` compiled mechanic, and `--bs-table-*` cascade). Look preserved 1:1 against the BS5 snapshot per maintainer ask ("same look"): 0.75rem inner cell padding × density, 1.25rem first/last edge cells (flush-in-card alignment with `--card-padding`), 0.75rem head font-size + 500 weight + muted color, 0.25rem `.table--sm` inner padding, 10% bg / 25% border on variant tints, 2px group-divider rule, transparent `--table-bg` so the table reads against parent surface (card / page). **BS5 layered-bg mechanic ported verbatim** — per-cell `box-shadow: inset 0 0 0 9999px var(--table-bg-state, var(--table-bg-type, transparent))` chain with `--table-bg-type` for static layers (striped, variant) and `--table-bg-state` for runtime states (hover, `[data-state="active"]`). Same chain for color. This is the only way `.table--hover` can light a striped+variant row without erasing the layers underneath; box-shadow inset paints over `background-color` instead of replacing it. **BEM rename map**: `.table-{intent}` → `.table__row--{intent}` (proper element-modifier, applied to `<tr>`); `.table-secondary` → `.table__row--neutral` (neutral fill via `--st-neutral` so warm/cool retints drag along — BS5's `.table-secondary` was monocolor near-black); `.table-header-alt` → `.table__head--alt`; `.table-group-divider` → `.table__body--divider`; `.table-{striped,striped-columns,hover,bordered,borderless,sm}` → `.table--{striped,striped-cols,hover,bordered,borderless,sm}` (root modifiers; `striped-columns` shortened to `striped-cols`). `.table-active` → `<tr data-state="active">` (V3.md §3.6 Radix-aligned selection, matches sidebar / dropdown / list-group). `.align-middle` (BS5 generic utility) → `.table--align-middle` (table-scoped modifier — generic align utilities haven't shipped yet; tucking it under `.table` keeps the surface clean until the layout-utilities PR lands). `.table-responsive` + `.table-responsive-{sm,md,lg,xl,xxl}` **kept flat** (they wrap the table, they are not a `.table` part); the breakpoint variants use the v3 `media-down()` mixin. **Color-mix replaces BS5's baked rgba on variants** — `color-mix(in oklch, var(--st-{intent}) 10%, transparent)` for bg, `25%, var(--st-border)` for border. Tracks the active `--st-*` per theme / per retint scope instead of locking to a build-time hex (BS5's `rgba(var(--bs-{intent}-rgb), 0.1)` worked the same way but only because the snapshot manually patched it; the stock BS5 `.table-{variant}` used `shift-color()` opaque pastels that read flat under dark mode). Row states route through the interactional trio: `--table-hover-bg → --st-accent`, `--table-active-bg → --st-highlight` (was `--bs-accent-active-bg` per snapshot — same intent). **17 component vars across 4 groups** — Geometry (5: `cell-padding-y/x`, `cell-padding-sm`, `edge-padding`, `group-divider-width`), Head (4: `head-font-size/weight/color`, `head-bg-alt`), Surface (3: `color`, `bg`, `border-color`), Row states (6: `striped-bg/color`, `hover-bg/color`, `active-bg/color`). All density-relevant geometry wraps `calc(X * var(--st-density))`. Plus four "layered slot" vars (`--table-bg-type`, `--table-color-type`, `--table-bg-state`, `--table-color-state`) which are mechanism-internal and intentionally absent from the Customization table — they're set/read by the mechanic, not consumer knobs. **Card integration kept** — `.card` already has `overflow: hidden` so the redundant `:has()` rule from the snapshot dropped; only the `border-bottom-width: 0` on the last row of a flush-in-card table survives (drops the double border between last row and card edge). Covers `<tfoot>` too via `> :last-child > tr:last-child`. **Bundle** — `_table.scss` uncommented in `bundles/stisla-full.scss`. Compiles clean against the `quiet-deps` sass run; build ships and `site-dist/tables.html` renders all 20 sections with v3 vocabulary (verified: zero stale `.table-{striped,active,hover,…}` or `.text-body-secondary` or `.badge-light-*` or `.btn-{sm,icon,ghost-secondary}` survivors). **Demo page (`src/site/pages/tables.njk`) sweep** — all 20 sections kept; class names swapped to v3 vocabulary (`.badge badge--soft badge--{intent}` for soft chips, `.btn btn--sm btn--ghost btn--neutral btn--icon-only` for ghost icons, `.btn btn--sm btn--outline btn--neutral` for bulk-actions, `.spinner spinner--sm` for inline deploy spinner, `.text-muted-foreground` for the email-secondary line per the memory'd muted-text rule, `.card__header card__header--alt` for the alt-fill card header, `card__body`/`card__footer` already standard). Layout utilities not yet shipped (`d-flex`, `gap-*`, `ms-auto`, `align-items-center`, `rounded-circle`, `m-0`/`mb-0`) replaced with inline `style="…"` blocks — same pattern the navbar demo used. **Customization section** added per the slider.njk reference shape: four grouped tables (Geometry / Head / Surface / Row states) under a single `<h2>Customization</h2>`, plus a closing paragraph documenting the `-state` over `-type` over base paint-chain so consumers know the mechanic without exposing it as a knob. **3.5a closes the first non-JS Display-family file.** Remaining display queue: list-group, breadcrumb, pagination, progress (the 0B file — pure write, no port reference); toast and accordion are JS-coordinated and slot into Step 4 interleave.

- 2026-06-09 — Step 3.4d done (sidebar) + retroactive navbar `__link` → `__button` rename. **Navbar rename (commit `7911013`)**: maintainer flagged "button" as the right anchor name for the chip — the element renders as `<a>` for navigation OR `<button>` for actions; the class describes the shape, not the underlying semantic. Surgical rename: `.navbar__link` → `.navbar__button` (selector) + `--navbar-link-*` → `--navbar-button-*` (9 vars) in `_navbar.scss` + `navbar.njk`. Comment headers updated ("Link — ghost chip" → "Button — ghost chip", brand "link chip" → "button chip"). Customization subhead "Link chip" → "Button chip". Zero behavioral change. Sets up vocabulary consistency with `.sidebar__button` in this same session. **Step 3.4d — `components/_sidebar.scss` (~470 lines, rewrite)** — largest BS5-coupled file in the migration (was 539 lines / 22 K). Full v3 model: BEM, `--st-*` tokens only, color-mix unused (sidebar doesn't derive states via mix — it reads the interactional trio `--st-accent` / `--st-highlight` directly), state attrs, hard-height contract per V3.md §3.4 + height architecture. **BEM rename map**: `.sidebar-header` → `.sidebar__header`, `.sidebar-content` → `.sidebar__content`, `.sidebar-footer` → `.sidebar__footer`, `.sidebar-logo` → `.sidebar__brand` (renamed — matches `.navbar__brand`, semantically tighter than "logo" which is just one valid content), `.sidebar-menu` → `.sidebar__menu`, `.sidebar-group` → `.sidebar__group`, `.sidebar-group-title` → `.sidebar__group-title`, `.sidebar-group-action` → `.sidebar__group-action`, `.sidebar-list` → `.sidebar__list`, `.sidebar-item` → `.sidebar__item`, `.sidebar-item-button` → `.sidebar__button` (matches `.navbar__button` from this session's earlier rename), `.sidebar-item-action` → `.sidebar__item-action`, `.sidebar-item-action-hover` → `.sidebar__item-action--reveal` (proper BEM modifier — "reveal" reads as the behavior), `.sidebar-submenu` → `.sidebar__submenu`, `.sidebar-sm` / `-lg` → `.sidebar--sm` / `.sidebar--lg`. **State hooks (V3.md §3.6)**: `.sidebar.is-collapsed` (rail mode, Stisla-original — listed explicitly in V3.md §3.6 table), `.sidebar__button[aria-current="page"]` (current page on nav links, primary a11y signal), `.sidebar__button[data-state="active"]` (Radix-aligned hook for non-link rows that toggle state — replaces the legacy `.active` escape hatch BS5 used), `.sidebar__item[data-state="open|closed"]` (submenu visibility, Radix Collapsible-aligned), `.sidebar__button[aria-expanded="true|false"]` (submenu trigger a11y). **Biggest behavioral shift — submenu drops BS5's `.collapse` plugin entirely.** Was: `data-bs-toggle="collapse"` + `data-bs-target="#…"` + `<div class="collapse"><div class="sidebar-submenu">…</div></div>` markup + BS5's Collapse JS measuring `scrollHeight` for the height animation. Now: `data-sidebar-submenu-toggle` on the trigger button (a delegated click handler in `src/js/index.js` flips `[data-state]` on the closest `.sidebar__item` between "open" and "closed" + updates `aria-expanded` on the trigger) + `.sidebar__item[data-state="closed"] > .sidebar__submenu { display: none }` for visibility. **No animation in 3.4d** — closed submenus snap shut. Animated height transitions deferred to **Step 4** (`Stisla.Sidebar` with the class + destroy + custom-events contract, V3.md §3.7), same deferred-to-Step-4 pattern the navbar 3.4c used. Documented in the file's JS header block. **29 component vars across 7 groups** (highest-var component shipped so far; 16 navbar / 11 app-shell / 9 slider previously held the title). All density-relevant vars wrap `calc(X * var(--st-density))`. Shell (5): bg / color / padding-y / padding-x / gap. Brand (3): brand-color / brand-icon-size / brand-gap. Button (12): height (2.25rem default, 2rem sm, 2.5rem lg per the size modifiers) / padding-x / radius (defaults to `var(--st-radius-sm)`) / gap / font-weight / color / hover-bg (→ `var(--st-accent)`) / hover-color / active-bg (→ `var(--st-highlight)`) / active-color / icon-size / icon-color. Item action (1): item-action-size. Group (4): group-gap / group-title-font-size / group-title-font-weight / group-title-color. Submenu (3): submenu-border-color / submenu-padding-start / submenu-margin-start (derived: `calc(button-padding-x + button-icon-size/2)` so the guide line sits centered on the parent icon's column). Motion (1): transition (used by rail soft-hides; zeroed under reduced-motion). **Hard-height contract** — `.sidebar__button` uses `height: calc(var(--sidebar-button-height) * var(--st-density))` + `padding-block: 0` + `line-height: 1` + `white-space: nowrap`. Single-line; truncates if width-forced. Matches the V3.md height-amendment pattern shared by `.btn` / `.form-control` / `.navbar__button`. **Padding-on-children, not root** — header / content / footer each own their edge padding so `.sidebar__content`'s padding box reaches the panel's right edge (scrollbar rides the rail itself) and button focus rings have interior room. Header-less / footer-less panels absorb the missing edge's padding via `.sidebar:not(:has(> .sidebar__header)) > .sidebar__content` (and same for footer). **Rail-width recipe** — `width = button-height + 2 × panel-padding-x`. At density 1 with defaults: 36 + 2×16 = 68px = 4.25rem (matches `.app-shell`'s shipped 4.25rem rail default). Sizes give 4rem (sm: 32 + 32) and 4.5rem (lg: 40 + 32). Panel-padding-x stays constant across sizes by design — only button geometry scales. Caught mid-write that the initial `--sidebar-padding-x: 0.5rem` default would produce 3.25rem rail, misaligned with app-shell — corrected to 1rem before any commit. **Rail-mode (`.is-collapsed`)** ported faithfully: panel-level `overflow: hidden`, brand-column re-center via header padding-inline calc that compensates for the brand-icon-size vs button-icon-size delta (`-(brand-icon - button-icon)/2`), soft-hide of label spans + brand wordmark + chevron + item-action (opacity + pointer-events: none, transitions live in the base rule so motion runs in both directions), max-height-to-0 collapse of group title + group-action rows so adjacent rows glide up to fill the closed space, submenu `display: none` regardless of `[data-state]`. **Group-action 2-col grid switch** — `.sidebar__group:has(> .sidebar__group-action)` flips the group from flex-column to a 2-col grid (title 1fr | action auto), with the list spanning both columns. Without that, the action would either steal the title's row or land below as a separate row. **Action-slot button uniform sizing** — `.sidebar__item-action .btn, .sidebar__group-action .btn` overrides `--btn-height: 1.5rem` / `--btn-padding-x: 0` / width 1.5rem / gap 0 so the consumer can pass any `.btn` tone (typically `.btn--ghost.btn--neutral.btn--icon-only`) and the slot owns the chrome. Source order (sidebar after btn in bundle) lands the override win. **Item action absolute positioning preserved** — overlays the button's right edge without affecting the button's hit area beyond a `padding-right: calc(--item-action-size + 0.25rem)` carve-out triggered via `:has(> .sidebar__item-action)`. The `--reveal` modifier (was `.sidebar-item-action-hover` in BS5) fades the action in on `.sidebar__item:hover, .sidebar__item:focus-within`. **JS — `src/js/index.js`** gained a third delegated click handler mirroring the app-shell + navbar pattern: closest `.sidebar__item` to a `[data-sidebar-submenu-toggle]` click, flip its `data-state`, update the trigger's `aria-expanded`. Three handlers now (app-shell collapse/visibility, navbar toggle, sidebar submenu) plus the app-shell backdrop dismiss — Step 4 promotes all four into Stisla.* classes with destroy + custom events. **Site chrome sweep — same commit.** `src/site/partials/_site-sidebar.njk` (the docs site's own nav, which uses the framework `.sidebar` component) had to rename in lockstep: `.sidebar-header` / `.sidebar-logo` / `.sidebar-content` / `.sidebar-menu` / `.sidebar-group` / `.sidebar-group-title` / `.sidebar-list` / `.sidebar-item` / `.sidebar-item-button` / `.sidebar-footer` / `.sidebar-sm` all converted to BEM. `src/site/scripts/site.js` line 10 — the active-link querySelector `.site-sidebar a.sidebar-item-button` → `.site-sidebar a.sidebar__button`. Without the sweep the docs site's own navigation would have rendered as unstyled markup post-commit. **`sidebar.njk` rewritten** — ~700 lines, 12 sections: Basic / With header + footer / Sizes (sm / default / lg side-by-side) / Groups + group action / Active state (`aria-current="page"` AND `data-state="active"` both shown) / Item actions (always + reveal-on-hover modifier) / **Nested submenu** (new markup using `data-sidebar-submenu-toggle` + `[data-state]`, with explicit note that animation lands in Step 4) / As a panel (frame with `--sidebar-bg: var(--st-surface)` + border + radius via inline styles) / **Recolor** (primary-tinted sidebar using `--st-primary` + `--st-primary-foreground` + `color-mix` derivations over `--st-primary-foreground` for hover/active/icon/title surfaces — replaces the BS5 `rgba(255,255,255,…)` hacks, demonstrates the v3 token-driven approach end-to-end) / Rail / mini mode (3 sizes side-by-side, all with `.is-collapsed`) / **Toggle with JS** (inline script trimmed: 4 lines instead of BS5's ~15 — the `Stisla.bs.Collapse.getOrCreateInstance().hide()` call is gone because submenus already hide via `display: none` when the sidebar gets `.is-collapsed`) / **Customization** (29-var section with 7 grouped sub-tables: Shell / Brand / Button / Item action / Group / Submenu / Motion — per the high-var-component convention, V3.md §3.10 + customization-table memory). Dark mode section dropped from the demo (not a one-component concern in v3 — tokens flip globally via `[data-theme="dark"]` / `.dark`, no per-component dark explanation needed). BS5 utility classes swept: `.text-bg-primary` / `.text-bg-secondary` on badges → `.badge.badge--primary` / `.badge`; `.btn-ghost-secondary` → `.btn.btn--ghost.btn--neutral.btn--icon-only`; `.border rounded-4 shadow-sm` on framed panels → inline styles using `--st-border` + `--st-radius`; `.position-absolute.start-0.top-0.h-100` on the toggle demo → inline `position: absolute; inset: 0 auto 0 0`; `.d-flex.flex-wrap.gap-3.align-items-start` on the sizes/rail rows → inline flex styles. Bundle order: `_sidebar` line 55 of `bundles/stisla-full.scss` uncommented. **Verification**: build green (40 pages render), 0 `--bs-*` in compiled CSS, 0 BS5-shape sidebar class names (`.sidebar-{header,content,footer,logo,menu,group,list,item-button,item-action,submenu,sm,lg}`) anywhere in `src/` or `site-dist/*.html` outside legitimate var names (`--sidebar-item-action-size`, `--sidebar-group-*`, `--sidebar-submenu-*`) and comment text, 0 `data-bs-*` attrs in rendered sidebar.html or app-shell.html, all 29 `--sidebar-*` vars emit, all 18 BEM selectors emit (`.sidebar`, `.sidebar.is-collapsed`, `.sidebar--sm`, `.sidebar--lg`, `.sidebar__brand`, `.sidebar__button`, `.sidebar__content`, `.sidebar__footer`, `.sidebar__group`, `.sidebar__group-action`, `.sidebar__group-title`, `.sidebar__header`, `.sidebar__item`, `.sidebar__item-action`, `.sidebar__item-action--reveal`, `.sidebar__list`, `.sidebar__menu`, `.sidebar__submenu`). 211 `.sidebar__button` hits in rendered sidebar.html (across the 12 demo sections and their escaped-source duplicates), 11 `data-sidebar-submenu-toggle` triggers, `data-state` properly emits as `"open" × 4`, `"closed" × 3`, `"active" × 3`. Bundle 66.5 → 75.4 KB raw (+8.9) / ~11.4 → 12.6 KB gz (~+1.2). Visual verification pending — maintainer to eyeball `/sidebar` in light + dark: (a) basic + with-header-footer in both themes for token-driven surfaces; (b) sizes row — sm/default/lg button heights track 32/36/40 at density 1, gutters identical across sizes; (c) group-action 2-col grid switch — title and `+` button share a row, list spans full width below; (d) active state — `aria-current="page"` link AND `[data-state="active"]` button both paint highlight chip; (e) item actions — always-visible badges in the right edge, hover-reveal `…` button fades in on row hover (also keyboard focus-within), faded action does not intercept clicks when hidden; (f) nested submenu — initial `[data-state="open"]` shows expanded, clicking the trigger flips to closed (CSS hides via `display: none`), chevron rotates 180° based on `aria-expanded`, the new "Billing" item starts collapsed and opens on click; (g) as-a-panel — surface bg + border + radius reads as a framed panel; (h) recolor — primary fill in light + dark, hover/active chips read against the primary fill (color-mix over `--st-primary-foreground` instead of rgba), icon + title colors muted appropriately; (i) rail mode — three sizes, each with the right rail width (4 / 4.25 / 4.5 rem), labels + chevron + item-action faded out, group titles collapsed, submenus hidden, brand wordmark faded but brand icon stays centered on the menu icon column; (j) toggle-with-JS — clicking the footer trigger flips to rail with width slide and label fade in sync, clicking again restores; (k) site chrome — the docs site's own left-side nav still renders correctly with the BEM classes (which it should after the partial + site.js sweep). Also confirm `/app-shell`, `/page-structure`, `/navbar`, `/customization`, and the home page all still render with the site chrome nav working. Next: **Step 3 moves into the Display family** (table → list-group → breadcrumb → pagination → progress → toast → accordion, per MIGRATION.md §3 step 5). The layout family is fully on the v3 model.
- 2026-06-09 — Step 3.4c done (navbar). **`components/_navbar.scss` (~190 lines)** — billed as a port in §4 but actually a near-total rewrite: BS5's `.navbar-brand`/`.navbar-nav`/`.nav-link`/`.navbar-toggler`/`.navbar-toggler-icon`/`.navbar-collapse`/`.navbar-expand-{breakpoint}` chrome is gone, the BS5 collapse plugin is gone, and the file no longer overlays on top of `bootstrap/scss/_navbar.scss`. BEM family from scratch: `.navbar` / `.navbar__brand` / `.navbar__toggle` / `.navbar__menu` / `.navbar__nav` / `.navbar__link`. **State hooks** — `.navbar__menu[data-state="open\|closed"]` for the mobile-folded state, `.navbar__link[data-state="active"]` for the current page (V3.md §3.3 + §3.6 — highlight chip, distinct from hover's accent chip), `.navbar__link[aria-disabled="true"]` for unreachable links. **Single collapse breakpoint** — folds at `lg` (no `.navbar-expand-{sm,md,xl,xxl}` variants ported; the BS5 `@each $breakpoint` loop with `media-breakpoint-down` is replaced with two `@include media-up(lg)` / `media-down(lg)` blocks). Customizable via overriding the `@media (max-width: 991.98px)` rules in a higher layer if a project genuinely needs a different breakpoint, but the default ships one decision instead of five modifier classes. **Auto-push trailing slot** — above lg, the `.navbar__nav` gets `margin-inline-end: auto` so any sibling inside `.navbar__menu` (search form, status pill, action button) naturally falls on the trailing edge — BS5's `.me-auto` convention without making consumers carry the class. **Hard-height chip + brand** — `.navbar__link` and `.navbar__brand` both use `height: calc(2.25rem * var(--st-density))` + `padding-block: 0` + `line-height: 1` per the V3.md height architecture amendment, so the row reads as one band and the density knob retunes everything in step. **Container-inside-navbar** — `.navbar:has(> .container)` (and `-fluid`) zeroes the bar's inline padding and the container takes over with `padding-block: var(--navbar-padding-y)` — the BS5 `.navbar > .container` use case (clamp the inner row to a column width that matches the page body) without ugly inline-style escapes. **16 component vars** in 3 groups: layout (5: padding-y, padding-x, gap, bg, color); link chip (9: height, padding-x, gap, radius, color, hover-bg, hover-color, active-bg, active-color); toggle + motion (2: toggle-size, transition). All density-relevant ones wrap `calc(X * var(--st-density))`. **Reduced motion** — `@media (prefers-reduced-motion: reduce)` zeroes link + toggle hover transitions. **JS** — `src/js/index.js` gained a third click handler mirroring the app-shell pattern: `[data-navbar-toggle]` finds the closest `.navbar` → finds its `.navbar__menu` → flips `data-state` between `"open"` and `"closed"` and updates `aria-expanded` on the toggle. Step 4 promotes this into `Stisla.Navbar` with the class + destroy + custom-events contract (V3.md §3.7). Bundle order: `_navbar` line 53 uncommented; `_sidebar` line 55 still commented (3.4d). **`navbar.njk` demo rewritten** — 6 sections: Basic / Active and disabled / With search / With status text / Container / Dropdowns (deferred-stub, says dropdowns land Step 4) / Customization. BS5 utilities (`.border`, `.rounded`, `.w-100`, `.me-auto`, `.ms-auto`, `.ms-0`, `.ms-lg-4`, `.d-flex`, `.gap-2`, `.bg-success`, `.rounded-circle`, `.d-inline-block`, `.me-2`, `.d-inline-flex`, `.align-items-center`) all swept — replaced with inline styles where layout-specific (`style="border: 1px solid var(--st-border); border-radius: var(--st-radius); width: 100%"` on each `.navbar`; flex inline styles on the search form + status text), or just dropped where v3's structural CSS now handles it (the auto-push, the chip layout). BS5 hooks (`data-bs-toggle="collapse"`, `data-bs-target="#…"`, `aria-controls`, `class="navbar navbar-expand-lg"`, `<span class="navbar-toggler-icon"></span>`, `<div class="collapse navbar-collapse" id="…">`, `.navbar-text`) replaced with v3 equivalents (`data-navbar-toggle`, `aria-controls` preserved, `class="navbar"`, `<i data-lucide="menu"></i>` inside the toggle, `<div class="navbar__menu" data-state="closed">`, no `.navbar-text` needed). The **"With dropdown"** section dropped entirely (dropdown component is Step 4); a one-line "Dropdowns" stub section explains the deferral. Search form uses `.form-control--sm` + `.btn--primary.btn--sm`. Customization section ships 3 grouped `<h3>` subtables under one `<h2>` per the high-var-component convention (16 vars > 10-cutoff threshold). **Verification**: build green (40 pages), 0 `--bs-*` in compiled CSS, 0 `.navbar-{brand,nav,toggler,collapse,expand,text}` or `.nav-link` or `.navbar-toggler-icon` class-name leaks in HTML or in our touched source files (the remaining `.collapse`/`.navbar-*` substrings in src/scss/components/ live in `_sidebar.scss` and `_nav.scss`, both still commented out in the bundle — Step 3.4d and Phase 2 cut respectively), 0 `data-bs-*` attrs in the rendered demo (10 `data-navbar-toggle` attrs across the 5 collapsible sections, all v3-native), all 16 `--navbar-*` vars emit in the compiled CSS, all 6 `.navbar__*` selectors emit. Bundle 61.7 → 66.5 KB raw (+4.8) / ~10.8 → ~11.4 KB gz (~+0.6). Visual verification pending — maintainer to eyeball `/navbar` in light + dark: chip + brand vertical alignment (the hard-height + line-height: 1 contract), hover/active chip contrast in both themes (accent vs highlight reads), focus halo on the link via `:focus-visible`, toggle button hover bg, the lg-down fold (resize narrower than 992px, click the hamburger to flip `data-state="open"` — menu drops to a full-width column below the brand+toggle row; nav links become full-width chips; search form + status text stack), container demo (the inner row's inset matches what a body `.container` would produce; bar still spans full width), keyboard nav (tab between links, Enter on the toggle to flip state, aria-expanded updates). Next: **Step 3.4d (`_sidebar.scss` rewrite — 22 K, the big one — last layout component before Step 3 moves into the Display + JS-coordinated families)**.
- 2026-06-09 — Step 3.4b done (page). **`components/_page.scss` (~105 lines)** rewritten on the v3 model — pure port, no design changes: BEM rename (`.page-header` → `.page__header`, `.page-header-title` → `.page__title`, `.page-header-action` → `.page__action`, `.page-section` → `.page__section`, `.page-section-header` → `.page__section-header`, `.page-section-title` → `.page__section-title`); 7 Sass `$page-*` defaults + `--bs-*` emits all dropped, replaced with 8 bare `--page-*` component-scoped vars per the slider/switch/toggle/app-shell convention (no `--st-` prefix on component vars, V3.md §3.4 fallback pattern). Every gap (`--page-section-gap` / `--page-header-gap` / `--page-title-gap` / `--page-action-gap` / `--page-section-inner-gap` / `--page-section-header-gap`) wraps `calc(X * var(--st-density))` so the global density knob retunes page rhythm with the rest of the system (V3.md §3.4). `--page-section-title-size` / `--page-section-title-weight` stay literal (font-size + weight are separate axes from density, matching `--btn-font-size`). **`.page__title` shape change**: BS5 set only `flex: 1 1 auto; min-width: 0`, relying on default `<h1>` `margin-bottom` + `.mb-1` utility to space the heading from the subtitle. With Preflight zeroing heading + paragraph margins (Step 3.x), the subtitle would sit immediately under the heading with no breathing room. Promoted `.page__title` to `display: flex; flex-direction: column; gap: var(--page-title-gap)` with a `0.25rem * density` gap so the heading + subtitle stack with a small consistent space, no utility needed. **`.page__section-title`**: dropped the explicit `margin: 0` declaration — Preflight already zeroes heading margins, so the rule is just font-size + weight + line-height now. Bundle order in `bundles/stisla-full.scss` — line 57 uncommented; `_navbar.scss` (line 53) + `_sidebar.scss` (line 55) still commented (Steps 3.4c / 3.4d). **`page-structure.njk` demo rewritten** — all 7 BEM renames applied across the 6 framed previews + 2 code-only blocks (15 instances of `.page-header*` / `.page-section*` total swept). BS5 utilities (`.h3.mb-0`/`.mb-1` on headings dropped — Preflight zeroes margins, the new `.page__title` gap covers the subtitle space; `.text-body-secondary` → `.text-muted-foreground`; `.btn-outline-secondary` → `.btn.btn--outline.btn--neutral`; `.btn-primary` → `.btn.btn--primary`; `.btn-sm` → `.btn--sm`; `.me-1` icon spacing dropped — `.btn`'s own internal layout handles icon gap). Demo box wrappers (`.p-4.border.rounded.bg-body.w-100`) swept to inline styles since the v3 utility set doesn't include them yet (`padding: 1rem; border: 1px solid var(--st-border); border-radius: var(--st-radius); background: var(--st-background); width: 100%`). **Two sections dropped from the original demo**: (a) the "Breadcrumb placement" demo — `.breadcrumb` is commented out in the bundle (Step 3.5 territory) and a bare `<ol>` with placement-only point would just show an un-rendered list, low signal/noise; (b) the closing "Putting it together" demo kept its breadcrumb stripped to skip the unstyled `<ol>` rather than carry a stub. The "Content width" code-only section had `.py-4` swept to an inline `padding: 1rem`. Lead paragraph trimmed to the docs voice rule (short, says what the component is). Added **Customization** section at file end — 8-row prose-styled vars table per the convention (Variable / Default / Use): `--page-section-gap`, `--page-header-gap`, `--page-title-gap`, `--page-action-gap`, `--page-section-inner-gap`, `--page-section-header-gap`, `--page-section-title-size`, `--page-section-title-weight`. **Verification**: build green (40 pages), 0 `--bs-*` in compiled CSS, 0 `.page-header*`/`.page-section*` class-name leaks in HTML or source under `src/`, all 8 `--page-*` vars emit in the compiled CSS, all 7 `.page__*` selectors emit. Bundle 59.23 → 61.7 KB raw (+2.47) / 10.32 → ~10.8 KB gz (~+0.5). Visual verification pending — maintainer to eyeball `/page-structure` in light + dark: the section gap rhythm (the "Putting it together" demo is the busiest layout to read); title + subtitle gap inside `.page__title` (Preflight stripped the default margin, the new flex gap restores it); action slot button row alignment (right edge of header + section-header); section-header recipe matches page-header at tighter scale; wrap behavior at narrow widths. Next: **Step 3.4c (`_navbar.scss` — 4 K port — Stisla original, low BS5 coupling, similar shape to app-shell)**, then 3.4d (`_sidebar.scss` — 22 K rewrite, the big one).
- 2026-06-09 — Step 3.4a done (app-shell). **`components/_app-shell.scss`** rewritten on the v3 model — pure port, no design changes: BEM rename (`.app-shell-main` → `.app-shell__main`, `.app-shell-body` → `.app-shell__body`); `--bs-*` token surface dropped, replaced with 11 bare `--app-shell-*` vars per the established slider/switch/toggle convention (no `--st-` prefix on component vars, V3.md §3.4 fallback pattern). State hooks `.is-sidebar-collapsed` + `.is-sidebar-visible` kept (V3.md §3.6: `.is-*` for Stisla-original states with no Radix counterpart). `@include media-breakpoint-down(...)` → `@include media-down(lg)`; BS5 `$zindex-fixed` literal replaced with a `--app-shell-sidebar-z: 1030` knob so the backdrop can derive its layer (`calc(--app-shell-sidebar-z - 1)`) and consumers can re-stack. New tokens map: outer/main bg + color all read `var(--st-background)` / `var(--st-foreground)`; sidebar widths are `16rem` / `4.25rem` literals; mobile drawer reads `var(--st-surface)` for surface; backdrop is a `oklch(0 0 0 / 0.5)` literal (fixed-dark in both themes — a theme-following backdrop would invert in dark mode and undercut its scrim purpose); drawer shadow is a literal `2px 0 8px -2px oklch(0 0 0 / 0.15)` (right-cast to signal slide-from-left motion; not `var(--st-shadow)` because the global shadow is a subtle ambient meant for cards). Added a `@media (prefers-reduced-motion: reduce)` block at file end that flips `transition-duration` to `0.01ms` (V3.md cross-browser commit). Bundle order in `bundles/stisla-full.scss` — line 56 uncommented; `_sidebar.scss` still commented (Step 3.4d). **JS**: `src/js/index.js` shipped its `data-app-shell-toggle="collapse|visibility"` handler since before Step 2; the class names it toggles are unchanged (`.is-sidebar-collapsed` / `.is-sidebar-visible`), so it keeps working without edits. Step 4 will promote that handler into `Stisla.AppShell` with the class + destroy + custom-events contract. **`app-shell.njk` demo page rewritten** — the original demo leaned hard on `.navbar` + `.sidebar` rich internals (`.navbar-expand-lg`, `.sidebar-header`, `.sidebar-logo`, `.sidebar-content`, `.sidebar-menu`, `.sidebar-group`, `.sidebar-list`, `.sidebar-item`, `.sidebar-item-button`), all of which are still commented out in the bundle (sidebar = Step 3.4d, navbar = Step 3.4c). Replaced with placeholder slots styled inline (`background: var(--st-surface-2)`; centered "Sidebar" / "Navbar" labels in `--st-muted-foreground`) so the demo shows app-shell's actual scope (positioning a sidebar slot next to a main slot) and not partially-styled chrome from components that haven't shipped. Lead paragraph updated to make the placeholder convention explicit. 7 sections: Basic / With top navbar (+ "Navbar inside main column" subhead) / With footer (uses inline `margin-top: auto` since v3 utility set has no `.mt-auto` yet) / Sticky sidebar (code-only) / Desktop collapse (rail) — interactive `data-app-shell-toggle="collapse"` button on a real `.btn--ghost.btn--neutral.btn--icon-only.btn--sm` / Mobile drawer — interactive `data-app-shell-toggle="visibility"` button / Customization — 11-row prose-styled vars table per the convention (Variable / Default / Use). BS5 utilities (`.border`, `.rounded`, `.overflow-hidden`, `.w-100`, `.text-body-secondary`, `.mb-0`, `.btn-icon`, `.btn-ghost-secondary`, `.rounded-circle`, `.d-flex`, `.gap-2`, `.align-items-center`, `.ms-auto`, `.d-lg-none`, `.flex-grow-1`, `.p-4`, `.py-3`, `.px-3`, `.px-4`, `.small`) all swept — replaced with inline styles where layout-specific, v3 utilities where one exists (`.text-muted-foreground`, `.small`, `.h6`), or just dropped where Preflight already handles it (`.mb-0` is redundant after the Preflight reset of `<p>` margin). Demo container wrappers retain the `transform: translate(0)` trick that creates a new containing block so `position: fixed` on the mobile drawer is confined to the demo box rather than escaping to viewport. **`page-structure.njk`** — 3 `.app-shell-main` → `.app-shell__main` renames in the code-only "Common page widths" section. **Verification**: build green (40 pages), 0 `--bs-*` in compiled CSS, 0 `app-shell-main`/`app-shell-body` class-name leaks in HTML or source (the only `app-shell-main` substring left is in `--app-shell-main-bg` and `--app-shell-main-color` var names — legitimate component-scoped vars per the BEM-element naming), all 11 `--app-shell-*` vars emit in the compiled CSS and surface in the customization table HTML, `@media (max-width: 991.98px)` emits once (the mobile-drawer block from `media-down(lg)`). Bundle 59.23 → 62.09 KB raw (+2.86) / 10.32 → 10.77 KB gz (+0.45). Visual verification pending — maintainer to eyeball `/app-shell` in light + dark: shell + sidebar slot proportions (16rem default sidebar against the rest as main), `:has(> .navbar)` flex-column switch on the "With top navbar" demo, footer pinning via `margin-top: auto`, collapse-rail button toggles the sidebar to 4.25rem with the 200ms ease (rail visuals will read empty until sidebar ships — that's expected), mobile-drawer behavior at narrow viewport (browser narrower than 992px): backdrop scrim at 50% black both themes, right-leaning shadow on the slid-in drawer, click outside to dismiss. Also `/page-structure` for the three code-only `.app-shell__main` references in "Common page widths". Next: **Step 3.4b (`_page.scss` port — 4 K Stisla original)**, then 3.4c (`_navbar.scss` — 4 K port), then 3.4d (`_sidebar.scss` — 22 K rewrite, the big one).
- 2026-06-09 — Step 3.3c done (slider, renamed from form-range; redesigned mid-session). **`components/_slider.scss` (~165 lines)** — single class `.slider` on `<input type="range">`, sizes via `--sm` / `--lg`. **Renamed `.form-range` → `.slider`** to match shadcn / Radix / Reka convention; same rationale as the `.switch` and `.toggle` extractions in 3.3b — name reads as intent, not BS5 implementation. **First pass landed a thumb-on-track design** (slim 6px pill track, 16px circular thumb sitting on top), then maintainer pushed for a redesign matching a reference mockup: track at `.form-control` height with a filled-progress segment (primary), thumb as a narrow vertical pill centered inside the track, white thumb with drop shadow for edge definition. **Final shape**: track height = `.form-control` height (sm 28 / default 36 / lg 44, density-scaled), track radius pill, filled segment paints from track-start to thumb position via two browser paths — Firefox uses native `::-moz-range-progress` (browser-managed width), webkit/blink uses the classic no-JS `box-shadow: -100vmax 0 0 100vmax var(--slider-fill)` on `::-webkit-slider-thumb` + `overflow: hidden` on `::-webkit-slider-runnable-track` to clip the giant shadow to track bounds. The shadow's right edge anchors to the thumb's right edge, so it follows the thumb as the user drags. Pure CSS, no JS, fully reactive. Thumb width sm 6 / default 8 / lg 10; thumb height = track-h - ~8px so the thumb is centered with comfortable inset. Thumb color is `#fff` literal (works on both filled primary AND unfilled neutral in both themes when paired with the drop shadow); `--slider-thumb-shadow` = `0 1px 3px oklch(0 0 0 / 0.25)` carries edge definition without a border. **Focus halo moved from thumb to SHELL** — `overflow: hidden` on the track would clip a thumb-based halo, so `.slider:focus-visible` paints the 3px primary halo on the input element itself (same vocabulary as `.form-control:focus-visible`, just wrapping the track instead of the thumb). Cross-browser pseudo footgun applies as usual: webkit + firefox pseudos cannot share a selector list, so every rule (track, thumb, progress, focus, active, validation, reduced-motion) emits twice via inline SCSS mixins (`@mixin slider-track`, `@mixin slider-thumb`). Webkit thumb needs `margin-top: calc((var(--slider-h) - var(--slider-thumb-h)) * var(--st-density) / 2)` to vertically center (Firefox auto-centers). `:active` scales the thumb vertically (`transform: scaleY(0.92)`) rather than darkening, since fill color flickering on press is distracting. `:disabled` opacity 0.55 + `pointer-events: none`. Firefox-only `::-moz-focus-outer { border: 0 }` kills the dotted inner ring. **Component vars (9, up from 7)**: `--slider-h` (replaces `--slider-track-h` — now the full shell height) / `--slider-thumb-w` (NEW — thumb is no longer circular) / `--slider-thumb-h` (replaces `--slider-thumb`) / `--slider-radius` / `--slider-track-bg` / `--slider-fill` (NEW — controls the filled segment via both Firefox's progress pseudo and webkit's shadow trick) / `--slider-thumb-bg` (`#fff` literal) / `--slider-thumb-shadow` (NEW — replaces `--slider-thumb-rim`; drop shadow > border for the vertical-pill aesthetic) / `--slider-ring`. **Validation**: `[aria-invalid="true"]` / `:user-invalid` flips `--slider-fill` to danger so the filled segment turns red; track-bg + thumb stay neutral/white; focus + invalid halo on the shell flips to danger. Cleaner read than the first-pass approach (which tinted the whole track) since the fill IS the primary visual now. Hooks match the rest of the form family (form-control, form-check, switch). `/form-range.njk` → `/slider.njk`; sidebar entry renamed "Range" → "Slider"; URL `/form-range` retired (stale `site-dist/form-range.html` deleted along with two other orphaned outputs — `close-button.html` from 3.2 + `validation.html` from 3.3a — that the render step doesn't auto-clean). **Cross-browser pattern**: webkit + firefox pseudo-elements (`::-webkit-slider-thumb`, `::-moz-range-thumb`, `::-webkit-slider-runnable-track`, `::-moz-range-track`) cannot share a selector list — any browser that doesn't recognize one pseudo drops the whole rule. Source uses two inline SCSS mixins (`@mixin slider-track`, `@mixin slider-thumb`) `@include`d under each vendor pseudo; compiled CSS is two duplicate-bodied rules per surface as required. Same footgun applies to `@media (prefers-reduced-motion: reduce)` and to `:focus-visible` / `:active` / validation rules — every one is duplicated per vendor. Webkit places the thumb on the track's top edge — needs `margin-top: calc((var(--slider-track-h) - var(--slider-thumb)) / 2)` to center vertically; Firefox auto-centers. **Component vars (7)**: `--slider-track-h` (0.375rem default / 0.25rem sm / 0.5rem lg) / `--slider-thumb` (1rem / 0.75rem / 1.25rem) / `--slider-radius` (999px — pill track, opts out of `--st-radius` per V3.md §3.4) / `--slider-track-bg` (`var(--st-neutral)` — exact slot V3.md §3.3 defines for filled-neutral rest surfaces) / `--slider-thumb-bg` (`var(--st-primary)`) / `--slider-thumb-rim` (`var(--st-surface)` — 2px rim keeps the thumb defined against track fill) / `--slider-ring` (`var(--st-ring)`). The shell height is set to `var(--slider-thumb)` so the thumb has vertical room either side of the track. **States**: `:focus-visible` paints a 3px transparent halo on the thumb via `box-shadow: 0 0 0 3px color-mix(in oklch, var(--slider-ring) 25%, transparent)` — same vocabulary as `.form-control:focus-visible`; `:active` darkens the thumb via `color-mix(in oklch, var(--slider-thumb-bg) 78%, black)` — same 78% as `.btn:active`; `:disabled` opacity 0.55 + `pointer-events: none`. Firefox-only `::-moz-focus-outer { border: 0 }` kills the dotted inner ring under the halo. **Validation hooks**: `[aria-invalid="true"]` (server / JS / explicit) + `:user-invalid` (native, after first interaction — fires on `step` mismatch + out-of-range value). Treatment tints the track danger via `color-mix(in oklch, var(--st-danger) 25%, var(--slider-track-bg))`; thumb stays brand so the user's interactive surface stays familiar; focus + invalid halo flips to danger. Matches the rest of the form family (form-control, form-check, switch all wire the same pair). **Reduced motion**: separate rules per vendor zero out the thumb transition. **Demo page rewritten** with 10 sections: Default / **Value display** (NEW — `<output>` paired with a one-line inline `oninput` handler for a reactive readout; no framework needed) / Min and max / Steps / **Sizes** (sm + default + lg) / Disabled / **Browser validation** (step="10" with seeded value="35" trips `:user-invalid`) / **Server validation** (`aria-invalid="true"` sticky) / **How the fill works** (NEW — explains the two browser paths for the no-JS fill technique) / **Customization** (9-row prose-styled vars table, up from 7). `.demo-stack` replaces BS5 `.w-100` wrapper; `max-width: 24rem` retained inline as the visual constraint (Range URL `/slider` is the new entry point). **V3.md update**: §3.4 opt-out list gains `.slider` track + thumb (pill is semantic). Bundle 56.6 → 59.23 KB raw (+2.6 KB) / 9.97 → 10.32 KB gz (+0.35 KB) — the filled-track redesign is essentially the same line count as the thumb-on-track first pass (the box-shadow trick is one line per vendor; the dropped `--slider-thumb-rim` border + the simpler `:active` rule offset the new `::-moz-range-progress` rule + the shell halo). `0` `--bs-*` leaks; `0` `.form-range` references in compiled CSS or in source under `src/scss`; the only remaining `form-range` mentions are (a) the rename-rationale comment at the top of `_slider.scss` (legitimate, kept), (b) MIGRATION.md historical breadcrumbs (kept). 40 pages render (was 40 — `/form-range` retired, `/slider` added; site-dist cleaned to match source). All 9 `--slider-*` vars present in compiled CSS; all sizes emit; all 6 vendor-pseudo blocks emit (`::-webkit-slider-runnable-track`, `::-webkit-slider-thumb`, `::-moz-range-track`, `::-moz-range-thumb`, `::-moz-range-progress`, `::-moz-focus-outer`); the giant `100vmax` box-shadow on the webkit thumb is present (`grep -c vmax` = 1). Visual verification pending — maintainer to eyeball `/slider` in light + dark: filled segment color at three sizes, thumb edge definition over both filled (primary) AND unfilled (neutral) portions in both themes, thumb vertical centering in webkit and firefox, focus halo on the shell (not the thumb), `:active` scaleY squish on press, **fill cross-browser parity (Firefox uses `::-moz-range-progress`, Chrome/Safari/Edge use the box-shadow trick — confirm both paint identically)**, value-display section's `<output>` updates reactively on input, browser validation (fill turns danger after click-then-blur on the step-mismatch input), server validation (fill turns danger immediately), disabled dim, sidebar entry reachable under Forms. Next: **Step 3.4 (layout family — app-shell / page / navbar / sidebar)**, sidebar last per MIGRATION.md §3 step 3 (largest BS5-coupled file). Visual verifications still outstanding from prior sessions: `/customization` literal-OKLCH revert (3ae26be), `/switch` + `/toggle` + `/checks-radios` + `/input-group` + `/buttons` (3.3b).
- 2026-06-09 — Base-color knob removed, math dropped, plain OKLCH literals adopted. The session started trying to fix the customization page's `--st-base: 30` warm preset (which read identical to default — no warmth visible). First attempt restructured the single `--st-base` (hue) into two knobs — `--st-base-hue` + `--st-base-chroma` — with per-tier chroma multipliers via `calc(var(--st-base-chroma) * Nx)` inside `oklch()`. **Did not work in the browser.** Even at chroma values dialed to 90 (wildly out of OKLCH range, should clamp to maximum saturation), the maintainer saw zero visible change. Whether it was a Safari/Chrome quirk with deeply nested var() inside calc() inside oklch(), an inheritance edge case, or pure browser-cache, the chain was too fragile to debug at the spec level. **Pivoted to HeroUI's model**: no runtime math, no calc() chain, no synthetic knobs. Every token is a plain OKLCH literal. To retint, override the affected tokens (the 9 neutrals: background, foreground, surface, surface-2, surface-3, border, muted-foreground, neutral, accent) in a parent scope — inline style on a wrapper, a class, a `[data-theme]` block. Components inside read tokens via `var()` and inherit the override automatically. CSS does the work; no special machinery. **Token surface back to 30**: `--st-base-hue` and `--st-base-chroma` dropped. **Default neutrals = pure gray** (`oklch(L 0 0)`); both light + dark. Was "imperceptibly cool" at chroma 0.003–0.005 before — visually identical to pure gray on any monitor, now honestly named. **Intent independence preserved**: primary/success/warning/danger/info untouched at their authored OKLCH values. `--st-warning-foreground` simplified to `oklch(0.27 0 0)` (dark text on yellow, theme-agnostic). Files touched: `src/scss/tokens/_theme.scss` (full rewrite — literals only, no calc, no math, both modes), `src/site/pages/customization.njk` (Warm + Cool sections each inline-override all 9 neutral tokens with hand-tuned OKLCH literals at hue 30 / 240 respectively; wrapper paints `background: var(--st-background)`; How-it-works copy reframed to "override tokens on a wrapper, components inherit via var()"), `V3.md` (§1 + rule 3 + §3.2 token list + §3.2 prose + §3.3 surface paragraph + §3.8 dark-mode example code + §3.9 + Phase 1 — all reverted from 31/calc-chain talk to 30/literal-overrides talk). Bundle stable, build green, `0` `calc(` inside `oklch(` in compiled CSS (verified via grep). 40 pages render. **Future tooling note**: a build-time theme generator (HeroUI-style UI that emits an OKLCH block from a hue + chroma input) is the right home for the "I want a custom tint" use case. Out of scope for 3.0; the docs mention it as future direction. **Pedagogical note for future agents on this codebase**: CSS does not have reliable cross-browser support for `oklch(L calc(var(--x) * N) var(--y))` chains. If you find yourself wanting that pattern, stop. Author the OKLCH values as literals; do the math at build/authoring time. CSS custom properties shine when you override the FINAL value, not when you try to parameterize the components of a color function. Visual verification pending — maintainer to eyeball `/customization` in light + dark: Default (pure gray surfaces), Warm (visible warm-cream wrapper + cards), Cool (visible blue-tinted wrapper + cards), and the unchanged presets (brutalist / soft / dense / violet primary / per-component) to confirm nothing regressed. Next: **Step 3.3c (form-range)**, then Step 3.4 (layout family).
- 2026-06-09 — Step 3.3b done (form-check + input-group). Two rewrites on the v3 model; 5 pieces all converted in one PR. **`_form-check.scss` (~190 lines)** — `.form-check` wrapper + `.form-check__input` + `.form-check__label` BEM. Wrapper modifiers compose flat at root per V3.md §3.6: `.form-check--inline` (inline-flex), `.form-check--reverse` (flex-direction: row-reverse + justify-content: flex-end), `.form-check--switch` (flips input from box to track), `.form-check--switch.form-check--lg` (44×24 large switch). One ruleset on `.form-check__input` covers both checkbox + radio; `[type="radio"]` overrides border-radius to 50%. Native `<input>` styled directly with `appearance: none` (Preflight in place — no UA default to fight); checked / indeterminate paints indicator via `background-image` data: URL (checkmark / dash / dot) over a `--st-primary` fill. SVG stroke / fill is literal white (same exception as `.form-control` chevron — data: URLs can't read CSS vars; white matches `--st-primary-foreground` in both default themes). State derivation via native pseudos: `:checked`, `:indeterminate`, `:hover` (idle only — skipped when focus / invalid / disabled own the state), `:focus-visible` (3px halo, same vocabulary as `.form-control`), `:disabled` (opacity 0.55). Validation drops BS5 entirely — no `.is-valid` / `.is-invalid` / `.was-validated`. v3 hooks: `[aria-invalid="true"]` (server / JS / explicit) + `:user-invalid` (native browser validation after first interaction). Same selector pair as form-control 3.3a; the screen-reader semantic comes for free with aria-invalid. **Switch contrast fix**: off-state track is `color-mix(in oklch, var(--st-foreground) 25%, transparent)` not `--st-neutral`. `--st-neutral` sits at ~96% lightness in light mode and gives a white thumb almost no contrast against the track (the same trap BS5 hit and patched in dark mode with `$gray-700`). The semi-transparent foreground mix resolves to a mid-gray (~70% lightness) that adapts per theme — readable thumb in both modes. Thumb is a `white` literal (not `--st-surface`, which is dark in dark mode and would vanish against the on-state primary track too). Thumb geometry lives in component-scoped vars (`--form-check-track-w` / `-track-h` / `-thumb` / `-inset`) so density and `--lg` recompute the radial-gradient cleanly; `background-position: var(--inset) center` (off) → `calc(100% - var(--inset)) center` (on) slides the thumb. No data: URL for the thumb — radial-gradient is fully token-driven. **`.btn-check` toggle**: HTML-only toggle pattern (no JS) for settings toggles, day-of-week pickers, segmented controls without state management. Hidden input + sibling `.btn` label; `:checked + .btn` flips four `--btn-*` vars (`--btn-bg` / `--btn-border-color` / `--btn-color` / `--btn-bevel`) to override the `.btn--outline.btn--primary` rest state into a filled-primary pressed state. Composes cleanly with the `.btn` rewrite from Step 3.1 since every tone publishes `--btn-tone` and the checked rule reads it. Pairs by convention with `.btn--outline.btn--primary` (other intents also work since `--st-primary-foreground` matches `--st-danger-foreground` etc. in v3 defaults — all white). `:focus-visible + .btn` paints the btn focus outline on the label. Distinct from the planned JS-driven `.btn-group[data-state="active"]` for tabs (that lands in Step 4); `.btn-check` is the no-JS native counterpart. **`_input-group.scss` (~115 lines)** — wrapper-owns-chrome model retained (already correct architecturally); rewrite swaps Sass vars for `--st-*` and trims dead surfaces. BEM rename: `.input-group-text` → `.input-group__text`, `.input-group-sm` → `.input-group--sm`, `.input-group-lg` → `.input-group--lg`. `:has(> .form-control:focus-visible)` drops `.form-select` and `.form-floating` from the focus selector list (`.form-select` is gone in v3 — folded into `.form-control` in 3.3a; `.form-floating` doesn't exist in v3). **Validation forwarding added** (was an outstanding TODO in the BS5 file comments): `:has(> [aria-invalid="true"]), :has(> :user-invalid)` paints the wrapper border + 3px danger halo. Same hooks as `.form-control` 3.3a — invalid child surfaces on the row as one unified state. **`overflow: hidden`** on the wrapper clips a `.btn` child's corners flush with the wrapper radius — lets a leading or trailing button sit inside without its corner peeking past the wrapper edge. Doesn't clip the focus halo (box-shadow is outside the box). `.form-control` children get `background: transparent; border-color: transparent; box-shadow: none` (including `:hover` + `:focus-visible` overrides) so the inner field's own halo doesn't fight the wrapper's group focus. `.btn` children keep their own fill (so a button slot reads as the affordance, not chrome). Sizes pull `--st-radius-{sm,lg}` for proportional corner scaling; `--input-group-padding-x` matches `.form-control` padding-x at each size so a leading addon's text x-baseline aligns with the input's text x-baseline across all sizes. `:has(> textarea) > .input-group__text { align-self: flex-start; height: var(--input-group-height); }` anchors addons to the first line when a textarea grows past the height floor. Bundle 47 → 55 KB raw (+8 KB across form-check + input-group), 9.8 KB gz. `0` `--bs-*` leaks; `0` legacy BS5 names (`form-check-input`, `form-switch`, `input-group-text`, etc.). Demo pages rewritten: **`/checks-radios`** — all classes renamed; BS5 utilities (`.w-100`, `.d-flex.flex-column.gap-3`, `.btn-outline-primary`) swapped for v3 (`.demo-stack`, `.demo-row`, `.btn .btn--outline .btn--primary`); new Validation section added; Settings row repurposed to use `.form-check--reverse` + `justify-content: space-between` inline (cleaner than the original `.d-flex.justify-content-between` outer wrapper with nested form-check). **`/input-group`** — all classes renamed; `<select class="form-select">` → `<select class="form-control">` (4 places); `.form-control-sm` / `.form-control-lg` → `.form-control--sm` / `--lg`; `.btn-primary` → `.btn .btn--primary`; `.btn-outline-secondary` → `.btn .btn--outline .btn--neutral`; new Validation section added showing wrapper-level validation forwarding. Other pages with BS5-ism leaks (dropdowns.njk's form-check, offcanvas.njk's form-select × 4) left untouched per existing policy — their components are still pending wholesale rewrite (dropdown / offcanvas are Step 4 JS-coordinated), and a partial fix there would mask the BS5-isms in place when those components come up. Visual verification pending — agent verified build green, grep clean, all BEM selectors emit; maintainer to eyeball in light + dark on `/checks-radios` (checkbox / radio indicator visibility, switch thumb contrast in both states + sizes, validation red border + halo, disabled dim cascade through `:has()`), `/input-group` (wrapper focus halo, group validation red border, button-as-child corner clipping via `overflow: hidden`, textarea-addon top anchor, addon icon size pinning), `/buttons` (toggle section). **Maintainer follow-ups within the same session**: (a) **indicator sizing** — checkmark / radio dot / indeterminate dash rendered tiny because the base rule clamped `background-size: 80% 80%` and the SVG paths only used the inner 60% of their viewBox. Dropped the 80% cap (base rule now sets `background-size: 100% 100%` so the indicator scales with input + density), redrew paths with larger coords (checkmark: `3 8.5 6.5 12 13 5` stroke 2.25; dash: `3 8 → 13 8` stroke 2.5; radio: r=4), bumped switch thumb 10 → 12 (default) and 18 → 20 (lg) with inset 3 → 2 so the thumb fills more of the track for a tighter iOS-style ratio. (b) **Hover-on-checked bug** — base hover rule's `:not()` exclusion was missing `:checked, :indeterminate`, so hovering a checked checkbox or radio dimmed its primary border to the generic hover gray. Added both pseudos to the exclusion list. (c) **`.btn-check` → `.toggle` rename + relocate** — the name `.btn-check` was BS5-inherited and obscure; the pattern's audience (people reaching for "toggle button") wouldn't think to look on a checkbox page. Renamed to `.toggle` (reads as intent, not implementation), moved to a new `components/_toggle.scss` so it stops cluttering `_form-check.scss`, uncommented import in bundle right after `_btn.scss`. Demo section dropped from `/checks-radios.njk`; new `<h2>Toggle</h2>` section spliced into `/buttons.njk` between Loading and "Works on any element" — checkbox single-toggle + radio segmented-group (Day/Week/Month) + a danger-tone variant to show `--btn-tone` cascading through the checked override. CSS rules unchanged from the prior `.btn-check` shape (input is `.visually-hidden`-style absolute; `:checked + .btn` overrides 4 `--btn-*` vars; `:focus-visible + .btn` paints the focus outline on the sibling label). The composition with `.btn`'s Step 3.1 tone system is intact — the rename is purely surface. Bundle 55.1 → 55.2 KB raw / 9.83 KB gz (negligible — `.toggle` ~25 lines replaces `.btn-check` ~25 lines). `0` `--bs-*` leaks; `0` `.btn-check` references in compiled CSS or rendered HTML. (d) **Switch extracted to its own component** — the `.form-check--switch` modifier model was wrong: the shape is genuinely different from checkbox / radio (track + thumb vs box + check), the use case is different (binary on/off setting vs option pick), and burying it under form-check made the docs harder to navigate. New `components/_switch.scss` (~135 lines) ships `.switch` / `.switch__input` / `.switch__label` with `--inline` / `--reverse` / `--lg` modifiers. Naming: `.switch` (not `.form-switch`) per maintainer preference — aligns with Radix / shadcn / Reka convention; collision-resistance traded for discoverability. Off-state track is `color-mix(in oklch, var(--st-foreground) 25%, transparent)` (same contrast logic from the original switch — `--st-neutral` reads too light against white thumb). Thumb is a `white` literal radial-gradient circle sliding via `background-position` between `--switch-inset` (off) and `calc(100% - --switch-inset)` (on). Component-scoped vars (`--switch-track-w` / `-track-h` / `-thumb` / `-inset` / `-off-bg` / `-on-bg` / `-thumb-color`) so density and `--lg` recompute cleanly. Validation hooks match form-check (aria-invalid + :user-invalid, convention path — fill follows on/off, border carries invalid). Switch sections lifted out of `_form-check.scss` (cleaner — the file now does just two things instead of three). `/switch` demo page lifts Default / Large / Settings row / Disabled / Without label sections from `/checks-radios`, adds the Browser validation + Server validation split matching form-check + form-control. `/checks-radios` lead paragraph trimmed and links to `/switch` for the track variant; the Reverse demo's switch row swapped to a reversed radio (so the section still shows two examples). Sidebar entry added under Forms group between Checks & radios and Range. (e) **toggle redesigned + extracted to its own page** — original treatment (outline-primary → filled-primary via `--btn-tone` cascade) was wrong for the segmented-control use case. Per maintainer reference (font-weight picker, alignment picker), the visual should be neutral-container: outline-neutral default, `--st-accent` fill when selected. Use case is "pick one of these", not "trigger this action" — primary fill reads as call-to-action and competes with the actual primary buttons in the same layout. Rewrote `_toggle.scss` (~95 lines): forces outline-neutral on `.toggle + .btn` regardless of `.btn--<tone>` modifiers (toggle owns the visual entirely; `.btn` just contributes shape — height, padding, density, focus ring). Off + hover → `--st-accent`; selected → `--st-accent` with border kept so segmented rows stay defined; selected + hover → `color-mix(--st-accent 80%, --st-foreground)` for subtle bolder fill. Focus ring on the label (input is visually hidden) via the standard `.btn` outline vocabulary. New `/toggle` docs page with How-it-works copy explaining the input + label pairing, Single toggle (checkbox-backed for Bold/Italic/Underline-style settings), Segmented (radio-backed for Day/Week/Month-style pickers), Icon-only (alignment picker with lucide icons + aria-label), Sizes (`.btn--sm` / `.btn--lg` on the label), Disabled, and Versus `.btn-group` (HTML-only no-JS vs JS-coordinated tabs — same visual, different state ownership). Toggle section dropped from `/buttons.njk`. Sidebar entry added between Buttons and Button group. Bundle 55.2 → 56.6 KB raw / 9.97 KB gz (+1.4 KB across the new switch component; toggle redesign offsets). `0` `--bs-*` leaks; `0` `.form-check--switch` references in compiled CSS. 40 pages rendered (was 38; new `/switch` + `/toggle`). Visual verification pending — maintainer to eyeball: `/switch` (track + thumb on/off contrast in both densities + sizes, settings row pinning, validation), `/toggle` (off/selected/hover/disabled in segmented row, icon-only alignment picker, size variants), `/checks-radios` (no leftover switch references, reverse-radio swap), `/buttons` (Toggle section cleanly gone). Next: **Step 3.3c (form-range)**, then Step 3.4 (layout family — app-shell / page / navbar / sidebar).
- 2026-06-08 — Prose follow-ups: expanded demo, vars-table cleanup, consolidation. **Demo expansion**: `/typography` prose article grew from a third-of-the-elements showcase to a full element sweep (h1+h2+h3, paragraphs with inline `<a>`/`<strong>`/`<em>`/`<code>`/`<kbd>`/`<small>`, ul with nested ul, ol, dl, blockquote, `<pre><code>` block, hr, `<img>` via data-URI SVG banner, `<figure>` with inline `<svg>` type-scale chart + figcaption, 6-row table). Lead copy above the demo lists every covered element so readers know what they're seeing. **Vars table cleanup**: `/typography` customization vars table dropped its `ui.demo()` wrapper and inline-styled `<td>` padding hack — it's reference content, not a demo. Wrapped in `<div class="prose">` so it picks up the prose table treatment, consistent with the article above. **Consolidation**: the site shipped its own 90-line `.prose` block in `site.scss` that styled every docs page (`base.njk` wraps content in `<div class="main-container prose">`) — and the framework shipped a separate `.prose` for long-form content. Two `.prose` definitions, name collision, only sort-of coexisting via `.not-prose` opt-out on demo viewers. Now the framework owns `.prose` and the site adds docs-context cascade overrides on `.main-container.prose`. One definition, two contexts. **`_prose.scss` refactor**: selectors moved from direct-child (`> h1`, `> p`, etc.) to descendant (`:where(h1)`, `:where(p)`) with the `.not-prose` opt-out gate (`:not(:where(.not-prose, .not-prose *))`) on every rule. `:where()` keeps prose-rule specificity at (0,1,0) so consumer overrides like `.main-container.prose h2 { … }` at (0,2,1) win cleanly without `!important`. Direct-child was wrong for the page-wrapper use case: docs pages structure content as `<header><h1>…</h1></header><section><h2>…</h2></section>` and headings aren't direct children of the wrapper. Descendant + `.not-prose` is the Tailwind Typography pattern; demos that need to escape prose styling drop `.not-prose` on the wrapper element. **`_demo.njk` macro arg**: `demo(prose=false)` accepts a `prose` arg; when `true`, omits `.not-prose` from the `.demo` wrapper so the inner content renders with framework prose. Used by `/typography`'s long-form article demo (the only demo whose preview IS prose content). **Architectural limit to remember**: nested `.prose` inside `.not-prose` cannot reactivate — the opt-out gate trips on the inner elements because they still have `.not-prose` as an ancestor. Tailwind Typography has the same limitation. The `prose=true` macro arg is the escape hatch. Any future docs page that needs to demo `.prose` content inside a demo viewer uses the same arg. **`site.scss` cuts**: deleted the 90-line custom `.prose` block, replaced with ~50-line `.main-container.prose` overrides — tighter docs heading sizes (1.75rem / 1.125rem / 1rem in fixed rem so they don't scale with prose em scale), muted body paragraph color (page chrome stays quiet, demos pop), section/header structure spacing (3rem inter-section). Same `:where()` + `.not-prose` opt-out pattern as the framework prose. **Bordered + rounded prose table**: framework prose `<table>` gains `border: 1px solid var(--st-border)` + `border-radius: var(--st-radius)` + `border-collapse: separate` + `border-spacing: 0` + `overflow: hidden`. `border-collapse: separate` is needed for `border-radius` to render cleanly (Preflight defaults to `collapse` which fights the radius at corners). `overflow: hidden` clips any consumer-added cell backgrounds (`thead { background: … }`) to the rounded outer shape. Bundle 45 → 47 KB raw (+2 KB across `.not-prose` gate selectors + table border rules), site stylesheet stable (~3.4 KB), 0 `--bs-*` leaks, build green. Pages to spot-check in light + dark: `/typography` (article reads as long-form, customization table reads as bordered rounded reference data, page chrome stays quiet), any other docs page (heading scale + muted paragraphs match the previous rhythm). Visual verification pending. Next: Step 3.3b (form-check + input-group).
- 2026-06-08 — Step 3.x done (Preflight + `.prose` foundation). `_reboot.scss` rewritten Preflight-style — universal `* { margin: 0; padding: 0; border: 0 solid }` kills default spacing; `h1`–`h6` `font-size: inherit; font-weight: inherit`; `<a>` `color: inherit; text-decoration: inherit`; `b/strong/em/i/small` lose visual treatment (semantics preserved, visuals stripped); `ol/ul/menu` `list-style: none`; form controls inherit font/color/letter-spacing; `<img>/<svg>/<video>/<canvas>/<audio>/<iframe>/<embed>/<object>` `display: block`; `img/video` `max-width: 100%; height: auto`. Kept v3 chrome: `<mark>` highlight, `<abbr[title]>` dotted underline, `<hr>` via `--st-border`, `:focus-visible` outline at `--st-ring`, `<sub>/<sup>` defaults, code-family inherits `--st-font-mono`. Fetched current Tailwind Preflight verbatim and adapted — the universal-`*` reset is the heavy lifter (kills `<p>/<h1-6>/<ul>/<ol>/<dl>/<blockquote>/<figure>` margins in one rule instead of one rule per element). **`_typography.scss` cuts**: dropped `.list-unstyled` (Preflight makes it redundant), `.list-inline` + `.list-inline-item` (BS5 layout shortcut, v3 equivalent is flex + gap), `.blockquote` + `.blockquote-footer` (long-form treatment, moved to `.prose`). Kept `.h1`–`.h6`, `.lead`, `.display-*` — app-level typographic patterns. **New `components/_prose.scss` (~210 lines)** — long-form treatment for article/CMS content. Element-level restoration for headings (em-based scale, density-scaled margins), paragraphs, lists with markers, dl, blockquote (left border + italic), hr, inline code (chip), pre (code block), images, figures, table. Inline `:not(pre) > code` is chip-style (bg + border + radius); `pre > code` resets to bare mono. Table inside prose is reading-table look (header underline only, no zebra/hover) — explicitly NOT shared with the `.table` component because dashboard table and reading table are different jobs. 5 customization vars: `--st-prose-line-height` 1.625 / `--st-prose-link-color` `var(--st-primary)` / `--st-prose-marker-color` `var(--st-muted-foreground)` / `--st-prose-quote-border` `var(--st-border)` / `--st-prose-headings-color` `var(--st-foreground)`. **Cascade-only nested overrides** — `.prose` doesn't invent prose-specific vars for nested components (no `--st-prose-kbd-padding-y`); instead `.prose { --st-kbd-padding-y: ... }` retunes the kbd component via the cascade. V3.md §3.4 component-scoped fallback pattern carries the customization for free. `.prose` does NOT set `max-width` — typography is treatment, parent is layout box; pattern is `<article style="max-width: 65ch"><div class="prose">…</div></article>`. **New `.link` utility** in `utilities/_text.scss` — Reboot strips `<a>` color + underline globally; `.link` opts back in for inline app-UI links (`color: var(--st-primary)` + `text-decoration: underline` + hover mix). **`/typography` page rewritten**: top reframed (two contexts — utilities for app UI, `.prose` for long-form); Headings demo uses `<p class="h1">…<p class="h6">` to show that `<h1>` element is now blank by default and the `.h1` class carries the visual; cut sections — inline elements (small/strong/em demos relied on auto-treatment now stripped), weight (`.fw-*`), size (`.fs-*`), color (`.text-*`), lists (`.list-*` utilities dropped), blockquote (utilities dropped); added long-form (`.prose`) section with full article showcase + customization vars table. **`/typography` doesn't demo single-property text utilities anymore** (`.fw-*`, `.fs-*`, `.text-*`, `.lh-*`) — they belong on a future `/text-utilities` page or similar. Page scope = source-file scope (V3.md §3.10 added). **Demo sweep**: `customization.njk` + `cards.njk` footer notes switched from `<small style="color: var(--st-muted-foreground)">` to `<span class="small text-muted-foreground">`; `spinners.njk` + `form-control.njk` bare `<a href>` cross-references gained `class="link"`. BS5-era demo pages (offcanvas, list-group, tooltips, breadcrumb, tabs, navs) carry `.list-unstyled` / bare `<a>` / `<small>` uses too but left untouched — their components are still pending wholesale rewrite, and a partial Preflight fix there would mask the BS5-isms in place. **V3.md updates**: §3.1 Reset row rewritten to describe the Preflight scope; new "Visual treatment — three layers, no gaps" paragraph at §3.1 close stating the rule (Preflight handles global reset → `.prose` handles long-form restoration → app UI composes from utilities + patterns + components; no utilities live in the gap); §3.10 "Pages mirror the source tree" bullet added (the typography/utilities split rule). Bundle 40 → 45 KB raw (+5 KB for `.prose`), 42 `.prose` selectors, 0 `--bs-*` leaks, all 5 `--st-prose-*` vars present. Verified: build green, 38 pages rendered. The card footer flex fix from the prior commit retires its motivating bug (inline strut leak) — Preflight stops the `<small>` 80% inheritance at the root, so flex on `.card__footer` becomes nice-to-have rather than load-bearing (left in place — it's a legitimate layout pattern for footer content rows). Next: Step 3.3b (form-check + input-group).
- 2026-06-08 — Card footer flex + diagnostic surfaced a foundation gap. `.card__footer` gained `display: flex; align-items: center; gap: 0.5rem` (keeping its existing y-padding). The fix breaks the **inline strut leak**: a bare `<span class="text-xs …">` inside the footer was inheriting `.card__footer`'s block `line-height: 1.5` strut because CSS computes a block container's line-box height as `max(strut, tallest-inline-line-height)` — the span's tighter `text-xs` line-height had zero effect, so any short label read low in the band as font-size shrank (Photoshop-style "anchored to the bottom" when scaling text down). Flex children get their own formatting context, so the span's own line-height applies. **Glue code reverted from earlier in the session**: the speculative `.text-xs/.text-sm/.text-base/.text-lg` utilities in `_text.scss`, the `<small>` → `<span class="text-sm …">` swaps in `/cards` and `/customization`, the prior `.card__footer` asymmetric padding patch, and a flex+min-height+`text-box-trim` attempt — all reverted before the flex fix landed. **🚧 NEXT — Preflight + prose foundation (architectural shift, do BEFORE more component PRs).** The diagnostic dug into Inter's metric imbalance (typoAscender ≈0.99em vs typoDescender ≈0.25em, ~4:1), then `<small>`'s 80% browser default, then the `<span>` strut leak — different faces of the same root cause: **HTML-element defaults leak into v3 components and the foundation doesn't intercept them**. Per-component padding/line-height patches never converge. Reference is Tailwind's Preflight: aggressive reset so every element starts as a blank canvas. Concretely: `<p>/<h1-6>/<ul>/<ol>/<dl>` lose margins + font-size diffs; `<small>/<b>/<strong>/<em>/<i>` lose visual treatment (no auto-italic, no auto-bold, no shrink); `<button>` inherits font + cursor; `<img>` becomes `display: block; max-width: 100%`; `<a>` drops default color/underline; root `font-size` unifies. Class-based styling becomes the only path to visual treatment — the existing `.h1`–`.h6` / `.lead` / `.display-*` / `.fw-*` / `.fs-*` utilities are already the destination, Preflight just removes the elements-doing-it-by-default fallback. Pair with a new `components/_prose.scss` (`.prose` / `.content`, ~500 lines, equivalent to `@tailwindcss/typography`) as the escape hatch where semantic typographic defaults are wanted — long-form pages, CMS body, markdown output; everything inside `.prose` gets the heading scale, paragraph margins, list markers, blockquote styling, etc. back. Why now: (a) aligns with the framework-agnostic vision in V3.md — porting v3 to React/Vue means porting a blank canvas, not a cascade of browser defaults; (b) solves an entire class of "element X misbehaving inside component Y" bugs (today's card footer is one face, future ones avoided wholesale); (c) clarifies the rule that visual treatment = utility/component class, not element identity. Files: `_reboot.scss` (expand aggressively — current reboot is small typographic resets, not yet Preflight-level), `_typography.scss` (heading-as-class utilities become the only path — comment update to reflect that), new `components/_prose.scss`, demos audit (`/typography` first — every `<small>` use needs the genuine-small-print-vs-muted-text-shorthand call; `<p>` paragraphs in demos that relied on default margins; `<ul>`/`<ol>` in feature lists that relied on default bullets and indent — those move inside `.prose` or get utility classes). Risks: bigger PR than today's bug; demo migration is wide but mechanical; `.prose` scope is the design question — what nests, how components inside prose behave (a `<button>` in prose stays a button), how it composes with `.card__body` (no, prose is the parent, card is content — sort it out in design). Scope as **Step 3.x — Preflight + prose foundation**, ship as one PR. The today's flex fix is tactical; the full strut/defaults bug retires when Preflight ships. Next: design `.prose` scope + draft the Preflight reset diff before any more component ports — and resume Step 3.3b (form-check + input-group) only after Preflight lands.
- 2026-06-08 — Height architecture amendment landed. `.btn` and `.form-control` (single-line) switch from `min-height` floor + padding tuning to hard `height: calc(var(--*-height) * var(--st-density))` with `padding-block: 0`. Native vertical centering handles the label/value. Sizes (`--sm` 28 / default 36 / `--lg` 44) get a per-size `--*-height` literal; the density multiplier on the base resolves them. **Multi-line opt-outs**: `textarea.form-control`, `select[multiple]`, `select[size]:not([size="1"])` flip to `height: auto` + `min-height` floor + their own `padding-block` — same shape; the textarea also keeps `line-height: 1.5` for multi-line leading. **`.btn--wrap` opt-out** for the rare multi-line button case (full-width form-submit with helper text inside, mostly): restores `height: auto` + `min-height` floor + `padding-block` + `white-space: normal` + `line-height: 1.25`. Documented as "opt-in flexibility, strict default" per the design-system convention (Radix/shadcn/Material). **`.btn` default contract** is now single-line, `white-space: nowrap`, `overflow: hidden`, `text-overflow: ellipsis` — over-constrained widths truncate instead of breaking the hard-height box. **`--btn-bevel` knob** lifts the inset top highlight off a hardcoded literal — `--btn-bevel: none` on `:root` ships a flat-button look; outline/ghost/soft already null it (was `box-shadow: none`; now `--btn-bevel: none` for consistency). **`--st-density` is now a real structural lever** — at `0.875` produces a 31.5px button + 31.5px input, not a 36px shell with shrunk inner padding. The 28.2px `.form-control--sm` band-aid (line-height 1.2 to pin natural box below min-height) is gone — line-height 1.2 stays because native input vertical centering is unaffected by it, but the band-aid framing is retired. Surface audit: `.icon-box` already used `calc(size * density)`; `.spinner` now does the same (compact dashboards compress spinners too); `.badge` stays on `min-height` (pill is intrinsic and adapts to text content — that's V3.md §3.4 opt-out behavior, not a height contract). `.form-control--plaintext` switches to `display: inline-flex` + `align-items: center` + hard `height` so it aligns with sibling fields at every density (was using a magic line-height that didn't track density). V3.md §3.2 density paragraph updated with the height-aware-component pattern. Files touched: `_btn.scss`, `_form-control.scss`, `_spinner.scss`, `V3.md`. Bundle delta to confirm at build verification. Next: build + grep verify (zero `--bs-*` leaks, sanity-check `/buttons` + `/form-control` + `/customization` at densities 0.875 / 1 / 1.125), then Step 3.3b (form-check + input-group) inherits this pattern.
- 2026-06-08 — Step 3.3a done (form-control). `_form-select.scss` **deleted**; `.form-select` removed from the public API entirely. `.form-control` now covers `<input>`, `<select>`, `<textarea>` via element/attribute selectors — `select.form-control` paints the chevron + sets `appearance: none` + adds inline-end padding for the chevron well; `select.form-control[multiple]` / `[size]:not([size="1"])` drops the chevron and restores native list rendering with `appearance: auto`; `textarea.form-control` drops the min-height clamp and gains `resize: vertical`; `.form-control[type="file"]` uses `display: flex` with `::file-selector-button` stretched edge-to-edge. Sizes 28 / 36 / 44 match `.btn`; `.form-control--{sm,lg}` pull `--st-radius-{sm,lg}` so corners scale proportionally with `--st-radius`. State derivation via `color-mix(in oklch, ...)` over `--st-*` tokens (no BS5 vars touched). Focus is a 3px transparent halo via `box-shadow` — different vocabulary from `.btn`'s opaque `outline`: fields already wear a 1px rim, so an opaque outline+offset reads aggressive on a continuously-bordered shape; the halo says "you're here" without competing. Documented in the file. **Validation drops BS5 entirely** — no `.is-valid`, no `.is-invalid`, no `.was-validated` wrapper. v3 hooks: `[aria-invalid="true"]` (server / JS / explicit) + `:user-invalid` (native validation after first interaction, not on initial render). Same a11y semantic for free (screen readers announce "invalid entry"), one declarative attribute instead of a class, and the same selector pair covers every input type. `/validation` page **deleted** (it documented the dropped BS5 patterns end-to-end — `.was-validated`, `.valid-feedback`, `.is-valid` on form-check, etc.); validation now demoed on `/form-control` as a section. Sidebar entry removed. Modifiers: `.form-control--{sm,lg,plaintext,color}`. **Color picker folded in** — `.form-control--color` on `<input type="color">`; swatch wears `(radius - padding)` so chip and frame share a center (V3.md §3.4 inner-radius rule). **Label + helper** ship in the same file as siblings (not BEM children): `.form-label`, `.form-text`, `.form-text--error`. Token surface stays at 30 — the only exception to the "literal colors only in `_theme.scss`" rule is the chevron SVG, which has two literals (one per theme) because `data:` URLs can't read CSS vars; documented in the file. Bundle 34.4 → 39.96 KB raw (+5.6 KB) / 6.8 → 7.80 KB gz. `0` `--bs-*` in compiled CSS (verified via grep). `/form-control` rewritten on v3 BEM (added Validation + Color picker sections; Plain text section pairs with `readonly` per native semantics); `/select` rewritten to `<select class="form-control">` (URL kept — the page now legitimately demos "the form control on a `<select>`", lead copy clarifies the same-class point). Side fixes: `app-shell.njk` and `list-group.njk` picked up the `.form-control-sm` → `.form-control--sm` rename (one-line bug fix each — size modifier was silently missing). `input-group.njk`, `offcanvas.njk`, and `_input-group.scss` / `_form-check.scss` still reference `.form-select` / `.form-control-sm` — left for **Step 3.3b** (input-group port rewrites the page; offcanvas leaves it for the offcanvas session) and `_form-check.scss` rewrites on its own port. Visual verification pending — agent grepped clean, build green; maintainer to eyeball `/form-control` and `/select` in light + dark (chevron stroke contrast, focus halo, color-picker swatch radius, `color-mix` invalid border). **Late-session band-aid:** `.form-control--sm` rendered at 28.2px (not 28) because the base `line-height: 1.4` pushed the natural box past the `min-height` floor. Patched to `line-height: 1.2` matching `.btn`; this is a band-aid, not the fix — see "next" below. **🚧 NEXT — height architecture amendment (do BEFORE Step 3.3b).** Every height-aware v3 component (`.btn`, `.form-control`, and the height-via-min-height surface bits — `.icon-box`, `.spinner`, `.badge`) sets `min-height` as a floor and tunes padding × line-height × font-size × border so the natural box lands at-or-below it. Two real pains: (1) brittle — adjusting any of those values risks overshooting the floor, which is exactly how the 28.2px miss happened; (2) **`--st-density` doesn't actually shrink components at <1** because `min-height` is a floor, not a clamp — only the padding shrinks, so the "compact" preset on `/customization` is cosmetic, not structural. Fix: switch `.btn` + `.form-control` to hard `height: calc(var(--*-height) * var(--st-density))` with `padding-block: 0` for single-line shapes (input/select/btn — native vertical centering handles content). Multi-line cases (`textarea`, `<select multiple>`, `<select size>`) opt out via `height: auto` + `min-height` floor + their own padding-block. Density then becomes a real lever: density 0.875 → 31.5px default form-control, truly compact. Touches `.btn` (Step 3.1), `.form-control` (this session), a once-over on `.icon-box` / `.spinner` / `.badge` height usage, and the V3.md §3.2 density-token doc (note that density multiplies `height`, not just padding). Must land BEFORE Step 3.3b so `_input-group.scss` sizing inherits the new pattern instead of porting the old one. Next: **height amendment**, then Step 3.3b (form-check + input-group).
- 2026-06-08 — Card follow-up to Step 3.1. Default `.card` now carries a one-pixel `--st-border` so the card reads as a card even with shadow turned off — matches the BS5 original (Stisla v2's `card_base` mixin had explicitly removed it, but with v3's new elevation values that left bare elevation-only cards looking unframed in dark mode and against `--st-surface-2` backdrops). `.card--bordered` → `.card--flat` rename: the modifier now only zeroes the shadow (border inherits from default) and the name describes the *result* rather than the redundantly-restated border. V3.md §3 BEM table + `/cards` demo updated; demo section renamed `Bordered` → `Flat` with copy explaining the default border. No new tokens, no bundle delta beyond the modifier rename.
- 2026-06-08 — Step 3 #2 done (surface family). Six components landed on the v3 model: `_alert.scss`, `_badge.scss`, `_icon-box.scss`, `_kbd.scss`, `_spinner.scss` (new file — was 0 B), `_placeholders.scss`. All BEM with intent modifiers composing flat (`.alert--primary`, `.badge--soft.badge--primary`, `.icon-box--danger`, `.spinner--grow.spinner--sm`, `.placeholder--glow`), all states via `color-mix(in oklch)`, all padding wrapped in `calc(X * var(--st-density))`. **Alert** requires a tone modifier — bare `.alert` renders transparent, same contract as `.btn`. Tones: `.alert--neutral` (surface bg + neutral border) + the five intents. Parts: `__heading` / `__description` / `__action` / `__link`; layout switches via `:has(.alert__description)` (single-line row → grid with row-span action). Leading icon is any direct `<svg>`/`<i>` child (matches `.btn`). `--alert-link-color` lives separate from `--alert-icon-color` so a neutral alert's inline link stays primary while intents flip both to the intent color. Dismiss is a plain `.btn--ghost.btn--neutral.btn--icon-only.btn--sm`, not a special control — `.btn-close` is gone, `/close-button` demo page deleted (sidebar entry removed). JS to actually close the alert lands in Step 4. **Badge** is pill-by-default (opts out of `--st-radius`, V3.md §3.4), 1.375rem (22px) min-height clamp, neutral filled with no modifier, `.badge--soft` mixes 15% over `--badge-tone` for tinted chips. `--badge-tone` defaults to `--st-muted-foreground` on the base class so the soft modifier reads the tone the intent published — order-independent cascade (`.badge--soft.badge--primary` and `.badge--primary.badge--soft` both render correctly). **Btn** picked up the rename `.btn--light` → `.btn--soft` for cross-component naming consistency — soft is now the only filled-tinted shape across `.btn` and `.badge`. **Icon-box** keeps the 36×36 tile (`--sm` 28, `--lg` 48); `--icon-box-tone` is the single knob for one-off colors. **Kbd** styles the native element directly — every `<kbd>` picks up the chip without a class (resolves the typography step's deferred styling). **Spinner** ships border (default) + `.spinner--grow`, three sizes, slow-spin under `prefers-reduced-motion: reduce` rather than freezing. **Placeholders** keeps the BS5 currentColor approach (wrap in `.text-muted-foreground` for the muted gray), `--glow` + `--wave` animations, both honor reduced-motion. **Utility:** `.visually-hidden` added to `utilities/_text.scss` under an Accessibility section — was missing from the v3 utility set, which caused spinner "Loading…" labels to render visibly and rotate with the spinner. Token surface stays at 30 — no new tokens added; everything composes from existing intents + interactional trio. Bundle 26.24 → 34.4 KB raw (+8.1 KB) / ~6.8 KB gz. `0` `--bs-*` in compiled CSS (verified via grep); demo HTMLs grep clean for BS5-ism leaks (no `data-bs-*`, no `text-bg-*`, no `badge-light-*`, no `.btn-close`, no `spinner-border`, no `text-body-secondary`). `.demo-stack` lost its `align-items: flex-start` so alert children stretch full-width (typography's stack uses still render correctly — block-level `<p>` children fill regardless); `.demo-preview > .alert` and `.demo-preview > .demo-stack:has(>.alert)` pin alerts to 100% width (the demo viewer's `justify-content: center` shrink-fits children by default, same opt-out pattern as `.table`). Demo pages rewritten on v3 BEM: `/alerts` (tones + heading/desc layout + action slot + dismiss + inline link), `/badge` (default + filled intents + soft + icon + loading + in-button), `/icon-box` (default + intents + shape + sizes + custom tone), `/spinners` (border + grow + colors + sizes + alignment + in-button), `/placeholders` (example card + sizes + colors + animation + buttons). Visual verification pending — maintainer eyeballed `/badge` (soft cascade), `/spinners` (visually-hidden), `/alerts` (full-width, tone contract); rest still to confirm in light + dark. Next: Step 3 #3 (form family — form-control, form-select, form-check, input-group; largest cluster, do as one block per MIGRATION.md §3 step 3).
- 2026-06-08 — Typography landed ahead of Step 3 #2 (surface family) since it's a foundation concern the surface components inherit from. **Baseline 14px** via `body { font-size: 0.875rem }`; `html` untouched so `1rem` stays = 16px and every rem-based component keeps its pixel meaning. Heading scale tuned for the 14px context: h1 2 / h2 1.5 / h3 1.25 / h4 1.125 / h5 1 / h6 0.875rem (32/24/20/18/16/14). `_reboot.scss` gained `<p>` / `<ul>` / `<ol>` / `<dl>` / `<dt>` / `<dd>` / `<blockquote>` / `<b>` / `<strong>` / `<abbr[title]>` / `<code>` resets — the small slice of BS5 reboot that's typography rather than layout. New `foundation/_typography.scss`: `.h1`–`.h6`, `.lead`, `.display-1`–`.display-6`, `.list-unstyled`, `.list-inline` / `.list-inline-item`, `.blockquote`, `.blockquote-footer`. New `utilities/_text.scss` bootstraps the `utilities/` layer: `.text-foreground`, `.text-muted-foreground`, `.text-{primary,success,warning,danger,info}`, `.fw-{light,normal,medium,semibold,bold}`, `.fs-1`–`.fs-6`, `.small`, `.lh-{1,sm,base,lg}`, `.text-{start,end,center}`, `.text-{lowercase,uppercase,capitalize}`, `.text-{nowrap,truncate,break}`. **BS5-ism cull:** the typography demo dropped `.text-body` / `.text-body-secondary` / `.text-body-tertiary` (BS5 names) for v3-named `.text-foreground` / `.text-muted-foreground`; tertiary was cut entirely since the token surface has only one muted tier. Token surface still 30 — no `--st-font-size-*` ramp added (same discipline as no `--st-space-*` ramp, V3.md §3.4). Bundle 24.6 → 26.24 KB raw (+1.6 KB) / 5.36 KB gz. `/typography` renders the full demo on v3 styles; `<kbd>` in the inline-elements + in-context demos stays unstyled until Step 3 #2 lands `_kbd.scss`. Visual verification pending — agent compiled + grepped (0 `--bs-*` leaks, all classes emitted, baseline confirmed); maintainer to eyeball `/typography` in light + dark. Next: Step 3 #2 (surface family — alert, badge, icon-box, kbd, spinner, placeholders).
- 2026-06-08 — Step 3.1 done (btn + card architecture proof). `_btn.scss` ships BEM v3 buttons — 4 curated tones (primary, neutral, tertiary, danger) × shapes (filled/outline/ghost/light) composing flat at the root via `--btn-tone`, plus sizes/icon shapes/loading/flush. Success/warning/info dropped as first-class button tones (rare in practice, WCAG-contrast hassle with white text); a "Custom color" section on `/buttons` documents the `--btn-bg` / `--btn-color` inline-override pattern for one-offs with a promotion-to-class guideline. Rim border + inset top highlight live on `.btn` base reading `--btn-bg`, so custom-color buttons inherit the bevel for free. Neutral filled gets an asymmetric rim via `color-mix(in oklch, var(--st-neutral) 85%, var(--st-muted-foreground))` so the border reads as recessed in light and rim-lit in dark. `_card.scss` ships BEM v3 cards — root + `__header/__body/__footer/__title/__subtitle/__text/__link/__image/__overlay`, `--card-bordered` modifier, position-aware image radii. `_buttons.scss` removed (renamed `_btn.scss` for singular consistency). Token surface bumped 28 → 30: added `--st-neutral` / `--st-neutral-foreground` interactional pair so neutral has a rest fill distinct from accent's hover semantic — V3.md §3.2/§3.3 + rule 3 updated. All states via `color-mix(in oklch)`; zero `--bs-*` in compiled CSS (verified via grep). Bundle 16 KB → 24.6 KB raw. New `/customization` smoke-test page replays btn + card under default, brutalist, soft, dense, violet, warm-neutrals, and per-component-radius presets — all via root-scoped inline `--st-*` overrides, zero CSS edits. `site.scss` retokened (`--bs-*` → `--st-*`) and gained `.demo-row` / `.demo-stack` / `.demo-grid` helpers used across demo pages. Sidebar theme-toggle button now uses v3 BEM. Buttons + cards demo pages rewritten — BS5 utilities (`.d-flex.gap-2`, etc.) replaced with `.demo-row`; nav-tabs/pills, list-group, `.text-bg-*`, and the btn-light/btn-dark explainer sections dropped (covered by future component PRs or cut entirely). Visual verification pending — agent can compile + grep but cannot eyeball; maintainer to confirm at /buttons, /cards, /customization in light + dark. Next: Step 3 #2 (surface family — alert, badge, icon-box, kbd, spinner, placeholders).
- 2026-06-08 — Step 2 done. 5 BS5-coupled token files deleted (`_colors`, `_maps`, `_root`, `_variables`, `_variables-dark`). `tokens/_theme.scss` ships the 28 `--st-*` OKLCH tokens (light + dark, `--st-base: 258` hue-locked to primary). Breakpoints moved `foundation/` → `tokens/` per V3.md §3.9. Bundle rebuilt with `@layer foundation, theme, components, utilities`; every BS5 `@import` gone; component imports commented out (Step 3 PRs uncomment as they land). `index.js` stripped of `import 'bootstrap'` + Popover/Tooltip auto-init; app-shell handlers kept. `bootstrap` + `@popperjs/core` deps removed. `stisla-full.css` 16 kB / 3.2 kB gz (down from 283 kB), site renders 39 pages with unstyled component markup — strict approach as designed. Mono stack is JetBrains Mono first. Next: Step 3 (btn + card — V3.md Phase 1 architecture proof). Blockers: none. OKLCH conversions for neutrals are estimates, validate during Step 3.
- 2026-06-08 — Step 2 approach locked: **strict.** Gut the BS5 token files, accept a broken bundle, let each Step 3 component PR heal the build incrementally. No interim BS5 shim. Trade: docs site renders broken between Step 2 and the end of Step 3.
- 2026-06-08 — Theme selector renamed `data-bs-theme` → `data-theme` + `.dark` class. V3.md §3.8 reworked to spec both conventions. `foundation/_mixins.scss` color-mode mixin emits `[data-theme="dark"], .dark`; overrides BS5's mixin so BS5's `_root.scss` dark block now flips on the Stisla selectors (no `data-bs-theme` shim needed). Compiled CSS has 4 paired dark blocks; zero `data-bs-theme` in rendered HTML. Spec, site CSS/JS, layout, and tools/nunjucks-filters all updated. The 3 residual `data-bs-theme` references in `tokens/_variables*.scss` + `components/_tooltip.scss` are dead comments in files slated for delete/rewrite — left alone.
- 2026-06-08 — Step 1 done. `src/scss/foundation/` lands with normalize, reboot, grid, containers, breakpoints, mixins. `LICENSES/` carries BS5 + modern-normalize MIT text. Bundle swap leaves the BS5 reboot/grid/containers behind. Reboot reads `--st-*` directly — no `--bs-*` fallbacks (V3.md §3: no --bs in v3 code). Bare surfaces (body fg/bg, link, hr, focus ring) until tokens land in Step 2; intended trade. **Scope shift:** `bootstrap` dep removal moved to Step 2 (tokens/* and components/* still reference BS5 vars and mixins — pulling the dep now would un-build the bundle). Next: Step 2 (token rewrite). No blockers.
- 2026-06-07 — Step 0 done. BS5 build snapshotted to `bs5-snapshot/` (44 files, 3.7 MB). Tagged `v3-bs5-snapshot` at commit `eba7fc1`. Next: Step 1 (foundation rewrite). No blockers.
- 2026-06-07 — V3.md spec locked. MIGRATION.md drafted. Step 0 (snapshot) not started. No blockers.

---

## Working in sessions

Every migration step is its own session — long-running work doesn't fit in one conversation. Each session should be self-bootstrapping from this file + V3.md + git history. No conversation context required.

### Maintainer: how to start a session

Open Claude Code in this repo and paste:

> "Let's continue the v3 migration. Read V3.md + MIGRATION.md, check git, and tell me what step we're on and what to do next."

That's it. Don't pre-load assumptions; the agent re-derives state from the docs + git.

### Agent: what to do on cold start

1. **Read V3.md.** The spec. Agent workflow rules at the top still apply — no writes until the maintainer says "go".
2. **Read MIGRATION.md.** Status (top) first, then the §3 step relevant to today's work, then the §4 disposition row if working on a component.
3. **Verify against git.** Don't trust Status blindly. Run `git log --oneline -20`, `git status`, `git tag --list 'v3-*'` to see what actually shipped. If the doc and git disagree, the doc is stale — flag it and propose an update.
4. **Identify next work.** §7 running checklist is the authoritative source for "what step are we on". Within a step, §3 has the sub-bullets; §4 has the per-component table.
5. **Confirm with the maintainer** what to do before touching code. Default proposal: "next unchecked box in §7" — but the maintainer may want to skip ahead, revisit, or branch off.
6. **At session end:** add one new line to the Status section (top of file). Format: `ISO date — what changed — blockers if any`. Commit as part of the session's final commit, or as a standalone `docs(v3): update migration status`.

### When in doubt

- Spec questions (token names, naming rules, what's in/out of scope) → V3.md.
- Migration questions (order, per-component action, comparison workflow) → MIGRATION.md.
- "Is this thing actually built?" → git, not the docs.

---

## 1. What exists today (BS5 v3, the thing we're migrating)

| Surface                | Counts              | Notes                                                                                                               |
| ---------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `src/scss/components/` | 30 files (~100 KB)  | Largest: `_sidebar.scss` 22 K, `_buttons.scss` 11 K, `_app-shell.scss` 6 K. `_progress.scss` is 0 B (placeholder).  |
| `src/scss/tokens/`     | 5 files (~50 KB)    | `_colors.scss`, `_maps.scss`, `_root.scss`, `_variables.scss`, `_variables-dark.scss` — all tightly coupled to BS5. |
| `src/scss/bundles/`    | `stisla-full.scss`  | Canonical BS5 customization workflow with Stisla overlays.                                                          |
| `src/js/`              | `index.js` (2.7 K)  | Re-exports `bootstrap`, auto-inits Popover/Tooltip, app-shell handlers.                                             |
| `src/site/pages/`      | 40 njk demos        | All use BS5 class hooks (`.fade`, `.show`), `data-bs-toggle`, `data-bs-target`.                                     |
| `package.json`         | `bootstrap: ^5.3.3` | Plus none of the new deps yet.                                                                                      |
| Foundation files       | —                   | `foundation/` directory doesn't exist; build pipeline relies on `@import 'bootstrap/scss/reboot'` etc.              |

---

## 2. Target

See V3.md. No re-litigation here. Token surface (§3.2), foundation (§3.1), naming (§3.6), spec-vs-impl split (§3.11) are all locked there.

---

## 3. Migration order

Each step lands and merges before the next starts. No "step 3 partial PR" overlapping step 4.

### Step 0 — Snapshot the BS5 version ✅

- [x] `npm run build` → captures current built site.
- [x] Copy build output to `bs5-snapshot/` at repo root (HTML, CSS, JS — no source).
- [x] Commit `bs5-snapshot/` with message `chore: snapshot v3 BS5 build before rewrite`.
- [x] Tag the commit: `git tag v3-bs5-snapshot`. Push tag with `git push origin v3-bs5-snapshot` when you next push the branch.
- [x] Verify: `npx serve bs5-snapshot` renders. Bookmark a few key pages (sidebar, modal, dropdowns, forms) for visual reference during rewrite.

**Comparison workflow after step 0:**

- Visual: open `bs5-snapshot/<page>.html` in one tab; live `npm run dev` of new build in another.
- Source: `git show v3-bs5-snapshot:src/scss/components/_btn.scss` next to your new `_btn.scss` in a split editor pane.
- Run-the-old-thing: `git worktree add ../stisla-bs5 v3-bs5-snapshot` for a parallel checkout if needed.

### Step 1 — Foundation rewrite (no visible UI change) ✅

- [x] Install deps: `@floating-ui/dom@1.7.6`, `focus-trap@8.2.1`, `tabbable@6.4.0`, `embla-carousel@8.6.0`. Vendor `modern-normalize@3.0.1` (don't depend on the package; copy the file).
- [ ] ~~Remove `bootstrap` from `package.json`.~~ **Moved to Step 2.** Tokens (`tokens/_variables.scss`) and components (`components/_app-shell.scss`, `_navbar.scss`) still reference BS5 vars (`$grid-breakpoints`) and the `bootstrap/scss/*` import chain in `stisla-full.scss` is the source of those vars. Removing `bootstrap` now un-builds the bundle. Step 2 (token rewrite) replaces the var surface and pulls the dep then.
- [x] Create `src/scss/foundation/` with:
  - `_normalize.scss` — vendored modern-normalize 3.0.1
  - `_reboot.scss` — Stisla opinions (~75 lines). Reads `--st-*` directly. No `--bs-*` references (V3.md §3). Affected surfaces (body fg/bg, link, focus ring, hr) sit on browser initial values until tokens land in Step 2.
  - `_grid.scss` + `_containers.scss` — forked from BS5. `--bs-gutter-*` renamed to `--st-grid-gutter-*`. RTL flips, `_root` emits, `.no-gutters`, `$enable-cssgrid` stripped. Mixins inlined (no separate `mixins/_grid.scss`).
  - `_breakpoints.scss` — forked from `bootstrap/scss/mixins/_breakpoints.scss`. `media-breakpoint-up/down/between/only` renamed to `media-up/down/between/only`. **Shim** at the bottom re-exports the old names so existing component files keep building until Step 3.
  - `_mixins.scss` — `color-mode` helper.
- [x] Add `LICENSES/bootstrap-MIT.txt` (full BS5 MIT text) + `LICENSES/modern-normalize-MIT.txt`. Each forked foundation file carries a credit header. README NOTICE block lists both.
- [x] Verify: `npm run build` is green. `stisla-full.css` 283 KB / 38 KB gz. modern-normalize banner present in output, `--st-grid-gutter-x` emits 44×, zero `--bs-gutter-x` in foundation-emitted CSS, `.container-{sm..xxl}` + `.col-md-{1..12}` all emit. 39 static pages render via `npm run build:site`.

### Step 2 — Token rewrite ✅

- [x] Delete `tokens/_colors.scss`, `_maps.scss`, `_root.scss`, `_variables.scss`, `_variables-dark.scss`.
- [x] Create `tokens/_theme.scss` — 28 OKLCH tokens, light + dark blocks. Single source of truth for color literals.
- [x] Move `foundation/_breakpoints.scss` → `tokens/_breakpoints.scss` (V3.md §3.9 puts it under `tokens/`). Bundle import path updated; `media-breakpoint-*` shim kept for the two components (`_navbar`, `_app-shell`) that still use BS5 names — retire during Step 3.
- [x] Update `bundles/stisla-full.scss` to the `@layer foundation, theme, components, utilities` order; every BS5 `@import` deleted; all components commented out (Step 3 PRs uncomment as they land).
- [x] Strip `import * as bs from 'bootstrap'` + Popover/Tooltip auto-init from `src/js/index.js`. Step 4 reintroduces via `@floating-ui/dom`.
- [x] `npm uninstall bootstrap @popperjs/core` — deps gone, lockfile refreshed.
- [x] All component files break here. That's intentional — fixed in step 3.

### Step 3 — Component rewrite (per-component, in dependency order)

See §4 disposition table for action per component.

Build order respects token + foundation dependencies:

1. **Tokens proof:** btn + card (V3.md Phase 1 architecture proof). Done before any other component.
2. **Surface family:** alert, badge, icon-box, kbd, spinner, placeholders (all use intent + surface tokens).
3. **Form family:** form-control (covers `<input>`, `<select>`, `<textarea>` — one class, element/attribute selectors layer on type-specific concerns), form-check, input-group, form-range. Originally planned as one block; split in practice because `_form-select.scss` collapsed into `_form-control.scss` (one concept, one class — V3.md §3.6) and the remaining three each have their own substantive surfaces. Foundation amendments — `height` vs `min-height`, then Preflight + `.prose` — interleaved between 3.3a and 3.3b; see §7 checklist for the canonical landing order. Sub-steps:
   - 3.3a — form-control (✓ done; also covers `<select>` + `<textarea>` and absorbs the dropped `_form-select.scss`)
   - 3.3b — form-check + input-group
   - 3.3c — slider (renamed from form-range; `.form-range` BS5 name dropped — matches shadcn/Radix/Reka convention, same rationale as switch + toggle)
4. **Layout:** app-shell, page, navbar, sidebar (sidebar last — biggest file).
5. **Display:** card, table, list-group, breadcrumb, pagination, progress, toast, accordion.
6. **JS-coordinated:** button-group, carousel (Embla), dropdown, modal, offcanvas, tooltip, popover. Each blocked by its step 4 JS component.

Per-component PRs land independently after the token rewrite (step 2) merges. Each PR updates the SCSS + the matching `.njk` demo page.

### Step 4 — JS rewrite (interleaved with step 3 JS-coordinated components)

- [x] `src/js/components/` directory created. ✅ (4.0 prelude)
- [x] `Stisla.init()` declarative scanner (`[data-stisla-*]` attrs → instantiate matching class). ✅ (4.0 prelude — `src/js/core/init.js`)
- [ ] Modal: `inert` on siblings + `focus-trap` + `data-state="open"` toggling + custom events.
- [ ] Offcanvas: same primitives as modal with transform animation.
- [ ] Dropdown: `@floating-ui/dom` + roving tabindex + `focus-trap` + outside-click + Escape.
- [ ] Tooltip: `@floating-ui/dom` + hover/focus delay state.
- [ ] Popover: `@floating-ui/dom` + `focus-trap` + outside-click + Escape.
- [ ] Accordion: native `<details>` or thin height-transition helper. Stay close to native.
- [ ] Tabs / segmented control: `data-state="active"` toggle on `.btn-group` items + arrow-key nav.
- [ ] Toast: queueing + dismiss + transitions.
- [ ] Carousel: thin Embla wrapper exposing Stisla class hooks.
- [ ] Bundle size check: ~25–30 KB gz total (deps + Stisla code).
- [ ] Replace `import * as bs from 'bootstrap'` and all `data-bs-*` scanners in `index.js`.

### Step 5 — Demo pages (`src/site/pages/`)

- [ ] Class name pass: `.btn-primary` → `.btn--primary`, `.modal-dialog` stays, `.modal-content` becomes `.modal__inner` (TBD per-component during step 3 rewrite).
- [ ] State attribute pass: `.fade.show` → `[data-state="open"]`. `.collapse.show` → `[data-state="open"]` or `<details open>`.
- [ ] Toggle attribute pass: `data-bs-toggle="modal"` → `data-stisla-toggle="modal"`. `data-bs-target` → `data-stisla-target`. Per-component conventions defined during step 3.
- [ ] Delete `navs.njk` (component family removed). Repurpose its content into `button-group.njk` as the tabs/segmented examples.
- [ ] Delete `scrollspy.njk` and `collapse.njk` if their content folds into other pages. Confirm before deleting.
- [ ] Add customization smoke-test page: brutalist preset, soft preset, dense preset, violet primary preset, warm-tinted neutrals preset — all via root token overrides only.

### Step 6 — Cleanup + 3.0.0-beta.1

- [ ] Remove `bs5-snapshot/` dir? Or keep until 3.0.0 final? Decision: keep until final; comparison value is still real during beta.
- [ ] Update README, CHANGELOG.
- [ ] Update `.github/workflows/authors.yml` (V3.md §5 known break).
- [ ] Tag `3.0.0-beta.1` for community review.
- [ ] After 3.0.0 final ships: `git rm -r bs5-snapshot/`; `git tag v3-bs5-snapshot-deleted` for posterity; delete this `MIGRATION.md`.

---

## 4. Per-component disposition

Action key:

- **port** — visual + behavior carry over with minimal change; just retoken and BEM-rename
- **rewrite** — start fresh on the v3 model; old file is reference only
- **write** — file is empty or doesn't exist; build from scratch
- **delete** — component family cut

| Component    | Size   | Action     | Notes                                                                                                                          |
| ------------ | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------ |
| accordion    | 4.5 K  | rewrite    | BS5 collapse JS dependency; new height-transition impl or native `<details>`                                                   |
| alert        | 4.1 K  | rewrite    | color-mix on `--st-*` intents; new BEM (`__icon`, `__heading`, `__description`, `__action`)                                    |
| app-shell    | 6.1 K  | port       | Stisla original; low BS5 coupling. Retoken + BEM verify                                                                        |
| badge        | 2.5 K  | rewrite    | color-mix variant model; opt-out of radius (pill)                                                                              |
| breadcrumb   | 1.9 K  | port       | Mostly clean; retoken                                                                                                          |
| button-group | 1.1 K  | rewrite    | Now also serves as tabs/segmented control with `data-state="active"`. Expanded scope                                           |
| buttons      | 11.2 K | rewrite    | Largest BS5 override pile (button-variant mixin); v3 BEM + color-mix from scratch                                              |
| card         | 1.9 K  | rewrite    | Surface tokens; BEM (`__header`, `__body`, `__footer`)                                                                         |
| carousel     | 3.0 K  | rewrite    | Now wraps Embla; styling minimal                                                                                               |
| dropdown     | 2.2 K  | rewrite    | Floating UI + `[data-state="open"]` + `[data-highlighted]`                                                                     |
| form-check   | 3.2 K  | rewrite    | Native `<input>` styling; cross-browser pass                                                                                   |
| form-control | 3.4 K  | rewrite ✅ | Hairiest BS5 forms override pile; v3 base + `:focus-visible` halo. Now covers `<input>` + `<select>` + `<textarea>` via element/attribute selectors. |
| form-select  | 1.8 K  | **delete** ✅ | Merged into `.form-control` — one concept, one class (V3.md §3.6). `<select class="form-control">` is the v3 select.        |
| icon-box     | 2.2 K  | port       | Stisla original; retoken                                                                                                       |
| input-group  | 3.6 K  | rewrite    | Adopts new form-control tokens                                                                                                 |
| kbd          | 670 B  | port       | Tiny; retoken                                                                                                                  |
| list-group   | 1.5 K  | rewrite    | `[data-state="active"]`; accent (hover) + highlight (active)                                                                   |
| modal        | 4.7 K  | rewrite    | New JS impl (focus-trap + inert + `[data-state="open"]`)                                                                       |
| nav          | 2.6 K  | **delete** | Family cut. See V3.md §4 Phase 2                                                                                               |
| navbar       | 4.0 K  | port       | Stisla original; retoken                                                                                                       |
| offcanvas    | 2.5 K  | rewrite    | Same primitives as modal                                                                                                       |
| page         | 4.0 K  | port       | Stisla original                                                                                                                |
| pagination   | 1.6 K  | rewrite    | `[data-state="active"]` for current page                                                                                       |
| placeholders | 328 B  | port       | Tiny                                                                                                                           |
| popover      | 1.1 K  | rewrite    | Floating UI                                                                                                                    |
| progress     | 0 B    | **write**  | File is empty. Build from scratch                                                                                              |
| sidebar      | 22.1 K | rewrite    | Largest file. Many custom features (collapse, sub-menus, footer). Core logic carries; tokens + state attrs are the new surface |
| table        | 3.7 K  | rewrite    | Surface tokens; hover/highlight on rows                                                                                        |
| toast        | 3.2 K  | rewrite    | New JS impl                                                                                                                    |
| tooltip      | 856 B  | rewrite    | Floating UI                                                                                                                    |

Demo pages (`src/site/pages/`) follow the matching component PR. Pages that exist without a backing component (e.g. typography, images) are port-only.

---

## 5. Risks / blockers

- **`_progress.scss` is 0 B.** No reference to port from. Build from scratch in step 3.
- **`navs.njk` deletion.** Component family cut; demo page content folds into `button-group.njk`. Confirm before deleting any user-visible URL.
- **BS5 JS hooks across .njk.** `data-bs-toggle`, `data-bs-target`, `.fade`, `.show`, `.collapsing` need a sed-replace pass per component during step 5. Easy to miss one.
- **`.github/workflows/authors.yml`** references deleted `dev/update-authors.js` (V3.md §5). Fix during step 6.
- **Dark mode regression risk.** Every component PR in step 3 must verify dark mode (V3.md Phase 2 checklist requires this).
- **Customization smoke test.** V3.md Phase 1 requires brutalist/soft/dense/violet/warm presets via root-only overrides. This is the architecture proof; if any preset doesn't work, the token model has a hole.

---

## 6. Don't migrate — these are additions, not migrations

Phase 7 (v3 stable, post-3.0) components from V3.md §4 are net-new. Don't bundle them with the migration:

- avatar
- otp
- custom-select
- autocomplete
- combobox
- preview-card
- command (spotlight)
- date-picker

Build them on top of the rewritten foundation after 3.0 ships.

---

## 7. Running checklist

- [x] Step 0 — Snapshot BS5 build, tag `v3-bs5-snapshot` ✅
- [x] Step 1 — Foundation rewrite (deps swap, `foundation/` files, `LICENSES/`) ✅
- [x] Step 2 — Token rewrite (`_theme.scss` + `_breakpoints.scss`) + remove `bootstrap` dep ✅
- [ ] Step 3 — Component rewrite (per §4 table, in order from §3 step 3)
  - [x] 3.1 — btn + card architecture proof (V3.md Phase 1) ✅
  - [x] 3.2 — surface family: alert, badge, icon-box, kbd, spinner, placeholders ✅
  - [x] 3.3a — form-control (covers `<input>` + `<select>` + `<textarea>`; absorbs the dropped `_form-select.scss`) ✅
  - [x] Height amendment — controls use `height` not `min-height` ✅ (`.btn`, `.form-control` single-line, `.icon-box`, `.spinner` all density-multiplied; `.btn--wrap` + textarea/multi-select opt out; `--btn-bevel` knob added)
  - [x] Preflight + `.prose` foundation ✅ (`_reboot.scss` Preflight-style; `_typography.scss` cut `.blockquote*` + `.list-*`; new `components/_prose.scss` + 5 `--st-prose-*` vars; `.link` utility added; `/typography` page rewritten; cascade-only nested-component overrides; V3.md §3.1 + §3.10 updated)
  - [x] 3.3b — form-check + input-group ✅
  - [x] 3.3c — slider (renamed from `.form-range`; new `_slider.scss`; `--slider-*` vars; sizes + validation hooks) ✅
  - [x] 3.4a — app-shell (port; BEM rename `__main`/`__body`; 11 `--app-shell-*` vars; `media-down(lg)`; `--app-shell-sidebar-z` replaces BS5 `$zindex-fixed`; demo uses placeholder sidebar/navbar slots until 3.4c/d ship) ✅
  - [x] 3.4b — page (port; BEM rename `__header`/`__title`/`__action`/`__section`/`__section-header`/`__section-title`; 8 `--page-*` vars all density-multiplied; `.page__title` promoted to flex-column with `--page-title-gap` to compensate for Preflight-stripped heading/paragraph margins) ✅
  - [x] 3.4c — navbar (port → near-rewrite in practice; BS5 chrome + collapse plugin gone; BEM `__brand`/`__toggle`/`__menu`/`__nav`/`__button` — `__link` renamed to `__button` retroactively in the 3.4d session for cross-component vocabulary consistency; `[data-state="open\|active"]` + `[aria-disabled]` hooks; single `lg` collapse breakpoint; hard-height chip per height architecture; container-inside-navbar `:has()`; 16 `--navbar-*` vars; `data-navbar-toggle` JS handler added to `src/js/index.js`; "With dropdown" section deferred to Step 4) ✅
  - [x] 3.4d — sidebar (rewrite — biggest BS5-coupled file, 539 → ~470 lines; BS5 `.collapse` plugin dropped, submenu uses `[data-state="open\|closed"]` + new `data-sidebar-submenu-toggle` JS handler with `display: none` for closed state, animation deferred to Step 4; full BEM rename with `.sidebar__brand` + `.sidebar__button` matching navbar; 29 `--sidebar-*` vars across 7 groups; `.is-collapsed` rail mode ported faithfully with soft-hide animations + brand re-center math; site chrome partial + site.js swept in same commit) ✅
  - [x] 3.5b — list-group (rewrite — second Display-family file; full v3 model from scratch — the BS5 file was a 36-line overlay that only patched what BS5 vars couldn't reach + relied on BS5 `_list-group.scss` for the base shape; v3 rewrites the entire shape; BEM rename `.list-group-item` → `.list-group__item`, `.list-group-flush` → `.list-group--flush`, `.list-group-horizontal{,-sm…xxl}` → root modifiers, `.list-group-numbered` → `--numbered`, intent variants → `.list-group__item--{intent}` element-modifiers, `.list-group-item-secondary` → `--neutral` (parity with `.table__row--neutral`); `.list-group-item-action` opt-in **dropped** — `a.list-group__item, button.list-group__item` derive interactivity from the element; `[data-state="active"]` + `[aria-current="page"/"true"]` paint highlight (Radix-aligned, matches sidebar / table); contextual variants use the same `color-mix(in oklab, ...)` recipe as table; inset focus halo with `z-index: 1` lift to clear the `overflow: hidden` clip; `.card > .list-group` direct-child auto-integration drops outer chrome so the settings demo lands flush in a card without `--flush`; 16 component vars across 4 groups (Geometry/Surface/Item states/Focus) under the single-table threshold; demo page swept across 13 sections including Settings + Payment methods + Contacts + Custom content) ✅
  - [x] 3.5a — table (rewrite — first Display-family file; BS5 `_tables.scss` mechanic ported verbatim via per-cell `box-shadow: inset 0 0 0 9999px var(--table-bg-state, var(--table-bg-type, transparent))` paint chain; full BEM rename `.table-{intent}` → `.table__row--{intent}`, `.table-header-alt` → `.table__head--alt`, `.table-group-divider` → `.table__body--divider`, `.table-{striped,striped-columns,hover,bordered,borderless,sm}` → root `.table--{…}` modifiers; `.table-active` → `<tr data-state="active">` Radix-aligned; `.table-secondary` → `.table__row--neutral` (tracks `--st-neutral` for retint); color-mix oklch tints on variants replace BS5's rgba; row hover/active route through `--st-accent` / `--st-highlight`; 17 `--table-*` vars across 4 groups + 4 mechanism-internal layered-slot vars hidden from Customization table; `.table-responsive{,-sm…xxl}` kept flat; demo page swept across 20 sections, Customization grouped tables added per slider.njk pattern) ✅
  - [x] 3.5c — breadcrumb (rewrite — third Display-family file, 54-line BS5 overlay → ~125-line v3 file; BEM `.breadcrumb-item` → `.breadcrumb__item`; `.active` BS5 escape hatch dropped — `[aria-current="page"]` is the entire signal since there's no chrome flip; **no `[data-state="active"]`** because breadcrumb signals current via text color, not via a highlight chip; chevron SVG uses `fill='currentColor'` so it tracks the trail color across themes + retints; focus halo on links via `:focus-visible` with positive `outline-offset` (no `overflow: hidden` to clip); single `--breadcrumb-gap` serves both item↔item and within-item spacing via flex `gap`; 12 component vars in a single ungrouped Customization table per slider.njk pattern; embedded-SVG demo's hardcoded gray swapped to `currentColor`) ✅
  - [x] 3.5e — progress (write — fifth and final Display-family file; pure write from the 0-byte placeholder, no BS5 reference to port; BEM `.progress` + `.progress__bar` with `.progress-bar` collapsing to canonical `block__element`; `.bg-{intent}` BS5 utilities → `.progress__bar--{intent}` element-modifiers (primary/success/warning/danger/info); BS5's `.progress-bar-striped` + `.progress-bar-animated` chained pair → single `.progress__bar--animated` with **a soft sheen that sweeps across the fill every 3 seconds** (1s sweep + 2s rest, "rare not constant" per maintainer call) instead of BS5's constant diagonal-stripe treadmill; striped variant **dropped wholesale**; `.progress-stacked` BS5 nested-progress wrapper **dropped** along with the segmented use case entirely (maintainer: "no real use cases") — track is `display: block` with one bar inside, bar defaults to `width: 0` so an authored-without-inline-width bar sits invisible; `.is-indeterminate` Stisla-original state hook (no Radix equivalent for progress) forces bar to 50% width + animates `translateX(-100%) → translateX(200%)` over 1.5s `ease-in-out infinite` (single shadcn-style sliding bar, not Material's two-segment chase); shimmer overlay suppressed in indeterminate so the moving bar is the only indicator; sizes 4 / 8 / 16 px via `.progress--sm` / default / `.progress--lg` retuning `--progress-height` in modifier scope (no per-size vars); track defaults to `--st-neutral` (rest fill weight for static neutral surfaces, V3.md §3.3) + bar defaults to `--st-primary`; **radius opts out of `--st-radius`** with `--progress-radius: 9999px` default (pill is intentional for a progress strip; added to V3.md §3.4 opt-out list); width transition tweens JS-driven progress changes over 0.6s; reduced-motion zeros all three motions (width transition, shimmer, indeterminate); 9 component vars in single ungrouped Customization table; demo page rewritten across 6 sections (Example / Basic / Intents / Sizes / Animated / Indeterminate) + Customization, dropping BS5 Labels / Height / Striped / Animated-stripes / Stacked sections and adding Indeterminate; verification clean — 0 `--bs-*` leaks, 0 BS5 leftover names in compiled CSS or rendered HTML, all 9 vars + all 12 BEM selectors emit, bundle 88.1 → 90.2 KB raw / ~15.0 KB gz flat; **closes the Display family** — remaining Phase-2 components are JS-coordinated and slot into Step 4) ✅
  - [x] 3.5d — pagination (rewrite — fourth Display-family file, 49-line BS5 overlay → ~180-line v3 file; BEM `.page-item` + `.page-link` dropped entirely and **collapsed to one name** `.pagination__button` on the interactive element directly — the `<li>` carries no BEM hook because it has no visible role, matches sidebar / navbar's `__button` cross-component vocab; new `.pagination__ellipsis` element for truncation gaps (`1 2 … 9 10`) — same chip footprint, non-interactive, tracks `--pagination-button-disabled-color`; `.pagination-sm` / `.pagination-lg` → `.pagination--sm` / `.pagination--lg` (root modifiers retune `--pagination-button-height` + `--pagination-button-padding-x` + `--pagination-font-size` in modifier scope — no per-size vars to maintain, matches `.btn--sm` pattern); `.pagination--center` / `.pagination--end` replace BS5's `.justify-content-center` / `.justify-content-end` utilities since v3 doesn't ship flex utilities; `.active` BS5 escape hatch dropped — `[data-state="active"]` + `[aria-current="page"]` paint `--st-highlight` (current page is persistent selected state, matches sidebar / list-group / table — **corrects the BS5 v3 file's hover/active conflation** which routed both to `--bs-accent-bg` "because chips are toggleable"; v3 splits the trio properly: hover = `--st-accent` transient, current = `--st-highlight` persistent); `.disabled` on `<li>` → `[aria-disabled="true"]` on the chip directly; interactive opt-in derives from element selector (`a.pagination__button, button.pagination__button` get cursor + hover + focus; `<span class="pagination__button">` for aria-disabled edge markers sits quiet — same pattern as list-group 3.5b's interactive-row derivation); **height architecture** — hard `height: var(--pagination-button-height)` + `padding-block: 0` + `min-width: var(--pagination-button-height)` so density genuinely shrinks the chip and single-glyph chips ("1") land as squares at the matching cadence while labels ("Previous") grow from `padding-inline` (same model as `.btn`, V3.md §3.4); sizes ship at 28 / 36 / 44 px under default density to match the button scale so a pagination row baselines with a "Load more" button; focus halo uses **positive `outline-offset: 2px`** because `.pagination` has no `overflow: hidden` (chips are gap-laid-out flex, nothing clips — same as breadcrumb, not list-group's inset trick); icons inside chips pin to `1em × 1em` with `flex-shrink: 0` so they scale with `--pagination-font-size` automatically (no per-size icon override); 16 component vars + 1 internal `--pagination-transition` (hidden from Customization table same way list-group hides its internal vars); single ungrouped Customization table at 16 rows under the 20-var threshold per slider.njk pattern; **demo page sweep** — 6 sections (Basic / With icons / Disabled / Ellipsis (new) / Sizes / Alignment) + Customization; BS5 utilities (`.d-flex flex-column gap-3 w-100`, `.justify-content-{center,end}`, `mb-0`) all swept — inline `style="display: flex; flex-direction: column; gap: 1rem; width: 100%"` for the multi-row demos (matches list-group / breadcrumb / navbar pattern); `--pagination-margin-bottom: 0` default eats the `mb-0` BS5 stutter; disabled demo's `<a aria-disabled>` → `<span class="pagination__button" aria-disabled="true">` (drop `href` so screen readers don't announce as a link — better a11y than the BS5 demo); ellipsis demo introduces the truncation pattern with 7 chips spanning 1 → 12. **Bundle**: `_pagination.scss` uncommented at line 60 of `bundles/stisla-full.scss`. **Verification**: build green (40 pages render), bundle 87.2 → 88.1 KB raw (+0.9) / ~14.4 → 14.7 KB gz (+0.3) — smallest delta in the display family alongside breadcrumb, expected for a single-element chip component with no layered-bg mechanic. 0 `--bs-*` leaks in compiled CSS. 0 BS5 leftover names (`page-item`, `page-link`, `pagination-sm`, `pagination-lg`, `.disabled` on `<li>`, `.active` on `<li>`) in rendered HTML or compiled CSS; the single `justify-content` hit in `pagination.html` is in the Customization section prose explaining the `--center` / `--end` modifiers, not a class leak. All 16 documented `--pagination-*` vars emit (verified by `grep -oE -- "--pagination-[a-z-]+"` across the compiled stylesheet) + the internal `--pagination-transition`. All 7 BEM selectors emit (`.pagination`, `.pagination--sm`, `.pagination--lg`, `.pagination--center`, `.pagination--end`, `.pagination__button`, `.pagination__ellipsis`). 97 `pagination__button` references in rendered HTML (across the 6 demo sections + escaped-source duplicates), 11 `data-state="active"`, 11 `aria-current="page"`, 2 `aria-disabled="true"`, 3 `pagination__ellipsis`, 3 each of `--sm` / `--lg` / `--center` / `--end`. Sass deprecation warnings unchanged (forked BS5 mixins, pre-existing noise). Site sidebar already pointed at `/pagination` (no rename needed). Visual verification pending — maintainer to eyeball `/pagination` in light + dark: (a) Basic — 5-chip row with chip 2 highlighted; hover any link → accent fill; (b) With icons — chevron icons pin to label baseline, scale with font; (c) Disabled — Previous chip greyed out with no pointer; (d) Ellipsis — `1 2 3 4 5 … 12` with the dots reading muted between chip 5 and chip 12 at the same height as the chips; (e) Sizes — three rows at 28 / 36 / 44 px under default density, icons scale with font; (f) Alignment — center and end rows shift across the demo viewer width; (g) Focus — tab onto any chip and the 2px outline paints at `--st-ring` with positive offset, no clipping; (h) Customization — 16-row table reads cleanly with the Variable / Default / Use columns; (i) Dark mode — accent + highlight track the dark block; chip text inverts naturally via `--st-foreground` / `--st-highlight-foreground`. **3.5d closes the fourth Display-family file.** Remaining display queue: progress (the 0B file — pure write, no port reference); toast + accordion are JS-coordinated and slot into Step 4 interleave.) ✅
  - [x] 3.6c polish — selector fix (`>` → `> * >` so the heading-wrapper `<h3>`/`<h2>` between `.accordion__item` and `.accordion__header` no longer breaks the closed-hover paint + open-state divider + chevron rotation rules — all three silently failed at initial land); radius-lg hierarchy (`card` + `accordion` + `list-group` bumped from `--st-radius` to `--st-radius-lg`, locking in the chip / element / frame three-tier rhythm); card refactored to gap-based composition (`.card__body { display: flex; flex-direction: column; gap: 0.5rem }`, dropped margins from `__title` / `__subtitle` / `__text` + the negative-top hack + the last-child workaround — matches the alert pattern already in `_alert.scss`); accordion `--accordion-margin-bottom` deleted entirely (parent owns inter-block spacing). Bundle 102.2 → 102.0 KB raw / 16.5 → 16.4 KB gz. Accordion var count 25 → 24. ✅
  - [x] 3.6c — accordion (rewrite — last CSS-leading Phase-2 component; **revised mid-session** after maintainer ref screenshot — open state is now a chip-style raise (`--st-surface-2` bg + `--st-border` rim on the whole `.accordion__item`) with a `border-bottom: 1px solid --st-border` divider between header and body, not a `--st-highlight` fill on the header; header text stays `--st-foreground` in both states; padding rebased to 12 / 16 px on both header and body (4 px frame + 16 px X = 20 px aligns with card padding); frame ships a `var(--st-shadow)` ambient elevation at rest; flush mode just drops the frame border + shadow, keeps root padding so items still chip out concentrically inside a card. Dropped the native `<details>` route in favour of the ARIA accordion pattern — `<button aria-expanded>` + `<div role="region" aria-labelledby>` — because `::details-content` + `interpolate-size` for animation are above the V3.md §2 browser baseline and because Radix/shadcn/Reka/bits-ui all ship the ARIA pattern, so the CSS file serves vanilla + future React/Vue impls per V3.md §3.11 with no glue layer. BEM rename: `.accordion-item` → `.accordion__item`; `.accordion-header` BS5 wrapper dropped as a class (heading wrap stays in markup, no class); `.accordion-button` → `.accordion__header` (the trigger IS the named part — same convention as Radix `<AccordionTrigger>`); `.accordion-collapse` + `.accordion-body` collapsed → `.accordion__body`; new `.accordion__icon` for the chevron (rotates 0→180° via CSS transform); `.accordion-flush` → `.accordion--flush`; `.collapsed` markup class dropped wholesale (`data-state="closed"` + `aria-expanded="false"` carry the meaning). State hooks: `.accordion__item[data-state="open|closed"]` is the paint hook; `.accordion__header[aria-expanded]` is the JS-flipped source. Concentric inner radius is the V3.md §3.4 canonical case — frame owns `--accordion-radius` + `--accordion-padding`, item `border-radius: calc(outer - padding)`. Color routing: closed hover = `--st-accent` on header (transient hover, sidebar/list-group/table family); open item = `--st-surface-2` chip bg + `--st-border` rim around the whole item + `border-bottom: 1px solid --st-border` divider between header and body; header text stays `--st-foreground` in both states; open-state hover stays flat inside the chip. Header lets padding own the height (no `min-height` or hard `height`) — long headers wrap naturally. Flush mode just drops the frame border + shadow (root padding kept so items chip out concentrically inside a parent card). Chevron rotation transitions today; body collapse uses `display: none` for the closed state — Step 4 swaps in JS-measured height transition keyed off `--accordion-body-transition-duration` (var exposed today so the customization surface is stable across the upgrade). Interim JS handler in `src/js/index.js` (~25 lines, mirrors `data-sidebar-submenu-toggle` shape): delegated click listener flips `data-state` + `aria-expanded`, supports `data-stisla-accordion-single` on the root for radio-group semantics. 25 component vars in 7 groups (Geometry/Frame surface/Open-item chip/Header/Icon/Body/Focus); grouped Customization tables per slider.njk. Bundle 97.8 → 102.2 KB raw / 15.8 → 16.5 KB gz. 0 `--bs-*` leaks; 0 BS5 class leftovers; all 25 vars + 6 BEM selectors emit; 33 ARIA wiring markers in the rendered demo; 5 open + 10 closed states across 28 demo items + 3 single-mode roots + 3 flush variants. 42 pages render. **Closes the CSS-leading cohort.** Next: Step 4 prelude — `src/js/components/` + `Stisla.Component` base + `Stisla.init()` scanner — before any JS-coordinated component lands.) ✅
  - [x] 3.6b — toggle + toggle-group split; button-group simplified (vision shift after 3.6a landed. `.btn-check` mechanic and `.toggle`'s input-driven model were doing the same job — and `.btn-group`'s "Toggle group" / "Tabs+segmented" / "Checkbox & radio" demo sections rendered visually identical patterns. Resolution: `.toggle` rewritten as standalone press button — three state sources (`[aria-pressed]`, `[data-state="active"]`, `.toggle-input:checked + &`) all paint same; outline-neutral rest, `--st-highlight` active bg + foreground-mix border ("slightly stronger than active bg" per maintainer spec). `.toggle-group` new — pill container with concentric inner radius (outer 36 px / padding 4 px / inner 28 px under default density; --sm 28/2/24, --lg 44/4/36); members go ghost-rest so the parent rim owns the frame; active member paints highlight bg + foreground-mix border (inherits standalone `.toggle` active rules). `.btn-group` shrunk back to purely visual grouping — stripped `.btn-check` rules, adjacency forms, z-index lifts. `_btn.scss` reverted — removed the `.btn-check:checked + &` synonym from all 8 active-state cascade rules. Demo pages: `/toggle` rewritten for new model with JS + form-data examples; `/toggle-group` new with single-select + multi-select + form-data variants + sizes + vertical + customization; `/button-group` lost 3 sections (now at /toggle-group). Site sidebar gained `/toggle-group`. V3.md Phase 2 list grew `toggle, toggle-group` entries; cut note rewrote — toggle-group single-select covers tabs/segmented; new paragraph spells out toggle-vs-button-group semantic split. Bundle 95.4 → 97.8 KB raw / 15.5 → 15.8 KB gz. 0 `--bs-*` leaks; 0 `btn-check` residue. 21 vars + 11 BEM selectors emit clean. 41 pages render. **Closes the toggle / button-group / tabs vocabulary cleanly.** Remaining JS-coordinated queue: accordion next (CSS today via native `<details>`), then with Step 4 — modal, offcanvas, dropdown, tooltip, popover, toast, carousel.) ✅
  - [x] 3.6a — button-group (rewrite — first JS-coordinated-family file, but the CSS lands without its JS half because the actual click behavior was already a BS5-data-attr concern; v3 ships the visual + a11y today, roving-tabindex + click handlers defer to Step 4. ~180-line file from the dead BS5 overlay. **BEM rename map**: `.btn-group-vertical` → `.btn-group--vertical` (proper modifier); `.btn-group-sm` / `.btn-group-lg` → `.btn-group--sm` / `.btn-group--lg`; `.btn-toolbar` stays its own block; `.btn-check` stays (input class); `.dropdown-toggle-split` **dropped** — the split-button caret reuses `.btn--icon-only` with a chevron, no split-specific class ships. **Sizes retune child --btn-* vars in modifier scope** — `.btn-group--sm > .btn { --btn-height: 1.75rem; --btn-padding-x: ...; --btn-font-size: ...; --btn-radius: var(--st-radius-sm) }` and the `--lg` mirror. No per-size vars on `.btn-group` itself; sizes match the standalone `.btn--sm` / `.btn--lg` cadence (28 / 36 / 44 px under default density) so a sized group sits in the same rhythm as a standalone button next to it. **Seam dedupe** — negative `margin-inline-start: -1px` (or `margin-top: -1px` for vertical) on every non-first member collapses adjacent 1px borders into a single line. Inner corners square (`border-radius: 0` on all members); outer corners re-round via `border-start-start-radius` / `border-end-start-radius` on first child and `border-start-end-radius` / `border-end-end-radius` on last child (logical properties so RTL flips automatically; the same logical-property pattern shows up in form-control / input-group). **Two adjacency forms** for the negative-margin rule: `> .btn + .btn` (consecutive plain buttons) and `> .btn + .btn-check + .btn` (input-driven `<input><label>` pairs interleaved) — covers both markup patterns without falling back to a `:nth-child` approach. **Stacking-order lift on interactive members** — every member sits `position: relative`. Hover lifts to `z-index: 1`, active states (`[aria-pressed="true"]` + `[data-state="active"]` + `.btn-check:checked + &`) to `z-index: 2`, focus-visible to `z-index: 3`. Without the lift the negative-margin seam-dedupe buries the active member's right border under the next sibling's left border (or the equivalent for vertical), and the focus halo gets clipped at the seam. **Active-state forwarding via _btn.scss touch** — single-pass edit added `.btn-check:checked + &` as a synonym alongside `[aria-pressed="true"]` + `[data-state="active"]` across all 8 active-state cascade rules in `_btn.scss` (base, neutral, tertiary, outline, ghost, soft, outline.neutral, ghost.neutral). Means a checked `<input>` paints its `<label class="btn">` correctly through every tone × shape combo automatically — no duplicating color-mix formulas in button-group. Cleaner than alternative approaches (separate per-tone-per-shape `:checked +` rules in button-group, or JS that toggles `aria-pressed` on label click). **.btn-check geometry** copied from `.visually-hidden` (position: absolute, 1×1px, clip rect 0, margin -1px) — not `display: none` / `visibility: hidden` because those drop the input from the a11y tree and tab order. Focus forwards to the label via `.btn-check:focus-visible + .btn { outline: 2px solid var(--st-ring); outline-offset: 2px }` so keyboard nav has a visible halo on the visible chip rather than the hidden input. Disabled forwards via `.btn-check:disabled + .btn { cursor: not-allowed; pointer-events: none; opacity: 0.55 }` (matches `.btn`'s own disabled paint). **.btn-toolbar — separate block** with `--btn-toolbar-gap: 0.5rem` default. `display: flex; flex-wrap: wrap; gap: var(--btn-toolbar-gap)` so multiple `.btn-group` children breathe without inline flex utilities at each call site. **Tabs / segmented control** — new section in the demo using `<div class="btn-group" role="tablist">` + `<button role="tab" data-state="active" aria-selected="true">` for the current member. `.btn`'s existing `[data-state="active"]` cascade paints the active member; no new var or class. **The base `.btn`'s active-state mechanic** (color-mix toward black for filled tones, toward background for tertiary, tint-on-transparent for outline/ghost/soft) handles every tabs/toggle paint case correctly — button-group stays purely about layout. **Split-button** demoed with two `.btn`s in the group: a regular button + a `.btn--icon-only` chevron trigger. Dropdown attach lives with the Stisla.Dropdown component in Step 4; `aria-haspopup="menu"` + `aria-expanded="false"` ship today so the markup wires cleanly when the JS arrives. **2 component vars only** (`--btn-group-radius` + `--btn-toolbar-gap`) — most of the configurability lives on the child `.btn` (via its own ~10 vars), not on the group. Single ungrouped Customization table with both, plus a pointer paragraph linking to `/buttons` for the child surface (same pointer-section pattern as toggle / select reuse). **Bundle**: `_button-group.scss` uncommented at line 51 of `bundles/stisla-full.scss`. **Demo page (`src/site/pages/button-group.njk`) sweep** — 11 sections (Basic / Outline / Mixed / Sizes / Toggle group / Tabs+segmented (new) / Checkbox & radio / With icons / Split button / Vertical / Toolbar) + Customization. BS5 vocab swept: `.btn-primary` → `.btn .btn--primary`, `.btn-outline-secondary` → `.btn .btn--outline .btn--neutral`, `.btn-icon` → `.btn--icon-only`, `.btn-group-vertical` → `.btn-group--vertical`, `.btn-group-sm` / `.btn-group-lg` → `.btn-group--sm` / `.btn-group--lg`, `.dropdown-toggle-split` dropped, `data-bs-toggle="button"` + `data-bs-toggle="dropdown"` removed from markup (declarative toggle JS lands in Step 4), `.active` on toggle members → `aria-pressed="true"`. `.d-flex flex-column gap-2 align-items-start` → inline `style="display: flex; flex-direction: column; gap: 0.5rem; align-items: flex-start"` (matches the v3 list-group / breadcrumb / navbar / pagination pattern since v3 doesn't ship flex utilities yet). Toggle-group section ships hardcoded `aria-pressed="true|false"` to demo the visual without the click handler. Tabs section uses `role="tablist"` + `role="tab"` + `aria-selected="true"` + `data-state="active"` on the current member. **Verification**: build green (40 pages render), bundle 90.2 → 95.3 KB raw (+5.1) / ~15.0 → 15.5 KB gz (+0.5). 0 `--bs-*` leaks in compiled CSS. 0 BS5 leftover names (`btn-group-vertical`, `btn-group-sm`, `btn-group-lg`, `btn-primary`, `btn-outline-secondary`, `btn-icon`, `dropdown-toggle-split`, `data-bs-toggle`) in rendered HTML. Both `--btn-group-*` + `--btn-toolbar-*` vars emit (verified by `grep -oE -- "--btn-(group|toolbar)-[a-z-]+"` across the compiled stylesheet). All 4 BEM selectors emit (`.btn-group`, `.btn-group--vertical`, `.btn-group--sm`, `.btn-group--lg`) + `.btn-check` + `.btn-toolbar`. 34 `btn-group` references + 3 each of `--sm` / `--lg` / `--vertical` in the rendered demo, 13 `btn-check`, 4 active-state markers (`data-state="active"` / `aria-pressed="true"` / `aria-selected="true"`). Sass deprecation warnings unchanged (forked BS5 mixins, pre-existing noise). Site sidebar already pointed at `/button-group` (no rename needed). **Visual verification pending** — maintainer to eyeball `/button-group` in light + dark: (a) Basic — 3 primary buttons reading as one chip with outer-only radius and one seam between each pair; (b) Outline — 3 outline buttons with dedupe; (c) Mixed — primary + outline-neutral pair; (d) Sizes — sm/default/lg rows at 28 / 36 / 44 px, every member in a sized group matches the corresponding `.btn--sm` / `.btn--lg` cadence; (e) Toggle group — Bold + Underline highlighted (aria-pressed true), Italic muted (aria-pressed false), active members lift above the seam so their darker border paints cleanly; (f) Tabs — Day highlighted via data-state="active" + aria-selected, Week / Month muted; (g) Checkbox/radio — clicking on a label flips the hidden input + the label's paint via the `.btn-check:checked + .btn` cascade (radios single-select via name=); (h) Icons — 3 icon-only outline buttons; (i) Split button — primary + chevron-only trigger, no functional menu yet (Step 4 work); (j) Vertical — 3 stacked buttons with top/bottom outer rounds + middle inner seams; (k) Toolbar — 3 groups gapped 0.5rem apart, wrap on narrow containers; (l) Focus — tab onto any member, the focus halo paints above the seam without clipping; (m) Dark mode — every variant tracks the dark block tokens; active aria-pressed states stay perceptibly darker than rest. **3.6a opens the JS-coordinated family.** Remaining queue: accordion (CSS today + native `<details>` later), then with Step 4 — modal, offcanvas, dropdown, tooltip, popover, toast, carousel. Each lands its CSS + demo sweep alongside its JS contract per V3.md §3.7.) ✅
- [ ] Step 4 — JS rewrite (interleaved with step 3 JS-coordinated components)
  - [x] 4.0 prelude — `src/js/core/` (Component base class + declarative scanner) + `src/js/components/` directory + `index.js` refactor (`Stisla = { version, Component, register, init, get }` public surface + HMR-guarded interim handlers) ✅
- [ ] Step 5 — Demo pages (.njk class + attr + delete pass)
- [ ] Step 6 — Cleanup, README/CHANGELOG, `3.0.0-beta.1` tag
- [ ] **After 3.0.0 final:** delete `bs5-snapshot/`, delete this file.
