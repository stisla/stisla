# Stisla v3 — BS5 → vanilla migration

> Transient. Tracks the refactor of the existing BS5-based v3 codebase to the V3.md spec.
> Delete this file once the migration ships (3.0.0).

**Spec:** V3.md (locked).
**Branch:** v3.
**Approach:** Option A — snapshot the BS5 version once, rewrite in place. See §3 step 0.

---

Prompt for each session:

"Let's continue the v3 migration. Read V3.md + MIGRATION.md, check git, and tell me what step we're on and what to do next."

---

## Status

_Latest entry first. One line per session: ISO date — step completed / current — blockers._

- 2026-06-10 — **Step 4.6 — toast (CSS rewrite + `Stisla.Toast` class + `Stisla.toast(...)` helper + `Stisla.toast.promise()` + demo rewrite)** — ran in parallel with the 4.4 tooltip session; merged cleanly via separate file scope (no overlap beyond the 1-line index.js + bundle adds). First component to ship both a markup-first path AND an imperative helper, since toast is the canonical "fire from code" case. **CSS — `_toast.scss` full rewrite** (~270 lines from the 103-line BS5-overlay file that referenced `$prefix` + `--bs-toast-*` + zeroed BS5 padding vars to consolidate the 16 px frame onto `.toast` itself — dead since Step 2 unbundled BS5 SCSS). **BEM rename map**: `.toast-container` → `.toast-region` (separate block — `role="region"` aria-live container persists while individual toasts come/go; two blocks model the persistence boundary); `.toast-header` → `.toast__header`; `.toast-body` → `.toast__body`; `.toast-action` → `.toast__action`; `.btn-close` (direct child of `.toast`) → `.toast__close` (dedicated ghost chip, no longer a `.btn-close` peer); `.fade .show` → `[data-state="open|closed"]`. **New BEM parts**: `.toast__icon` (separate leading-slot grid cell — promoted out of `.toast__header` to mirror the `.alert__icon` pattern from 3.5a; lets single-line + multi-line toasts align with the same grid rule), `.toast__timestamp` (optional muted right-of-title slot for "just now", "10 mins ago"). **Region placement modifiers**: `.toast-region--top-start`, `--top-center`, `--top-end` (default), `--bottom-start`, `--bottom-center`, `--bottom-end`. Bottom variants use `flex-direction: column-reverse` so newest toast lands at the bottom edge with older ones pushing up; top variants append naturally. **Snapshot-derived visual identity carried forward** (verified against `bs5-snapshot/assets/stisla-full.css` + `eba7fc1:src/scss/components/_toast.scss`): `--bs-toast-max-width: 350px` → `--toast-max-width: 21.875rem` (350 px exact); `--bs-toast-border-radius: var(--bs-border-radius-lg)` (1rem) → `--toast-radius: var(--st-radius-lg)` (matches dialog + card large-frame tier from 3.6c); `--bs-toast-bg: var(--bs-surface)` → `--toast-bg: var(--st-surface)`; `--bs-toast-border-color: var(--bs-border-color)` opaque (NOT translucent — snapshot forced opaque, preserving) → `--toast-border-color: var(--st-border)`; `--bs-toast-box-shadow: var(--bs-box-shadow)` → `--toast-shadow: var(--st-shadow)`; `--bs-toast-font-size: 0.875rem` → `--toast-header-font-size`/`--toast-body-font-size: 0.875rem`; `--bs-toast-zindex: 1090` → `--toast-z-index: 1090` (above dialog 1055 — a toast can confirm a dialog outcome that's still on screen); `--bs-toast-spacing: 1.5rem` (24 px between toasts) → `--toast-region-gap: 1rem` (16 px — snapshot's 24 px felt heavy for the v3 cadence, 16 px sits between dropdown's 8 px and snapshot's 24 px); 16 px root padding preserved on `.toast` itself. **Anatomy delta** vs. snapshot: snapshot's `.toast` was a 2-col grid `[1fr | auto]` with icon nested inside `.toast-header` flex row; v3 lifts to a 3-col `[auto | 1fr | auto]` grid (icon | content stack | close) so the icon owns its own cell — alignment stays clean across single-line and multi-line toasts. No-icon case handled via `:has()` (V3.md baseline Safari 16.4+): `.toast:not(:has(.toast__icon)) { grid-template-columns: 1fr auto; > * { grid-column: 1 } > .toast__close { grid-column: 2 } }`. **Close chip deviation**: snapshot used a raw 0.875 rem (14 px) close icon with no chip frame; v3 promotes to a 1.5 rem (24 px) ghost-button chip with `--st-accent` hover — smaller than dialog's 1.75 rem (dialog dominates the viewport; toast shouldn't) but tappable. **27 component vars in 8 groups** (one-tier `--toast-*` + `--toast-region-*` naming, matches 4.1–4.4 convention): Region geometry (4: inset 1 rem, gap 1 rem, max-width 24 rem, z-index 1090); Geometry (5: min-width 18 rem, max-width 21.875 rem, padding 1 rem, radius `--st-radius-lg`, z-index 1090); Surface (4: bg, color, border-color, shadow); Header (3: gap 0.75 rem, font-weight 600, font-size 0.875 rem); Body (2: color `--st-muted-foreground`, font-size 0.875 rem); Icon (2: size 1.125 rem, color); Close chip (4: size, color, color-hover, bg-hover); Action + Motion (3: action-gap 0.375 rem, action-margin-top 0.25 rem, transition-duration 0.2 s). **Intent variants** (`.toast--primary/--success/--warning/--danger/--info`) each shift ONLY `--toast-icon-color` — surface stays neutral so stacked toasts of mixed intent read as one family (Sonner-style). No `.toast--filled` variant; if/when consumers want filled-intent toasts later it lands as a separate modifier. No surface tint, no border tint, no `color-mix()` per-state vars to maintain — single-var override per intent, can't drift. **Entry/exit motion**: `opacity: 0` + `translateY(-0.5rem)` rest, `transform: none` on `[data-state="open"]`, 0.2 s transition; bottom-region toasts override to `translateY(0.5rem)` rest so the slide direction matches the stack growth direction. Reduced motion zeros the transition. **JS — `src/js/components/toast.js`** (~360 lines). `class Toast extends Component` with `static eventNamespace = 'toast'` + `static defaults = { autohide: true, delay: 4000, pauseOnHover: true, pauseOnFocus: true }`. Methods: **`open()`** restarts timer on re-trigger (markup-first repeat-click pattern), emits cancelable `opening` → forces reflow → rAF flips `data-state="open"` + `aria-hidden="false"` → starts autohide timer if enabled → awaits transition → emits non-cancelable `opened`. **`close()`** emits cancelable `closing` → clears timer → flips state + aria-hidden → awaits transition → emits `closed` → if `this._owned` (helper-created instance), removes node from DOM and destroys. **`toggle()`** flips based on current state. **`update(patch)`** in-place content swap: rewrites `.toast__header`/`.toast__body` textContent (creating the part if absent), removes/adds intent modifier class, swaps `.toast__icon` contents, re-tunes `autohide`/`delay`, restarts timer if open + autohide on. Refreshes Lucide via `window.lucide.createIcons()` if the SDK is on the page (handles SVG rewrites in newly-injected `<i data-lucide>` nodes). Emits non-cancelable `updated`. **Used by `promise()`** to morph loading → settled. **`destroy()`** clears timer before delegating to `super.destroy()` which removes the auto-bound pause/resume listeners via `this._listeners` array. **Pause-on-hover / pause-on-focus**: constructor binds `mouseenter`/`mouseleave` + `focusin`/`focusout` via `this.on(el, ...)`. Each instance tracks `_timer` + `_timerStartedAt` + `_timerRemaining`; pause clears the timeout + accumulates elapsed; resume re-schedules with the remaining ms. WCAG 2.2.1 compliance (auto-dismissing notifications must pause on user interaction). Per-toast pause (not stack-wide); good enough for v3.0, can promote to region-wide later if a real user need surfaces. **Owned flag** (`this._owned`): helper-created toasts set `_owned = true` so `close()` removes the node from DOM after the transition; markup-first toasts leave `_owned = false` so the node stays for re-trigger. **`_waitForTransition()`** is the same defensive promise wrapper as dialog/drawer (parses `getComputedStyle` duration, listens for `transitionend` filtered to `e.target === el`, fallback `setTimeout` at `total * 1.5 + 50ms`, short-circuits under `prefers-reduced-motion`). **Imperative helper** `export function toast(opts)`: resolves region (selector → element → first `.toast-region` → first `[data-stisla-toast-region="default"]` → auto-create one in `<body>` with `--top-end` placement) → builds DOM node via `buildToastNode(opts)` (`.toast` root with intent class, role `alert`/`status` based on variant, aria-live `assertive`/`polite`, aria-atomic, aria-hidden seeded "true", `data-state="closed"`; `.toast__icon` with passed-in icon or `VARIANT_ICONS[variant]` Lucide `<i>`; `.toast__header` if title; `.toast__close` always (for autohide-false toasts the user needs an out); `.toast__body` if description; `.toast__action` with `.btn.btn--sm.btn--ghost.btn--neutral` if action) → refreshes Lucide → mounts to region → constructs Toast instance with `_owned = true` → rAF defers `open()` so the closed→open transition runs. Returns the Toast instance for manual `.close()`. **Named shortcuts**: `toast.success(title, opts)`, `toast.error(title, opts)`, `toast.warning(title, opts)`, `toast.info(title, opts)` — each wraps `toast({ ...opts, title, variant: '<intent>' })` so the default Lucide icon (check-circle-2 / x-circle / triangle-alert / info) auto-fills via the variant → icon map. `toast.error` maps to `variant: 'danger'` for the CSS modifier (matches v3 convention; "error" is the helper API name, "danger" is the visual class). **Promise integration** `toast.promise(promise, opts)`: builds a loading toast with `icon: buildSpinnerNode()` (reuses `.spinner.spinner--sm` from `_spinner.scss` — no new component, ~0.875 rem ring with 1.5 px stroke, density-multiplied) + `autohide: false`. Wraps the promise in `Promise.resolve()` so both real Promises and synchronous values flow through; `.then(data)` calls `t.update({ title: resolve(opts.success, data), variant: 'success', icon: <check-circle-2>, autohide: true, delay })`; `.catch(err)` calls `t.update({ ..., variant: 'danger', icon: <x-circle>, ... })`. Success/error fields accept strings OR functions of `(data)`/`(err)`. Optional `description: { loading, success, error }` object form for matching descriptions per state. **Returns the Toast instance** (not the promise) — the original promise is NOT intercepted, so callers' downstream `.then`/`.catch` still run. Critical: a logging promise that resolves shouldn't trigger the caller's error handler just because the toast helper saw it. **Auto-region creation**: if no region exists in DOM, helper appends a `<div class="toast-region toast-region--top-end" data-stisla-toast-region="default" role="region" aria-label="Notifications">` to `<body>` on first call. Consumers who pre-mount a region in markup (better for SSR / no FOUC) get that one used instead; named regions via `data-stisla-toast-region="my-name"` for multi-region apps (one top-end for system, one bottom-start for chat). **Module-level**: single delegated click handler bound once via `window.__stislaToastBound` HMR sentinel (mirrors 4.1/4.2/4.3 pattern). Routes `[data-stisla-toast-trigger="<id>"]` → `Toast.getOrCreate(getElementById(id)).open()` with per-attr opts re-read on every open (belt-and-braces over `Stisla.init()` scan; also covers HMR + DOM-swap cases where the instance might be stale). Routes `[data-stisla-toast-dismiss]` → `getInstance(closest('.toast')).close()`. **`src/js/index.js`** adds `import { Toast, toast }` + `register('toast', Toast)` + exposes both `Stisla.Toast` (class) and `Stisla.toast` (helper function with `.success`/`.error`/`.warning`/`.info`/`.promise` attached) — two-name pattern lets consumers use either the class for full control or the helper for the common case. **Bundle**: `_toast.scss` uncommented at line 67 of `bundles/stisla-full.scss`. **Page rename**: `src/site/pages/toasts.njk` → `toast.njk` to match `dialog.njk`/`drawer.njk`/`dropdown.njk`/`tooltip.njk` singular convention; sidebar nav `/toasts` → `/toast` (text "Toasts" → "Toast") in `_site-sidebar.njk`. Old `toasts.njk` deleted in the same commit; stale `site-dist/toasts.html` will need a manual cleanup post-merge same as the tooltip session did (vite/render-site don't clean output dirs — flag for future page renames). **Demo page rewrite** — 9 sections (Basic, Anatomy, Intent variants, Trigger, Auto-hide and persistent, Position, JS helper, Promise, Customization) replacing the BS5-vocabulary 9-section original. **Static demos** (Basic/Anatomy/Intent) drop `.toast` inline in the demo frame with `data-state="open"` — no region needed since `.toast` carries its own visual styling and the region is just a position-fixed stack container. **Live demos** (Trigger/Auto-hide/Position/JS helper/Promise) use 6 pre-mounted named regions at the top of the content block (empty regions are invisible — `pointer-events: none`, no children) so corner-anchored toasts paint at the correct viewport edges. A page-level `<script>` block at the bottom wires `[data-demo-toast-position]` / `[data-demo-toast-shortcut]` / `[data-demo-toast-action]` / `[data-demo-toast-promise]` attribute hooks to live `Stisla.toast(...)` calls — lives outside any `{% call ui.demo() %}` so it doesn't appear in the source viewer; only the markup with `data-*` attrs does. **Promise demo** alternates resolved/rejected via a session-scoped boolean (`window.__toastPromiseDemo`) so a maintainer can see both transitions without reload. **BS5 vocab swept**: `data-bs-dismiss="toast"` → `data-stisla-toast-dismiss`; `.toast-container` → `.toast-region` + 6 placement modifiers; `.toast-header` → `.toast__header`; `.toast-body` → `.toast__body`; `.toast-action` → `.toast__action`; `.btn-close` → `.toast__close`; `.toast.show`/`.toast.fade` → `[data-state="open|closed"]`; `.bg-primary`/`.bg-danger`/`.text-white` → `.toast--<intent>` modifiers (icon-only paint, not surface fill); `.position-fixed`/`.top-0`/`.end-0`/`.p-3` BS5 utility cluster → `.toast-region--top-end` modifier (single attribute replaces five); `.btn-primary` → `.btn .btn--primary`; `.btn-outline-secondary` → `.btn .btn--outline.btn--neutral`; `.btn-ghost-secondary` → `.btn .btn--ghost.btn--neutral`; `.link-body-emphasis text-decoration-none` → `.btn--ghost btn--neutral` for action-row buttons (matches v3 alert action pattern); `.text-body-tertiary fw-normal` → `.toast__timestamp` (new dedicated part); `.rounded-circle` (on `<img>`) → inline `style="border-radius: 50%"` if needed (no `<img>` in v3 toast demos; avatar use case deferred to a future avatar component per Phase 7); `.d-flex flex-column gap-2` etc. → inline `style="display: flex; ..."` per the established v3 idiom (v3 doesn't ship flex utilities yet). **Lead paragraph** per `[[feedback_docs_voice]]`: short, no em-dashes, no pixel callouts ("A non-blocking status message. Lives in a corner region, auto-dismisses, and pauses on hover." — 17 words). **Customization section** ships 8 grouped tables (Region geometry / Geometry / Surface / Header / Body / Icon / Close chip / Action and motion) at 27 rows total, slider.njk reference shape. **Verification**: build green (42 pages render), bundle CSS 18.69 → 19.38 KB gz (+0.69 from toast); JS 18.57 → 20.48 KB gz (+1.91 from toast — class + helper + promise + DOM builders + 6-position resolver) / 66.21 → 71.32 KB raw (+5.11). Total JS now 20.48 KB gz, well inside V3.md §3.1's ~25–30 KB gz budget; ~4.5–9.5 KB gz headroom for 4.5 popover (reuses FUI + focus-trap, ~1 KB gz) + 4.7 carousel (Embla ~6 KB gz — tightest fit). 0 `--bs-*` leaks in compiled CSS (`grep -c -- "--bs-" site-dist/assets/stisla-full.css` = 0). 0 BS5 vocab in rendered `/toast` HTML (strict class-attribute grep across `toast-(container|header|body|action)`, `btn-close`, `data-bs-`, `btn-primary`, `btn-secondary`, `btn-ghost-secondary`, `btn-outline-secondary`, `rounded-circle`, `text-body-tertiary`, `text-body-secondary`, `link-body-emphasis`, `text-decoration-none`, `position-(fixed|static)`, `top-0`, `end-0`, `p-(0|3)`, `d-flex`, `flex-column`, `gap-(2|3)` all return zero hits). All 27 `--toast-*` + `--toast-region-*` vars emit (`grep -oE -- "--toast-(region-)?[a-z-]+" | sort -u | wc -l` = 27). All 19 BEM selectors emit (`.toast`, `.toast--primary`, `.toast--success`, `.toast--warning`, `.toast--danger`, `.toast--info`, `.toast-region`, `.toast-region--top-start`, `.toast-region--top-center`, `.toast-region--top-end`, `.toast-region--bottom-start`, `.toast-region--bottom-center`, `.toast-region--bottom-end`, `.toast__icon`, `.toast__header`, `.toast__body`, `.toast__action`, `.toast__close`, `.toast__timestamp`). All 6 Stisla data-attrs render in HTML (`data-stisla-toast` 6×, `-trigger` 6×, `-dismiss` 20×, `-autohide` 4×, `-delay` 5×, `-region` 8×). JS bundle contains `__stislaToastBound` sentinel (2× — guard + assignment), `"toast"` register key, `Toast:` class export, `"toast-region"` runtime selector, `data-stisla-toast` attr literal, and `"spinner spinner--sm"` class string (proof that the promise loader builds the spinner correctly without a new component). **Dialog + Drawer + Dropdown + Tooltip regression check**: all four prior component sentinels still present (`__stislaDialogBound`/`__stislaDrawerBound`/`__stislaDropdownBound` 2× each; tooltip uses per-instance listeners so no module sentinel by design). All five component classes coexist in the Stisla global export. **Visual + interaction verification pending** — maintainer to eyeball `/toast` in light + dark, exercising: (a) Basic — static toast paints in demo frame with correct surface/border/shadow at `--st-radius-lg` (16 px) outer radius; (b) Anatomy — 3-col grid layout, icon col 1 / content stack col 2 / close col 3, action row sits below body with the right margin-top, timestamp reads muted next to title; (c) Intent variants — all 5 modifiers shift only the icon color, surface stays neutral, danger toast carries `role="alert"` + `aria-live="assertive"`; (d) Trigger — click button opens toast at top-end region, autohides after 4 s, re-clicking restarts timer; (e) Auto-hide vs persistent — auto-hide toast dismisses after 4 s, persistent toast stays until close button; both pause on hover/focus; (f) Position — 6 buttons each spawn a toast in its corresponding region; bottom regions stack new-at-bottom (column-reverse); top regions stack new-at-top; (g) JS helper — 4 shortcut buttons fire correct-intent toasts with default Lucide icons; action button fires a toast with inline "Undo" button that dismisses on click + nests a success toast; (h) Promise — first click shows loading spinner toast, settles into success after 1.5 s; second click rejects, shows danger toast with x-circle icon; pause-on-hover affects the post-settle autohide window; (i) Customization — 8 grouped tables read cleanly with Variable/Default/Use columns; (j) Pause-on-hover — hovering any open toast clears its timer; leaving resumes with remaining ms (timer doesn't fully reset, picks up where it left off — verify by hovering for 2 s then leaving on a 4 s toast: should dismiss after ~2 more seconds, not a fresh 4); (k) Dark mode — surface, border, body color, close chip all adapt via token swap; intent icon colors stay vivid; (l) Reduced motion — transitions zeroed, toasts snap open/closed without translate, no shake; (m) Lucide refresh — `Stisla.toast.success()` shows the check-circle-2 SVG correctly (proof that `window.lucide.createIcons()` runs after DOM insertion); (n) Auto-region — opening a console and calling `Stisla.toast({title: "test"})` from a page without pre-mounted regions auto-creates a `.toast-region--top-end` in body. **Step 4.6 closes — first component to ship both markup-first AND imperative paths; toast/dialog asymmetry is intentional (dialog is always markup-first since the content is the point; toast is often programmatic since the message is dynamic).** Next: **4.5 popover** (third Floating UI consumer — click-triggered surface with focus-trap and outside-click dismiss; reuses FUI + focus-trap, no new deps) and **4.7 carousel** (Embla wrapper, +~6 KB gz — tightest fit against the JS budget; will need to verify the headroom math at landing time).

- 2026-06-10 — **Step 4.4 — tooltip (CSS rewrite + `Stisla.Tooltip` class + demo rewrite)** — second Floating UI consumer; reuses the `@floating-ui/dom` bundle from 4.3 with no new deps. **CSS — `_tooltip.scss` full rewrite** (~130 lines from the 19-line BS5-override stub that referenced `$prefix` + `--bs-tooltip-*` + `--bs-emphasis-color` — dead since Step 2 unbundled BS5 SCSS). **BEM rename map**: `.tooltip-inner` → `.tooltip__inner`; `.tooltip-arrow` → `.tooltip__arrow`; `.bs-tooltip-{top,end,bottom,start}` BS5 wrapper classes dropped (Floating UI sets `data-placement` on the root after every position pass; CSS keys off `[data-placement^="top|bottom|left|right"]` for the arrow side + entry transform direction); `.fade .show` → `[data-state="open|closed"]`. **No intent modifiers** (`.tooltip--primary` etc.) — tooltips are informational; danger/success would be a category error. Consumers retune via `--tooltip-bg`/`--tooltip-color` inline overrides. **Inverse surface treatment**: `--tooltip-bg: var(--st-foreground)` + `--tooltip-color: var(--st-background)` — chip reads dark-on-light, light-on-dark via the token swap; no `[data-theme]` override block needed. Distinguishes from popover (`var(--st-surface)`) and matches Radix/shadcn convention. **Radius opt-in** at `--st-radius-sm` (8 px under default 12 px `--st-radius` per 3.6c three-tier rhythm — same small-chip tier as `.kbd`/`.badge`); a tooltip is a label, not a card. **12 component vars in 4 groups** (one-tier `--tooltip-*` naming, matches 4.1–4.3 convention): Geometry (7: max-width 240 px, padding-y 6 px, padding-x 10 px, radius, z-index 1050 between drawer 1045 + dialog 1055, font-size 0.8125rem, line-height 1.4); Surface (3: bg inverse, color inverse, shadow soft drop `0 4px 12px -2px` with `color-mix(in oklch, var(--st-foreground) 12%, transparent)`); Arrow (1: size 8 px); Motion (1: transition-duration 0.15s). **Arrow geometry**: 8 px square rotated 45°; CSS pins the cross-axis offset per placement via `calc(var(--tooltip-arrow-size) * -0.5)` so half the rotated square sits inside `.tooltip__inner` (covered by its bg) and half sticks out as the visible triangle. No z-index trick: the arrow is first in DOM, the inner second + static-positioned, so the inner's bg paints over the arrow's overlap naturally. **Entry motion = fade + 4 px translate toward trigger** so the chip eases away from the anchored edge. Per-placement resting transform: top → `translateY(4px)`, bottom → `translateY(-4px)`, left → `translateX(4px)`, right → `translateX(-4px)`; all collapse to `transform: none` under `[data-state="open"]`. JS seeds `[data-placement]` from opts before the first show; Floating UI's resolved placement (post-flip) overwrites it on every position pass, so the transform direction tracks the *actual* anchored edge, not the requested one. **`position: fixed` strategy** (matches dropdown 4.3) so any ancestor with `overflow: hidden` can't crop the chip; FUI's `strategy: 'fixed'` computes top/left in viewport coords. Same `transform`/`filter`/`will-change`/`contain` containing-block caveat carries over from dropdown. **No scroll lock** (unlike dropdown 4.3) — tooltips dismiss on `mouseleave`/`blur` so a long-lived reposition-during-scroll isn't a concern; `autoUpdate()` re-positions cheaply for the brief window the chip is up. **`pointer-events: none`** on the root so the chip never intercepts pointer events from the trigger or layers beneath. **JS — `src/js/components/tooltip.js`** (~280 lines). `class Tooltip extends Component` with `static eventNamespace = 'tooltip'` + `static defaults = { placement: 'top', offset: 8, delay: 600, closeDelay: 0, trigger: 'hover focus', html: false, title: null }`. **Per-instance DOM build** in constructor: creates `.tooltip[role="tooltip"]#stisla-tooltip-N` + `.tooltip__arrow` + `.tooltip__inner`, but does NOT append until first `show()`. Avoids body bloat across many tooltips that never open. Element stays in DOM after first show; `destroy()` removes it. **Floating UI middleware chain**: `offset(opts.offset)` → `flip({ padding: 8 })` → `shift({ padding: 8 })` → `arrow({ element: this._arrowEl, padding: 4 })`. The `arrow` middleware is the only new FUI export pulled in beyond dropdown's `offset/flip/shift/size` — accounts for ~0.2 KB gz of the bundle delta. `middlewareData.arrow` returns `{ x }` for top/bottom placements and `{ y }` for left/right; JS writes whichever is non-null to the arrow element's inline `left`/`top`. **Title resolution** in order: `opts.title` (imperative) → `data-stisla-tooltip-title` attr → native `[title]` attr. The native `title` is **migrated and stripped** on construction so the browser tooltip doesn't double-fire next to ours (matches BS5 behavior consumers expect); `destroy()` restores the original. Empty title → `show()` bails silently (no chip, no visible "label-less" tooltip). **`aria-describedby`** set on trigger to the tooltip's `id` on open, cleared on close. Set-then-clear (vs. permanently set) keeps SR announcements scoped to actual hover/focus and matches BS5/Radix; the permanent-aria-describedby alternative would over-announce because SR reads aria-describedby targets regardless of visibility. **Delay state via `setTimeout`**: `_scheduleShow()` clears any pending close timer, schedules a `show()` after `opts.delay` (default 600 ms — prevents flash on incidental hover); `_scheduleHide()` clears any pending open timer, schedules `hide()` after `opts.closeDelay` (default 0 ms — non-interactive so no grace window needed). The clear-on-opposite-event pattern handles the "user hovered in, then out before delay fired" case cleanly. **Trigger wiring** per `opts.trigger`: `'hover focus'` (default) wires `mouseenter`/`mouseleave` + `focusin`/`focusout`; `'hover'` skips focus; `'focus'` skips hover; `'manual'` wires nothing and leaves `show()`/`hide()` to a consumer script. All listeners recorded via `this.on()` so `destroy()` detaches them via the Component base. **HTML mode** (`opts.html === true` after `=== 'true'` string normalization): renders title as `innerHTML`. Default `false` writes via `textContent` (XSS-safe). Kept narrow on purpose — anything beyond a `<kbd>` chip or two belongs in a popover. **No module-level delegated handler** (unlike dropdown's click-outside / item-click routes): tooltip listeners are per-instance on the trigger; there's nothing global to bind. The `setContent(title)` method exposes a runtime content swap path for consumers that re-key tooltips from JS. **`Stisla.init()` integration**: `[data-stisla-tooltip]` triggers get instantiated by the scanner on `DOMContentLoaded`; per-attr opts (`-title`, `-placement`, `-delay`, `-close-delay`, `-trigger`, `-html`, `-offset`) merged via `readOpts()`. **`src/js/index.js`** adds `import { Tooltip }` + `register('tooltip', Tooltip)` + exposes `Stisla.Tooltip`. **Bundle**: `_tooltip.scss` uncommented at line 69 of `bundles/stisla-full.scss`. **Page rename**: `src/site/pages/tooltips.njk` → `tooltip.njk` to match `dialog.njk`/`drawer.njk`/`dropdown.njk` singular convention; sidebar nav `/tooltips` → `/tooltip` (text "Tooltips" → "Tooltip") in `_site-sidebar.njk`. Stale `site-dist/tooltips.html` deleted post-build (vite/render-site don't clean output dirs; the orphaned file would have lingered forever otherwise — flag for future page renames). **Demo page rewrite** — 10 sections (Basic, Placements, Triggers, Delay, On a link, Icon-only triggers, HTML content, Long content, Disabled trigger, Customization) replacing the BS5-vocabulary 6-section original. Added: **Triggers** (hover-only / focus-only / hover+focus to demo the new opt), **Delay** (0 / 600 / 1200 ms to demo the delay knob). Carried forward: BS5 demo's link, icon-only, HTML content, long content, disabled trigger sections — same UX coverage with v3 vocab. **BS5 vocab swept**: `data-bs-toggle="tooltip"` → `data-stisla-tooltip`; `data-bs-title` → `data-stisla-tooltip-title`; `data-bs-placement` → `data-stisla-tooltip-placement`; `data-bs-html` → `data-stisla-tooltip-html`; `.btn-primary` → `.btn .btn--primary`; `.btn-outline-secondary` → `.btn .btn--outline.btn--neutral`; `.btn-outline-danger` → `.btn .btn--outline.btn--danger`; `.btn-icon` → `.btn--icon` modifier; BS5 utilities (`.d-inline-block`, `.d-flex flex-wrap gap-2`) → inline `style="..."` per the established v3 idiom (v3 doesn't ship flex utilities yet). **Lead paragraph** per `[[feedback_docs_voice]]`: short, no em-dashes, no pixel callouts ("A small floating label that names or describes the trigger it sits over." — 14 words). **Customization section** ships 4 grouped tables (Geometry/Surface/Arrow/Motion) at 12 rows total, slider.njk reference shape. **Verification**: build green (42 pages render — unchanged from 4.3 since the page rename is 1:1), bundle CSS 114.2 → 116.3 KB raw (+2.1) / 18.31 → 18.69 KB gz (+0.38); JS 58.4 → 62.98 KB raw (+4.58) / 17.31 → 18.57 KB gz (+1.26) — JS gz delta is Tooltip class (~1.0 KB gz) + FUI `arrow` middleware (~0.26 KB gz). Total JS still inside V3.md §3.1's ~25–30 KB gz budget; ~6.4 KB gz headroom for 4.5 popover (reuses FUI + focus-trap, ~1 KB), 4.6 toast (no deps, ~2 KB), 4.7 carousel (Embla ~6 KB gz — tightest fit). 0 `--bs-*` leaks in compiled CSS (`grep -c -- "--bs-" site-dist/assets/stisla-full.css` = 0). 0 BS5 vocab in rendered `/tooltip` HTML (strict class-attribute grep across `data-bs-toggle="tooltip"`, `data-bs-title`, `data-bs-placement`, `data-bs-html`, `.bs-tooltip-`, `btn-primary`, `btn-outline-secondary`, `btn-outline-danger`, `btn-icon`, `text-body-secondary` all return zero hits). All 12 `--tooltip-*` vars emit (`grep -oE -- "--tooltip-[a-z-]+"`: arrow-size, bg, color, font-size, line-height, max-width, padding-x, padding-y, radius, shadow, transition-duration, z-index). All 3 BEM selectors emit (`.tooltip`, `.tooltip__arrow`, `.tooltip__inner`). All 6 Stisla data-attrs render across the 10 demos (`data-stisla-tooltip` 49×, `-title` 49×, `-placement` 17×, `-trigger` 5×, `-delay` 4×, `-html` 3×). JS bundle contains the `"tooltip"` register key, `eventNamespace` literal (2×), `stisla-tooltip-` id template, `tooltip__inner`/`tooltip__arrow` runtime selectors, `aria-describedby` setter, and FUI's `arrow` middleware (the new export beyond dropdown's chain). **Dialog + Drawer + Dropdown regression check**: `__stislaDialogBound` + `__stislaDrawerBound` + `__stislaDropdownBound` sentinels still present (1× each — grep shows once because the strings appear in both the guard and the assignment but the simpler check still confirms presence), `"dialog"`/`"drawer"`/`"dropdown"` event-namespace literals all bundled. All four component sentinels coexist. **Visual + interaction verification pending** — maintainer to eyeball `/tooltip` in light + dark, exercising: (a) Basic — hover trigger opens after 600 ms, focus trigger opens immediately if hover already opened (state shared per instance), `mouseleave`/`blur` closes; (b) Placements — top/right/bottom/left + the four `-start`/`-end` corner variants all paint at correct sides with arrow on the correct edge; flip() auto-flips a top-placed tooltip below trigger when scrolled near viewport top; (c) Triggers — hover-only skips keyboard focus, focus-only skips mouse hover, manual opt wires nothing; (d) Delay — instant chip pops immediately, lazy chip waits 1.2 s — pointer-leave during delay cancels the pending show cleanly; (e) On a link — inline anchors anchor the tooltip without breaking inline flow; (f) Icon-only triggers — small square buttons with tooltips as labels; `aria-label` still set as SR-without-hover fallback; (g) HTML content — `<kbd>` chips render inside the tooltip; (h) Long content — wraps at 240 px max-width to a second line; (i) Disabled trigger — wrapper `<span tabindex="0">` receives hover/focus and shows the tooltip even though the inner button is `disabled`; (j) Customization — 4 grouped tables read cleanly with Variable/Default/Use columns; (k) Dark mode — inverse surface flips with theme via token swap, no `[data-theme]` block; (l) Reduced motion — transitions zeroed, chip snaps open/closed without translate; (m) `aria-describedby` — present on trigger only while tooltip is open (verified via inspector); (n) Native `[title]` migration — a trigger that had a `title` attr loses it on init and gets it back on `destroy()`. **Step 4.4 closes — second Floating UI landing; tooltip pattern (lazy DOM, per-instance listeners, delay state, aria-describedby toggle, no scroll lock) sets the template for any future short-lived anchored overlay.** Next: **4.5 popover** — third Floating UI consumer. Closer to dropdown shape (focus-trap, outside-click dismiss, content surface using `--st-surface` not the tooltip inverse, larger geometry) but click-triggered not hover. Then 4.6 toast (no Floating UI; queueing + dismiss + transitions — running in parallel session), 4.7 carousel (Embla wrapper, +~6 KB gz).

- 2026-06-10 — **Step 4.3 — dropdown (CSS rewrite + `Stisla.Dropdown` class + demo rewrite)** — first Floating UI consumer; the `@floating-ui/dom` ~6 KB gz dep lands here and becomes the anchored-positioning primitive that 4.4 tooltip + 4.5 popover reuse. **CSS — `_dropdown.scss` full rewrite** (~310 lines, the old 60-line BS5-overlay file wiped; it referenced `$prefix` + `--bs-*` vars + `.show` flag, dead since Step 2 unbundled BS5 SCSS). **BEM rename map**: `.dropdown-item` → `.dropdown-menu__item`; `.dropdown-header` → `.dropdown-menu__header`; `.dropdown-divider` → `.dropdown-menu__divider`. New parts: `.dropdown-menu__group` (role="group" wrapper), `.dropdown-menu__icon` (leading slot), `.dropdown-menu__indicator` (leading check-mark slot for menuitemcheckbox/radio), `.dropdown-menu__shortcut` (trailing keystroke chip — auto-margin-inline-start pushes to row end), `.dropdown-menu__item--danger` (destructive variant). **Dropped wholesale**: `.dropdown-toggle` (trigger is just `.btn` now; consumers drop a caret SVG inline if wanted — matches Radix `<DropdownMenuTrigger>`); `.dropup`/`.dropend`/`.dropstart` BS5 wrapper classes (Floating UI computes placement; `data-stisla-dropdown-placement="top|right-start|left-end|bottom-end|..."` is the per-instance opt + `flip()` middleware auto-flips when the menu would overflow viewport); `.dropdown-menu.show` (replaced by `[data-state="open|closed"]`, canonical Radix-aligned paint hook); `.dropdown-toggle-split` (already gone in 3.6a). **Concentric inner radius (V3.md §3.4 canonical case)**: `--dropdown-radius: var(--st-radius)` outer (12 px medium tier per 3.6c hierarchy) + `--dropdown-padding: 0.25rem` (4 px) → `--dropdown-item-radius: calc(var(--dropdown-radius) - var(--dropdown-padding))` = 8 px inner. Matches the prior v3 visual identity (10rem min-width, 32 px row min-height, 2 px gap, 14 px font, 4 px inset, 8 px row radius) so dimensions carry forward unchanged from the BS5 era — only the var namespace + token routing change. **Color model** (corrects the prior v3's BS5 hover/active conflation, same fix landed in 3.5b list-group / 3.5d pagination): rest = transparent + `--st-foreground`; hover + `[data-highlighted]` = `--st-accent` (V3.md §3.3 transient interactional); `[aria-current="true"]` OR `[data-state="active"]` = `--st-highlight` + `--st-highlight-foreground` (persistent selected); disabled (`:disabled` / `[aria-disabled]`) = `--st-muted-foreground` + no pointer; `--danger` = `--st-danger` color with `color-mix(in oklch, var(--st-danger) 12%, transparent)` hover bg (soft tint so destructive rows never share visual weight with routine items). Active paint wins over hover via specificity so an active row stays highlighted even on hover. Icons inside items track `--st-muted-foreground` at rest, `inherit` on hover/highlighted/active/`--danger` — destructive rows get red icons alongside red text (single-line fix maintainer caught from the demo screenshot). **33 component vars in 8 groups** (one-tier `--dropdown-*` naming, matches 4.1/4.2 convention): Geometry (7: width-min 10rem, padding 0.25rem, radius, item-radius derived, gap 2px, z-index 1030 — below drawer 1045 + dialog 1055, font-size 0.875rem); Surface (4: bg `--st-surface`, color `--st-foreground`, border-color `--st-border`, shadow `--st-shadow`); Item (5: padding-y/x, min-height density-multiplied, gap 0.5rem, icon-size 1rem); Item states (7: bg-hover, color-hover, bg-active, color-active, color-disabled, color-danger, bg-danger-hover); Header (5: padding-y/x, font-size 0.75rem, font-weight 600, color); Divider (2: color, margin-y 0.5rem); Shortcut + Motion (3: shortcut color, font-size, transition-duration 0.15s). **JS — `src/js/components/dropdown.js`** (~360 lines). `class Dropdown extends Component` with `static eventNamespace = 'dropdown'` + `static defaults = { placement: 'bottom-start', offset: 8, autoClose: 'both', focus: true }`. Trigger discovered by `document.querySelector('[data-stisla-dropdown-trigger="<menuId>"]')` so the trigger can sit inside or outside the `.dropdown` wrapper (a consumer-side concern); dev warn if the menu lacks an `id`. **Floating UI middleware chain**: `offset(opts.offset)` → `flip({ padding: 8 })` (auto-flips on viewport overflow) → `shift({ padding: 8 })` (slides along axis when corner-aligned) → `size({ apply: ({ availableHeight }) => { maxHeight: max(120, availableHeight - 8); overflowY: auto } })` so a long menu in a short viewport scrolls inside. **`position: fixed` strategy** (not absolute) so the menu escapes any ancestor with `overflow: hidden` — matters in real apps where a sidebar or card might clip its overflow. CSS matches with `position: fixed` on `.dropdown-menu`; Floating UI's `strategy: 'fixed'` computes top/left in viewport coords. Caveat noted in the SCSS: ancestors with `transform`/`filter`/`will-change`/`contain: layout|paint` become the containing block for fixed elements, which would re-introduce clipping — would need to switch to a portal pattern if that ever bites in practice. **Scroll-lock via `<html>.is-dropdown-open`** + `openCount` stack counter — same pattern as Dialog (`is-dialog-open`) + Drawer (`is-drawer-open`). Locking the body sidesteps the visible Floating UI repositioning lag that would otherwise happen on every scroll frame (the menu is fixed; the trigger scrolls; reposition has a 1-frame lag = visible "shake"). Radix's `DropdownMenu` itself doesn't scroll-lock (only their `Dialog`/`AlertDialog` do) — we're diverging from Radix's dropdown specifically but matching Stisla's Dialog/Drawer convention; maintainer call. **`flex-shrink: 0` on `.dropdown-menu > *`** — the menu is `display: flex; flex-direction: column`; without this rule, when Floating UI's size middleware caps `max-height`, flex's default `flex-shrink: 1` would squish the last child (e.g. the form-demo's submit button) to fit instead of letting the menu's `overflow-y: auto` handle overflow by scrolling. Was the real fix the user caught — initial commits had the symptom (squished submit button) and I lazy-bumped the demo container's min-height instead of fixing the root cause; maintainer pushed back. **Mouse hover policy — keyboard-only highlight** (locked after maintainer feedback): mouse hovering an item paints `:hover` via CSS but does NOT set `data-highlighted` (the keyboard-focus marker). Native menu behavior, not the `<select>`-style auto-track-on-hover that the initial implementation had. On open, no item is pre-highlighted; the menu container itself gets focus (`tabindex=-1`) so document keydown sees the menu in the focus chain. First keyboard Down/Home/letter seeds `data-highlighted` on the first enabled item; first Up/End on the last; subsequent presses move ±1 / first / last / typeahead. Enter/Space activates the keyboard-highlighted row if one exists. Tab closes the menu and lets focus advance naturally. ESC closes + returns focus to trigger. Persistent active paint (`aria-current="true"` / `data-state="active"`) stays put regardless of mouse activity. **menuitemcheckbox + menuitemradio** click handling (Radix-parity): clicking a `role="menuitemcheckbox"` flips `data-state` + `aria-checked`; clicking a `role="menuitemradio"` checks it + unchecks every sibling radio in the closest `[role="group"]`. CSS `.dropdown-menu__indicator { visibility: hidden }` + `[data-state="checked"] .dropdown-menu__indicator { visibility: visible }` paints the leading check glyph; using `visibility` (not `display`) preserves the leading footprint so menus with mixed checked/unchecked items stay column-aligned. **Module-level**: single delegated click handler bound once via `window.__stislaDropdownBound` sentinel (HMR-safe, mirrors 4.1/4.2 pattern). Routes `[data-stisla-dropdown-trigger="<id>"]` → `Dropdown.getOrCreate(getElementById(id)).toggle()` with per-attr opts re-read on every toggle (belt-and-braces over `Stisla.init()` scan). Item clicks honor `opts.autoClose`: `'both'` (default) or `'inside'` closes on item activation; `'outside'` keeps menu open (form-as-menu pattern); per-item `data-stisla-dropdown-auto-close="false"` opts out individually (radio/checkbox items that should keep the menu open between toggles). Outside-click via `document.pointerdown` capture; respects `autoClose === 'outside' | 'both'`. **`src/js/index.js`** adds `import { Dropdown }` + `register('dropdown', Dropdown)` + exposes `Stisla.Dropdown`. **Bundle**: `_dropdown.scss` uncommented at line 51 of `bundles/stisla-full.scss`. **Page rename**: `src/site/pages/dropdowns.njk` → `dropdown.njk` to match `dialog.njk`/`drawer.njk` singular convention; sidebar nav `/dropdowns` → `/dropdown` in `_site-sidebar.njk`. **Demo page rewrite** — 11 sections (Basic, With icons, Headers and dividers, Active and disabled, Destructive items (new), Checkbox items (new), Radio items (new), Keyboard shortcuts (new), Form inside, Placement, Customization). Replaces the BS5 demo's "Split button" (already shown in /button-group with `aria-haspopup="menu"`) + "Drop direction" (which used the dropped wrapper classes). **BS5 vocab swept**: `data-bs-toggle="dropdown"` → `data-stisla-dropdown-trigger="menuId"` (single attr, no separate target needed); `data-bs-auto-close="outside"` → `data-stisla-dropdown-auto-close="outside"`; `.dropdown-toggle` removed (trigger is `.btn`); `.dropdown-menu.show` → `[data-state="open"]`; `.dropup`/`.dropend`/`.dropstart` → `data-stisla-dropdown-placement="..."`; `.dropdown-item` → `.dropdown-menu__item`; `.dropdown-header` → `.dropdown-menu__header`; `.dropdown-divider` → `.dropdown-menu__divider`; `.active` on items → `aria-current="true"` (preferred) or `data-state="active"`; `.disabled` on items → `aria-disabled="true"` on anchors or native `disabled` on buttons; `.btn-primary` → `.btn .btn--primary`, `.btn-outline-secondary` → `.btn .btn--outline.btn--neutral`; BS5 utilities (`.d-flex`, `.flex-wrap`, `.gap-2`, `.mb-3`, `.w-100`, `.p-3`) → inline `style="..."` per the established v3 idiom. **Customization section** ships 8 grouped tables (Geometry/Surface/Item/Item states/Header/Divider/Shortcut + motion) at 33 rows total, slider.njk reference shape. **Verification**: build green (42 pages render), bundle CSS 111.36 → ~114.2 KB raw (+2.8) / 17.85 → 18.31 KB gz (+0.46); JS 33.88 → ~58.4 KB raw (+24.5) / 9.74 → 17.31 KB gz (+7.57) — JS jump is `@floating-ui/dom` (~6 KB gz) + Dropdown class (~1.5 KB gz). Total JS still inside V3.md §3.1's ~25–30 KB gz budget; ~8–13 KB gz headroom for the remaining components (4.4 tooltip + 4.5 popover reuse Floating UI for free; 4.6 toast no deps; 4.7 carousel adds Embla ~6 KB gz). 0 `--bs-*` leaks in compiled CSS. 0 BS5 vocab in rendered `/dropdown` HTML (strict class-attribute grep across `dropdown-(toggle|item|header|divider)`, `dropup`, `dropend`, `dropstart`, `dropdown-toggle-split`, `data-bs-toggle`, `data-bs-auto-close`, `btn-primary`, `btn-outline-secondary`, `btn-icon`, `d-flex`, `mb-*`, `w-100`, `visually-hidden`, `form-label` — only false-positives are `flex-wrap` inside inline `style="..."` and `form-label` which is a v3-current form-control class). All 33 `--dropdown-*` vars emit (`grep -oE -- "--dropdown-[a-z-]+"`). All 11 BEM selectors emit (`.dropdown`, `.dropdown-menu`, `.dropdown-menu__item`, `.dropdown-menu__item--danger`, `.dropdown-menu__header`, `.dropdown-menu__divider`, `.dropdown-menu__group`, `.dropdown-menu__icon`, `.dropdown-menu__indicator`, `.dropdown-menu__shortcut`). All 4 Stisla data-attrs render across the 11 demos (`data-stisla-dropdown`, `-trigger`, `-placement`, `-auto-close` at 71/27/9/8 hits respectively). JS bundle contains `__stislaDropdownBound` sentinel (2× — guard + assignment), `is-dropdown-open` scroll-lock class, Floating UI internals (`placement` 22×, `rootBoundary` 3×, `boundary` 3×, `bottom-start` 1× confirming the strategy + middleware chain are bundled), `menuitemcheckbox` + `menuitemradio` role strings (for the click-flip path). **Dialog + Drawer regression check**: `__stislaDialogBound` + `__stislaDrawerBound` sentinels still present (2× each), `is-dialog-open` + `is-drawer-open` scroll-lock classes still emit. All three component sentinels coexist. **Demo nav update**: `_site-sidebar.njk` `/dropdowns` → `/dropdown` (text "Dropdowns" → "Dropdown") to match the singular naming. **Visual + interaction verification done** in-session (maintainer screenshots + iterative fixes): (a) destructive item icon now red alongside the text via inherit cascade; (b) submit button inside form-menu no longer crushed by flex-shrink; (c) menu escapes `.demo` overflow clip via position: fixed; (d) scroll-locked body so no Floating UI reposition lag on scroll; (e) mouse hover doesn't auto-track keyboard highlight (keyboard-only). **Step 4.3 closes the first Floating UI landing.** Next: **4.4 tooltip** — second Floating UI consumer, no deps add since `@floating-ui/dom` is bundled; hover/focus delay state, no focus-trap (tooltips don't trap), no scroll-lock (tooltips don't lock), `[role="tooltip"]` semantics, `aria-describedby` wiring. Then 4.5 popover (Floating UI + focus-trap + outside-click — closer to dropdown but with focus-trap since it's a content container). Then 4.6 toast (no Floating UI; queueing + dismiss + transitions). Then 4.7 carousel (Embla wrapper, adds ~6 KB gz).

- 2026-06-10 — **Step 4.2 — offcanvas (CSS rewrite + `Stisla.Offcanvas` class + demo rewrite)** — second JS-coordinated component; first reuse of the 4.1 contract on a new shape. **CSS — `_offcanvas.scss` full rewrite** (~250 lines, the old 83-line BS5-vocabulary file wiped; it referenced `$prefix`, BS5 `--bs-*` vars, was dead code since Step 2 unbundled BS5 SCSS). **BEM rename map**: `.offcanvas-backdrop` → `.offcanvas__backdrop` (moves *inside* the root, same structural binding 4.1 did for modal); `.offcanvas-header/-title/-body/-footer/-close` → `.offcanvas__header/__title/__body/__footer/__close`; `.offcanvas-start/-end/-top/-bottom` → `.offcanvas--start/--end/--top/--bottom` on the root; `.fade .show` → `[data-state="open|closed"]`. **`.offcanvas__content` introduced** as a new BEM part (BS5 had no equivalent — the panel was directly the `.offcanvas` element). Reason: needed a *transform target* for the slide that isn't the root, because the root has to stay full-viewport (`position: fixed; inset: 0`) so the backdrop can paint behind. The content sits absolutely-positioned inside, anchored to the relevant edge, and is what slides. **Default placement = `--end`** (matches Radix Sheet, common dashboard right-drawer case); bare `.offcanvas` paints as end via a `.offcanvas, .offcanvas--end { … }` shared selector. **Sharp viewport-edge corners** are the affordance — no radius var (override inline if a consumer wants rounded inner corners). **Only the inner edge carries a border** (the edge facing the viewport): `--end` gets `border-inline-start`, `--start` gets `border-inline-end`, `--top` gets `border-bottom`, `--bottom` gets `border-top`. Logical properties for free RTL when it lands. **20 component vars in 7 groups** (one-tier `--offcanvas-*` naming, mirrors 4.1's `--modal-*`): Geometry (4: width 22rem, height 16rem, padding, z-index 1045 — one tier below modal's 1055), Surface (4: bg, color, border-color, shadow — softer `0 0 40px -10px` vs. modal's `0 10px 40px -10px` since the panel is anchored not floating), Backdrop (2: bg `oklch(0 0 0 / 0.55)` + blur 12px — same treatment as modal so a stacked open reads consistently), Title (2: font-size 1.125rem, weight 600), Close chip (4: size 1.75rem, color `--st-muted-foreground`, hover color `--st-foreground`, hover bg `--st-accent` — **ghost button** vs. modal's frosted dark chip, mirrors the prior v3 file's note: offcanvas always has a header so the close sits *inline* at the trailing edge of the header row), Footer (3: padding-y, bg `--st-surface-2`, border — same two-tone seam as modal), Motion (1: transition-duration 0.25s — slightly slower than modal's 0.2s because slide distance is longer than scale). **Placement transitions via translateX/Y**: `--start` translateX(-100%) → 0; `--end` translateX(100%) → 0; `--top` translateY(-100%) → 0; `--bottom` translateY(100%) → 0. Backdrop opacity fade identical to modal. The transform target is `.offcanvas__content`, not the root, so the backdrop stays put while the content slides. **Static-backdrop shake** picks the keyframe axis from the placement: `--start/--end` uses `offcanvas-shake-x`; `--top/--bottom` uses `offcanvas-shake-y` (so the nudge reads as "stuck along the slide axis" instead of always horizontal). **Scroll lock**: `<html>.is-offcanvas-open` — separate from modal's `is-modal-open` so the two counters are independent (a modal opened over an offcanvas inspector locks once; closing the modal first doesn't release the lock the offcanvas is still holding). **Backdrop opt-out**: `data-stisla-offcanvas-backdrop="false"` on the root hides `.offcanvas__backdrop` via CSS attr selector — no markup change needed, JS just skips the dismiss-on-click path. **Reduced motion** zeros both content transitions and the shake keyframes. **JS — `src/js/components/offcanvas.js`** (~245 lines, near-mirror of `modal.js` — same `Component` base, `static eventNamespace = 'offcanvas'`, `static defaults = { backdrop: true, keyboard: true, focus: true, returnFocus: true, scroll: false }`). Methods mirror modal exactly: `open()` (cancelable opening event, save return focus, inert siblings, scroll-lock counter increment IF `!opts.scroll`, force `display: block` + reflow + rAF + flip `data-state="open"` + activate focus-trap + wire ESC keydown + await transition + non-cancelable `opened`), `close()` (mirror), `toggle()`, `shake()` (toggles `.is-shaking` on `.offcanvas__content` instead of `.modal__dialog`), `destroy()` (force-cleans an open instance — counter decrement, inert removal, trap deactivate — before super), `_inertSiblings`, `_waitForTransition`, `_firstTabbable` (all identical to modal). **Differentiators from `Modal`**: (1) `scroll: false` default with `scroll: true` opt that **skips the scroll-lock counter entirely** — supports the BS5 "filter drawer that doesn't lock the body" pattern. Two-line conditional in `open()` and `close()`, real differentiator from modal. Opt normalization: attribute parser may hand us `"true"`/`"false"` strings, so `this.opts.scroll = this.opts.scroll === true || this.opts.scroll === 'true'` upfront. (2) Wait-for-transition target is `_content` (the thing that actually transitions) not `_dialog` (modal's name). (3) Backdrop `false` opt-out path renders the backdrop element but CSS-hides it; JS skips the static-shake branch since the backdrop never receives clicks. **Module-level**: single delegated click handler bound once via `window.__stislaOffcanvasBound` HMR sentinel (mirrors `__stislaModalBound`); routes `[data-stisla-offcanvas-trigger="<id>"]` → `Offcanvas.getOrCreate(getElementById(id)).open()` with per-attr opts re-read on every open (belt-and-braces over `Stisla.init()` scan, same pattern as 4.1's 30eedb4 fix); `[data-stisla-offcanvas-dismiss]` → `inst.close()` with the static-backdrop shake branch when `inst.opts.backdrop === 'static'` AND the dismiss is `.offcanvas__backdrop`. Explicit dismiss controls (`.offcanvas__close`, footer buttons) always close even in static mode. **`src/js/index.js`** adds `import { Offcanvas }` + `register('offcanvas', Offcanvas)` + exposes `Stisla.Offcanvas`. **Bundle**: `_offcanvas.scss` uncommented at line 72 of `bundles/stisla-full.scss`. **Demo page (`src/site/pages/offcanvas.njk`) full rewrite** — 7 sections (Basic, Placements, Sized, Body scroll allowed, Static backdrop, No backdrop, Customization) replacing the BS5-vocabulary 6-section original. **Dropped wholesale**: the old "Responsive" section (`.offcanvas-{sm|md|lg|xl|xxl}` — collapse-only-below-breakpoint behavior). Responsive sidebar behavior is what `.sidebar` is for in v3; offcanvas is purely the edge-anchored panel concept. Same simplification 4.1 did with `.modal-dialog-centered` ("center is the v3 default"). **BS5 vocab swept**: `data-bs-toggle="offcanvas"` + `data-bs-target="#X"` → `data-stisla-offcanvas-trigger="X"`; `data-bs-dismiss="offcanvas"` → `data-stisla-offcanvas-dismiss`; `data-bs-backdrop="static"`/`"false"` → `data-stisla-offcanvas-backdrop="static"`/`"false"`; `data-bs-keyboard="false"` → `data-stisla-offcanvas-keyboard="false"`; `data-bs-scroll="true"` → `data-stisla-offcanvas-scroll="true"`; `.offcanvas-*` → BEM `__`/`--` per rename map; `<h5 class="offcanvas-title">` → `<h3 class="offcanvas__title">` (semantic level bump, h3 fits the demo page hierarchy under h1/h2); `.btn-primary`/`.btn-secondary`/`.btn-outline-secondary`/`.btn-ghost-secondary` → `.btn .btn--primary`/`.btn .btn--neutral`/`.btn .btn--outline.btn--neutral`/`.btn .btn--ghost.btn--neutral`; `.form-select` → `.form-control` (per Step 3.3a `.form-select` is removed from public API entirely; `<select>` reads `form-control` vars); `.icon-box-*` → `.icon-box.icon-box--*`; `.text-body-secondary` → `.text-muted-foreground` (per `[[feedback_muted_text_utility]]`); `.list-group-flush` → `.list-group--flush`; `.list-group-item` → `.list-group__item`; `.form-check-input/-label` → `.form-check__input/__label`; `.badge-light-{primary,success,warning}` → `.badge .badge--{primary,success,warning}`; `--bs-icon-box-size` → `--icon-box-size`; all BS5 utilities (`d-flex`, `gap-2`, `mb-3`, `flex-grow-1`, `flex-shrink-0`, `me-2`, `ms-auto`, `p-0`, `p-lg-0`, `w-100`, `mx-auto`, `nav-pills`, `nav-link`, `flex-column`, `rounded-circle`, `border-bottom`, `fw-medium`, `list-unstyled`, `small`, `d-lg-none`) → inline `style=""` per the v3 idiom established across list-group/breadcrumb/navbar/pagination/progress/modal demos (v3 doesn't ship flex utilities yet). **Lead paragraph rewritten** per `[[feedback_docs_voice]]`: short, no em-dashes, no pixel callouts ("An edge-anchored panel for side drawers, filters, and quick captures." — 11 words). **Customization section** ships 7 grouped tables (Geometry/Surface/Backdrop/Title/Close chip/Footer/Motion) at 20 rows total, slider.njk reference shape. **Verification**: build green (42 pages render), bundle CSS 105.99 → 111.36 KB raw (+5.37) / 17.21 → 17.85 KB gz (+0.64); JS 28.93 → 33.88 KB raw (+4.95) / 9.40 → 9.74 KB gz (+0.34) — gz delta is small because Offcanvas reuses focus-trap/tabbable already in the bundle (zero new deps) and gz finds heavy repetition between the modal + offcanvas class shapes. Total JS still well under V3.md §3.1's 25-30 KB gz budget; ~15 KB gz headroom for the remaining 4.3-4.7 components. 0 `--bs-*` leaks in compiled CSS. 0 BS5 vocab in rendered `/offcanvas` HTML (strict class-attribute grep across `offcanvas-(backdrop|header|title|body|footer|close|start|end|top|bottom|sm|md|lg|xl|xxl)`, `btn-(primary|secondary|danger|success|ghost-secondary|outline-secondary|outline-danger)`, `text-body-secondary`, `icon-box-(danger|success|info|warning|primary|secondary)`, `d-flex`, `d-inline-block`, `d-lg-none`, `justify-content-*`, `flex-(grow|shrink|wrap)-*`, `flex-column`, `nav-tabs`, `nav-pills`, `nav-link`, `list-group-flush`, `list-group-item`, `list-unstyled`, `fw-medium`, `p-0`, `p-lg-0`, `me-*`, `ms-auto`, `mb-*`, `w-100`, `small`, `border-bottom`, `rounded-circle`, `badge-light-*` all return zero hits). All 20 `--offcanvas-*` vars emit in compiled CSS (`grep -oE -- "--offcanvas-[a-z-]+"`). All 12 BEM selectors emit (`.offcanvas`, `.offcanvas--start`, `.offcanvas--end`, `.offcanvas--top`, `.offcanvas--bottom`, `.offcanvas__backdrop`, `.offcanvas__content`, `.offcanvas__header`, `.offcanvas__title`, `.offcanvas__body`, `.offcanvas__footer`, `.offcanvas__close`). All 6 Stisla data-attrs render in HTML (`data-stisla-offcanvas`, `data-stisla-offcanvas-backdrop`, `data-stisla-offcanvas-dismiss`, `data-stisla-offcanvas-keyboard`, `data-stisla-offcanvas-scroll`, `data-stisla-offcanvas-trigger`). JS bundle contains `__stislaOffcanvasBound` sentinel (2× — guard + assignment), `is-offcanvas-open` scroll-lock class, `offcanvas__content` + `offcanvas__backdrop` runtime selectors, and the `"offcanvas"` register key. **Modal regression check**: `__stislaModalBound` sentinel still present (2×), `is-modal-open` scroll-lock class still emits, `stisla:modal:*` events unaffected. Both component sentinels coexist. **Visual + interaction verification pending** — maintainer to eyeball `/offcanvas` in light + dark, exercising: (a) Basic — trigger opens end-drawer, focus moves to first tabbable (Title input), ESC closes, focus returns to trigger; (b) Placements — start/end/top/bottom all paint at correct edges with the right slide direction; only inner-edge border visible on each; (c) Sized — inline `--offcanvas-width: 28rem` retunes the panel without modifier; (d) Body scroll — page behind keeps scrolling while panel open, `<html>.is-offcanvas-open` NOT applied, counter NOT incremented; (e) Static backdrop — backdrop click shakes content along the slide axis without closing, ESC inert (because `keyboard: false`), explicit dismiss buttons still close; (f) No backdrop — backdrop element hidden via CSS attr selector, page behind fully interactive, panel sits alongside content; (g) Customization — 7 grouped tables read cleanly with Variable/Default/Use columns; (h) Focus trap — tab cycles inside panel, doesn't escape to background; (i) Scroll lock — page behind doesn't reflow when locking panel opens; (j) Stacked open — opening modal over offcanvas inspector both lock; closing modal first doesn't release the lock the offcanvas still holds (independent counters); (k) Dark mode — surface, border, backdrop dim, close chip ghost-button hover all adapt via `--st-accent` swap; (l) Reduced motion — transitions zeroed, panels snap open/close, shake doesn't run. **Step 4.2 closes — first reuse of 4.1's contract; offcanvas + modal coexist as the two centered/anchored overlay families.** Next: **4.3 dropdown** — first Floating UI consumer (`@floating-ui/dom` ~6 KB gz lands here), introduces the anchored-positioning primitive that 4.4 tooltip + 4.5 popover then layer on. Roving-tabindex menu navigation, click-outside dismiss, ESC, optional submenus.

- 2026-06-10 — **Step 4.1 — modal (CSS rewrite + `Stisla.Modal` class + demo rewrite)** — first JS-coordinated component lands; sets the contract every later component (offcanvas, dropdown, tooltip, popover, toast, carousel) copies. **CSS — `_modal.scss` full rewrite** (~250 lines, the old 127-line BS5-overlay file wiped; it referenced `$prefix`, BS5 Sass vars, `.modal-dialog-centered` BS5 vocab and was dead code since Step 2 unbundled BS5 SCSS). **BEM rename map**: `.modal-backdrop` (BS5 sibling) → `.modal__backdrop` (child of `.modal`, moves inside so the click-target is structurally bound + scoped DOM ops are simpler); `.modal-dialog` → `.modal__dialog`; `.modal-content` → `.modal__content`; `.modal-header` → `.modal__header`; `.modal-title` → `.modal__title` (heading tag stays consumer's choice, font-size pinned via `--modal-title-font-size: 1.125rem`, same heading-tag-agnostic pattern accordion 3.6c uses with `* > .accordion__header`); `.modal-body` → `.modal__body`; `.modal-footer` → `.modal__footer`; `.modal-close` → `.modal__close` (kept the floating frosted dismiss chip, rerouted from rgba literals to `color-mix(in oklch, var(--st-foreground|background) X%, transparent)` so light + dark modes both adapt without a `@include color-mode(dark)` override block — drops 8 lines from old file); `.modal-sm`/`-lg`/`-xl`/`-fullscreen` → `.modal--sm`/`--lg`/`--xl`/`--fullscreen` on the **root** not the dialog (cleaner — JS reads sizes off root; modifiers retune `--modal-width` in their own scope so no per-size vars to maintain); `.modal-dialog-centered` **dropped wholesale** (center is the v3 default, matches shadcn/Radix); `.modal-dialog-top`/`-bottom` → `.modal__dialog--top`/`--bottom`; `.modal-dialog-scrollable` → `.modal__dialog--scrollable`; `.fade .show` **dropped wholesale** (replaced by `data-state="open|closed"` on the root, the canonical Radix-aligned paint hook). **18 component vars in 6 groups** (one-tier `--modal-*` naming, matches recent landings 3.5d/3.5e/3.6c — NOT two-tier `--st-modal-*` indirection; the modal can read global tokens like `var(--st-radius-lg)` directly since the radius-opt-in pattern from V3.md §3.4 has been simplified across landings): Geometry (5: width, margin, padding, radius, z-index), Surface (4: bg, color, border-color, shadow), Backdrop (2: backdrop-bg, backdrop-blur), Title (2: title-font-size, title-font-weight), Close chip (4: close-size, close-bg, close-bg-hover, close-color), Motion (1: transition-duration). **Radius**: `--modal-radius: var(--st-radius-lg)` — large-surface tier, matches card + accordion + list-group from 3.6c (Step 3.6c's three-tier radius rhythm: sm 8px for chips, default 12px for medium elements, lg 16px for large frames; modal is now the fourth large-frame component, the last one called out in 3.6c's note that future modal + offcanvas land at `--st-radius-lg` when Step 4 ships). **Open/close paint via data-state + CSS transitions**: `.modal { display: none }` rest; `.modal[data-state="open"] { display: flex }` shown; `.modal__backdrop { opacity: 0 }` + `.modal__dialog { opacity: 0; transform: translateY(-1rem) scale(0.98) }` rest; `.modal[data-state="open"] .modal__backdrop { opacity: 1 }` + `.modal[data-state="open"] .modal__dialog { opacity: 1; transform: none }` shown. Both backdrop + dialog transition the same 0.2s duration so the lift reads coordinated (single `transitionend` listener in JS keys off the dialog). The display-none → display-flex flip is the only way to keep modal fully out of focus order + AT tree when closed; the JS adds a `requestAnimationFrame` between setting `display: flex` and flipping `data-state="open"` so the transitions actually run (without the rAF the browser collapses both changes into one paint and skips the transition). **Static-backdrop shake animation** via `.modal__dialog.is-shaking { animation: modal-shake 0.2s ease-in-out }` — JS adds the class on backdrop click when `opts.backdrop === 'static'`, removes after 250ms. **Scroll lock**: `<html>.is-modal-open { overflow: hidden; scrollbar-gutter: stable }` — Stisla-original state hook (V3.md §3.6 `.is-*` for non-Radix concepts); JS adds the class on first modal open + removes when count returns to zero (counter handles edge cases where modal A's transitionend fires after modal B opens, though stacked-modal is explicitly out of scope for 4.1). `scrollbar-gutter` reserves the scrollbar's width so the page doesn't reflow horizontally when locked. **Reduced motion**: single `@media (prefers-reduced-motion: reduce)` block zeros both the backdrop/dialog transitions AND the shake keyframe — JS also short-circuits the `_waitForTransition` promise via `matchMedia` so opened/closed events fire on next frame instead of after a 0-duration transition that may never emit `transitionend`. **Reused floating dismiss chip** — old `.modal-close` had `rgba(0, 0, 0, 0.55)` + `@include color-mode(dark) { .modal-close { --bg: rgba(255, 255, 255, 0.1) ... } }` block; new `.modal__close` derives bg from `color-mix(in oklch, var(--st-foreground) 55%, transparent)` which automatically inverts in dark mode (var swap on `--st-foreground` flips light → dark and the mix tracks). Color routes through `color-mix(in oklch, var(--st-background) 92%, transparent)` — same inversion logic for the icon. 4 vars expose all four states (rest bg, hover bg, icon color) — one-block scope, no dark-mode override needed. **JS — `src/js/components/modal.js`** (~220 lines, the first occupant of the `src/js/components/` directory Step 4.0 prelude scaffolded). `class Modal extends Component` with `static eventNamespace = 'modal'` + `static defaults = { backdrop: true, keyboard: true, focus: true, returnFocus: true }`. Methods: **`open()`** emits cancelable `stisla:modal:opening` → returns if vetoed → saves `document.activeElement` for return-focus → walks modal → body inerting siblings at each ancestor level (custom `_inertSiblings(true)` so the modal's ancestor chain stays interactive, everything outside drops from focus + AT tree — chose this over BS5's "relocate modal to body" portal pattern because relocation breaks `<form>` containment + Vite HMR semantics) → adds `<html>.is-modal-open` if first modal → forces `display: flex` inline + reflow → `requestAnimationFrame` flips `data-state="open"` + `aria-hidden="false"` → creates `focus-trap` instance on `.modal__content` with `initialFocus` defaulting to `[autofocus]` element or first tabbable (fallbacks to content itself if no tabbables — `fallbackFocus: this._content`) and `escapeDeactivates: false` + `returnFocusOnDeactivate: false` because we handle both ourselves → adds keydown listener for ESC → awaits `_waitForTransition(this._dialog)` then fires non-cancelable `:opened`. **`close()`** mirrors: emits cancelable `:closing` → returns if vetoed → deactivates focus-trap (try/catch guards against deactivate-while-not-active) → removes keydown listener → flips `data-state="closed"` + `aria-hidden="true"` → awaits transition → clears inline display, un-inerts siblings, decrements scroll-lock counter (removes `.is-modal-open` from `<html>` only when no modals remain open), restores focus to the saved `_returnFocusEl` if `opts.returnFocus` → fires non-cancelable `:closed`. **`toggle()`** dispatches based on current `data-state`. **`shake()`** removes + re-adds `.is-shaking` on the dialog with an offsetWidth reflow between (forces the CSS animation to re-run even when triggered in rapid succession) + setTimeout cleanup at 250ms. **`destroy()`** force-cleans an open modal without transitions before calling super: deactivates focus-trap, removes keydown listener, un-inerts siblings, decrements scroll-lock counter — so `getOrCreate()` returning a new instance on a re-init doesn't strand `<html>.is-modal-open` or `inert` attributes from the destroyed instance. **`_waitForTransition(el)`** is a defensive promise wrapper: detects `prefers-reduced-motion` (resolves next frame), computes max transitionDuration from `getComputedStyle` (resolves next frame if zero), listens for `transitionend` filtered to `e.target === el` (so a child transition doesn't false-fire), AND sets a `setTimeout` fallback at `total * 1.5 + 50ms` (covers the rare case where `transitionend` never fires — paint thrash, tab visibility change mid-transition). **`_inertSiblings(on)`** walks from `this.el` up to `<body>`, inerting siblings at each level; saves the inerted elements in `_inertCleanup` array and only removes inert from those (preserves any inert attrs the consumer set independently). **Module-level**: single delegated click handler bound once at module load, guarded by `window.__stislaModalBound` sentinel (HMR-safe, mirrors the Step 4.0 prelude pattern for the interim handlers + the existing `__stislaInterimBound`); handler routes `[data-stisla-modal-trigger="<id>"]` → `Modal.getOrCreate(getElementById(id)).open()` and `[data-stisla-modal-dismiss]` → `getInstance(closest('.modal')).close()`; static-backdrop branch: if `inst.opts.backdrop === 'static'` AND `dismiss.classList.contains('modal__backdrop')` → `inst.shake()` instead of `.close()`. Explicit dismiss controls (`.modal__close` button, footer buttons with `data-stisla-modal-dismiss`) always close even in static mode — only the backdrop element shakes. **`src/js/index.js`** registers via `register('modal', Modal)` and exposes `Stisla.Modal = Modal` so consumers can `new Stisla.Modal(el, opts)` directly OR rely on the declarative scanner (idempotent — `data-stisla-modal` markup auto-instantiates on `DOMContentLoaded`). **Demo page (`src/site/pages/modal.njk`) full rewrite** — 10 sections (Basic, Sizes, Positioning, Scrollable, Static backdrop, Media hero, Lightbox, Confirmation, Success, Customization) replacing the BS5-vocabulary 9-section original; the new Customization section adds 6 grouped tables (Geometry/Surface/Backdrop/Title/Close chip/Motion) at 18 rows total per `slider.njk` reference. **BS5 vocab swept across the demo**: `data-bs-toggle="modal"` + `data-bs-target="#X"` → `data-stisla-modal-trigger="X"` (collapsed into a single attr); `data-bs-dismiss="modal"` → `data-stisla-modal-dismiss`; `data-bs-backdrop="static"` → `data-stisla-modal-backdrop="static"` (read by `readOpts` via per-attr kebab→camel); `data-bs-keyboard="false"` → `data-stisla-modal-keyboard="false"`; `.modal-dialog` etc. → BEM `__` versions; `<h5 class="modal-title">` → `<h3 class="modal__title">` (semantic level bump, v3 has no opinion on the level so chose h3 to fit page hierarchy under h1); `.btn-primary` → `.btn .btn--primary`; `.btn-ghost-secondary` → `.btn .btn--ghost.btn--neutral`; `.btn-outline-secondary` → `.btn .btn--outline.btn--neutral`; `.btn-outline-danger` → `.btn .btn--outline.btn--danger`; `.btn-danger` → `.btn .btn--danger`; `.btn-success` → `.btn .btn--success`; `.icon-box-danger` → `.icon-box.icon-box--danger.icon-box--round`; `.icon-box-success` → matching; `--bs-icon-box-size` → `--icon-box-size` (the modern one-tier var name); `.text-body-secondary` → `.text-muted-foreground` (per `[[feedback_muted_text_utility]]`); `.text-center`, `.pt-4`, `.mb-0/1/2/3`, `.w-100`, `.justify-content-center`, `.justify-content-end`, `.d-flex flex-wrap gap-2`, `.d-inline-block`, `.bg-transparent`, `.border-0`, `.shadow-none`, `.img-fluid`, `.rounded-top`, `.rounded` — all converted to inline `style="..."` per `[[feedback_muted_text_utility]]` + the established list-group/breadcrumb/navbar/pagination/progress pattern (v3 doesn't ship flex utilities yet, so inline style is the v3-idiomatic equivalent); `.link-primary` → `.link`. **Lead paragraph rewritten** per `[[feedback_docs_voice]]`: short, no em-dashes, no pixel callouts ("An overlay dialog that holds focus until the user resolves a task." — 13 words vs. old 23). **Confirmation modal** uses `role="alertdialog"` + `aria-describedby` (BS5 demo used `role="dialog"`) — a11y correctness for destructive-action dialogs that should preserve attention until user response. **Lightbox** technique: inline `style="--modal-bg: transparent; --modal-border-color: transparent; --modal-shadow: none"` on the `.modal` root strips the surface chrome without a `.modal--lightbox` modifier — shows the per-instance var override pattern in action. **Bundle**: `_modal.scss` uncommented at line 68 of `bundles/stisla-full.scss`. JS registered in `src/js/index.js`. **Verification**: build green (42 pages render), bundle CSS 102.0 → 105.99 KB raw (+3.99) / 16.45 → 17.21 KB gz (+0.76); JS 3.69 → 28.93 KB raw (+25.24) / 1.39 → 9.40 KB gz (+8.01) — the JS jump is focus-trap (~5 KB gz) + tabbable (~2 KB gz) + the Modal class itself (~1.4 KB gz). Per V3.md §3.1 the total JS budget is ~25-30 KB gz; we're at 9.40 KB gz with base + modal + focus-trap — leaves ~15-20 KB gz for the remaining 6 components (4.2 offcanvas reuses focus-trap free, 4.3-4.5 add `@floating-ui/dom` ~6 KB gz, 4.6 toast is JS-only no deps, 4.7 carousel adds Embla ~6 KB gz). Bundle should fit. 0 `--bs-*` leaks in compiled CSS. 0 BS5 vocab in rendered `/modal` HTML (`data-bs-*`, `modal-dialog`, `modal-content`, `modal-header`, `modal-body`, `modal-footer`, `modal-title` class form, `modal-close`, `modal-backdrop` class form, `modal-sm`/`-lg`/`-xl`/`-fullscreen`, `modal-dialog-centered`/`-scrollable`/`-top`/`-bottom`, `btn-primary`/`-secondary`/`-danger`/`-success`/`-ghost-secondary`/`-outline-secondary`/`-outline-danger`, `text-body-secondary`, `icon-box-danger`/`-success`, `link-primary`, `d-flex`/`d-inline-block`, `justify-content-*`, `mb-*`/`pt-*`/`w-100`, `img-fluid`, `rounded-top`/`rounded`, `bg-transparent`/`border-0`/`shadow-none`, `text-center` all return zero hits via the strict class-attribute grep — substring matches inside `--modal-backdrop-*` and `--modal-title-*` var names in the Customization section are false positives, filtered out). All 18 `--modal-*` vars emit in compiled CSS (`grep -oE -- "--modal-[a-z-]+"`). All 16 BEM selectors emit (`.modal`, `.modal--sm`, `.modal--lg`, `.modal--xl`, `.modal--fullscreen`, `.modal__backdrop`, `.modal__dialog`, `.modal__dialog--top`, `.modal__dialog--bottom`, `.modal__dialog--scrollable`, `.modal__content`, `.modal__header`, `.modal__title`, `.modal__body`, `.modal__footer`, `.modal__close`). All 5 Stisla data-attrs render in HTML (`data-stisla-modal`, `data-stisla-modal-backdrop`, `data-stisla-modal-dismiss`, `data-stisla-modal-keyboard`, `data-stisla-modal-trigger`). All 11 `modal__*` BEM markers render. CSS hooks `modal-shake` keyframes + `is-shaking` selector + `is-modal-open` selector all emit. focus-trap deps wired in JS bundle (6 focus-trap option strings all present: `escapeDeactivates`, `returnFocusOnDeactivate`, `allowOutsideClick`, `clickOutsideDeactivates`, `fallbackFocus`, `initialFocus`). The four Step 4.0 prelude interim handlers preserved (`__stislaInterimBound`, `data-app-shell-toggle`, `data-stisla-accordion-trigger`, etc. all present alongside the new `__stislaModalBound` sentinel). **Visual + interaction verification pending** — maintainer to eyeball `/modal` in light + dark, exercising: (a) Basic — trigger opens, focus moves to first tabbable (email input), ESC closes, focus returns to trigger; (b) Sizes — sm/default/lg/xl/fullscreen all paint correctly, fullscreen edge-bleeds, dialog width retunes via modifier scope; (c) Positioning — top/center/bottom land at correct cross-axis position via auto margins; (d) Scrollable — long body scrolls inside, header + footer pin; (e) Static backdrop — backdrop click shakes dialog without closing, ESC inert (because `keyboard: false`), `.modal__close` button still closes; (f) Media hero — image flush above body, frosted close chip readable over photo; (g) Lightbox — surface chrome stripped via inline `--modal-*` overrides, image fills, close chip floats; (h) Confirmation — `role="alertdialog"`, `.icon-box--danger.icon-box--round` paints, footer buttons centered; (i) Success — `.icon-box--success.icon-box--round`, full-width "View order" button; (j) Customization — 6 grouped tables read cleanly with Variable/Default/Use columns; (k) Focus — tab cycles inside modal, doesn't escape to background; (l) Scroll lock — page behind doesn't reflow when modal opens; (m) Dark mode — surface, border, backdrop dim, close chip all adapt; (n) Reduced motion — transitions zeroed, modals snap open/close. **Step 4.1 closes — first JS-coordinated component shipped, contract proven.** Next: **4.2 offcanvas** — same primitives (focus-trap, inert siblings, scroll lock, data-state, escape + dismiss attrs) repurposed for a side-drawer instead of a centered card. Then 4.3-4.5 Floating UI trio (dropdown, tooltip, popover — `@floating-ui/dom` lands in 4.3), 4.6 toast, 4.7 carousel. Each per the unified-PR plan locked in 3.6c.

- 2026-06-10 — **Step 4.0 prelude — `Stisla.Component` base class + declarative scanner + JS infrastructure** — first JS-side delta of the migration; no components ship in this PR, just the contract every subsequent JS-coordinated component (modal, offcanvas, dropdown, tooltip, popover, toast, carousel) gets built on top of. Two new files under `src/js/core/`: **`component.js`** (~80 lines) — exports `Component` base class + `getInstance(el)`; module-private `WeakMap` stashes one instance per element; static `eventNamespace`/`defaults`/`getOrCreate(el, opts)`; instance methods `on(target, type, handler)` (records bindings for leak-free destroy), `emit(name, detail, { cancelable, bubbles })` (single dispatch path, defaults cancelable+bubbling so "before" events can `preventDefault()`, "after" events pass `{ cancelable: false }`), `destroy()` (detaches recorded listeners, `INSTANCES.delete(el)`, nulls `this.el` so deferred RAF/transitionend callbacks bail safely); constructor auto-destroys any prior instance on re-init with a dev-only `console.warn` (gated on `import.meta.env.DEV`, dead-code-eliminated in prod) so the htmx/Turbo re-swap case works cleanly without masking accidental double-instantiation. **`init.js`** (~65 lines) — exports `register(name, Class)` + `init(root)` + `readOpts(el, name, Class)`; module-private `Map` registry; `init()` walks `[data-stisla-<name>]` for each registered name, skips elements with existing instances (idempotent so it survives manual re-scans after DOM swaps), builds opts via the shared reader; opts precedence locked at **`Class.defaults` → `data-stisla-opts` JSON (escape hatch, `try/catch` with `console.warn` on parse error) → per-attribute `data-stisla-<name>-<key>` (canonical, kebab→camel, `JSON.parse` with raw-string fallback so `"static"` stays string + `"true"` becomes `true` + `"42"` becomes `42`) → imperative `opts` arg from `new Class(el, opts)`** — locking both conventions in the prelude prevents 4.1 modal from setting an ad-hoc precedent the next seven components copy. SSR-safe auto-init at the bottom (`document.readyState === 'loading'` → `DOMContentLoaded` listener; otherwise immediate) so plain-HTML users keep the "just include the script" UX. **`src/js/components/`** directory created (empty, `.gitkeep` to track) — 4.1 modal lands the first occupant. **`src/js/index.js` refactor** (117 → ~140 lines): imports + re-exports as `Stisla = { version, Component, register, init, get }`, sets `window.Stisla`, then wraps the **four interim delegated handlers** (app-shell collapse/visibility + backdrop dismiss, navbar toggle, sidebar submenu toggle, accordion trigger) in a `window.__stislaInterimBound` sentinel + adds `import.meta.hot?.accept()` so Vite HMR doesn't double-bind on hot reload (real bug surfaced by the Plan agent — would've manifested as "every click fires twice after I save a file" in dev). The four handler bodies stay byte-identical — each promotes to a proper `Stisla.<AppShell|Navbar|Sidebar|Accordion>` class in its own future session, NOT in this prelude (per the Status log breadcrumbs of each component). **Design decisions locked** (5 deltas from the Plan agent design review on top of the initial draft): (1) `static eventNamespace` field on each class instead of register-time mutation — class stays self-describing, direct `new Stisla.Modal(el)` works without the registry; (2) single `emit()` path with optional `{ cancelable, bubbles }` overrides instead of subclasses constructing their own CustomEvents — keeps "did this event fire in the right namespace?" easy to audit + leaves room for future devtools/logging hooks; (3) re-init = destroy old + dev-warn + replace, NOT throw (htmx case) and NOT silently return existing (which BS5 chose and which caused real confusion when opts differed between calls); (4) `this.el = null` in `destroy()` + early-return on `!this.el` guard in public methods — prevents use-after-destroy from deferred callbacks landing mid-teardown (Plan agent flagged this will bite modal/offcanvas if not gated upfront); (5) scanner only supports `[data-stisla-<name>]` — the legacy non-namespaced attrs (`data-app-shell-toggle`, `data-navbar-toggle`, `data-sidebar-submenu-toggle`) stay in the interim handlers and die when their component is promoted; don't teach the scanner two conventions. **Phase 1 exploration** surfaced one already-committed fact worth recording: `Stisla.bs.Toast` + `Stisla.bs.ScrollSpy` are BS5-leftover references in `toasts.njk` + `scrollspy.njk` + `_demo.njk` + `site.js` — explicitly out of scope for the prelude (they get swept when toast lands in Step 4, scrollspy is probably deleted per §5 Step 5). No package.json deps added (Floating UI + focus-trap + Embla land with the components that need them — 4.1 pulls focus-trap, 4.3 pulls Floating UI, 4.7 pulls Embla). **Verification**: build green (42 pages render — unchanged from the 3.6c polish baseline), `stisla.js` 2.7 → 3.69 KB raw (+1.0) / ~1.0 → 1.39 KB gz (+0.4) — net new JS is the base class + scanner + opts reader; CSS bundle untouched at 102.0 KB raw / 16.45 KB gz. Compiled `stisla.js` smoke-checked end-to-end: `const S = { version: "3.0.0-alpha.0", Component: d, register: m, init: l, get: u }` + `window.Stisla = S` + auto-init via `DOMContentLoaded` (or immediate if `readyState !== "loading"`) + `__stislaInterimBound` sentinel + all four interim handlers present byte-for-byte under the guard + `stisla:${eventNamespace}:${name}` template in `emit()` + WeakMap registry intact. The dev-only re-init warn correctly dead-code-eliminates in prod (no `import.meta.env.DEV` literal anywhere in the bundle). Vite log shows 6 modules transformed (was 4 — `component.js` + `init.js` added). No `--bs-*` leaks (CSS-only carryover check; prelude didn't touch CSS). Public API smoke test paste-able in dev tools console once the dev server picks up the change — `window.Stisla.Component`, `Stisla.register('foo', class extends Stisla.Component { static eventNamespace = 'foo' })`, `Stisla.get(el)` all wire correctly. **4.0 closes the prelude.** Next: **4.1 modal** — the canonical reference component for the contract. Modal pulls `focus-trap` + `inert` on siblings (background scroll lock + AT-hide), keys off `data-state="open|closed"`, fires `stisla:modal:open` / `:opening` (cancelable before-events) + `:opened` / `:closed` (non-cancelable after-events once the open/close transition end fires). Modal CSS exists today (Step 3.5 carry-over — well, actually the file is empty / un-touched; need to verify the disposition table before 4.1 starts). Then 4.2 offcanvas (reuses modal primitives), 4.3-4.5 Floating UI trio (dropdown, tooltip, popover — `@floating-ui/dom` lands in 4.3), 4.6 toast, 4.7 carousel (Embla wrapper). Each lands CSS + JS + demo together per the unified-PR plan locked in 3.6c.

- 2026-06-10 — **Step 3.6c polish — selector fix, radius-lg hierarchy, card gap refactor, accordion margin drop** — same-session follow-up after maintainer eyeballed the initial 3.6c land. Four corrections, none of them visible without running the page. (1) **Selector bug — `>` to `> * >`.** All three open-state CSS hooks in `_accordion.scss` were using the direct-child combinator (`.accordion__item[data-state="open"] > .accordion__header`), but the demo (and the canonical ARIA accordion markup) wraps the `<button class="accordion__header">` inside an `<h2>`/`<h3>` heading for screen-reader navigation. Direct-child never matched — closed-hover paint, open-state divider line, and the chevron rotation **all silently failed** from the initial 3.6c land. Fixed by switching the affected three rules to `.accordion__item[data-state="..."] > * > .accordion__header` — the intermediate level matches any wrapper element (`<h2>` ... `<h6>`, also bare wrappers if a consumer skips the heading semantic) and crucially **doubles as a scope fence** for nested accordions inside an open body: an inner header sits at `.outer-item > .outer-body > .inner-accordion > .inner-item > * > .inner-header`, which the outer's `> * >` pattern can't reach. The icon rotation rule stays direct-child on the last hop (`.accordion__header > .accordion__icon`) because the SVG is a direct child of the button itself. (2) **Radius hierarchy: `--st-radius-lg` for large-surface frames.** Audited every component's radius source. Bumped three frame-owning components from `var(--st-radius)` (12 px) to `var(--st-radius-lg)` (16 px under default density): `.card` (was the canonical 12 px card, now reads softer), `.accordion` (frame), `.list-group` (frame around content rows). The three-tier rhythm reads cleanly: **`--st-radius-sm` (8 px) for chips** (kbd, breadcrumb hover paint, icon-box, sidebar/navbar internal buttons, `.btn--sm`, `.form-control--sm`, placeholders); **`--st-radius` (12 px) for medium elements** (btn, alert, form-control, input-group, toggle, toggle-group, button-group, pagination, popover, dropdown-menu, toast); **`--st-radius-lg` (16 px) for large surfaces** (card, accordion, list-group; future modal + offcanvas land here too when Step 4 ships). Accordion bonus: with 16 px outer + 4 px wrapper inset, the inner item lands at `calc(16px - 4px) = 12 px` — exactly the standalone `--st-radius` default — so the inner chip reads like a normal rounded element framed by a softer outer rim. Mirrors the shadcn convention (alert ships at `rounded-lg` too there) but maintainer left alert on `--st-radius` for this pass — the inline-with-forms argument held; alert often sits in tight form layouts where a 16 px banner above 12 px inputs would read inconsistent. (3) **Card refactored to gap-based composition** (matching the alert pattern already in `_alert.scss`). `.card__title` had `margin: 0 0 0.5rem`, `.card__subtitle` had the `margin: -0.25rem 0 0.75rem` negative-top hack to claw back the title's bottom margin, and `.card__text` had `margin: 0 0 1rem` + a `:last-child { margin-bottom: 0 }` workaround — three tells of the legacy typography-margin model, all of which now disappear. Replaced with: `.card__body { display: flex; flex-direction: column; gap: 0.5rem }` (parent owns sibling rhythm), all per-element margins stripped, `.card__text` block removed entirely (its color-inheritance behavior inside `.card__overlay` still applies via the existing descendant rule). The trigger was the maintainer screenshot showing `.card__title` reading off-center inside `.card__header` — `.card__header` is `display: flex; align-items: center; gap: 0.5rem`, so the title's `margin-bottom: 0.5rem` got baked into its flex margin box and the visible text got pushed UP off-center. Removing the title's margin lets `align-items: center` actually centre the text; the body's flex+gap takes over the body-context rhythm that title-margin used to provide. (4) **Accordion `--accordion-margin-bottom` deleted entirely** (maintainer call). The base `.accordion { margin-bottom: 1rem }` was the same legacy pattern — accordion shouldn't claim inter-block spacing; the parent (`.card__body`, page section, whatever) owns it. Matches alert and matches the gap-based card now. The `.accordion--flush` modifier also drops the `--accordion-margin-bottom: 0` override I'd just added, because the var no longer exists to override. Customization table loses the Geometry row for `--accordion-margin-bottom`. **Verification**: build green (42 pages render), bundle 102.2 → 102.0 KB raw (−0.2) / 16.5 → 16.4 KB gz (−0.1) — small shrink from dropped vars + dropped card margins. 0 `--bs-*` leaks. Accordion var count 25 → **24** (Geometry shrinks from 4 to 3: radius, padding, gap). Selector fix verified in compiled CSS — all three open-state rules now show `>*>` in the minified output (`grep -E "data-state=(open|closed)" site-dist/assets/stisla-full.css`). Card simplification verified — `.card__title{font-size:1rem;font-weight:600;line-height:1.3}` (no margin), `.card__subtitle{font-size:.875rem;color:var(--st-muted-foreground);font-weight:500}` (no margin, no negative top), `.card__body{display:flex;flex-direction:column;gap:.5rem;padding:var(--card-padding)}` (gap-based). Radius routing verified — `.card{--card-radius: var(--st-card-radius, var(--st-radius-lg))...}`, `.list-group{--list-group-radius: var(--st-list-group-radius, var(--st-radius-lg))...}`, `.accordion{--accordion-radius: var(--st-radius-lg)...}`. Visual verification pending: eyeball `/accordion` (closed-hover paints accent now; opening an item shows surface-2 chip + rim + bottom divider; chevron rotates 180°; flush-in-card sits clean with no margin gap below); `/card` (title in header reads centred, no off-axis pull); `/list-group` (frame reads softer at 16 px). **3.6c polish closes the accordion family clean.** No further follow-ups expected before Step 4 prelude.

- 2026-06-10 — Step 3.6c — `_accordion.scss` rewrite (~190 lines, full from-scratch). BEM (`__item`, `__header`, `__body`, `__icon`); `data-state="open|closed"` + `aria-expanded` state hooks. Dropped native `<details>` route — `::details-content` animation not in Firefox; ARIA accordion pattern matches Radix/shadcn so the CSS file serves vanilla + future React/Vue impls. Concentric inner radius via `--accordion-padding`. Closed-hover = `--st-accent`; open item = `--st-surface-2` chip + `--st-border` rim; header text stays `--st-foreground` in both states (open chip is the open-signal, not a color flip). Body collapse instant via `display: none`; measured-height transition deferred to Step 4. Interim delegated click handler in `index.js` flips `data-state` + `aria-expanded`; `data-stisla-accordion-single` enables radio-group semantics. 25 component vars in 7 groups. See 3.6c polish (next entry up) for the `> * >` selector fix.

- 2026-06-10 — Step 3.6b — toggle + toggle-group split; button-group simplified. Vision shift after 3.6a landed (3 button-group demo sections rendered the same pattern; `.toggle` and `.btn-check` did the same job). Resolution: three components, clean semantic boundaries. `.toggle` rewritten as standalone two-state press button — state from `[aria-pressed]`, `[data-state="active"]`, or `.toggle-input:checked +` (all paint identically); outline-neutral rest, `--st-highlight` active bg + foreground-mix border. New `.toggle-group` pill container with concentric corners (outer 36 / padding 4 / inner 28 px), ghost-rest members so parent rim owns the frame. `.btn-group` purged of `.btn-check` rules; `_btn.scss` reverted (`.btn-check:checked +` synonym removed from all 8 cascade rules). New `/toggle-group` page; `/button-group` lost 3 sections. V3.md Phase 2 list grew toggle + toggle-group entries.

- 2026-06-10 — Step 3.6a — `_button-group.scss` rewrite (~180 lines, first JS-coordinated-family file; CSS today, roving-tabindex + click handlers defer to Step 4). BEM modifiers (`--vertical`, `--sm`, `--lg`); sizes retune child `--btn-*` vars in scope (no per-size vars); seam dedupe via -1px logical margin; z-index lift on interactive members (hover/active/focus) so the seam dedupe doesn't bury the active member's border. `.btn-check` retained at this stage (removed in 3.6b); `.btn-toolbar` separate block. `_btn.scss` touch — added `.btn-check:checked + &` synonym to all 8 active-state cascade rules (reverted in 3.6b). 2 component vars (`--btn-group-radius` + `--btn-toolbar-gap`).

- 2026-06-10 — Step 3.5e — `_progress.scss` write (~140 lines, pure write from the 0-byte placeholder; V3.md §5 risk #1 retired). Two-element BEM (`.progress` + `.progress__bar`); intent modifiers `__bar--{primary,success,warning,danger,info}`; sizes 4/8/16 px via `--sm`/`--lg` retuning `--progress-height`. Animated = soft sheen across fill every 3s (1s sweep + 2s rest, "rare not constant" per maintainer — replaces BS5's constant diagonal scroll). Striped + stacked variants **dropped wholesale**. `.is-indeterminate` Stisla-original state hook forces bar to 50% width + animates `translateX(-100%) → translateX(200%)` over 1.5s (single shadcn-style sliding bar). Pill radius opt-out added to V3.md §3.4. Track defaults `--st-neutral`, bar `--st-primary`. 9 component vars. **Closes the Display family** (3.5a–3.5e).

- 2026-06-10 — Step 3.5d — `_pagination.scss` rewrite (~180 lines). BEM collapses `.page-item` + `.page-link` → single `.pagination__button` on the interactive element (matches `sidebar__button`/`navbar__button` family); new `.pagination__ellipsis` element for truncation gaps. Critical correction vs BS5 v3 overlay: split hover + active states properly — `[data-state="active"]`/`[aria-current="page"]` → `--st-highlight`, hover → `--st-accent` (overlay had conflated both to accent "because chips are toggleable"; v3 follows the established sidebar/list-group/table routing). Hard height + min-width contract; positive `outline-offset` on focus (no `overflow: hidden` to clip). Sizes 28/36/44 px via modifier scope; alignment `--center`/`--end` modifiers. 16 component vars + 1 internal.

- 2026-06-10 — Step 3.5c — `_breadcrumb.scss` rewrite (~125 lines). BEM `.breadcrumb__item`; `[aria-current="page"]` only (text-only component, no `data-state` chrome flip — no chrome flip means no chrome hook). Interactional trio routes through the color knob, not bg: `--breadcrumb-color` rests at `--st-muted-foreground`, hover + active lift to `--st-foreground`. Chevron uses `fill="currentColor"` (BS5 baked `#6c757d`) so it tracks the trail color across themes + retints. Single-gap flex layout: one `--breadcrumb-gap` serves both item↔item and within-item (divider↔text, icon↔text) spacing. 12 component vars.

- 2026-06-10 — Step 3.5b — `_list-group.scss` rewrite (~210 lines). BEM (`__item`, `--flush`, `--horizontal*`, `--numbered`, `__item--{intent}`); `.list-group-item-action` opt-in **dropped** — interactivity derives from the element (`a.list-group__item`/`button.list-group__item` get hover+focus; static `<li>` sits quiet). `[data-state="active"]` + `[aria-current="page"]` paint `--st-highlight`; hover paints `--st-accent`. Active beats hover via specificity. No layered-slot mechanic (rows only stack 2 layers; state fully overrides variant). Intents use `color-mix(in oklab, ...)` — oklab not oklch because surface is chroma-0 neutral and oklch would pull mixes to hue 0. Inset focus halo (negative offset + z-index lift) because `.list-group` has `overflow: hidden`. Auto-integration with `.card > .list-group` (drops outer chrome — no `--flush` needed in cards). 16 component vars in 4 groups.

- 2026-06-09 — Step 3.5a — `_table.scss` rewrite (~210 lines, first Display-family). Look preserved 1:1 vs BS5 snapshot per maintainer ("same look"). BS5 layered-bg mechanic ported verbatim: per-cell `box-shadow: inset 0 0 0 9999px var(--table-bg-state, var(--table-bg-type, transparent))` chain — `--table-bg-type` for static layers (striped, variant), `--table-bg-state` for runtime states (hover, active). The only way `.table--hover` can light a striped+variant row without erasing layers. BEM (`__row--{intent}`, `__head--alt`, `__body--divider`); `.table-active` → `<tr data-state="active">`; `.table-secondary` → `__row--neutral` (tracks `--st-neutral` for retint). Color-mix replaces BS5's baked rgba on variants. 17 component vars in 4 groups + 4 mechanism-internal slot vars (hidden from Customization table).

- 2026-06-09 — Step 3.4d — `_sidebar.scss` rewrite (~470 lines, largest BS5-coupled file). Full BEM rename (`__header`/`__content`/`__footer`/`__brand`/`__menu`/`__group`/`__list`/`__item`/`__button`/`__submenu`/etc.); `.is-collapsed` rail mode (Stisla-original `.is-*` state); submenu drops BS5 `.collapse` plugin → `[data-state="open|closed"]` + interim delegated click handler in `index.js` (`data-sidebar-submenu-toggle` flips `data-state` + `aria-expanded`). No animation in 3.4d — closed submenus snap shut; animated height transition deferred to Step 4. Hard-height contract on `.sidebar__button` (height architecture). Rail-width recipe: `button-height + 2 × panel-padding-x` (defaults to 4.25 rem, matches `.app-shell`). Group-action 2-col grid switch via `:has()`. Site chrome sweep — `_site-sidebar.njk` partial + `site.js` querySelector swept in lockstep (docs site uses framework `.sidebar`). 29 component vars in 7 groups. **Also**: retroactive `.navbar__link` → `.navbar__button` rename (commit `7911013`) for cross-component vocab consistency (the element renders as `<a>` for nav OR `<button>` for action; class describes shape, not semantic).
- 2026-06-09 — Step 3.4c — `_navbar.scss` (~190 lines, near-total rewrite). BEM family from scratch (`__brand`/`__toggle`/`__menu`/`__nav`/`__link` → later renamed `__button` in 3.4d session). BS5 chrome + collapse plugin gone. State hooks: `__menu[data-state="open|closed"]` for mobile fold, `__link[data-state="active"]` for current page, `[aria-disabled="true"]` for unreachable links. Single collapse breakpoint (lg) — no per-breakpoint modifier variants. Auto-push trailing slot via `margin-inline-end: auto` on `.navbar__nav` (BS5's `.me-auto` convention without making consumers carry the class). Hard-height chip + brand (height architecture). `.navbar:has(> .container)` zeroes inline padding so container takes over. Interim `data-navbar-toggle` click handler in `index.js`. 16 component vars.
- 2026-06-09 — Step 3.4b — `_page.scss` (~105 lines) BEM port. `.page-header*`/`.page-section*` → `.page__header`/`.page__title`/`.page__action`/`.page__section`/etc. 8 `--page-*` vars, all density-multiplied gaps. `.page__title` promoted to `flex-direction: column` + `--page-title-gap` (Preflight zeroed default `<h1>` margin, so subtitle space needs to come from gap).
- 2026-06-09 — Step 3.4a — `_app-shell.scss` BEM port (`__main`, `__body`). 11 `--app-shell-*` vars; `.is-sidebar-collapsed`/`.is-sidebar-visible` state hooks (Stisla-original `.is-*`). `--app-shell-sidebar-z: 1030` knob lets backdrop derive layer + consumers re-stack. Backdrop is `oklch(0 0 0 / 0.5)` literal (fixed-dark in both themes — a theme-following backdrop would undercut its scrim purpose). `data-app-shell-toggle="collapse|visibility"` handler already shipped pre-Step-2. Demo uses placeholder sidebar/navbar slots until 3.4c/3.4d ship.
- 2026-06-09 — Step 3.3c — `.form-range` renamed `.slider` (matches shadcn/Radix/Reka). Final design (after first-pass thumb-on-track redesigned mid-session per maintainer ref): track at `.form-control` height with a filled-progress segment via two browser paths — Firefox uses native `::-moz-range-progress`; webkit uses the classic no-JS `box-shadow: -100vmax 0 0 100vmax var(--slider-fill)` on the thumb + `overflow: hidden` on the track to clip. Pure CSS, no JS, fully reactive. White thumb (vertical pill) with drop shadow for edge definition. Focus halo moved to shell (track's `overflow: hidden` would clip a thumb-based halo). 9 component vars. **Cross-browser footgun**: webkit + firefox pseudos cannot share selector lists — every rule duplicates per vendor via SCSS mixins (`@mixin slider-track`/`@mixin slider-thumb`).
- 2026-06-09 — Base-color knob removed; plain OKLCH literals adopted. First attempt added `--st-base-hue` + `--st-base-chroma` with per-tier multipliers via `calc(var(--st-base-chroma) * Nx)` inside `oklch()` — didn't render in browser. Even at chroma 90 (wildly out of range, should clamp to max saturation), zero visible change. Deeply nested var()-in-calc()-in-oklch() chain too fragile to debug at the spec level. Pivoted to HeroUI model: every token is a plain OKLCH literal; retint by overriding the 9 neutrals in a parent scope (inline style, class, or `[data-theme]` block). Default neutrals = pure gray (`oklch(L 0 0)`). Token surface stays at 30. **Pedagogical note**: CSS does not have reliable cross-browser support for `oklch(L calc(var(--x) * N) var(--y))` chains. Author OKLCH values as literals; do the math at build/authoring time. CSS custom properties shine when you override the FINAL value, not when you try to parameterize the components of a color function. A build-time theme generator (HeroUI-style UI that emits an OKLCH block from a hue + chroma input) is the right home for that use case; out of scope for 3.0.
- 2026-06-09 — Step 3.3b — `_form-check.scss` + `_input-group.scss` rewrites; extracted `.switch` + `.toggle` as their own components mid-session. `.form-check__input`/`__label` BEM; native pseudos (`:checked`/`:indeterminate`/`:user-invalid`) drive state; `[aria-invalid="true"]` + `:user-invalid` replaces BS5 `.is-invalid`/`.was-validated`. Switch contrast fix: off-state track is `color-mix(in oklch, var(--st-foreground) 25%, transparent)` — `--st-neutral` reads too light against white thumb. `_input-group.scss` retains wrapper-owns-chrome; `:has(> [aria-invalid])` adds validation forwarding (was outstanding TODO). New `_switch.scss` (`.switch` not `.form-switch`, per Radix/shadcn) + `_toggle.scss` (segmented control with `--st-accent` fill, neutral container) as separate files + demo pages. Same-session follow-ups: indicator sizing (dropped 80% cap), hover-on-checked bug (`:not()` exclusion), `.btn-check` → `.toggle` rename + relocate, switch + toggle extracted from form-check.
- 2026-06-08 — Prose follow-ups: consolidation + demo expansion + bordered tables. Framework now owns `.prose`; site adds `.main-container.prose` docs-context cascade overrides (one definition, two contexts). Selectors moved direct-child → `:where()` descendant with `.not-prose` opt-out gate (Tailwind Typography pattern; specificity stays (0,1,0) so consumer overrides win without `!important`). `_demo.njk` macro gains `prose=true` arg for demos whose preview IS prose content. Bordered+rounded prose tables (`border-collapse: separate` + `overflow: hidden`). **Architectural limit**: nested `.prose` inside `.not-prose` cannot reactivate — Tailwind Typography has the same limitation. `prose=true` macro arg is the escape hatch.
- 2026-06-08 — Step 3.x — Preflight + `.prose` foundation. `_reboot.scss` rewritten Preflight-style — universal `* { margin: 0; padding: 0; border: 0 solid }` kills default spacing; heading/anchor/list/form-control resets; `b/strong/em/small` lose visual treatment (semantics preserved). `_typography.scss` cut `.list-unstyled`/`.list-inline*`/`.blockquote*` (moved to prose); kept `.h1`–`.h6`/`.lead`/`.display-*`. New `components/_prose.scss` (~210 lines) is the long-form escape hatch with 5 customization vars + cascade-only nested overrides (no `--st-prose-kbd-*` invented vars; `.prose { --st-kbd-padding-y: ... }` retunes via cascade). `.prose` does NOT set `max-width` (typography is treatment, parent is layout box). New `.link` utility (Reboot strips `<a>` globally; `.link` opts back in). `/typography` page rewritten as two contexts (utilities for app UI, `.prose` for long-form). V3.md §3.1 + §3.10 updated. **Rule**: visual treatment = utility/component class, never element identity.
- 2026-06-08 — Card footer + `<small>` strut leak diagnosis. `.card__footer` gained `display: flex` to break the inline-strut inheritance from block line-height (CSS computes block container line-box as `max(strut, tallest-inline-line-height)`; flex children get their own formatting context). Bigger picture: HTML-element defaults leak into v3 components and the foundation didn't intercept — drove the decision to ship Preflight + `.prose` foundation next (Step 3.x).
- 2026-06-08 — Height architecture amendment. `.btn` + `.form-control` (single-line) switch from `min-height` floor + padding tuning → hard `height: calc(var(--*-height) * var(--st-density))` + `padding-block: 0`. Native vertical centering handles the label/value. Multi-line cases (`textarea`, `select[multiple]`, `select[size]:not([size="1"])`, `.btn--wrap`) opt out via `height: auto` + `min-height`. `--st-density` becomes a real structural lever (0.875 yields 31.5 px, not a 36 px shell with shrunk padding). `--btn-bevel` knob extracted (`--btn-bevel: none` ships a flat-button look). V3.md §3.2 density paragraph updated.
- 2026-06-08 — Step 3.3a — `_form-control.scss`. `_form-select.scss` **deleted**; `.form-select` removed from public API. `.form-control` covers `<input>`/`<select>`/`<textarea>`/`<input type="file">`/`<input type="color">` via element/attribute selectors — `select.form-control` paints chevron; `select[multiple]`/`select[size]:not([size="1"])` opts out + restores native list rendering; `textarea.form-control` drops min-height clamp + gains `resize: vertical`. Sizes 28/36/44 match `.btn`. Focus = 3px transparent halo via `box-shadow` (continuously-bordered shape; opaque outline+offset would read aggressive). **Validation drops BS5 entirely** → `[aria-invalid="true"]` + `:user-invalid` (no `.is-invalid`/`.was-validated`). `/validation` page deleted (now demoed on `/form-control`). Color picker folded in (`.form-control--color`). Token surface stays at 30 (chevron SVG is the documented `data:`-URL exception — two literals, one per theme).
- 2026-06-08 — Card follow-up to 3.1. Default `.card` now carries a one-pixel `--st-border` so the card reads as a card even with shadow turned off (matches BS5 original; v3's new elevation values left bare elevation-only cards looking unframed in dark mode + against `--st-surface-2` backdrops). `.card--bordered` → `.card--flat` rename: modifier now only zeroes the shadow.
- 2026-06-08 — Step 3 #2 done (surface family). Six components: `_alert.scss`, `_badge.scss`, `_icon-box.scss`, `_kbd.scss`, `_spinner.scss` (new — was 0 B), `_placeholders.scss`. All BEM with intent modifiers composing flat; all states via `color-mix(in oklch)`; all padding wraps `calc(X * var(--st-density))`. **Alert** requires a tone modifier (bare `.alert` transparent, same contract as `.btn`); `:has(.alert__description)` switches layout single-line→grid; dismiss is plain `.btn--ghost.btn--neutral.btn--icon-only.btn--sm` (`.btn-close` gone, `/close-button` page deleted). **Badge** pill-by-default (opts out of `--st-radius`); `--badge-tone` is the cascade primitive — `.badge--soft.badge--primary` and `.badge--primary.badge--soft` both render correctly. **Btn** `--light` → `--soft` rename for cross-component naming. **Kbd** styles `<kbd>` element directly (no class). **Spinner** new file. `.visually-hidden` added to `utilities/_text.scss`. Token surface stays at 30.
- 2026-06-08 — Typography landed ahead of surface family. Baseline 14 px via `body { font-size: 0.875rem }` (html untouched so 1 rem stays = 16 px and every rem-based component keeps its pixel meaning). Heading scale tuned for the 14 px context. `_reboot.scss` gained the typography slice (p/ul/ol/dl/dt/dd/blockquote/b/strong/abbr/code). New `foundation/_typography.scss` (`.h1`–`.h6`, `.lead`, `.display-*`, lists, blockquote). New `utilities/_text.scss` bootstraps the utilities layer. BS5 `.text-body-*` swapped for `.text-foreground`/`.text-muted-foreground` (tertiary cut — single muted tier). Token surface stays at 30 (no `--st-font-size-*` ramp).
- 2026-06-08 — Step 3.1 done (btn + card architecture proof). `_btn.scss` (renamed from `_buttons.scss`) ships 4 curated tones (primary/neutral/tertiary/danger) × 4 shapes (filled/outline/ghost/light) composing flat via `--btn-tone`. Success/warning/info dropped as first-class button tones (rare; WCAG-contrast hassle); `/buttons` "Custom color" section documents the `--btn-bg`/`--btn-color` inline-override pattern. Rim border + inset top highlight on `.btn` base reading `--btn-bg` so custom colors inherit the bevel for free. `_card.scss` ships BEM (`__header`/`__body`/`__footer`/`__title`/`__subtitle`/`__text`/`__link`/`__image`/`__overlay`). Token surface 28 → 30 (added `--st-neutral` + `--st-neutral-foreground`). New `/customization` smoke-test page replays presets via root-scoped inline `--st-*` overrides, zero CSS edits. `site.scss` retokened; `.demo-row`/`.demo-stack`/`.demo-grid` helpers added.
- 2026-06-08 — Step 2 done. 5 BS5-coupled token files deleted (`_colors`, `_maps`, `_root`, `_variables`, `_variables-dark`). `tokens/_theme.scss` ships the 28 `--st-*` OKLCH tokens (light + dark, `--st-base: 258` hue-locked to primary). Breakpoints moved `foundation/` → `tokens/` per V3.md §3.9. Bundle rebuilt with `@layer foundation, theme, components, utilities`; every BS5 `@import` gone; component imports commented out (Step 3 PRs uncomment as they land). `index.js` stripped of `import 'bootstrap'` + Popover/Tooltip auto-init; app-shell handlers kept. `bootstrap` + `@popperjs/core` deps removed. `stisla-full.css` 16 kB / 3.2 kB gz (down from 283 kB), site renders 39 pages with unstyled component markup — strict approach as designed. Mono stack is JetBrains Mono first. Next: Step 3 (btn + card — V3.md Phase 1 architecture proof). Blockers: none. OKLCH conversions for neutrals are estimates, validate during Step 3.
- 2026-06-08 — Step 2 approach locked: **strict.** Gut the BS5 token files, accept a broken bundle, let each Step 3 component PR heal the build incrementally. No interim BS5 shim. Trade: docs site renders broken between Step 2 and the end of Step 3.
- 2026-06-08 — Theme selector renamed `data-bs-theme` → `data-theme` + `.dark` class. V3.md §3.8 reworked to spec both conventions. `foundation/_mixins.scss` color-mode mixin emits `[data-theme="dark"], .dark`; overrides BS5's mixin so BS5's `_root.scss` dark block now flips on the Stisla selectors (no `data-bs-theme` shim needed). Compiled CSS has 4 paired dark blocks; zero `data-bs-theme` in rendered HTML. Spec, site CSS/JS, layout, and tools/nunjucks-filters all updated. The 3 residual `data-bs-theme` references in `tokens/_variables*.scss` + `components/_tooltip.scss` are dead comments in files slated for delete/rewrite — left alone.
- 2026-06-08 — Step 1 done. `src/scss/foundation/` lands with normalize, reboot, grid, containers, breakpoints, mixins. `LICENSES/` carries BS5 + modern-normalize MIT text. Bundle swap leaves the BS5 reboot/grid/containers behind. Reboot reads `--st-*` directly — no `--bs-*` fallbacks (V3.md §3: no --bs in v3 code). Bare surfaces (body fg/bg, link, hr, focus ring) until tokens land in Step 2; intended trade. **Scope shift:** `bootstrap` dep removal moved to Step 2 (tokens/* and components/* still reference BS5 vars and mixins — pulling the dep now would un-build the bundle). Next: Step 2 (token rewrite). No blockers.
- 2026-06-07 — Step 0 done. BS5 build snapshotted to `bs5-snapshot/` (44 files, 3.7 MB). Tagged `v3-bs5-snapshot` at commit `eba7fc1`. Next: Step 1 (foundation rewrite). No blockers.
- 2026-06-07 — V3.md spec locked. MIGRATION.md drafted. Step 0 (snapshot) not started. No blockers.

---

## Working in sessions

Every migration step is its own session — long-running work doesn't fit in one conversation. Each session should be self-bootstrapping from this file + V3.md + git history. No conversation context required.

### Maintainer: how to start a session

Open Claude Code in this repo and paste:

> "Let's continue the v3 migration. Read V3.md + MIGRATION.md, check git, and tell me what step we're on and what to do next."

That's it. Don't pre-load assumptions; the agent re-derives state from the docs + git.

### Agent: what to do on cold start

1. **Read V3.md.** The spec. Agent workflow rules at the top still apply — no writes until the maintainer says "go".
2. **Read MIGRATION.md.** Status (top) first, then the §3 step relevant to today's work, then the §4 disposition row if working on a component.
3. **Verify against git.** Don't trust Status blindly. Run `git log --oneline -20`, `git status`, `git tag --list 'v3-*'` to see what actually shipped. If the doc and git disagree, the doc is stale — flag it and propose an update.
4. **Identify next work.** §7 running checklist is the authoritative source for "what step are we on". Within a step, §3 has the sub-bullets; §4 has the per-component table.
5. **Confirm with the maintainer** what to do before touching code. Default proposal: "next unchecked box in §7" — but the maintainer may want to skip ahead, revisit, or branch off.
6. **At session end:** add one new line to the Status section (top of file). Format: `ISO date — what changed — blockers if any`. Commit as part of the session's final commit, or as a standalone `docs(v3): update migration status`.

### When in doubt

- Spec questions (token names, naming rules, what's in/out of scope) → V3.md.
- Migration questions (order, per-component action, comparison workflow) → MIGRATION.md.
- "Is this thing actually built?" → git, not the docs.

---

## 1. What exists today (BS5 v3, the thing we're migrating)

| Surface                | Counts              | Notes                                                                                                               |
| ---------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `src/scss/components/` | 30 files (~100 KB)  | Largest: `_sidebar.scss` 22 K, `_buttons.scss` 11 K, `_app-shell.scss` 6 K. `_progress.scss` is 0 B (placeholder).  |
| `src/scss/tokens/`     | 5 files (~50 KB)    | `_colors.scss`, `_maps.scss`, `_root.scss`, `_variables.scss`, `_variables-dark.scss` — all tightly coupled to BS5. |
| `src/scss/bundles/`    | `stisla-full.scss`  | Canonical BS5 customization workflow with Stisla overlays.                                                          |
| `src/js/`              | `index.js` (2.7 K)  | Re-exports `bootstrap`, auto-inits Popover/Tooltip, app-shell handlers.                                             |
| `src/site/pages/`      | 40 njk demos        | All use BS5 class hooks (`.fade`, `.show`), `data-bs-toggle`, `data-bs-target`.                                     |
| `package.json`         | `bootstrap: ^5.3.3` | Plus none of the new deps yet.                                                                                      |
| Foundation files       | —                   | `foundation/` directory doesn't exist; build pipeline relies on `@import 'bootstrap/scss/reboot'` etc.              |

---

## 2. Target

See V3.md. No re-litigation here. Token surface (§3.2), foundation (§3.1), naming (§3.6), spec-vs-impl split (§3.11) are all locked there.

---

## 3. Migration order

Each step lands and merges before the next starts. No "step 3 partial PR" overlapping step 4.

### Step 0 — Snapshot the BS5 version ✅

- [x] `npm run build` → captures current built site.
- [x] Copy build output to `bs5-snapshot/` at repo root (HTML, CSS, JS — no source).
- [x] Commit `bs5-snapshot/` with message `chore: snapshot v3 BS5 build before rewrite`.
- [x] Tag the commit: `git tag v3-bs5-snapshot`. Push tag with `git push origin v3-bs5-snapshot` when you next push the branch.
- [x] Verify: `npx serve bs5-snapshot` renders. Bookmark a few key pages (sidebar, modal, dropdowns, forms) for visual reference during rewrite.

**Comparison workflow after step 0:**

- Visual: open `bs5-snapshot/<page>.html` in one tab; live `npm run dev` of new build in another.
- Source: `git show v3-bs5-snapshot:src/scss/components/_btn.scss` next to your new `_btn.scss` in a split editor pane.
- Run-the-old-thing: `git worktree add ../stisla-bs5 v3-bs5-snapshot` for a parallel checkout if needed.

### Step 1 — Foundation rewrite (no visible UI change) ✅

- [x] Install deps: `@floating-ui/dom@1.7.6`, `focus-trap@8.2.1`, `tabbable@6.4.0`, `embla-carousel@8.6.0`. Vendor `modern-normalize@3.0.1` (don't depend on the package; copy the file).
- [ ] ~~Remove `bootstrap` from `package.json`.~~ **Moved to Step 2.** Tokens (`tokens/_variables.scss`) and components (`components/_app-shell.scss`, `_navbar.scss`) still reference BS5 vars (`$grid-breakpoints`) and the `bootstrap/scss/*` import chain in `stisla-full.scss` is the source of those vars. Removing `bootstrap` now un-builds the bundle. Step 2 (token rewrite) replaces the var surface and pulls the dep then.
- [x] Create `src/scss/foundation/` with:
  - `_normalize.scss` — vendored modern-normalize 3.0.1
  - `_reboot.scss` — Stisla opinions (~75 lines). Reads `--st-*` directly. No `--bs-*` references (V3.md §3). Affected surfaces (body fg/bg, link, focus ring, hr) sit on browser initial values until tokens land in Step 2.
  - `_grid.scss` + `_containers.scss` — forked from BS5. `--bs-gutter-*` renamed to `--st-grid-gutter-*`. RTL flips, `_root` emits, `.no-gutters`, `$enable-cssgrid` stripped. Mixins inlined (no separate `mixins/_grid.scss`).
  - `_breakpoints.scss` — forked from `bootstrap/scss/mixins/_breakpoints.scss`. `media-breakpoint-up/down/between/only` renamed to `media-up/down/between/only`. **Shim** at the bottom re-exports the old names so existing component files keep building until Step 3.
  - `_mixins.scss` — `color-mode` helper.
- [x] Add `LICENSES/bootstrap-MIT.txt` (full BS5 MIT text) + `LICENSES/modern-normalize-MIT.txt`. Each forked foundation file carries a credit header. README NOTICE block lists both.
- [x] Verify: `npm run build` is green. `stisla-full.css` 283 KB / 38 KB gz. modern-normalize banner present in output, `--st-grid-gutter-x` emits 44×, zero `--bs-gutter-x` in foundation-emitted CSS, `.container-{sm..xxl}` + `.col-md-{1..12}` all emit. 39 static pages render via `npm run build:site`.

### Step 2 — Token rewrite ✅

- [x] Delete `tokens/_colors.scss`, `_maps.scss`, `_root.scss`, `_variables.scss`, `_variables-dark.scss`.
- [x] Create `tokens/_theme.scss` — 28 OKLCH tokens, light + dark blocks. Single source of truth for color literals.
- [x] Move `foundation/_breakpoints.scss` → `tokens/_breakpoints.scss` (V3.md §3.9 puts it under `tokens/`). Bundle import path updated; `media-breakpoint-*` shim kept for the two components (`_navbar`, `_app-shell`) that still use BS5 names — retire during Step 3.
- [x] Update `bundles/stisla-full.scss` to the `@layer foundation, theme, components, utilities` order; every BS5 `@import` deleted; all components commented out (Step 3 PRs uncomment as they land).
- [x] Strip `import * as bs from 'bootstrap'` + Popover/Tooltip auto-init from `src/js/index.js`. Step 4 reintroduces via `@floating-ui/dom`.
- [x] `npm uninstall bootstrap @popperjs/core` — deps gone, lockfile refreshed.
- [x] All component files break here. That's intentional — fixed in step 3.

### Step 3 — Component rewrite (per-component, in dependency order)

See §4 disposition table for action per component.

Build order respects token + foundation dependencies:

1. **Tokens proof:** btn + card (V3.md Phase 1 architecture proof). Done before any other component.
2. **Surface family:** alert, badge, icon-box, kbd, spinner, placeholders (all use intent + surface tokens).
3. **Form family:** form-control (covers `<input>`, `<select>`, `<textarea>` — one class, element/attribute selectors layer on type-specific concerns), form-check, input-group, form-range. Originally planned as one block; split in practice because `_form-select.scss` collapsed into `_form-control.scss` (one concept, one class — V3.md §3.6) and the remaining three each have their own substantive surfaces. Foundation amendments — `height` vs `min-height`, then Preflight + `.prose` — interleaved between 3.3a and 3.3b; see §7 checklist for the canonical landing order. Sub-steps:
   - 3.3a — form-control (✓ done; also covers `<select>` + `<textarea>` and absorbs the dropped `_form-select.scss`)
   - 3.3b — form-check + input-group
   - 3.3c — slider (renamed from form-range; `.form-range` BS5 name dropped — matches shadcn/Radix/Reka convention, same rationale as switch + toggle)
4. **Layout:** app-shell, page, navbar, sidebar (sidebar last — biggest file).
5. **Display:** card, table, list-group, breadcrumb, pagination, progress, toast, accordion.
6. **JS-coordinated:** button-group, carousel (Embla), dropdown, modal, drawer, tooltip, popover. Each blocked by its step 4 JS component.

Per-component PRs land independently after the token rewrite (step 2) merges. Each PR updates the SCSS + the matching `.njk` demo page.

### Step 4 — JS rewrite (interleaved with step 3 JS-coordinated components)

- [x] `src/js/components/` directory created. ✅ (4.0 prelude)
- [x] `Stisla.init()` declarative scanner (`[data-stisla-*]` attrs → instantiate matching class). ✅ (4.0 prelude — `src/js/core/init.js`)
- [ ] Modal: `inert` on siblings + `focus-trap` + `data-state="open"` toggling + custom events.
- [ ] Drawer: same primitives as modal with transform animation.
- [ ] Dropdown: `@floating-ui/dom` + roving tabindex + `focus-trap` + outside-click + Escape.
- [ ] Tooltip: `@floating-ui/dom` + hover/focus delay state.
- [ ] Popover: `@floating-ui/dom` + `focus-trap` + outside-click + Escape.
- [ ] Accordion: native `<details>` or thin height-transition helper. Stay close to native.
- [ ] Tabs / segmented control: `data-state="active"` toggle on `.btn-group` items + arrow-key nav.
- [ ] Toast: queueing + dismiss + transitions.
- [ ] Carousel: thin Embla wrapper exposing Stisla class hooks.
- [ ] Bundle size check: ~25–30 KB gz total (deps + Stisla code).
- [ ] Replace `import * as bs from 'bootstrap'` and all `data-bs-*` scanners in `index.js`.

### Step 5 — Demo pages (`src/site/pages/`)

- [ ] Class name pass: `.btn-primary` → `.btn--primary`, `.modal-dialog` stays, `.modal-content` becomes `.modal__inner` (TBD per-component during step 3 rewrite).
- [ ] State attribute pass: `.fade.show` → `[data-state="open"]`. `.collapse.show` → `[data-state="open"]` or `<details open>`.
- [ ] Toggle attribute pass: `data-bs-toggle="modal"` → `data-stisla-toggle="modal"`. `data-bs-target` → `data-stisla-target`. Per-component conventions defined during step 3.
- [ ] Delete `navs.njk` (component family removed). Repurpose its content into `button-group.njk` as the tabs/segmented examples.
- [ ] Delete `scrollspy.njk` and `collapse.njk` if their content folds into other pages. Confirm before deleting.
- [ ] Add customization smoke-test page: brutalist preset, soft preset, dense preset, violet primary preset, warm-tinted neutrals preset — all via root token overrides only.

### Step 6 — Cleanup + 3.0.0-beta.1

- [ ] Remove `bs5-snapshot/` dir? Or keep until 3.0.0 final? Decision: keep until final; comparison value is still real during beta.
- [ ] Update README, CHANGELOG.
- [ ] Update `.github/workflows/authors.yml` (V3.md §5 known break).
- [ ] Tag `3.0.0-beta.1` for community review.
- [ ] After 3.0.0 final ships: `git rm -r bs5-snapshot/`; `git tag v3-bs5-snapshot-deleted` for posterity; delete this `MIGRATION.md`.

---

## 4. Per-component disposition

Action key:

- **port** — visual + behavior carry over with minimal change; just retoken and BEM-rename
- **rewrite** — start fresh on the v3 model; old file is reference only
- **write** — file is empty or doesn't exist; build from scratch
- **delete** — component family cut

| Component    | Size   | Action     | Notes                                                                                                                          |
| ------------ | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------ |
| accordion    | 4.5 K  | rewrite    | BS5 collapse JS dependency; new height-transition impl or native `<details>`                                                   |
| alert        | 4.1 K  | rewrite    | color-mix on `--st-*` intents; new BEM (`__icon`, `__heading`, `__description`, `__action`)                                    |
| app-shell    | 6.1 K  | port       | Stisla original; low BS5 coupling. Retoken + BEM verify                                                                        |
| badge        | 2.5 K  | rewrite    | color-mix variant model; opt-out of radius (pill)                                                                              |
| breadcrumb   | 1.9 K  | port       | Mostly clean; retoken                                                                                                          |
| button-group | 1.1 K  | rewrite    | Now also serves as tabs/segmented control with `data-state="active"`. Expanded scope                                           |
| buttons      | 11.2 K | rewrite    | Largest BS5 override pile (button-variant mixin); v3 BEM + color-mix from scratch                                              |
| card         | 1.9 K  | rewrite    | Surface tokens; BEM (`__header`, `__body`, `__footer`)                                                                         |
| carousel     | 3.0 K  | rewrite    | Now wraps Embla; styling minimal                                                                                               |
| dropdown     | 2.2 K  | rewrite    | Floating UI + `[data-state="open"]` + `[data-highlighted]`                                                                     |
| form-check   | 3.2 K  | rewrite    | Native `<input>` styling; cross-browser pass                                                                                   |
| form-control | 3.4 K  | rewrite ✅ | Hairiest BS5 forms override pile; v3 base + `:focus-visible` halo. Now covers `<input>` + `<select>` + `<textarea>` via element/attribute selectors. |
| form-select  | 1.8 K  | **delete** ✅ | Merged into `.form-control` — one concept, one class (V3.md §3.6). `<select class="form-control">` is the v3 select.        |
| icon-box     | 2.2 K  | port       | Stisla original; retoken                                                                                                       |
| input-group  | 3.6 K  | rewrite    | Adopts new form-control tokens                                                                                                 |
| kbd          | 670 B  | port       | Tiny; retoken                                                                                                                  |
| list-group   | 1.5 K  | rewrite    | `[data-state="active"]`; accent (hover) + highlight (active)                                                                   |
| modal        | 4.7 K  | rewrite    | New JS impl (focus-trap + inert + `[data-state="open"]`)                                                                       |
| nav          | 2.6 K  | **delete** | Family cut. See V3.md §4 Phase 2                                                                                               |
| navbar       | 4.0 K  | port       | Stisla original; retoken                                                                                                       |
| drawer       | 2.5 K  | rewrite    | Same primitives as modal                                                                                                       |
| page         | 4.0 K  | port       | Stisla original                                                                                                                |
| pagination   | 1.6 K  | rewrite    | `[data-state="active"]` for current page                                                                                       |
| placeholders | 328 B  | port       | Tiny                                                                                                                           |
| popover      | 1.1 K  | rewrite    | Floating UI                                                                                                                    |
| progress     | 0 B    | **write**  | File is empty. Build from scratch                                                                                              |
| sidebar      | 22.1 K | rewrite    | Largest file. Many custom features (collapse, sub-menus, footer). Core logic carries; tokens + state attrs are the new surface |
| table        | 3.7 K  | rewrite    | Surface tokens; hover/highlight on rows                                                                                        |
| toast        | 3.2 K  | rewrite    | New JS impl                                                                                                                    |
| tooltip      | 856 B  | rewrite    | Floating UI                                                                                                                    |

Demo pages (`src/site/pages/`) follow the matching component PR. Pages that exist without a backing component (e.g. typography, images) are port-only.

---

## 5. Risks / blockers

- **`_progress.scss` is 0 B.** No reference to port from. Build from scratch in step 3.
- **`navs.njk` deletion.** Component family cut; demo page content folds into `button-group.njk`. Confirm before deleting any user-visible URL.
- **BS5 JS hooks across .njk.** `data-bs-toggle`, `data-bs-target`, `.fade`, `.show`, `.collapsing` need a sed-replace pass per component during step 5. Easy to miss one.
- **`.github/workflows/authors.yml`** references deleted `dev/update-authors.js` (V3.md §5). Fix during step 6.
- **Dark mode regression risk.** Every component PR in step 3 must verify dark mode (V3.md Phase 2 checklist requires this).
- **Customization smoke test.** V3.md Phase 1 requires brutalist/soft/dense/violet/warm presets via root-only overrides. This is the architecture proof; if any preset doesn't work, the token model has a hole.

---

## 6. Don't migrate — these are additions, not migrations

Phase 7 (v3 stable, post-3.0) components from V3.md §4 are net-new. Don't bundle them with the migration:

- avatar
- otp
- custom-select
- autocomplete
- combobox
- preview-card
- command (spotlight)
- date-picker

Build them on top of the rewritten foundation after 3.0 ships.

---

## 7. Running checklist

- [x] Step 0 — Snapshot BS5 build, tag `v3-bs5-snapshot` ✅
- [x] Step 1 — Foundation rewrite (deps swap, `foundation/` files, `LICENSES/`) ✅
- [x] Step 2 — Token rewrite (`_theme.scss` + `_breakpoints.scss`) + remove `bootstrap` dep ✅
- [ ] Step 3 — Component rewrite (per §4 table, in order from §3 step 3)
  - [x] 3.1 — btn + card architecture proof (V3.md Phase 1) ✅
  - [x] 3.2 — surface family: alert, badge, icon-box, kbd, spinner, placeholders ✅
  - [x] 3.3a — form-control (covers `<input>` + `<select>` + `<textarea>`; absorbs the dropped `_form-select.scss`) ✅
  - [x] Height amendment — controls use `height` not `min-height` ✅ (`.btn`, `.form-control` single-line, `.icon-box`, `.spinner` all density-multiplied; `.btn--wrap` + textarea/multi-select opt out; `--btn-bevel` knob added)
  - [x] Preflight + `.prose` foundation ✅ (`_reboot.scss` Preflight-style; `_typography.scss` cut `.blockquote*` + `.list-*`; new `components/_prose.scss` + 5 `--st-prose-*` vars; `.link` utility added; `/typography` page rewritten; cascade-only nested-component overrides; V3.md §3.1 + §3.10 updated)
  - [x] 3.3b — form-check + input-group ✅
  - [x] 3.3c — slider (renamed from `.form-range`; new `_slider.scss`; `--slider-*` vars; sizes + validation hooks) ✅
  - [x] 3.4a — app-shell (port; BEM rename `__main`/`__body`; 11 `--app-shell-*` vars; `media-down(lg)`; `--app-shell-sidebar-z` replaces BS5 `$zindex-fixed`; demo uses placeholder sidebar/navbar slots until 3.4c/d ship) ✅
  - [x] 3.4b — page (port; BEM rename `__header`/`__title`/`__action`/`__section`/`__section-header`/`__section-title`; 8 `--page-*` vars all density-multiplied; `.page__title` promoted to flex-column with `--page-title-gap` to compensate for Preflight-stripped heading/paragraph margins) ✅
  - [x] 3.4c — navbar (port → near-rewrite in practice; BS5 chrome + collapse plugin gone; BEM `__brand`/`__toggle`/`__menu`/`__nav`/`__button` — `__link` renamed to `__button` retroactively in the 3.4d session for cross-component vocabulary consistency; `[data-state="open\|active"]` + `[aria-disabled]` hooks; single `lg` collapse breakpoint; hard-height chip per height architecture; container-inside-navbar `:has()`; 16 `--navbar-*` vars; `data-navbar-toggle` JS handler added to `src/js/index.js`; "With dropdown" section deferred to Step 4) ✅
  - [x] 3.4d — sidebar (rewrite — biggest BS5-coupled file, 539 → ~470 lines; BS5 `.collapse` plugin dropped, submenu uses `[data-state="open\|closed"]` + new `data-sidebar-submenu-toggle` JS handler with `display: none` for closed state, animation deferred to Step 4; full BEM rename with `.sidebar__brand` + `.sidebar__button` matching navbar; 29 `--sidebar-*` vars across 7 groups; `.is-collapsed` rail mode ported faithfully with soft-hide animations + brand re-center math; site chrome partial + site.js swept in same commit) ✅
  - [x] 3.5b — list-group (rewrite — second Display-family file; full v3 model from scratch — the BS5 file was a 36-line overlay that only patched what BS5 vars couldn't reach + relied on BS5 `_list-group.scss` for the base shape; v3 rewrites the entire shape; BEM rename `.list-group-item` → `.list-group__item`, `.list-group-flush` → `.list-group--flush`, `.list-group-horizontal{,-sm…xxl}` → root modifiers, `.list-group-numbered` → `--numbered`, intent variants → `.list-group__item--{intent}` element-modifiers, `.list-group-item-secondary` → `--neutral` (parity with `.table__row--neutral`); `.list-group-item-action` opt-in **dropped** — `a.list-group__item, button.list-group__item` derive interactivity from the element; `[data-state="active"]` + `[aria-current="page"/"true"]` paint highlight (Radix-aligned, matches sidebar / table); contextual variants use the same `color-mix(in oklab, ...)` recipe as table; inset focus halo with `z-index: 1` lift to clear the `overflow: hidden` clip; `.card > .list-group` direct-child auto-integration drops outer chrome so the settings demo lands flush in a card without `--flush`; 16 component vars across 4 groups (Geometry/Surface/Item states/Focus) under the single-table threshold; demo page swept across 13 sections including Settings + Payment methods + Contacts + Custom content) ✅
  - [x] 3.5a — table (rewrite — first Display-family file; BS5 `_tables.scss` mechanic ported verbatim via per-cell `box-shadow: inset 0 0 0 9999px var(--table-bg-state, var(--table-bg-type, transparent))` paint chain; full BEM rename `.table-{intent}` → `.table__row--{intent}`, `.table-header-alt` → `.table__head--alt`, `.table-group-divider` → `.table__body--divider`, `.table-{striped,striped-columns,hover,bordered,borderless,sm}` → root `.table--{…}` modifiers; `.table-active` → `<tr data-state="active">` Radix-aligned; `.table-secondary` → `.table__row--neutral` (tracks `--st-neutral` for retint); color-mix oklch tints on variants replace BS5's rgba; row hover/active route through `--st-accent` / `--st-highlight`; 17 `--table-*` vars across 4 groups + 4 mechanism-internal layered-slot vars hidden from Customization table; `.table-responsive{,-sm…xxl}` kept flat; demo page swept across 20 sections, Customization grouped tables added per slider.njk pattern) ✅
  - [x] 3.5c — breadcrumb (rewrite — third Display-family file, 54-line BS5 overlay → ~125-line v3 file; BEM `.breadcrumb-item` → `.breadcrumb__item`; `.active` BS5 escape hatch dropped — `[aria-current="page"]` is the entire signal since there's no chrome flip; **no `[data-state="active"]`** because breadcrumb signals current via text color, not via a highlight chip; chevron SVG uses `fill='currentColor'` so it tracks the trail color across themes + retints; focus halo on links via `:focus-visible` with positive `outline-offset` (no `overflow: hidden` to clip); single `--breadcrumb-gap` serves both item↔item and within-item spacing via flex `gap`; 12 component vars in a single ungrouped Customization table per slider.njk pattern; embedded-SVG demo's hardcoded gray swapped to `currentColor`) ✅
  - [x] 3.5e — progress (write — fifth and final Display-family file; pure write from the 0-byte placeholder, no BS5 reference to port; BEM `.progress` + `.progress__bar` with `.progress-bar` collapsing to canonical `block__element`; `.bg-{intent}` BS5 utilities → `.progress__bar--{intent}` element-modifiers (primary/success/warning/danger/info); BS5's `.progress-bar-striped` + `.progress-bar-animated` chained pair → single `.progress__bar--animated` with **a soft sheen that sweeps across the fill every 3 seconds** (1s sweep + 2s rest, "rare not constant" per maintainer call) instead of BS5's constant diagonal-stripe treadmill; striped variant **dropped wholesale**; `.progress-stacked` BS5 nested-progress wrapper **dropped** along with the segmented use case entirely (maintainer: "no real use cases") — track is `display: block` with one bar inside, bar defaults to `width: 0` so an authored-without-inline-width bar sits invisible; `.is-indeterminate` Stisla-original state hook (no Radix equivalent for progress) forces bar to 50% width + animates `translateX(-100%) → translateX(200%)` over 1.5s `ease-in-out infinite` (single shadcn-style sliding bar, not Material's two-segment chase); shimmer overlay suppressed in indeterminate so the moving bar is the only indicator; sizes 4 / 8 / 16 px via `.progress--sm` / default / `.progress--lg` retuning `--progress-height` in modifier scope (no per-size vars); track defaults to `--st-neutral` (rest fill weight for static neutral surfaces, V3.md §3.3) + bar defaults to `--st-primary`; **radius opts out of `--st-radius`** with `--progress-radius: 9999px` default (pill is intentional for a progress strip; added to V3.md §3.4 opt-out list); width transition tweens JS-driven progress changes over 0.6s; reduced-motion zeros all three motions (width transition, shimmer, indeterminate); 9 component vars in single ungrouped Customization table; demo page rewritten across 6 sections (Example / Basic / Intents / Sizes / Animated / Indeterminate) + Customization, dropping BS5 Labels / Height / Striped / Animated-stripes / Stacked sections and adding Indeterminate; verification clean — 0 `--bs-*` leaks, 0 BS5 leftover names in compiled CSS or rendered HTML, all 9 vars + all 12 BEM selectors emit, bundle 88.1 → 90.2 KB raw / ~15.0 KB gz flat; **closes the Display family** — remaining Phase-2 components are JS-coordinated and slot into Step 4) ✅
  - [x] 3.5d — pagination (rewrite — fourth Display-family file, 49-line BS5 overlay → ~180-line v3 file; BEM `.page-item` + `.page-link` dropped entirely and **collapsed to one name** `.pagination__button` on the interactive element directly — the `<li>` carries no BEM hook because it has no visible role, matches sidebar / navbar's `__button` cross-component vocab; new `.pagination__ellipsis` element for truncation gaps (`1 2 … 9 10`) — same chip footprint, non-interactive, tracks `--pagination-button-disabled-color`; `.pagination-sm` / `.pagination-lg` → `.pagination--sm` / `.pagination--lg` (root modifiers retune `--pagination-button-height` + `--pagination-button-padding-x` + `--pagination-font-size` in modifier scope — no per-size vars to maintain, matches `.btn--sm` pattern); `.pagination--center` / `.pagination--end` replace BS5's `.justify-content-center` / `.justify-content-end` utilities since v3 doesn't ship flex utilities; `.active` BS5 escape hatch dropped — `[data-state="active"]` + `[aria-current="page"]` paint `--st-highlight` (current page is persistent selected state, matches sidebar / list-group / table — **corrects the BS5 v3 file's hover/active conflation** which routed both to `--bs-accent-bg` "because chips are toggleable"; v3 splits the trio properly: hover = `--st-accent` transient, current = `--st-highlight` persistent); `.disabled` on `<li>` → `[aria-disabled="true"]` on the chip directly; interactive opt-in derives from element selector (`a.pagination__button, button.pagination__button` get cursor + hover + focus; `<span class="pagination__button">` for aria-disabled edge markers sits quiet — same pattern as list-group 3.5b's interactive-row derivation); **height architecture** — hard `height: var(--pagination-button-height)` + `padding-block: 0` + `min-width: var(--pagination-button-height)` so density genuinely shrinks the chip and single-glyph chips ("1") land as squares at the matching cadence while labels ("Previous") grow from `padding-inline` (same model as `.btn`, V3.md §3.4); sizes ship at 28 / 36 / 44 px under default density to match the button scale so a pagination row baselines with a "Load more" button; focus halo uses **positive `outline-offset: 2px`** because `.pagination` has no `overflow: hidden` (chips are gap-laid-out flex, nothing clips — same as breadcrumb, not list-group's inset trick); icons inside chips pin to `1em × 1em` with `flex-shrink: 0` so they scale with `--pagination-font-size` automatically (no per-size icon override); 16 component vars + 1 internal `--pagination-transition` (hidden from Customization table same way list-group hides its internal vars); single ungrouped Customization table at 16 rows under the 20-var threshold per slider.njk pattern; **demo page sweep** — 6 sections (Basic / With icons / Disabled / Ellipsis (new) / Sizes / Alignment) + Customization; BS5 utilities (`.d-flex flex-column gap-3 w-100`, `.justify-content-{center,end}`, `mb-0`) all swept — inline `style="display: flex; flex-direction: column; gap: 1rem; width: 100%"` for the multi-row demos (matches list-group / breadcrumb / navbar pattern); `--pagination-margin-bottom: 0` default eats the `mb-0` BS5 stutter; disabled demo's `<a aria-disabled>` → `<span class="pagination__button" aria-disabled="true">` (drop `href` so screen readers don't announce as a link — better a11y than the BS5 demo); ellipsis demo introduces the truncation pattern with 7 chips spanning 1 → 12. **Bundle**: `_pagination.scss` uncommented at line 60 of `bundles/stisla-full.scss`. **Verification**: build green (40 pages render), bundle 87.2 → 88.1 KB raw (+0.9) / ~14.4 → 14.7 KB gz (+0.3) — smallest delta in the display family alongside breadcrumb, expected for a single-element chip component with no layered-bg mechanic. 0 `--bs-*` leaks in compiled CSS. 0 BS5 leftover names (`page-item`, `page-link`, `pagination-sm`, `pagination-lg`, `.disabled` on `<li>`, `.active` on `<li>`) in rendered HTML or compiled CSS; the single `justify-content` hit in `pagination.html` is in the Customization section prose explaining the `--center` / `--end` modifiers, not a class leak. All 16 documented `--pagination-*` vars emit (verified by `grep -oE -- "--pagination-[a-z-]+"` across the compiled stylesheet) + the internal `--pagination-transition`. All 7 BEM selectors emit (`.pagination`, `.pagination--sm`, `.pagination--lg`, `.pagination--center`, `.pagination--end`, `.pagination__button`, `.pagination__ellipsis`). 97 `pagination__button` references in rendered HTML (across the 6 demo sections + escaped-source duplicates), 11 `data-state="active"`, 11 `aria-current="page"`, 2 `aria-disabled="true"`, 3 `pagination__ellipsis`, 3 each of `--sm` / `--lg` / `--center` / `--end`. Sass deprecation warnings unchanged (forked BS5 mixins, pre-existing noise). Site sidebar already pointed at `/pagination` (no rename needed). Visual verification pending — maintainer to eyeball `/pagination` in light + dark: (a) Basic — 5-chip row with chip 2 highlighted; hover any link → accent fill; (b) With icons — chevron icons pin to label baseline, scale with font; (c) Disabled — Previous chip greyed out with no pointer; (d) Ellipsis — `1 2 3 4 5 … 12` with the dots reading muted between chip 5 and chip 12 at the same height as the chips; (e) Sizes — three rows at 28 / 36 / 44 px under default density, icons scale with font; (f) Alignment — center and end rows shift across the demo viewer width; (g) Focus — tab onto any chip and the 2px outline paints at `--st-ring` with positive offset, no clipping; (h) Customization — 16-row table reads cleanly with the Variable / Default / Use columns; (i) Dark mode — accent + highlight track the dark block; chip text inverts naturally via `--st-foreground` / `--st-highlight-foreground`. **3.5d closes the fourth Display-family file.** Remaining display queue: progress (the 0B file — pure write, no port reference); toast + accordion are JS-coordinated and slot into Step 4 interleave.) ✅
  - [x] 3.6c polish — selector fix (`>` → `> * >` so the heading-wrapper `<h3>`/`<h2>` between `.accordion__item` and `.accordion__header` no longer breaks the closed-hover paint + open-state divider + chevron rotation rules — all three silently failed at initial land); radius-lg hierarchy (`card` + `accordion` + `list-group` bumped from `--st-radius` to `--st-radius-lg`, locking in the chip / element / frame three-tier rhythm); card refactored to gap-based composition (`.card__body { display: flex; flex-direction: column; gap: 0.5rem }`, dropped margins from `__title` / `__subtitle` / `__text` + the negative-top hack + the last-child workaround — matches the alert pattern already in `_alert.scss`); accordion `--accordion-margin-bottom` deleted entirely (parent owns inter-block spacing). Bundle 102.2 → 102.0 KB raw / 16.5 → 16.4 KB gz. Accordion var count 25 → 24. ✅
  - [x] 3.6c — accordion (rewrite — last CSS-leading Phase-2 component; **revised mid-session** after maintainer ref screenshot — open state is now a chip-style raise (`--st-surface-2` bg + `--st-border` rim on the whole `.accordion__item`) with a `border-bottom: 1px solid --st-border` divider between header and body, not a `--st-highlight` fill on the header; header text stays `--st-foreground` in both states; padding rebased to 12 / 16 px on both header and body (4 px frame + 16 px X = 20 px aligns with card padding); frame ships a `var(--st-shadow)` ambient elevation at rest; flush mode just drops the frame border + shadow, keeps root padding so items still chip out concentrically inside a card. Dropped the native `<details>` route in favour of the ARIA accordion pattern — `<button aria-expanded>` + `<div role="region" aria-labelledby>` — because `::details-content` + `interpolate-size` for animation are above the V3.md §2 browser baseline and because Radix/shadcn/Reka/bits-ui all ship the ARIA pattern, so the CSS file serves vanilla + future React/Vue impls per V3.md §3.11 with no glue layer. BEM rename: `.accordion-item` → `.accordion__item`; `.accordion-header` BS5 wrapper dropped as a class (heading wrap stays in markup, no class); `.accordion-button` → `.accordion__header` (the trigger IS the named part — same convention as Radix `<AccordionTrigger>`); `.accordion-collapse` + `.accordion-body` collapsed → `.accordion__body`; new `.accordion__icon` for the chevron (rotates 0→180° via CSS transform); `.accordion-flush` → `.accordion--flush`; `.collapsed` markup class dropped wholesale (`data-state="closed"` + `aria-expanded="false"` carry the meaning). State hooks: `.accordion__item[data-state="open|closed"]` is the paint hook; `.accordion__header[aria-expanded]` is the JS-flipped source. Concentric inner radius is the V3.md §3.4 canonical case — frame owns `--accordion-radius` + `--accordion-padding`, item `border-radius: calc(outer - padding)`. Color routing: closed hover = `--st-accent` on header (transient hover, sidebar/list-group/table family); open item = `--st-surface-2` chip bg + `--st-border` rim around the whole item + `border-bottom: 1px solid --st-border` divider between header and body; header text stays `--st-foreground` in both states; open-state hover stays flat inside the chip. Header lets padding own the height (no `min-height` or hard `height`) — long headers wrap naturally. Flush mode just drops the frame border + shadow (root padding kept so items chip out concentrically inside a parent card). Chevron rotation transitions today; body collapse uses `display: none` for the closed state — Step 4 swaps in JS-measured height transition keyed off `--accordion-body-transition-duration` (var exposed today so the customization surface is stable across the upgrade). Interim JS handler in `src/js/index.js` (~25 lines, mirrors `data-sidebar-submenu-toggle` shape): delegated click listener flips `data-state` + `aria-expanded`, supports `data-stisla-accordion-single` on the root for radio-group semantics. 25 component vars in 7 groups (Geometry/Frame surface/Open-item chip/Header/Icon/Body/Focus); grouped Customization tables per slider.njk. Bundle 97.8 → 102.2 KB raw / 15.8 → 16.5 KB gz. 0 `--bs-*` leaks; 0 BS5 class leftovers; all 25 vars + 6 BEM selectors emit; 33 ARIA wiring markers in the rendered demo; 5 open + 10 closed states across 28 demo items + 3 single-mode roots + 3 flush variants. 42 pages render. **Closes the CSS-leading cohort.** Next: Step 4 prelude — `src/js/components/` + `Stisla.Component` base + `Stisla.init()` scanner — before any JS-coordinated component lands.) ✅
  - [x] 3.6b — toggle + toggle-group split; button-group simplified (vision shift after 3.6a landed. `.btn-check` mechanic and `.toggle`'s input-driven model were doing the same job — and `.btn-group`'s "Toggle group" / "Tabs+segmented" / "Checkbox & radio" demo sections rendered visually identical patterns. Resolution: `.toggle` rewritten as standalone press button — three state sources (`[aria-pressed]`, `[data-state="active"]`, `.toggle-input:checked + &`) all paint same; outline-neutral rest, `--st-highlight` active bg + foreground-mix border ("slightly stronger than active bg" per maintainer spec). `.toggle-group` new — pill container with concentric inner radius (outer 36 px / padding 4 px / inner 28 px under default density; --sm 28/2/24, --lg 44/4/36); members go ghost-rest so the parent rim owns the frame; active member paints highlight bg + foreground-mix border (inherits standalone `.toggle` active rules). `.btn-group` shrunk back to purely visual grouping — stripped `.btn-check` rules, adjacency forms, z-index lifts. `_btn.scss` reverted — removed the `.btn-check:checked + &` synonym from all 8 active-state cascade rules. Demo pages: `/toggle` rewritten for new model with JS + form-data examples; `/toggle-group` new with single-select + multi-select + form-data variants + sizes + vertical + customization; `/button-group` lost 3 sections (now at /toggle-group). Site sidebar gained `/toggle-group`. V3.md Phase 2 list grew `toggle, toggle-group` entries; cut note rewrote — toggle-group single-select covers tabs/segmented; new paragraph spells out toggle-vs-button-group semantic split. Bundle 95.4 → 97.8 KB raw / 15.5 → 15.8 KB gz. 0 `--bs-*` leaks; 0 `btn-check` residue. 21 vars + 11 BEM selectors emit clean. 41 pages render. **Closes the toggle / button-group / tabs vocabulary cleanly.** Remaining JS-coordinated queue: accordion next (CSS today via native `<details>`), then with Step 4 — modal, offcanvas, dropdown, tooltip, popover, toast, carousel.) ✅
  - [x] 3.6a — button-group (rewrite — first JS-coordinated-family file, but the CSS lands without its JS half because the actual click behavior was already a BS5-data-attr concern; v3 ships the visual + a11y today, roving-tabindex + click handlers defer to Step 4. ~180-line file from the dead BS5 overlay. **BEM rename map**: `.btn-group-vertical` → `.btn-group--vertical` (proper modifier); `.btn-group-sm` / `.btn-group-lg` → `.btn-group--sm` / `.btn-group--lg`; `.btn-toolbar` stays its own block; `.btn-check` stays (input class); `.dropdown-toggle-split` **dropped** — the split-button caret reuses `.btn--icon-only` with a chevron, no split-specific class ships. **Sizes retune child --btn-* vars in modifier scope** — `.btn-group--sm > .btn { --btn-height: 1.75rem; --btn-padding-x: ...; --btn-font-size: ...; --btn-radius: var(--st-radius-sm) }` and the `--lg` mirror. No per-size vars on `.btn-group` itself; sizes match the standalone `.btn--sm` / `.btn--lg` cadence (28 / 36 / 44 px under default density) so a sized group sits in the same rhythm as a standalone button next to it. **Seam dedupe** — negative `margin-inline-start: -1px` (or `margin-top: -1px` for vertical) on every non-first member collapses adjacent 1px borders into a single line. Inner corners square (`border-radius: 0` on all members); outer corners re-round via `border-start-start-radius` / `border-end-start-radius` on first child and `border-start-end-radius` / `border-end-end-radius` on last child (logical properties so RTL flips automatically; the same logical-property pattern shows up in form-control / input-group). **Two adjacency forms** for the negative-margin rule: `> .btn + .btn` (consecutive plain buttons) and `> .btn + .btn-check + .btn` (input-driven `<input><label>` pairs interleaved) — covers both markup patterns without falling back to a `:nth-child` approach. **Stacking-order lift on interactive members** — every member sits `position: relative`. Hover lifts to `z-index: 1`, active states (`[aria-pressed="true"]` + `[data-state="active"]` + `.btn-check:checked + &`) to `z-index: 2`, focus-visible to `z-index: 3`. Without the lift the negative-margin seam-dedupe buries the active member's right border under the next sibling's left border (or the equivalent for vertical), and the focus halo gets clipped at the seam. **Active-state forwarding via _btn.scss touch** — single-pass edit added `.btn-check:checked + &` as a synonym alongside `[aria-pressed="true"]` + `[data-state="active"]` across all 8 active-state cascade rules in `_btn.scss` (base, neutral, tertiary, outline, ghost, soft, outline.neutral, ghost.neutral). Means a checked `<input>` paints its `<label class="btn">` correctly through every tone × shape combo automatically — no duplicating color-mix formulas in button-group. Cleaner than alternative approaches (separate per-tone-per-shape `:checked +` rules in button-group, or JS that toggles `aria-pressed` on label click). **.btn-check geometry** copied from `.visually-hidden` (position: absolute, 1×1px, clip rect 0, margin -1px) — not `display: none` / `visibility: hidden` because those drop the input from the a11y tree and tab order. Focus forwards to the label via `.btn-check:focus-visible + .btn { outline: 2px solid var(--st-ring); outline-offset: 2px }` so keyboard nav has a visible halo on the visible chip rather than the hidden input. Disabled forwards via `.btn-check:disabled + .btn { cursor: not-allowed; pointer-events: none; opacity: 0.55 }` (matches `.btn`'s own disabled paint). **.btn-toolbar — separate block** with `--btn-toolbar-gap: 0.5rem` default. `display: flex; flex-wrap: wrap; gap: var(--btn-toolbar-gap)` so multiple `.btn-group` children breathe without inline flex utilities at each call site. **Tabs / segmented control** — new section in the demo using `<div class="btn-group" role="tablist">` + `<button role="tab" data-state="active" aria-selected="true">` for the current member. `.btn`'s existing `[data-state="active"]` cascade paints the active member; no new var or class. **The base `.btn`'s active-state mechanic** (color-mix toward black for filled tones, toward background for tertiary, tint-on-transparent for outline/ghost/soft) handles every tabs/toggle paint case correctly — button-group stays purely about layout. **Split-button** demoed with two `.btn`s in the group: a regular button + a `.btn--icon-only` chevron trigger. Dropdown attach lives with the Stisla.Dropdown component in Step 4; `aria-haspopup="menu"` + `aria-expanded="false"` ship today so the markup wires cleanly when the JS arrives. **2 component vars only** (`--btn-group-radius` + `--btn-toolbar-gap`) — most of the configurability lives on the child `.btn` (via its own ~10 vars), not on the group. Single ungrouped Customization table with both, plus a pointer paragraph linking to `/buttons` for the child surface (same pointer-section pattern as toggle / select reuse). **Bundle**: `_button-group.scss` uncommented at line 51 of `bundles/stisla-full.scss`. **Demo page (`src/site/pages/button-group.njk`) sweep** — 11 sections (Basic / Outline / Mixed / Sizes / Toggle group / Tabs+segmented (new) / Checkbox & radio / With icons / Split button / Vertical / Toolbar) + Customization. BS5 vocab swept: `.btn-primary` → `.btn .btn--primary`, `.btn-outline-secondary` → `.btn .btn--outline .btn--neutral`, `.btn-icon` → `.btn--icon-only`, `.btn-group-vertical` → `.btn-group--vertical`, `.btn-group-sm` / `.btn-group-lg` → `.btn-group--sm` / `.btn-group--lg`, `.dropdown-toggle-split` dropped, `data-bs-toggle="button"` + `data-bs-toggle="dropdown"` removed from markup (declarative toggle JS lands in Step 4), `.active` on toggle members → `aria-pressed="true"`. `.d-flex flex-column gap-2 align-items-start` → inline `style="display: flex; flex-direction: column; gap: 0.5rem; align-items: flex-start"` (matches the v3 list-group / breadcrumb / navbar / pagination pattern since v3 doesn't ship flex utilities yet). Toggle-group section ships hardcoded `aria-pressed="true|false"` to demo the visual without the click handler. Tabs section uses `role="tablist"` + `role="tab"` + `aria-selected="true"` + `data-state="active"` on the current member. **Verification**: build green (40 pages render), bundle 90.2 → 95.3 KB raw (+5.1) / ~15.0 → 15.5 KB gz (+0.5). 0 `--bs-*` leaks in compiled CSS. 0 BS5 leftover names (`btn-group-vertical`, `btn-group-sm`, `btn-group-lg`, `btn-primary`, `btn-outline-secondary`, `btn-icon`, `dropdown-toggle-split`, `data-bs-toggle`) in rendered HTML. Both `--btn-group-*` + `--btn-toolbar-*` vars emit (verified by `grep -oE -- "--btn-(group|toolbar)-[a-z-]+"` across the compiled stylesheet). All 4 BEM selectors emit (`.btn-group`, `.btn-group--vertical`, `.btn-group--sm`, `.btn-group--lg`) + `.btn-check` + `.btn-toolbar`. 34 `btn-group` references + 3 each of `--sm` / `--lg` / `--vertical` in the rendered demo, 13 `btn-check`, 4 active-state markers (`data-state="active"` / `aria-pressed="true"` / `aria-selected="true"`). Sass deprecation warnings unchanged (forked BS5 mixins, pre-existing noise). Site sidebar already pointed at `/button-group` (no rename needed). **Visual verification pending** — maintainer to eyeball `/button-group` in light + dark: (a) Basic — 3 primary buttons reading as one chip with outer-only radius and one seam between each pair; (b) Outline — 3 outline buttons with dedupe; (c) Mixed — primary + outline-neutral pair; (d) Sizes — sm/default/lg rows at 28 / 36 / 44 px, every member in a sized group matches the corresponding `.btn--sm` / `.btn--lg` cadence; (e) Toggle group — Bold + Underline highlighted (aria-pressed true), Italic muted (aria-pressed false), active members lift above the seam so their darker border paints cleanly; (f) Tabs — Day highlighted via data-state="active" + aria-selected, Week / Month muted; (g) Checkbox/radio — clicking on a label flips the hidden input + the label's paint via the `.btn-check:checked + .btn` cascade (radios single-select via name=); (h) Icons — 3 icon-only outline buttons; (i) Split button — primary + chevron-only trigger, no functional menu yet (Step 4 work); (j) Vertical — 3 stacked buttons with top/bottom outer rounds + middle inner seams; (k) Toolbar — 3 groups gapped 0.5rem apart, wrap on narrow containers; (l) Focus — tab onto any member, the focus halo paints above the seam without clipping; (m) Dark mode — every variant tracks the dark block tokens; active aria-pressed states stay perceptibly darker than rest. **3.6a opens the JS-coordinated family.** Remaining queue: accordion (CSS today + native `<details>` later), then with Step 4 — modal, offcanvas, dropdown, tooltip, popover, toast, carousel. Each lands its CSS + demo sweep alongside its JS contract per V3.md §3.7.) ✅
- [ ] Step 4 — JS rewrite (interleaved with step 3 JS-coordinated components)
  - [x] 4.0 prelude — `src/js/core/` (Component base class + declarative scanner) + `src/js/components/` directory + `index.js` refactor (`Stisla = { version, Component, register, init, get }` public surface + HMR-guarded interim handlers) ✅
  - [x] 4.3 — dropdown (rewrite — first Floating UI consumer; `@floating-ui/dom` lands here as the anchored-positioning primitive 4.4 tooltip + 4.5 popover reuse for free; full BEM rename `.dropdown-item` → `.dropdown-menu__item` family + new `__group` / `__icon` / `__indicator` / `__shortcut` / `__item--danger` parts; `.dropdown-toggle` + `.dropup`/`.dropend`/`.dropstart` BS5 wrapper classes dropped wholesale — Floating UI's `data-stisla-dropdown-placement` opt + `flip()` middleware handle placement; concentric inner radius via `--dropdown-radius` − `--dropdown-padding`; 33 `--dropdown-*` vars across 8 groups; `position: fixed` strategy so the menu escapes any ancestor `overflow: hidden`; scroll-lock via `<html>.is-dropdown-open` matching Dialog/Drawer convention (sidesteps the Floating UI reposition lag on scroll); `flex-shrink: 0` on `.dropdown-menu > *` so the size-middleware's max-height cap scrolls the menu instead of squishing children; keyboard-only highlight (mouse uses native `:hover`, doesn't auto-track `data-highlighted` like a `<select>`); menuitemcheckbox + menuitemradio click-toggle Radix-parity. Page rename `dropdowns.njk` → `dropdown.njk` to match `dialog.njk`/`drawer.njk` singular convention; site sidebar updated `/dropdowns` → `/dropdown`. Bundle CSS +2.8 KB raw / +0.46 KB gz; JS +24.5 KB raw / +7.57 KB gz (Floating UI ~6 + Dropdown ~1.5 gz). Total JS 17.31 KB gz, ~8–13 KB gz headroom under §3.1 budget for the remaining 4.4–4.7 components.) ✅
- [ ] Step 5 — Demo pages (.njk class + attr + delete pass)
- [ ] Step 6 — Cleanup, README/CHANGELOG, `3.0.0-beta.1` tag
- [ ] **After 3.0.0 final:** delete `bs5-snapshot/`, delete this file.
