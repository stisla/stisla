{
  "name": "@stisla/css",
  "version": "0.0.0",
  "description": "Stisla vanilla CSS bundle — zero-build distribution (tokens + components + utilities). NOTE: same published name as the legacy packages/css; only one publishes at cutover.",
  "license": "MIT",
  "exports": {
    ".": "./dist/stisla.css",
    "./full": "./dist/stisla-full.css",
    "./utilities": "./dist/utilities.css",
    "./components/*": "./dist/components/*.css",
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "sideEffects": [
    "*.css"
  ],
  "scripts": {
    "build": "echo 'TODO Phase 4: tailwindcss bundle (@theme static + @source inline)' && exit 0"
  },
  "peerDependencies": {
    "@stisla/tokens": "workspace:*"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0"
  }
}
