{
  "name": "stisla-next",
  "private": true,
  "version": "0.0.0",
  "description": "Stisla rewrite — constraint + Tailwind-scale era. Real npm workspaces. See ../ARCHITECTURE.md and ../ROADMAP.md.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter playground dev"
  }
}
