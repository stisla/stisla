{
  "name": "@stisla-template/dashboard-html",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Meridian — Stisla dashboard template, plain-HTML edition (Nunjucks → static HTML). Consumes @stisla/* from workspace source in dev; the zip build pins the CDN. Not published.",
  "scripts": {
    "dev": "vite",
    "build": "node build.mjs"
  },
  "dependencies": {
    "@stisla/style": "workspace:*",
    "@stisla/vanilla": "workspace:*"
  },
  "devDependencies": {
    "@iconify-json/solar": "^1.2.0",
    "@iconify/utils": "^2.3.0",
    "@tailwindcss/cli": "^4.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "nunjucks": "^3.2.4",
    "tailwindcss": "^4.0.0",
    "vite": "^8.0.0"
  }
}
