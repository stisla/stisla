# Flat node_modules (npm-like) so the playground's Vite aliases resolve cross-package
# source + peer deps (react, @base-ui/react). Disk dedup is retained — pnpm still
# hardlinks from its global content-addressable store regardless of linker mode.
node-linker=hoisted
