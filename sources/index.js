'use strict';

const basepath = 'node_modules/@stisla/assets';

const stisla = {
  path: {
    scss: basepath + '/scss',
    pages: basepath + '/pages',
    js: basepath + '/js',
    fonts: basepath + '/fonts',
    img: basepath + '/img'
  }
}

exports.default = stisla;
