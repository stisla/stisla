# Stisla v3

> Single source of truth for v3 design, scope, and progress.
> Conversations get lost; this file doesn't. Update as decisions are made.

---

## Agent workflow rules (read before doing anything)

You are an AI agent working on Stisla v3. Before you do **anything**:

1. **Read this entire V3.md.** It survives across sessions; the conversation doesn't.
2. **Before suggesting any Bootstrap customization, read the relevant docs page** at <https://getbootstrap.com/docs/5.3/customize/> — overview, sass, options, color, color-modes, components, css-variables, optimize. Don't guess at what BS5 has or how it's named; the maintainer will catch it.
3. **When docs are ambiguous, grep `node_modules/bootstrap/scss`** for the actual implementation. The source is ground truth.
4. **Plan in chat first.** Lay out what you intend to write, the exact files, the exact override values, the import order. Wait for the maintainer's "go". **Do not write files until the plan is approved.**
5. **Follow BS5 conventions; don't invent.** Zero `--stisla-*` runtime vars in v3. Customization flows through `--bs-*` (root) and BS5's per-component vars. Don't add loops, layers, or aliases BS5 doesn't already ship.
6. **One Sass file = one job.** See `src/scss/tokens/` for the Phase 1 pattern.
7. **Never push or merge** without explicit maintainer approval.

Skipping these wastes a round trip. They're not aspirational.

---

## 1. Vision

Stisla v3 is a full rewrite. CSS-first admin framework built on Bootstrap 5.3:

- **Theme-able via CSS variables** — rebrand by overriding `--bs-*` at runtime (with caveats; see §3.1).
- **Framework-agnostic** — vanilla JS that React/Vue/Svelte can wrap trivially.
- **Showcased through the site itself** — `stisla.dev` IS the docs. Component pages double as live demos with view-source; template pages double as marketing and as downloadable starters.
- **Honest about scope** — minimal core, no abandoned dependencies, no half-finished features.

What v3 is NOT:

- Not an upgrade from v2 (clean break, no migration path).
- Not a kitchen sink (no rich-text editor, no datatables, no chart library bundled).
- Not jQuery-based.

---

## 2. Decisions (locked)

| Area | Decision | Why |
| --- | --- | --- |
| Base framework | Bootstrap 5.3, with BS6 forward-compatibility in mind | BS5.3 already exposes most CSS vars we need; BS6 not ready |
| Theming model | BS5 `--bs-*` vars only at runtime. Stisla owns the Sass-time values; BS5 emits the `:root` vars. | One naming API. Customization is BS5 conventions all the way down. |
| Distribution | npm + CDN | Standard for CSS frameworks; covers both ecosystems |
| Repo shape | Single tree, no monorepo tools | Simpler; framework and site share build deps |
| Site templating | Nunjucks (templates are also the deliverables) | Clean HTML output; same source for live demo + downloadable template |
| Build tooling | Vite (dev server + JS/CSS bundling), Sass via Vite, Nunjucks via custom Vite plugin + render script, PostCSS for autoprefixer | Gulp drops out |
| JS | Vanilla, class-based, `destroy()` mandatory, custom events | Framework-agnostic by contract |
| Browser support | Last 2 versions Chrome/Edge/Firefox, Safari 15.4+ | Buys `color-mix()`, `:has()`, `@layer`, container queries |
| v2 compatibility | None. Clean break. | Full rewrite, not worth migration cost |
| v2 originals | Cut from 3.0. Reappear as recipe pages, or as components in 3.1+ if utilities can't do it. | Most were card + utility compositions |
| Home | `stisla.dev` | New domain; v2 stays at getstisla.com as archive |

---

## 3. Architecture

### 3.1 Token layers

**One layer at runtime: Bootstrap's `--bs-*` CSS variables.** Stisla's contribution lives at Sass build time — we override BS5's `!default` Sass vars with our hand-picked palette and BS5 emits the correct `:root` vars from there.

The override sequence follows Bootstrap's "Option B" customization workflow exactly:

```
functions → our variables + variables-dark → BS5 variables + variables-dark
         → our maps → BS5 maps → mixins → utilities → root → our root extensions
         → reboot → ... → utilities/api
```

Our `_variables.scss` / `_variables-dark.scss` parallel BS5's pair: light values set defaults via `!default` before BS5 loads its own, dark values feed BS5's `_variables-dark.scss` the same way.

**No `--stisla-*` runtime vars.** Bootstrap already names what we need:

- `--bs-primary`, `--bs-blue`, `--bs-gray-100..900`, etc. for colors
- `--bs-body-bg`, `--bs-body-color`, `--bs-emphasis-color`, `--bs-secondary-color`, `--bs-tertiary-color` for body/text
- `--bs-border-color` for borders
- The theme-color triplet (`--bs-primary-bg-subtle`, `--bs-primary-text-emphasis`, `--bs-primary-border-subtle`) for tinted callouts/alerts/badges
- Per-component vars (`--bs-card-bg`, `--bs-btn-hover-bg`, etc.) for component-level overrides

Hover / active / disabled are seeded from our palette via existing BS5 component vars (e.g., `$nav-link-disabled-color: $gray-300`). No new var categories.

**Stisla-emitted runtime vars (carve-outs).** Anything we emit beyond BS5's defaults lives in `src/scss/tokens/_root.scss` and is documented here:

- `--bs-border-color-subtle` (#f3f4f7) — sibling to `--bs-border-color`. The design system needs a neutral subtle-border token; BS5's `*-border-subtle` are color-tinted per theme color and don't cover the neutral case. Naming kept in the `--bs-*` namespace and the `-subtle` convention so it reads as a natural extension, not an invention.
- `--bs-surface` (#fff light / #1e2128 dark placeholder) and `--bs-surface-alt` (#f9fafb light / #16181d dark placeholder) — shared two-tone neutral surfaces. Today: `.card` body sits on `--bs-surface`, `.card-footer` and `.card-header-alt` sit on `--bs-surface-alt`. Next: modal, dropdown, popover, list-group will repoint their `--bs-*-bg` at `var(--bs-surface)` as they're restyled, so the two-tone pattern is a single design-system swap instead of a per-component carve-out. Lives at root level rather than bound to `.card` so any component-level rule can read the same vars.

**Dark mode** uses BS5's built-in `[data-bs-theme="dark"]`. BS5 auto-generates dark-mode triplet variants via `shade-color()` / `tint-color()` from our base values.

Two patterns, both per the 5.3 [customize/color-modes](https://getbootstrap.com/docs/5.3/customize/color-modes/) docs:

1. **Body / text / border / theme-color-triplet tokens** — set the matching `$*-dark` Sass variable in `src/scss/tokens/_variables-dark.scss` (e.g., `$body-bg-dark`, `$border-color-dark`, `$primary-text-emphasis-dark`). BS5's `_variables-dark.scss` consumes them and emits to `[data-bs-theme="dark"]` automatically.

2. **Component-specific bgs (card-bg, modal-bg, dropdown-bg, etc.) and Stisla-extension runtime vars** — BS5 deliberately doesn't ship `$card-bg-dark` etc. Instead, override the CSS variable inside BS5's `color-mode()` mixin in the relevant component file (`src/scss/components/_<name>.scss`) or in `_root.scss` for global carve-outs:

   ```scss
   @include color-mode(dark) {
     .card {
       --#{$prefix}card-bg: <dark value>;
     }
   }
   ```

   Compiles to `[data-bs-theme="dark"] .card { --bs-card-bg: ... }`. We use the mixin (not hand-written `[data-bs-theme="dark"]` rules) so the cascade obeys BS5's `$color-mode-type` switch — if BS5 changes the selector strategy in a future release, our code adapts.

**Runtime override caveat (important).** BS5 components (buttons, alerts, badges, cards, etc.) compile their colors at Sass build time from `$primary`, `$danger`, etc. Overriding `--bs-primary` at runtime does NOT recolor them. What DOES respond at runtime:

- Utility classes that use `var(--bs-primary-rgb)`: `.bg-primary`, `.text-bg-primary`, `.text-primary`, `.border-primary`
- Anything you write that explicitly references `var(--bs-primary)`

For component recoloring, users rebuild from Sass. This is a BS5 architectural limit; document it in the Phase 5 site docs.

### 3.2 JS contract

Three rules for every Stisla JS component:

1. **Class-based** — `new Stisla.X(el, opts)`.
2. **Destroy method** — every instance has `.destroy()`. Non-negotiable; this is what makes React unmount work.
3. **Custom events** — emit DOM events (`stisla:dropdown:open`, etc.), not callbacks.

Plus a declarative path for plain-HTML users:

```html
<div data-stisla-dropdown>...</div>
<script>Stisla.init();</script>
```

BS5 vanilla JS is re-exported under `Stisla.bs.*` so users get one entry point.

### 3.3 Repo layout

```
/src/
  scss/
    tokens/        ← Sass-time tokens (_colors, _variables, _variables-dark, _maps, _root)
    components/    ← thin BS5-component overrides (_buttons, _card, ...)
    bundles/       ← bundle entry points (stisla-full.scss)
  js/
    core/          ← BS5 vanilla re-exports (Phase 3)
    components/    ← Stisla components (3.1+)
    index.js
  site/
    pages/         ← /buttons, /cards, ... (component demos)
    templates/     ← dashboard-1, auth, ... (full templates)
    layouts/
    partials/
/tools/            ← build scripts (vite-plugin-nunjucks.mjs, render-site.mjs)
/dist/             ← built CSS+JS bundles for npm + CDN (Phase 6)
/site-dist/        ← built static site for stisla.dev
vite.config.mjs
postcss.config.mjs
```

### 3.4 Bundles

**3.0 ships one CSS bundle:** `stisla-full.css` (~231 KB unminified, ~31 KB gzipped) and one JS bundle: `stisla.js`.

Bundle splitting (`stisla-tokens.css` for headless themers, `stisla-core.css` for slim builds without Stisla originals, `stisla-components.css` for Stisla-only bolt-on use) is post-3.0 scope. Add when there's a real ask.

### 3.5 Site conventions (stisla.dev)

Patterns the site uses to demo components — site-only, never bundled into the framework CSS/JS.

- **Per-component pages.** One `.njk` per Phase 2 component under `src/site/pages/<name>.njk`. Mirror the variant/state coverage from Bootstrap's own component docs — no use case left unstyled.
- **Demo viewer.** `src/site/partials/_demo.njk` exposes `{% call ui.demo() %}<body>{% endcall %}` — framed preview on top, escaped source below. Single source of truth via Nunjucks `caller()`. Stacked layout for now; swap to BS5 nav-tabs after that component is restyled (touches the macro only).
- **Dedent filter.** `tools/nunjucks-filters.mjs` strips common leading whitespace from demo bodies so code blocks render flush-left while .njk source stays indented to match its surroundings.
- **Site JS.** `src/site/scripts/site.js` (built as `site-dist/assets/site.js`) holds demo-only behaviors via `data-demo-*` attribute conventions (`data-demo-loading`, `data-theme-toggle`, ...). Event-delegated, declarative. Framework JS bundle (`src/js/index.js`) stays clean.
- **Site stylesheet.** Site chrome (`.demo`, `.demo-preview`, `.demo-code`, `.theme-icon-*`, etc.) lives in `src/site/styles/site.scss` and builds as a separate Vite entry to `site-dist/assets/site-styles.css`. Linked from `base.njk` alongside `stisla-full.css`. Never bundled into the framework.
- **Theme toggle.** `<button data-theme-toggle>` in the top nav of `base.njk` flips `[data-bs-theme]` on `<html>` and persists to `localStorage`. A tiny FOUC-prevention script in `<head>` applies the saved theme synchronously before paint.

---

## 4. Phases

Each phase has explicit "done when" criteria. No vague endings.

### Phase 0 — Foundation
- [x] `v3` branch cut
- [x] `V3.md` committed (this file)
- [x] Repo dirs scaffolded per §3.3
- [x] Vite + Sass + Nunjucks + PostCSS build pipeline runs `npm run dev` and `npm run build`
- [x] `hello world` page renders with hot reload

### Phase 1 — Tokens + theme
- [x] Primitive scales drafted (6 ramps × 9 stops from `color.json`)
- [x] BS5 vars seeded from primitives via Sass overrides (canonical "Option B" workflow)
- [x] `stisla-full.css` builds clean (no deprecation noise from BS5 internals)
- [x] Dark mode works via BS5's `[data-bs-theme="dark"]` (toggle on hello-world page)
- [x] Brand recolor demo: runtime utility tinting works via `--bs-primary` + `--bs-primary-rgb`; honestly notes the BS5 component-level limit

### Phase 2 — Core CSS
- [ ] BS5 restyled: buttons, forms, cards, modals, dropdowns, nav, navbar, alerts, badges, tables, pagination, tabs, accordion, toasts, offcanvas
  - [x] buttons — sizes (28/36/44), 8/12/16 radius scale, Tailwind-style shadow + inset highlight, per-variant 1px ring, `.btn-icon` square modifier, `.btn-loading` spinner; demo at `/buttons`
  - [x] cards — borderless (0px) with 16px radius, soft ambient shadow, 24px body padding, 16/24 cap padding, transparent header bg, 16px title, muted subtitle; surface system promoted to `--bs-surface` / `--bs-surface-alt` (powers `.card-footer` two-tone + new `.card-header-alt` modifier); demo at `/cards`
- [ ] Dark mode verified for each
- [ ] Visual parity check against BS5 base (BS5 semantics preserved; we restyle)

### Phase 3 — JS contract
- [ ] `Stisla.init()` declarative scanner
- [ ] One reference component fully implements the contract (class + destroy + events)
- [ ] BS5 vanilla re-exports under `Stisla.bs.*`
- [ ] `stisla.js` (UMD) + `stisla.esm.js` build

### Phase 4 — Templates
- [ ] `dashboard-1` built using only Phase 1+2+3 output
- [ ] `auth` (login + register) built same way
- [ ] Per-template download zip generated at build time

### Phase 5 — Site (stisla.dev)
- [ ] Landing page
- [ ] Component pages for everything in Phase 2 with view-source panel
- [ ] Template pages (Phase 4) with download button
- [ ] Foundational docs: installation, theming, JS API, browser support, BS5 runtime-override caveats
- [ ] Patterns pages (the old v2 originals reborn as recipes)

### Phase 6 — Release
- [ ] npm publish (package name TBD — see §5)
- [ ] CDN URLs verified (jsdelivr / unpkg)
- [ ] `stisla.dev` live with DNS
- [ ] git tag `v3.0.0`
- [ ] CHANGELOG / announcement

---

## 5. Open questions

- [ ] Adopt CSS `@layer` ordering for predictable cascade? Likely: `@layer reset, tokens, bootstrap, stisla, utilities`.
- [ ] Per-template downloadable zip — build-time script or runtime endpoint? (Lean: static build-time script.)
- [ ] Ship a CLI scaffolder in 3.0 or wait for demand? (Lean: defer.)
- [ ] npm package name(s) — `stisla`, `@stisla/css` + `@stisla/js`, or single `stisla` with subpath exports?
- [ ] Keep v2 archive at `getstisla.com`, or 301 to `stisla.dev/v2`?
- [ ] `.github/workflows/authors.yml` references the deleted `dev/update-authors.js`. Update or remove before merging `v3` to `master`.

## 6. Out of scope (3.0)

These get recipe pages on the site pointing users to "bring your own". They do NOT ship in the CSS or JS bundles.

- Rich-text editor (was summernote in v2)
- Datatables
- Chart library (chart.js etc.)
- File upload widget (was dropzone)
- Date/time pickers
- Maps (was gmaps/jqvmap)
- Calendar (was fullcalendar)
- RTL stylesheet (defer to 3.1)
- i18n examples
- Framework adapter packages (React/Vue/Svelte wrappers)
- Migration tooling from v2
- v2 original components (reborn as recipes; promote to real components in 3.1+ only when utilities can't do it)
