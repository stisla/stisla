// Vanilla core bundle — no optional components (no carousel, no date-picker, etc.).
// Pre-compiled CSS + JS bundles. ~24-26 KB gz each.
import '@stisla/css';
import '@stisla/vanilla';

import './styles.css';
