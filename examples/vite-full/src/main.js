// Full distribution — core + every integration shipped at this version.
// Includes carousel today; date-picker / custom-select / etc. land here later.
import '@stisla/css/full';
import '@stisla/vanilla/full';

import './styles.css';
