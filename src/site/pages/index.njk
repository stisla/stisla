{% extends "layouts/base.njk" %}

{% block title %}Stisla v3 · hello world{% endblock %}

{% block content %}
  <h1>Stisla v3</h1>
  <p class="lead">
    Phase 1 → tokens wired through Bootstrap. Try the controls below.
  </p>

  <div class="d-flex gap-2 align-items-center mb-4 mt-4 not-prose">
    <label class="d-inline-flex align-items-center gap-2">
      <span class="small text-body-secondary">Primary</span>
      <input id="pick-primary" type="color" value="#2563eb" class="form-control form-control-color" />
    </label>
    <button id="reset-primary" type="button" class="btn btn-link">reset</button>
  </div>

  <div class="d-flex flex-wrap gap-2 mb-4 not-prose">
    <button class="btn btn-primary">Primary</button>
    <button class="btn btn-secondary">Secondary</button>
    <button class="btn btn-success">Success</button>
    <button class="btn btn-danger">Danger</button>
    <button class="btn btn-warning">Warning</button>
    <button class="btn btn-info">Info</button>
    <button class="btn btn-light">Light</button>
    <button class="btn btn-dark">Dark</button>
  </div>

  <div class="row g-3 mb-4 not-prose">
    <div class="col-md-4">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Card surface</h5>
          <p class="card-text text-body-secondary">White card on tinted body: the layered surface effect.</p>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="alert alert-primary mb-0">A primary alert (subtle bg + emphasis text + border)</div>
    </div>
    <div class="col-md-4">
      <div class="alert alert-success mb-0">A success alert</div>
    </div>
  </div>

  <h2>Runtime tinting (responds to picker)</h2>
  <p>
    Utilities below use <code>var(--bs-primary)</code> / <code>--bs-primary-rgb</code>
    at runtime, so the picker updates them live. Buttons above are compiled at Sass
    build time and don't respond: that's a Bootstrap architectural limit.
  </p>
  <div class="d-flex flex-wrap gap-3 align-items-center not-prose">
    <span class="badge text-bg-primary">.text-bg-primary</span>
    <span class="px-3 py-2 bg-primary text-white rounded">.bg-primary</span>
    <span class="text-primary fw-semibold">.text-primary</span>
    <span class="px-3 py-2 border border-primary border-2 rounded">.border-primary</span>
  </div>

  <script>
    const html = document.documentElement;
    const hexToRgb = (hex) => {
      const v = hex.replace('#', '');
      const r = parseInt(v.slice(0, 2), 16);
      const g = parseInt(v.slice(2, 4), 16);
      const b = parseInt(v.slice(4, 6), 16);
      return `${r}, ${g}, ${b}`;
    };
    document.getElementById('pick-primary').addEventListener('input', (e) => {
      html.style.setProperty('--bs-primary', e.target.value);
      html.style.setProperty('--bs-primary-rgb', hexToRgb(e.target.value));
    });
    document.getElementById('reset-primary').addEventListener('click', () => {
      html.style.removeProperty('--bs-primary');
      html.style.removeProperty('--bs-primary-rgb');
      document.getElementById('pick-primary').value = '#2563eb';
    });
  </script>
{% endblock %}
