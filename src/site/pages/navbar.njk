{% extends "layouts/base.njk" %}
{% import "partials/_demo.njk" as ui %}

{% block title %}Navbar · Stisla{% endblock %}

{% block content %}
  <header>
    <h1>Navbar</h1>
    <p class="lead">A top bar with a brand mark, a row of nav links, and optional trailing content.</p>
  </header>

  <section>
    <p>Above the collapse breakpoint (<code>lg</code>), the bar reads as one row. Below, the nav and any trailing extras fold into a column behind a toggle. Resize the preview to a narrow width to see it fold. Drop the bar inside an <code>.app-shell__main</code> or at the top of <code>&lt;body&gt;</code>.</p>
  </section>

  <section>
    <h2>Basic</h2>
    <p>Wrap a <code>.navbar__brand</code>, a <code>.navbar__toggle</code>, and a <code>.navbar__menu</code> with its <code>.navbar__nav</code> list. Mark the current page with <code>data-state="active"</code> on the matching <code>.navbar__button</code>.</p>
    {% call ui.demo() %}
    <nav class="navbar navbar--block" data-stisla-navbar style="border: 1px solid var(--st-border); border-radius: var(--st-radius);">
      <a class="navbar__brand" href="#">Acme</a>
      <button type="button" class="navbar__toggle" data-stisla-navbar-toggle aria-controls="navbarBasic" aria-expanded="false" aria-label="Toggle navigation">
        <i data-lucide="menu"></i>
      </button>
      <div class="navbar__menu" id="navbarBasic" data-state="closed">
        <ul class="navbar__nav">
          <li><a class="navbar__button" data-state="active" aria-current="page" href="#">Home</a></li>
          <li><a class="navbar__button" href="#">Features</a></li>
          <li><a class="navbar__button" href="#">Pricing</a></li>
          <li><a class="navbar__button" href="#">About</a></li>
        </ul>
      </div>
    </nav>
    {% endcall %}
  </section>

  <section>
    <h2>Active and disabled</h2>
    <p>Active links carry <code>data-state="active"</code> (highlight chip); unreachable links carry <code>aria-disabled="true"</code> (dim + no pointer events). Hover is the soft accent chip; the active treatment is the highlight chip so the current row reads steadier than a transient hover.</p>
    {% call ui.demo() %}
    <nav class="navbar navbar--block" data-stisla-navbar style="border: 1px solid var(--st-border); border-radius: var(--st-radius);">
      <a class="navbar__brand" href="#">Acme</a>
      <button type="button" class="navbar__toggle" data-stisla-navbar-toggle aria-controls="navbarStates" aria-expanded="false" aria-label="Toggle navigation">
        <i data-lucide="menu"></i>
      </button>
      <div class="navbar__menu" id="navbarStates" data-state="closed">
        <ul class="navbar__nav">
          <li><a class="navbar__button" href="#">Overview</a></li>
          <li><a class="navbar__button" data-state="active" aria-current="page" href="#">Reports</a></li>
          <li><a class="navbar__button" href="#">Settings</a></li>
          <li><a class="navbar__button" aria-disabled="true">Billing</a></li>
        </ul>
      </div>
    </nav>
    {% endcall %}
  </section>

  <section>
    <h2>With status text</h2>
    <p>Drop any element inside <code>.navbar__menu</code> for a trailing status pill, workspace name, or counter. Without a nav before it, push the element to the trailing edge with <code>margin-inline-start: auto</code>.</p>
    {% call ui.demo() %}
    <nav class="navbar navbar--block" data-stisla-navbar style="border: 1px solid var(--st-border); border-radius: var(--st-radius);">
      <a class="navbar__brand" href="#">Acme</a>
      <button type="button" class="navbar__toggle" data-stisla-navbar-toggle aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <i data-lucide="menu"></i>
      </button>
      <div class="navbar__menu" id="navbarText" data-state="closed">
        <span class="d-flex flex-wrap align-items-center gap-2 ms-auto text-muted-foreground">
          <span class="d-inline-block" style="width: 0.375rem; height: 0.375rem; border-radius: 50%; background: var(--st-success);"></span>
          All systems normal
        </span>
      </div>
    </nav>
    {% endcall %}
  </section>

  <section>
    <h2>Container</h2>
    <p>Drop a <code>.container</code> or <code>.container-fluid</code> as the bar's direct child to clamp inner content to the standard column widths. The bar still spans full width; the container takes over the inline padding so the inner row shares its inset with a matching container in the page body.</p>
    {% call ui.demo() %}
    <nav class="navbar navbar--block" data-stisla-navbar style="border: 1px solid var(--st-border); border-radius: var(--st-radius);">
      <div class="container">
        <a class="navbar__brand" href="#">Acme</a>
        <button type="button" class="navbar__toggle" data-stisla-navbar-toggle aria-controls="navbarContainer" aria-expanded="false" aria-label="Toggle navigation">
          <i data-lucide="menu"></i>
        </button>
        <div class="navbar__menu" id="navbarContainer" data-state="closed">
          <ul class="navbar__nav">
            <li><a class="navbar__button" data-state="active" aria-current="page" href="#">Home</a></li>
            <li><a class="navbar__button" href="#">Features</a></li>
            <li><a class="navbar__button" href="#">Pricing</a></li>
          </ul>
        </div>
      </div>
    </nav>
    {% endcall %}
  </section>

  <section>
    <h2>Dropdowns</h2>
    <p>Dropdown menus inside <code>.navbar__nav</code> land with the dropdown component in Step 4. Until then, link rows that anchor a menu render as plain links.</p>
  </section>

  <section>
    <h2>Customization</h2>
    <p>Seventeen variables retune <code>.navbar</code> without touching component CSS. Override on the <code>.navbar</code> itself, on a parent scope, or on <code>:root</code>.</p>

    <h3>Layout</h3>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Use</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--navbar-padding-block</code></td>
          <td>Vertical padding inside the bar</td>
        </tr>
        <tr>
          <td><code>--navbar-padding-inline</code></td>
          <td>Horizontal padding inside the bar; zeroed when a container child is present</td>
        </tr>
        <tr>
          <td><code>--navbar-gap</code></td>
          <td>Space between brand, menu, and toggle</td>
        </tr>
        <tr>
          <td><code>--navbar-bg</code></td>
          <td>Bar background</td>
        </tr>
        <tr>
          <td><code>--navbar-color</code></td>
          <td>Bar foreground (brand + toggle inherit)</td>
        </tr>
      </tbody>
    </table>

    <h3>Button chip</h3>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Use</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--navbar-button-height</code></td>
          <td>Hard height of <code>.navbar__brand</code> and <code>.navbar__button</code></td>
        </tr>
        <tr>
          <td><code>--navbar-button-padding-inline</code></td>
          <td>Horizontal chip padding</td>
        </tr>
        <tr>
          <td><code>--navbar-brand-padding-inline</code></td>
          <td>Horizontal padding on <code>.navbar__brand</code>; defaults to the button chip's inset so brand text sits on the same x-grid as button text. Override to <code>0</code> for a flush brand.</td>
        </tr>
        <tr>
          <td><code>--navbar-button-gap</code></td>
          <td>Space between adjacent chips in <code>.navbar__nav</code></td>
        </tr>
        <tr>
          <td><code>--navbar-button-radius</code></td>
          <td>Chip corner radius</td>
        </tr>
        <tr>
          <td><code>--navbar-button-color</code></td>
          <td>Rest text color</td>
        </tr>
        <tr>
          <td><code>--navbar-button-bg-hover</code></td>
          <td>Hover + focus chip fill</td>
        </tr>
        <tr>
          <td><code>--navbar-button-color-hover</code></td>
          <td>Hover + focus text color</td>
        </tr>
        <tr>
          <td><code>--navbar-button-bg-active</code></td>
          <td>Current-page chip fill (<code>data-state="active"</code>)</td>
        </tr>
        <tr>
          <td><code>--navbar-button-color-active</code></td>
          <td>Current-page text color</td>
        </tr>
      </tbody>
    </table>

    <h3>Toggle + motion</h3>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Use</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--navbar-toggle-size</code></td>
          <td>Square hamburger button size</td>
        </tr>
        <tr>
          <td><code>--navbar-transition-duration</code></td>
          <td>Chip + toggle hover transition speed</td>
        </tr>
      </tbody>
    </table>
  </section>
{% endblock %}
