{% extends "layouts/base.njk" %}
{% import "partials/_demo.njk" as ui %}

{% block title %}Validation · Stisla v3{% endblock %}

{% block content %}
  <header>
    <h1>Validation</h1>
    <p class="lead">Status styles and feedback messages for form fields, driven by either the browser or your own server response.</p>
  </header>

  <section>
    <h2>States</h2>
    <p>Add <code>.is-valid</code> or <code>.is-invalid</code> to any form control to mark the field. Pair with a sibling <code>.valid-feedback</code> or <code>.invalid-feedback</code> for the message. Use this pattern when state comes back from a server response or your own client logic.</p>
    {% call ui.demo() %}
    <div class="d-flex flex-column gap-3 w-100" style="max-width: 24rem;">
      <div>
        <label for="validName" class="form-label">Name</label>
        <input type="text" class="form-control is-valid" id="validName" value="Ada Lovelace" />
        <div class="valid-feedback">Looks good.</div>
      </div>
      <div>
        <label for="invalidEmail" class="form-label">Email</label>
        <input type="email" class="form-control is-invalid" id="invalidEmail" value="ada@" />
        <div class="invalid-feedback">Enter a complete email address.</div>
      </div>
    </div>
    {% endcall %}
  </section>

  <section>
    <h2>Browser validation</h2>
    <p>Add <code>.was-validated</code> to the <code>&lt;form&gt;</code> after a submit attempt to light up native HTML5 constraints (<code>required</code>, <code>type="email"</code>, <code>minlength</code>, etc.) without per-field classes. Add <code>novalidate</code> to suppress the browser's own bubbles and let the inline feedback do the work.</p>
    {% call ui.demo() %}
    <form class="was-validated d-flex flex-column gap-3 w-100" style="max-width: 24rem;" novalidate>
      <div>
        <label for="nativeName" class="form-label">Full name</label>
        <input type="text" class="form-control" id="nativeName" value="Grace Hopper" required />
        <div class="invalid-feedback">Name is required.</div>
      </div>
      <div>
        <label for="nativeEmail" class="form-label">Email</label>
        <input type="email" class="form-control" id="nativeEmail" required />
        <div class="invalid-feedback">Enter a valid email.</div>
      </div>
    </form>
    {% endcall %}
  </section>

  <section>
    <h2>On submit</h2>
    <p>Listen for <code>submit</code>, call <code>checkValidity()</code>, and add <code>.was-validated</code> to flip the form into its validated state. <code>preventDefault()</code> keeps the browser from posting so you can hand off to <code>fetch</code> once the form is clean.</p>
    {% call ui.demo() %}
    <form id="submitForm" class="d-flex flex-column gap-3 w-100" style="max-width: 24rem;" novalidate>
      <div>
        <label for="submitName" class="form-label">Full name</label>
        <input type="text" class="form-control" id="submitName" required />
        <div class="invalid-feedback">Name is required.</div>
      </div>
      <div>
        <label for="submitEmail" class="form-label">Email</label>
        <input type="email" class="form-control" id="submitEmail" required />
        <div class="invalid-feedback">Enter a valid email.</div>
      </div>
      <div class="form-check">
        <input class="form-check-input" type="checkbox" id="submitTerms" required />
        <label class="form-check-label" for="submitTerms">I agree to the terms</label>
        <div class="invalid-feedback">You must agree before continuing.</div>
      </div>
      <button type="submit" class="btn btn-primary align-self-start">Sign up</button>
    </form>
    <script>
      (function () {
        const form = document.getElementById('submitForm');
        form.addEventListener('submit', (event) => {
          event.preventDefault();
          form.classList.add('was-validated');
          if (form.checkValidity()) {
            // hand off to fetch / your API
          }
        });
      })();
    </script>
    {% endcall %}
  </section>

  <section>
    <h2>Tooltip feedback</h2>
    <p>Swap <code>.valid-feedback</code> / <code>.invalid-feedback</code> for <code>.valid-tooltip</code> / <code>.invalid-tooltip</code> to float the message over the layout instead of pushing siblings down. The parent needs <code>position: relative</code>.</p>
    {% call ui.demo() %}
    <div class="d-flex flex-column gap-4 w-100" style="max-width: 24rem;">
      <div class="position-relative">
        <label for="tipUser" class="form-label">Username</label>
        <input type="text" class="form-control is-valid" id="tipUser" value="ada" />
        <div class="valid-tooltip">Available.</div>
      </div>
      <div class="position-relative">
        <label for="tipCity" class="form-label">City</label>
        <input type="text" class="form-control is-invalid" id="tipCity" />
        <div class="invalid-tooltip">City is required.</div>
      </div>
    </div>
    {% endcall %}
  </section>

  <section>
    <h2>Select</h2>
    <p>The same classes work on <code>.form-select</code>.</p>
    {% call ui.demo() %}
    <div class="d-flex flex-column gap-3 w-100" style="max-width: 24rem;">
      <div>
        <label for="validRegion" class="form-label">Region</label>
        <select class="form-select is-valid" id="validRegion">
          <option>Asia Pacific</option>
        </select>
        <div class="valid-feedback">Region set.</div>
      </div>
      <div>
        <label for="invalidPlan" class="form-label">Plan</label>
        <select class="form-select is-invalid" id="invalidPlan">
          <option value="" selected>Choose a plan</option>
          <option value="starter">Starter</option>
          <option value="pro">Pro</option>
        </select>
        <div class="invalid-feedback">Pick a plan to continue.</div>
      </div>
    </div>
    {% endcall %}
  </section>

  <section>
    <h2>Checks &amp; radios</h2>
    <p>Apply the state class to the <code>.form-check-input</code>. Feedback can sit alongside the label inside the same <code>.form-check</code>.</p>
    {% call ui.demo() %}
    <div class="d-flex flex-column gap-3 w-100" style="max-width: 24rem;">
      <div class="form-check">
        <input class="form-check-input is-invalid" type="checkbox" id="agreeTerms" required />
        <label class="form-check-label" for="agreeTerms">I agree to the terms</label>
        <div class="invalid-feedback">You must agree before continuing.</div>
      </div>
      <div class="form-check">
        <input class="form-check-input is-valid" type="checkbox" id="agreeMarketing" checked />
        <label class="form-check-label" for="agreeMarketing">Send me product updates</label>
        <div class="valid-feedback">Preference saved.</div>
      </div>
    </div>
    {% endcall %}
  </section>

  <section>
    <h2>File input</h2>
    <p>State classes also apply to <code>type="file"</code>. The selector button and filename stay legible against the field's tinted background.</p>
    {% call ui.demo() %}
    <div class="d-flex flex-column gap-3 w-100" style="max-width: 24rem;">
      <div>
        <label for="validUpload" class="form-label">Logo</label>
        <input type="file" class="form-control is-valid" id="validUpload" />
        <div class="valid-feedback">Uploaded.</div>
      </div>
      <div>
        <label for="invalidUpload" class="form-label">Avatar</label>
        <input type="file" class="form-control is-invalid" id="invalidUpload" />
        <div class="invalid-feedback">Pick a file under 2&nbsp;MB.</div>
      </div>
    </div>
    {% endcall %}
  </section>
{% endblock %}
